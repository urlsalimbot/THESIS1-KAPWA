10010	IGNORE	Cookie No HttpOnly Flag — csrf-token is intentionally JS-readable for the double-submit CSRF pattern (see kapwa-server/src/common/csrf.guard.ts)
40018	IGNORE	SQL Injection — verified false positive; TypeORM/`$1` parameterized queries, manual `?search=' OR 1=1--` probe returns 200 with an empty result set
