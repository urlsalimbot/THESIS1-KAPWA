# PDF export test renders

Every PDF-producing code path in `kapwa-server` is invoked with representative
data and rendered to PNG so the output can be reviewed without a running stack.

- **PDFs:** generated by the real builders and services (repository/service
  dependencies are stubbed with fixture data).
- **PNGs:** rendered with `pdftoppm -r 110`. Single-page exports use
  `<name>.png`; multi-page exports use `<name>-pN.png`.

## Inventory

| # | Export | Source | Paper | Pages | Size |
|---|---|---|---|---|---|
| 01 | Certificate of Eligibility | `cases/case-documents.builder.ts` | 21 × 7 cm strip | 1 | 84,270 B |
| 02 | Petty Cash Voucher | `cases/case-documents.builder.ts` | 21 × 14 cm strip | 1 | 2,926 B |
| 03 | Incident Report Form (Blotter) | `irf/irf-pdf.builder.ts` | A4 | 1 | 240,156 B |
| 04 | General Intake Sheet (GIS) | `gis/gis-pdf.builder.ts` | A4 | 1 | 88,619 B |
| 05 | Family Access Card | `access-cards/access-card-pdf.builder.ts` | A4 | 2 | 6,238 B |
| 08 | Certificate of Eligibility (generic) | `export/export.service.ts` | A4 | 1 | 2,217 B |
| 09 | Certificate of Referral | `export/export.service.ts` | A4 | 1 | 2,229 B |
| 13 | CSR bundle (cover + PCV + COE + IRF + GIS) | `cases/cases-export.service.ts` | mixed | 5 | 411,219 B |

**Result:** 8 / 8 exports generated successfully across 13 pages.

Removed from the system (code paths, API routes and rendered files): Family Case
Study Report PDF, Certificate of Indigency, Audit Log PDF, Service Summary PDF
and Compliance Report PDF. The non-PDF variants (audit-log CSV, compliance CSV,
service-summary CSV/XLSX, fund-utilization XLSX) remain.

## Automated layout checks

Two objective checks run against every generated PDF:

1. **Overlapping text** — `pdftotext -bbox` word rectangles are compared
   pairwise; any pair whose intersection exceeds 25% of the smaller rectangle is
   a collision. All 13 pages are clean.
2. **Off-page text** — every word rectangle must fall inside its page box.
   All 13 pages are in bounds.

Labels that could collide with a neighbour are shrink-fitted to their cell, and
cell values wrap and clip inside their own cell, so the layout stays collision
free regardless of the loaded data.

## Paper sizes

`Certificate of Eligibility` and `Petty Cash Voucher` are printed on cut strips
of bond paper, so they use custom page sizes (see `COE_PAGE` / `PCV_PAGE` in
`cases/case-documents.builder.ts`):

- Certificate of Eligibility — 21 cm × 7 cm (landscape strip)
- Petty Cash Voucher — 21 cm × 14 cm (landscape strip); **Part II
  ("to be filled up upon liquidation") is deliberately left blank** and is only
  completed at liquidation time.

## Seals and logos

- `norzagaray-bulacan-official-logo.png` — Municipality of Norzagaray badge,
  printed in the Incident Report Form header (left of the title).
- `DSWD-Logo.png` — printed in the Incident Report Form header (right) and on
  the Certificate of Eligibility and General Intake Sheet.

Both live in `kapwa-server/src/gis/assets/` and are copied to `dist` via the
`nest-cli.json` assets entry.

## Regenerating

The generation harness was a temporary script and is not committed. Re-create it
from the export inventory above, or drive the same builders through the existing
Jest specs in `src/**/*.spec.ts`.
