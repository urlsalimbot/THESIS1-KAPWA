---
phase: 12-accessibility-differentiators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/package.json
  - kapwa-client/src/components/Layout.tsx
  - kapwa-client/src/components/ErrorBoundary.tsx
  - kapwa-client/src/components/PageShell.tsx
  - kapwa-client/src/components/a11y/SkipToContent.tsx
  - kapwa-client/src/components/a11y/AriaLiveRegion.tsx
  - kapwa-client/src/components/a11y/FocusTrap.tsx
  - kapwa-client/src/components/a11y/index.ts
  - kapwa-client/src/hooks/usePiiMasking.ts
  - kapwa-client/src/lib/pii-utils.ts
  - kapwa-client/src/components/pii/MaskedField.tsx
  - kapwa-client/src/components/pii/index.ts
autonomous: true
requirements:
  - ACC-01
  - ACC-04
must_haves:
  truths:
    - All interactive elements are keyboard-reachable with visible focus rings
    - Screen reader users receive announcements for toast notifications and loading state changes
    - PII fields are masked for non-worker roles without page reload
    - Click-to-reveal temporarily unmasks a field and logs to consent ledger
    - Revoked consent masks data for everyone including workers
    - Button labels and ARIA attributes describe action purpose
  artifacts:
    - path: kapwa-client/src/components/a11y/SkipToContent.tsx
      provides: Skip-to-content link as first focusable element on route render
      contains: main-content href
    - path: kapwa-client/src/components/a11y/AriaLiveRegion.tsx
      provides: aria-live polite region for dynamic announcements
      exports: AriaLiveRegion
    - path: kapwa-client/src/components/Layout.tsx
      provides: Wire SkipToContent into layout
      contains: import SkipToContent
    - path: kapwa-client/src/hooks/usePiiMasking.ts
      provides: Core masking hook with consent awareness
      exports: usePiiMasking
    - path: kapwa-client/src/components/pii/MaskedField.tsx
      provides: Click-to-reveal masked field component
      exports: MaskedField
  key_links:
    - from: kapwa-client/src/components/pii/MaskedField.tsx
      to: kapwa-client/src/hooks/usePiiMasking.ts
      via: imports usePiiMasking hook
    - from: kapwa-client/src/components/pii/MaskedField.tsx
      to: kapwa-client/src/lib/pii-utils.ts
      via: imports maskDisplay strings
---

<objective>
Establish WCAG 2.1 AA accessibility foundation (SkipToContent, aria-live regions, keyboard navigation) and PII masking infrastructure (consent-aware usePiiMasking hook with click-to-reveal).

Purpose: Create the foundational accessibility layer and data privacy components that all subsequent Phase 12 features build upon. Accessibility components (SkipToContent, AriaLiveRegion, FocusTrap) are reusable across every page. PII masking components (usePiiMasking, MaskedField) enforce RA 10173 compliance per D-10/D-11/D-12.

Output: Reusable a11y components, PII masking hook with MaskedField component, wired into Layout/ErrorBoundary/PageShell.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-accessibility-differentiators/12-CONTEXT.md
@.planning/phases/12-accessibility-differentiators/12-RESEARCH.md
@kapwa-client/src/components/Layout.tsx
@kapwa-client/src/components/ErrorBoundary.tsx
@kapwa-client/src/components/PageShell.tsx
@kapwa-client/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install a11y testing deps and create a11y component suite</name>
  <files>
    kapwa-client/package.json
    kapwa-client/src/components/a11y/SkipToContent.tsx
    kapwa-client/src/components/a11y/AriaLiveRegion.tsx
    kapwa-client/src/components/a11y/FocusTrap.tsx
    kapwa-client/src/components/a11y/index.ts
  </files>
  <action>
    Install @axe-core/playwright@4.12.1 and vitest-axe@0.1.0 as devDependencies via npm install --save-dev (per RESEARCH.md #Package Legitimacy Audit — both OK/Approved). Do not remove existing packages.

    Create kapwa-client/src/components/a11y/SkipToContent.tsx:
    - Anchor element (<a>) with href="#main-content"
    - CSS: sr-only by default (sr-only class), on focus becomes visible: focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-white focus:text-black focus:rounded-md focus:shadow-lg
    - Text: "Skip to main content"

    Create kapwa-client/src/components/a11y/AriaLiveRegion.tsx:
    - Region <div> with aria-live="polite" and aria-atomic="true"
    - Props: message (string), as (default "div"), className
    - Only render children when message is non-empty (useEffect to set visible after message change)
    - Export as default + named

    Create kapwa-client/src/components/a11y/FocusTrap.tsx:
    - Container that traps tab focus within its children
    - Props: active (boolean), children, initialFocusRef (optional RefObject)
    - When active, intercept Tab/Shift+Tab and cycle focus through focusable children
    - Use useCallback + useRef for keydown handler
    - When active and Escape pressed, call onEscape prop
    - Export FocusTrap

    Create kapwa-client/src/components/a11y/index.ts barrel export re-exporting all three components.
  </action>
  <verify>
    <automated>ls kapwa-client/src/components/a11y/ && node -e "require("assert").ok(require("fs").existsSync("kapwa-client/src/components/a11y/index.ts"))"</automated>
  </verify>
  <done>Four a11y files exist in components/a11y/ directory with barrel export. npm list shows @axe-core/playwright and vitest-axe installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create PII masking infrastructure</name>
  <files>
    kapwa-client/src/hooks/usePiiMasking.ts
    kapwa-client/src/lib/pii-utils.ts
    kapwa-client/src/components/pii/MaskedField.tsx
    kapwa-client/src/components/pii/index.ts
  </files>
  <action>
    Create kapwa-client/src/lib/pii-utils.ts:
    - Define MaskableField type union: name, phone, address, dob, philsys
    - MASK_DISPLAY Record mapping each field to masked string (see RESEARCH.md code example — name to asterisks, phone to mask pattern, address to asterisks, dob to date mask, philsys to segment mask)
    - WORKER_ROLES constant array: social_worker, admin, coordinator
    - Export MASK_DISPLAY, WORKER_ROLES, type MaskableField

    Create kapwa-client/src/hooks/usePiiMasking.ts following RESEARCH.md Pattern 2:
    - Import useAuth from @/lib/auth-context
    - Import MaskableField, MASK_DISPLAY, WORKER_ROLES from pii-utils
    - Accept consentStatus: active | revoked parameter
    - shouldMask computed: revoked consent masks ALL; only WORKER_ROLES see full data when consent is active
    - getDisplayValue(field, realValue): returns masked string when shouldMask and field not revealed, else realValue
    - revealField(field, beneficiaryId, reason): adds field to revealed Set, POST to /api/beneficiaries/{beneficiaryId}/audit/unmask (per D-11), sets 30s auto-mask timeout (agent discretion default)
    - Cleanup all timeouts on unmount via useEffect return
    - Export usePiiMasking

    Create kapwa-client/src/components/pii/MaskedField.tsx:
    - Props: field (MaskableField), value (string), beneficiaryId (string), consentStatus (active|revoked), label (string)
    - Uses usePiiMasking hook internally
    - Renders label + value display area
    - When masked: shows masked value + Eye icon button to reveal
    - On reveal click: prompts for reason (small text input or use default "Field view"), calls revealField, shows real value + EyeOff icon
    - After 30s timeout: auto-masks back
    - Accessible: aria-label on button, role="button" with tabIndex
    - Export MaskedField

    Create kapwa-client/src/components/pii/index.ts barrel export.
  </action>
  <verify>
    <automated>ls kapwa-client/src/hooks/usePiiMasking.ts kapwa-client/src/components/pii/</automated>
  </verify>
  <done>usePiiMasking hook, pii-utils, MaskedField component, and barrel index all exist.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SkipToContent and AriaLiveRegion into Layout/ErrorBoundary/PageShell</name>
  <files>
    kapwa-client/src/components/Layout.tsx
    kapwa-client/src/components/ErrorBoundary.tsx
    kapwa-client/src/components/PageShell.tsx
  </files>
  <action>
    Modify kapwa-client/src/components/Layout.tsx:
    - Import SkipToContent from ./a11y/SkipToContent
    - Remove existing inline skip link (the sr-only <a href="#main-content"> near line 81)
    - Replace with <SkipToContent /> at same position (before SyncStatusBanner)
    - Import AriaLiveRegion from ./a11y/AriaLiveRegion
    - Add <AriaLiveRegion message={offline ? "You are offline" : ""} /> after SkipToContent to announce connectivity state changes

    Modify kapwa-client/src/components/ErrorBoundary.tsx:
    - Import AriaLiveRegion from ./a11y/AriaLiveRegion
    - In error render branches (both offline and render-error), wrap main heading/icon in AriaLiveRegion with role="alert" to announce error state to screen readers
    - Use aria-live assertive for errors (more urgent than polite)
    - On retry click, clear the aria-live region

    Modify kapwa-client/src/components/PageShell.tsx:
    - Import AriaLiveRegion from ./a11y/AriaLiveRegion
    - Add AriaLiveRegion that announces when cachedAt changes or when stale data is displayed
    - Announce: "Showing cached data from {time}" when isStale is true
    - Keep existing Clock icon visual indicator
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20; echo "---"; grep -c "SkipToContent|AriaLiveRegion" kapwa-client/src/components/Layout.tsx kapwa-client/src/components/ErrorBoundary.tsx kapwa-client/src/components/PageShell.tsx</automated>
  </verify>
  <done>Existing Skip-to-content link replaced with SkipToContent component. AriaLiveRegion wired into Layout, ErrorBoundary, and PageShell for connectivity and error announcements. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side a11y code→DOM | No untrusted input enters a11y components; messages are hardcoded or app-state derived |
| PII reveal→API | Unmask request sends field, beneficiaryId, reason to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | PII masking hook | mitigate | shouldMask prevents display of unmasked data; server-side check also required (defense in depth) |
| T-12-02 | Tampering | Unmask API call | mitigate | Audit log on server for every unmask event (per D-11) — handled by backend endpoint, frontend fires POST with beneficiaryId, field, reason |
| T-12-03 | Information Disclosure | aria-live region | accept | aria-live reads current DOM text — masked values remain masked; no PII leak risk |
| T-12-SC | Tampering | npm install @axe-core/playwright, vitest-axe | mitigate | Both packages OK/Approved per RESEARCH.md Package Legitimacy Audit — no checkpoint needed |
</threat_model>

<verification>
- All 3 tasks produce files on disk
- npm test passes after all changes
- grep confirms SkipToContent imported in Layout.tsx
- grep confirms usePiiMasking used in MaskedField.tsx
</verification>

<success_criteria>
- [ ] SkipToContent appears as first focusable element on layout render
- [ ] AriaLiveRegion announces offline state changes
- [ ] ErrorBoundary error messages announced to screen readers
- [ ] MaskedField shows asterisks for non-worker roles
- [ ] Click-to-reveal shows value for 30s then re-masks
- [ ] All existing tests pass
</success_criteria>
