---
phase: 12-accessibility-differentiators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/src/hooks/useSlaTimer.ts
  - kapwa-client/src/lib/sla-utils.ts
  - kapwa-client/src/components/sla/SlaTimer.tsx
  - kapwa-client/src/components/sla/SlaTooltip.tsx
  - kapwa-client/src/components/sla/index.ts
  - kapwa-client/src/hooks/useDebouncedSearch.ts
  - kapwa-client/src/components/search/GlobalSearch.tsx
  - kapwa-client/src/components/search/index.ts
  - kapwa-client/src/components/Topbar.tsx
autonomous: true
requirements:
  - DIF-01
  - DIF-03
  - DIF-05
user_setup:
  - service: backend
    why: SLA timers need slaHours field in program configuration
    action: Ensure program config table has slaHours (integer, hours) column
  - service: backend
    why: GlobalSearch needs /api/beneficiaries?search=:query&limit=:n endpoint
    action: Verify existing GIS-04 endpoint supports search + limit params
must_haves:
  truths:
    - "Cases display color-coded SLA countdown indicators (green/amber/red)"
    - "SLA thresholds are configurable per program type"
    - "Global search bar searches by name, ID, and barangay"
    - "Quick actions (New Intake, Approvals Queue) appear as topbar buttons"
    - "Cmd+K / Ctrl+K keyboard shortcut opens global search"
  artifacts:
    - path: kapwa-client/src/hooks/useSlaTimer.ts
      provides: Elapsed time calculation with color threshold logic
      exports: useSlaTimer
    - path: kapwa-client/src/components/sla/SlaTimer.tsx
      provides: Color-coded elapsed time display
      exports: SlaTimer
    - path: kapwa-client/src/components/search/GlobalSearch.tsx
      provides: cmdk-based command palette search
      exports: GlobalSearch
    - path: kapwa-client/src/components/Topbar.tsx
      provides: Quick action buttons and GlobalSearch integration
      contains: import GlobalSearch
  key_links:
    - from: kapwa-client/src/components/sla/SlaTimer.tsx
      to: kapwa-client/src/hooks/useSlaTimer.ts
      via: imports useSlaTimer hook
    - from: kapwa-client/src/components/Topbar.tsx
      to: kapwa-client/src/components/search/GlobalSearch.tsx
      via: renders GlobalSearch component
---

<objective>
Deliver RA 11032 SLA timers with color-coded indicators (D-14/D-15/D-16/D-17), global search via cmdk (D-20), and quick action buttons in the topbar (D-18/D-19).

Purpose: Three independent differentiating features built in parallel -- SLA timers for processing compliance, global search for field worker productivity, and quick actions for common workflows. All three wire into the existing Topbar/Dashboard layout.

Output: useSlaTimer hook + SlaTimer/SlaTooltip components, useDebouncedSearch hook + GlobalSearch cmdk component, updated Topbar with quick actions and search.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-accessibility-differentiators/12-CONTEXT.md
@.planning/phases/12-accessibility-differentiators/12-RESEARCH.md
@kapwa-client/src/components/Topbar.tsx
@kapwa-client/src/components/data-table/DataTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SLA timer infrastructure</name>
  <files>
    kapwa-client/src/hooks/useSlaTimer.ts
    kapwa-client/src/lib/sla-utils.ts
    kapwa-client/src/components/sla/SlaTimer.tsx
    kapwa-client/src/components/sla/SlaTooltip.tsx
    kapwa-client/src/components/sla/index.ts
  </files>
  <action>
    Create kapwa-client/src/lib/sla-utils.ts:
    - Define SlaStatus type: compliant | warning | breached
    - THRESHOLDS constant: warning at 0.7 (70% of SLA), breach at 0.9 (90%)
    - Helper getThresholdColor(status): returns Tailwind color class (green-500, amber-500, red-500)
    - Helper getThresholdBgColor(status): returns muted background variant
    - Helper formatElapsed(elapsedHours): returns display string (less than 24h shows Nh, more shows Nd Nh)
    - Export all

    Create kapwa-client/src/hooks/useSlaTimer.ts following RESEARCH.md Pattern 3:
    - Accept stageStartedAt (ISO 8601 string), slaHours (number), pollIntervalMs (default 60000)
    - State: now (Date.now) updated on interval
    - Calculate elapsedMs = now - new Date(stageStartedAt).getTime()
    - Convert to elapsedHours, compute fractionUsed = elapsedHours / slaHours
    - Determine status via THRESHOLDS constants
    - Return { elapsedDisplay, status, fractionUsed } with useMemo stability
    - Cleanup interval on unmount
    - Export useSlaTimer

    Create kapwa-client/src/components/sla/SlaTimer.tsx:
    - Props: stageStartedAt (string), slaHours (number), size (sm|md|lg default md), showIcon (boolean default true)
    - Uses useSlaTimer hook internally
    - Renders flex row with optional Clock icon + elapsed display + status dot (8px circle)
    - Color via getThresholdColor matching status (green/amber/red)
    - Accessible: role=status, aria-label with elapsed hours and status
    - Export SlaTimer

    Create kapwa-client/src/components/sla/SlaTooltip.tsx:
    - Props: stageStartedAt, slaHours, elapsedDisplay, status
    - Renders breakdown: stage start date, total SLA hours, elapsed, remaining
    - Uses shadcn Tooltip wrapper (Tooltip, TooltipContent, TooltipTrigger)
    - Export SlaTooltip

    Create kapwa-client/src/components/sla/index.ts barrel export.
  </action>
  <verify>
    <automated>ls kapwa-client/src/components/sla/SlaTimer.tsx kapwa-client/src/hooks/useSlaTimer.ts</automated>
  </verify>
  <done>SLA hook, utils, SlaTimer, SlaTooltip, and barrel export exist on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Create Global Search with cmdk</name>
  <files>
    kapwa-client/src/hooks/useDebouncedSearch.ts
    kapwa-client/src/components/search/GlobalSearch.tsx
    kapwa-client/src/components/search/index.ts
  </files>
  <action>
    Create kapwa-client/src/hooks/useDebouncedSearch.ts:
    - Accept query (string), delay (number default 300), limit (number default 10)
    - State: results (array), loading (boolean)
    - useEffect: debounce timer on query change; on expiry fetch /api/beneficiaries?search=QUERY&limit=N
    - Use AbortController for cancellation
    - Skip fetch on empty query
    - Return { results, loading }
    - Export useDebouncedSearch

    Create kapwa-client/src/components/search/GlobalSearch.tsx:
    - Uses shadcn Popover + cmdk Command (Command, CommandInput, CommandList, CommandItem, CommandEmpty, CommandLoading)
    - State: open (boolean), query (string)
    - Uses useDebouncedSearch hook
    - Cmd+K / Ctrl+K keyboard shortcut toggle (useEffect keydown listener)
    - On item select: navigate to /beneficiaries/{id}, close popover
    - Results display: two-line (fullName, controlNo + barangay)
    - Trigger: button with Search icon, placeholder text, keyboard hint
    - Hidden on mobile via hidden md:flex
    - Accessible: aria-label on trigger, role=combobox on input
    - Export GlobalSearch

    Create kapwa-client/src/components/search/index.ts barrel export.
  </action>
  <verify>
    <automated>ls kapwa-client/src/components/search/GlobalSearch.tsx kapwa-client/src/hooks/useDebouncedSearch.ts</automated>
  </verify>
  <done>GlobalSearch component and useDebouncedSearch hook exist.</done>
</task>

<task type="auto">
  <name>Task 3: Add quick actions and GlobalSearch to Topbar</name>
  <files>
    kapwa-client/src/components/Topbar.tsx
  </files>
  <action>
    Modify kapwa-client/src/components/Topbar.tsx:
    - Import GlobalSearch from ./search/GlobalSearch
    - Import Plus, CheckSquare from lucide-react
    - Import useNavigate from react-router-dom
    - Import Tooltip, TooltipContent, TooltipTrigger, TooltipProvider from @/components/ui/tooltip

    In right-side button group before NotificationsDropdown:
    - Add New Intake button (Plus icon, navigates to /intake) if role in [admin, social_worker, coordinator]
    - Add Approvals Queue button (CheckSquare icon, navigates to /approvals) if role in [admin, social_worker]
    - Use w-9 h-9 rounded-full style matching existing icon buttons
    - Wrap in Tooltip with aria-label
    - Show count badge on Approvals if pending approvals > 0

    Replace existing search Input (the hidden md:relative md:block div) with <GlobalSearch />
    Keep all existing elements: NotificationsDropdown, MessagesPopover, Help circle, Avatar, theme toggle
    Export TopbarProps unchanged
  </action>
  <verify>
    <automated>grep -c 'GlobalSearch' kapwa-client/src/components/Topbar.tsx</automated>
  </verify>
  <done>Topbar shows New Intake / Approvals Queue buttons and GlobalSearch replacing plain search input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search input to API | User types search query, sent to /api/beneficiaries endpoint |
| SLA timer to display | All SLA values derived from server timestamps, client-side calculation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | GlobalSearch results | mitigate | Backend GIS-04 filters by role/barangay; frontend only displays what API returns |
| T-12-05 | Tampering | SLA timer calculation | accept | Timestamps from server (stageStartedAt); client only formats elapsed. 60s polling sufficient for visual indicator |
| T-12-SC | Tampering | No new packages in this plan | accept | No package installs |
</threat_model>

<verification>
- All 3 tasks produce files on disk
- grep confirms GlobalSearch in Topbar.tsx
- npm run build passes
</verification>

<success_criteria>
- [ ] SlaTimer renders color-coded elapsed time with status dot
- [ ] GlobalSearch opens on Cmd+K, filters results by name/ID/barangay
- [ ] Topbar shows New Intake and Approvals Queue buttons for authorized roles
- [ ] Existing search input replaced with GlobalSearch
- [ ] npm run build passes
</success_criteria>