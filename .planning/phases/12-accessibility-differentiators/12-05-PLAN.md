---
phase: 12-accessibility-differentiators
plan: 05
type: execute
wave: 3
autonomous: false
depends_on: ['12-01', '12-02', '12-03', '12-04']
files_modified:
  - kapwa-client/src/index.css
  - kapwa-client/src/tests/a11y/axe-setup.ts
  - kapwa-client/src/tests/a11y/pages.test.ts
  - kapwa-client/src/tests/a11y/components.test.ts
  - kapwa-client/src/tests/pii/masking.test.ts
  - kapwa-client/src/tests/sla/timer.test.ts
  - kapwa-client/src/tests/bulk-actions/selection.test.ts
  - kapwa-client/src/tests/search/global.test.ts
autonomous: false
requirements:
  - ACC-01
  - ACC-02
must_haves:
  truths:
    - "All text meets WCAG 2.1 AA minimum contrast (4.5:1 body, 3:1 large)"
    - "All interactive elements are keyboard-reachable with visible focus rings"
    - "No keyboard focus traps exist in the application"
    - "axe-core CI tests detect accessibility regressions on PR"
    - "Component-level vitest-axe tests verify a11y of individual components"
    - "PII masking hook unit tests pass"
    - "SLA timer unit tests pass"
    - "Bulk selection state unit tests pass"
  artifacts:
    - path: kapwa-client/src/tests/a11y/axe-setup.ts
      provides: Shared AxeBuilder fixture for Playwright tests
      contains: AxeBuilder withTags wcag2a wcag2aa wcag21aa
    - path: kapwa-client/src/tests/a11y/pages.test.ts
      provides: Per-page accessibility audits via Playwright
      contains: test.describe each page
    - path: kapwa-client/src/tests/a11y/components.test.ts
      provides: Per-component vitest-axe audits
      contains: toHaveNoViolations
    - path: kapwa-client/src/tests/pii/masking.test.ts
      provides: PII hook unit tests
    - path: kapwa-client/src/tests/sla/timer.test.ts
      provides: SLA timer calculation tests
  key_links:
    - from: kapwa-client/src/tests/a11y/axe-setup.ts
      to: @axe-core/playwright
      via: imports AxeBuilder and configures tags
    - from: kapwa-client/src/tests/a11y/components.test.ts
      to: vitest-axe
      via: imports axe function and toHaveNoViolations matcher
---

<objective>
Final accessibility audit and polishing: WCAG 2.1 AA color contrast audit (D-04), keyboard navigation audit (D-03), NVDA screen reader testing instructions (D-02), and comprehensive test suite creation for axe-core CI (D-01), PII masking, SLA timers, and bulk operations.

Purpose: This is the final phase's last plan -- the v1.1 UI/UX Overhaul capstone. Automated a11y testing ensures regressions are caught on every future PR. Manual testing instructions for NVDA and keyboard-only navigation provide the final human-verification layer. Unit tests for all new hooks/components provide ongoing quality assurance.

Output: Color contrast fixes in CSS, keyboard navigation improvements, E2E axe-core test suite, unit tests for all new Phase 12 hooks and components.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-accessibility-differentiators/12-CONTEXT.md
@.planning/phases/12-accessibility-differentiators/12-RESEARCH.md
@kapwa-client/src/index.css
@kapwa-client/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color contrast audit and CSS fixes</name>
  <files>
    kapwa-client/src/index.css
  </files>
  <action>
    Run contrast audit using browser DevTools (manual) and automated checks:

    Review existing CSS custom properties in @layer base :root and .dark:
    - Verify --accent (#0369A1) against --background (#F8FAFC): ratio approximately 5.1:1 (passes AA for body text 4.5:1)
    - Verify --muted-foreground (#64748B) against --background (#F8FAFC): ratio approximately 3.9:1 (FAILS 4.5:1 body, check if used for body text; acceptable for disabled/inactive elements per WCAG)
    - Verify --muted-foreground (#64748B) against --muted (#E8ECF1): ratio approximately 2.5:1 (accept if used only for decorative/disabled elements)
    - Verify --border (#E2E8F0) against --background (#F8FAFC): ratio ~1.2:1 (borders are not text, no contrast requirement)
    - Verify --destructive (#DC2626) against --background (#F8FAFC): ~5.7:1 (passes)

    In dark mode:
    - Verify --muted-foreground (#94A3B8) against --background (#0F172A): ~6.1:1 (passes)
    - Verify --accent (#38BDF8) against --background (#0F172A): ~5.8:1 (passes)
    - Verify --border (#334155) against --background (#0F172A): ~1.7:1 (borders only)

    If any body-text color fails 4.5:1 ratio per WCAG 1.4.3:
    - Adjust color value to meet minimum contrast
    - Keep within existing color palette (do not introduce new brand colors)
    - Document the change with a comment referencing WCAG SC 1.4.3

    Add a contrast audit comment section in index.css under a11y section noting the verified ratios for future reference.
    No structural CSS changes -- only color value adjustments if needed.
  </action>
  <verify>
    <automated>echo "Manual: Verify using browser DevTools contrast checker against WCAG 2.1 AA (4.5:1 body, 3:1 large text)"</automated>
  </verify>
  <done>All text colors meet WCAG 2.1 AA minimum contrast (4.5:1 body, 3:1 large text). Non-text/border elements accept lower ratios.</done>
</task>

<task type="auto">
  <name>Task 2: Keyboard navigation audit and final focus management</name>
  <files>
    kapwa-client/src/components/Topbar.tsx
    kapwa-client/src/components/Layout.tsx
  </files>
  <action>
    Audit keyboard navigation across all interactive components for WCAG 2.1.1 (Keyboard) and 2.4.3 (Focus Order):

    Review and fix if needed:
    - Verify Topbar quick action buttons have visible focus rings (focus-visible:ring-2 focus-visible:ring-ring)
    - Verify GlobalSearch popover handles Escape to close and focus returns to trigger (cmdk handles this natively, just verify)
    - Verify BulkActionBar buttons are in logical tab order (appear after table rows, before footer)
    - Verify BulkApproveDialog traps focus (shadcn Dialog handles this natively, just verify)
    - Verify SlaTimer/SlaTooltip is keyboard-accessible (tooltip on focus+blur, not just hover)
    - Verify SkipToContent is first focusable element on every page (already done in Plan 12-01 Task 3, verify no regressions)
    - Verify all icon-only buttons have accessible names (aria-label or aria-labelledby)

    Fix any issues found:
    - Add focus-visible styles where missing
    - Add aria-label to icon-only buttons
    - Ensure proper tabIndex (no positive tabIndex values, use 0 or -1 only)
    - Verify no focus traps in custom components (FocusTrap should only activate when active prop is true)

    Add manual testing instructions for NVDA (D-02):
    - In a verification task comment or checkpoint, document:
      - "Open NVDA screen reader"
      - "Navigate through Dashboard, Cases, Beneficiaries pages using Tab and arrow keys"
      - "Verify toast notifications are announced (aria-live)"
      - "Verify error boundary messages are announced (role=alert)"
      - "Verify DataTable row actions are accessible via keyboard"
  </action>
  <verify>
    <automated>echo "Manual: Tab through every page. All interactive elements must receive focus and activate on Enter/Space. NVDA test: all dynamic content announced."</automated>
  </verify>
  <done>All interactive elements keyboard-reachable with visible focus rings. NVDA manual test instructions documented. No focus traps.</done>
</task>

<task type="auto">
  <name>Task 3: Create axe-core E2E test suite and unit tests</name>
  <files>
    kapwa-client/src/tests/a11y/axe-setup.ts
    kapwa-client/src/tests/a11y/pages.test.ts
    kapwa-client/src/tests/a11y/components.test.ts
    kapwa-client/src/tests/pii/masking.test.ts
    kapwa-client/src/tests/sla/timer.test.ts
    kapwa-client/src/tests/bulk-actions/selection.test.ts
    kapwa-client/src/tests/search/global.test.ts
  </files>
  <action>
    Create test infrastructure following RESEARCH.md Validation Architecture and Pattern 1:

    kapwa-client/src/tests/a11y/axe-setup.ts:
    - Export shared AxeBuilder configuration with tags [wcag2a, wcag2aa, wcag21aa]
    - Export DEFAULT_INCLUDE = '#main-content'
    - Export DEFAULT_EXCLUDE = ['.third-party']

    kapwa-client/src/tests/a11y/components.test.ts (vitest-axe component-level):
    - Test PageShell has no violations
    - Test EmptyState (all 4 variants) has no violations
    - Test ErrorBoundary error state has no violations
    - Test SkipToContent has no violations
    - Test MaskedField has no violations
    - Test SlaTimer has no violations
    - Test BulkActionBar has no violations
    - Use axe from vitest-axe with toHaveNoViolations matcher
    - Import and render each component with minimal required props

    kapwa-client/src/tests/a11y/pages.test.ts (Playwright page-level):
    - Test Dashboard page has no violations
    - Test Cases page has no violations
    - Test Intake page has no violations
    - Test Beneficiaries page has no violations
    - Test Approvals page has no violations
    - Use AxeBuilder from axe-setup.ts for each
    - Include #main-content scope, exclude known third-party elements
    - Use page.goto + waitForLoadState('networkidle') before scan

    kapwa-client/src/tests/pii/masking.test.ts:
    - Test usePiiMasking shouldMask when consent is revoked (returns always masked)
    - Test usePiiMasking shouldMask when role is mayor/auditor/claimant (returns masked without consent issue)
    - Test usePiiMasking shouldMask is false for social_worker with active consent
    - Test getDisplayValue returns masked string when masked
    - Test getDisplayValue returns realValue when revealed
    - Test MaskedField renders Eye icon when masked
    - Test MaskedField shows value on reveal click
    - Mock useAuth and API calls with vi.mock
    - Use vi.useFakeTimers for 30s auto-mask timeout test

    kapwa-client/src/tests/sla/timer.test.ts:
    - Test elapsed display format (hours only, days+hours)
    - Test status is compliant when fractionUsed < 0.7
    - Test status is warning when 0.7 <= fractionUsed < 0.9
    - Test status is breached when fractionUsed >= 0.9
    - Test poll interval updates elapsed time
    - Use vi.useFakeTimers for interval control

    kapwa-client/src/tests/bulk-actions/selection.test.ts:
    - Test DataTable renders checkbox column when enableRowSelection is true
    - Test DataTable does NOT render checkbox column when enableRowSelection is false/unset
    - Test row selection state updates on checkbox click
    - Test select-all checkbox selects all visible rows
    - Test BulkActionBar appears when selectedCount > 0
    - Test BulkActionBar hidden when selectedCount is 0

    kapwa-client/src/tests/search/global.test.ts:
    - Test useDebouncedSearch debounces fetch calls
    - Test useDebouncedSearch skips fetch on empty query
    - Test useDebouncedSearch cancels pending request on new query
    - Test GlobalSearch opens/closes on Cmd+K
    - Test GlobalSearch shows results in dropdown

    IMPORTANT: Write each test file with proper imports and describe/it blocks. Tests should be runnable via npm test.
    Use vi.mock for external dependencies. Use @testing-library/react for component rendering.
    Do NOT modify existing test files -- only create new ones.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>All test files exist. npm test passes. axe-core component-level tests show zero violations for known components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Entire Phase 12 feature set: a11y infrastructure, PII masking, SLA timers, global search, quick actions, bulk operations, role-specific dashboards, accessibility audit fixes, and test suite.</what-built>
  <how-to-verify>
    1. Run the full test suite: cd kapwa-client && npm test -- --run
    2. Run the build: cd kapwa-client && npm run build
    3. Start the dev server: cd kapwa-client && npm run dev
    4. Navigate to /dashboard as each role and verify:
       - Social Worker/Admin: see existing stat cards + QuickActionPanel + SLA timer column in cases table
       - Claimant: see case timeline, service history, Access Card link
       - Mayor: aggregate stats, no PII
       - Auditor: audit log viewer, hash-chain status
       - Coordinator: barangay-scoped data
    5. Check topbar: New Intake + Approvals Queue buttons visible for social_worker, GlobalSearch present
    6. Test keyboard: Tab through entire app -- all interactive elements reachable with visible focus rings
    7. Run NVDA (if available): Tab navigation + verify dynamic content announcements
    8. Open browser DevTools contrast checker: verify text meets 4.5:1 ratio
    9. Test PII masking: view beneficiary as non-worker role -- fields show asterisks; click Eye to reveal
    10. Test bulk ops: navigate to approvals page, enable Select Mode, select 2+ cards, click Bulk Approve, verify confirmation dialog and progress toast
    11. Test SLA timer: open a case with SLA data -- verify color indicator (green/amber/red dot)
    12. Test global search: press Ctrl+K, type a name, verify results appear, select result to navigate
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code to application code | Tests import and exercise application components; no production data is used |
| axe-core scan to CI pipeline | Result determines PR pass/fail; false positives must be scoped via include/exclude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering | axe-core CI gate | accept | axe-core runs in CI against rendered output; uses include/exclude to scope to owned components (per RESEARCH.md Pitfall 1) |
| T-12-SC | Tampering | No new packages | accept | No package installs in this plan (deps installed in Plan 12-01) |
</threat_model>

<verification>
- npm test passes (all tests including new a11y/PII/SLA/bulk/search tests)
- npm run build passes
- grep confirms all test files exist
- Human checkpoint verifies interactive behavior across all 6 roles
</verification>

<success_criteria>
- [ ] Full test suite green: npm test -- --run
- [ ] Build passes: npm run build
- [ ] All 8 test files exist in tests/ directories
- [ ] axe-core component tests show zero violations
- [ ] Color contrast passes WCAG 2.1 AA (4.5:1 body, 3:1 large text)
- [ ] Keyboard navigation: every interactive element receives focus with visible rings
- [ ] Human checkpoint confirms all Phase 12 features working
- [ ] Phase 12 complete -- v1.1 UI/UX Overhaul milestone delivered
</success_criteria>