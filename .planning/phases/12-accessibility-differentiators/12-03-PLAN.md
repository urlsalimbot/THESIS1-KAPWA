---
phase: 12-accessibility-differentiators
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - kapwa-client/src/components/data-table/DataTable.tsx
  - kapwa-client/src/components/data-table/index.ts
  - kapwa-client/src/lib/api.ts
  - kapwa-client/src/pages/ApprovalPipelinePage.tsx
  - kapwa-client/src/components/bulk-actions/BulkActionBar.tsx
  - kapwa-client/src/components/bulk-actions/BulkApproveDialog.tsx
  - kapwa-client/src/components/bulk-actions/BulkProgressToast.tsx
  - kapwa-client/src/components/bulk-actions/index.ts
autonomous: true
requirements:
  - DIF-04
  - DIF-06
must_haves:
  truths:
    - "DataTable shows checkbox per row for selection"
    - "Select all N records checkbox selects all rows on current page"
    - "Selected row count displays in bulk action bar"
    - "Summary dialog before execution: count and preview of selected items"
    - "Sonner toast shows live progress during bulk operation"
    - "Per-item success/failure feedback in progress toast"
  artifacts:
    - path: kapwa-client/src/components/data-table/DataTable.tsx
      provides: Row selection with checkbox column added
      contains: enableRowSelection, getRowId, rowSelection state
    - path: kapwa-client/src/components/bulk-actions/BulkActionBar.tsx
      provides: Sticky toolbar with Bulk Approve/Reassign/Export buttons
      exports: BulkActionBar
    - path: kapwa-client/src/components/bulk-actions/BulkApproveDialog.tsx
      provides: Confirmation dialog with count and preview
      exports: BulkApproveDialog
    - path: kapwa-client/src/components/bulk-actions/BulkProgressToast.tsx
      provides: Live progress toast with per-item feedback
      exports: BulkProgressToast
  key_links:
    - from: kapwa-client/src/components/bulk-actions/BulkActionBar.tsx
      to: kapwa-client/src/components/bulk-actions/BulkApproveDialog.tsx
      via: Opens dialog on confirm click
    - from: kapwa-client/src/components/bulk-actions/BulkApproveDialog.tsx
      to: kapwa-client/src/components/bulk-actions/BulkProgressToast.tsx
      via: Calls showProgressToast on execute
    - from: kapwa-client/src/components/bulk-actions/BulkActionBar.tsx
      to: kapwa-client/src/lib/api.ts
      via: imports bulkApprove, bulkReassign, bulkExport
---

<objective>
Deliver bulk operations on DataTable rows: bulk approve, bulk reassign, bulk export (D-21). Implement checkbox selection (D-22), summary confirmation dialog (D-23), and Sonner progress toast with per-item feedback (D-24).

Purpose: Enable social workers and admins to process multiple cases (approve/reassign/export) in one action instead of one-by-one, significantly improving throughput for batch approval workflows.

Output: Enhanced DataTable with row selection, BulkActionBar, BulkApproveDialog, BulkProgressToast, and backend API functions.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-accessibility-differentiators/12-CONTEXT.md
@.planning/phases/12-accessibility-differentiators/12-RESEARCH.md
@kapwa-client/src/components/data-table/DataTable.tsx
@kapwa-client/src/components/data-table/index.ts
@kapwa-client/src/lib/api.ts
@kapwa-client/src/pages/ApprovalPipelinePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DataTable with row selection support</name>
  <files>
    kapwa-client/src/components/data-table/DataTable.tsx
    kapwa-client/src/components/data-table/index.ts
  </files>
  <action>
    Modify kapwa-client/src/components/data-table/DataTable.tsx:

    Add new props to DataTableProps (optional, preserve backward compat):
    - enableRowSelection (boolean, default false)
    - rowSelection (Record<string, boolean>)
    - onRowSelectionChange (updater function)
    - getRowId (function to return unique ID per row, default uses index)

    When enableRowSelection is true:
    - Pass rowSelection state, onRowSelectionChange, getRowId, enableRowSelection to useReactTable config
    - Add checkbox column as first column (id: select):
      - Header: Checkbox with indeterminate state for page select-all
      - Cell: Checkbox per row with disabled state for non-selectable rows
      - Assign aria-label on both header and cell checkboxes
    - Do NOT add sorting or hiding on the select column
    - TableRow data-state already handles selected styling via existing data-state={row.getIsSelected() && 'selected'}

    When enableRowSelection is false: behave exactly as before (no checkbox column, no selection state).

    Import Checkbox from @/components/ui/checkbox
    Export updated DataTableProps and DataTable (no breaking changes)

    Modify kapwa-client/src/components/data-table/index.ts:
    - Export new types: BulkAction, BulkActionHandler if needed
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>DataTable accepts enableRowSelection/rowSelection/onRowSelectionChange props without breaking existing usage. Checkbox column renders conditionally. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create bulk action components</name>
  <files>
    kapwa-client/src/components/bulk-actions/BulkActionBar.tsx
    kapwa-client/src/components/bulk-actions/BulkApproveDialog.tsx
    kapwa-client/src/components/bulk-actions/BulkProgressToast.tsx
    kapwa-client/src/components/bulk-actions/index.ts
  </files>
  <action>
    Create kapwa-client/src/components/bulk-actions/BulkActionBar.tsx:
    - Props: selectedCount (number), selectedIds (string[]), onApprove, onReassign, onExport, onClearSelection
    - Sticky bottom bar with selected count: N selected
    - Buttons: Approve (check icon), Reassign (user icon), Export (download icon), Clear (x icon)
    - Only visible when selectedCount > 0
    - Use shadcn Button variants: Approve=default, Reassign=secondary, Export=outline, Clear=ghost
    - Animates in/out with tailwind transition
    - Export BulkActionBar

    Create kapwa-client/src/components/bulk-actions/BulkApproveDialog.tsx:
    - Uses shadcn Dialog (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter)
    - Props: open, onOpenChange, selectedCount, selectedIds, onConfirm, title (default Approve Cases), description
    - Shows summary: You are about to approve {selectedCount} cases per D-23
    - Optional textarea for reason/notes
    - Confirm button with loading state
    - Cancel button
    - Export BulkApproveDialog

    Create kapwa-client/src/components/bulk-actions/BulkProgressToast.tsx:
    - Uses Sonner toast (toast.loading, toast.success, toast.error)
    - Export function showBulkProgress(selectedIds: string[], actionFn: (id: string) => Promise<void>):
      - Create toast with initial message Approving N cases...
      - Sequential loop: for each id call actionFn, update toast to Completed: M/N
      - On success per item: increment counter, update toast
      - On failure per item: toast.error with failed ID
      - On all complete: toast.success with total count or toast.warning with partial success
      - Following RESEARCH.md code example for bulk action handler pattern
    - Export showBulkProgress (function, not component)

    Create kapwa-client/src/components/bulk-actions/index.ts barrel export.
  </action>
  <verify>
    <automated>ls kapwa-client/src/components/bulk-actions/</automated>
  </verify>
  <done>BulkActionBar, BulkApproveDialog, showBulkProgress function, and barrel export exist.</done>
</task>

<task type="auto">
  <name>Task 3: Add bulk API functions and wire into ApprovalPipelinePage</name>
  <files>
    kapwa-client/src/lib/api.ts
    kapwa-client/src/pages/ApprovalPipelinePage.tsx
  </files>
  <action>
    Modify kapwa-client/src/lib/api.ts (add after existing approveCase function):
    - bulkApprove(ids: string[], signature?: string): POST /api/cases/bulk-approve with { ids, signature }
    - bulkReassign(ids: string[], newWorkerId: string): POST /api/cases/bulk-reassign with { ids, newWorkerId }
    - bulkExport(ids: string[], format: csv|pdf): POST /api/cases/bulk-export with { ids, format }
    - Each returns Promise with results array [{id, success, error?}]
    - Use apiFetch helper for auth headers

    Modify kapwa-client/src/pages/ApprovalPipelinePage.tsx (NOT the pipeline workflow, but keep its existing behavior for single-case approvals):
    - Import BulkActionBar, BulkApproveDialog, showBulkProgress from new components
    - Add selection state for bulk operations
    - Add row selection props to the pipeline's data display (if using DataTable, enable row selection; if using the existing card layout, the cards are not a DataTable and bulk ops apply at a different scope)
    - IMPORTANT: The existing ApprovalPipelinePage uses a card-based Kanban layout (not DataTable). Bulk operations D-21 specify bulk approve/reassign/export for case management pages. Add a separate bulk action toolbar below the Kanban or integrate into the page header with a select mode toggle.
    - Keep existing single-case approval workflow intact (do NOT remove or modify signature pad or individual approval buttons)
    - Add selectable mode toggle in page header: Select Mode button toggles checkbox on each Kanban card
    - When in select mode: show checkbox on each card, show BulkActionBar at bottom
    - Wire BulkApproveDialog + showBulkProgress for bulk approve action
  </action>
  <verify>
    <automated>grep -c 'bulkApprove\|bulkReassign\|bulkExport' kapwa-client/src/lib/api.ts</automated>
  </verify>
  <done>API functions bulkApprove, bulkReassign, bulkExport exist. ApprovalPipelinePage has select mode with BulkActionBar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bulk operation request to API | Selected row IDs sent to bulk endpoints |
| export request to server | Selected IDs and format sent for data export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Denial of Service | Bulk approve/reassign | mitigate | Client-side page size limit (agent discretion: max 100 items per bulk operation); backend enforces limit independently |
| T-12-07 | Information Disclosure | Bulk export | mitigate | D-13: exports masked by default. Unmasked export requires reason logged to audit trail (handled by backend) |
| T-12-08 | Tampering | Bulk reassign | mitigate | Backend validates target worker role and permissions per ABAC policy |
| T-12-SC | Tampering | No new packages | accept | No package installs in this plan |
</threat_model>

<verification>
- grep confirms bulkApprove, bulkReassign, bulkExport in api.ts
- ls confirms bulk-actions/ directory exists
- npm test passes
- npm run build succeeds
</verification>

<success_criteria>
- [ ] DataTable renders checkbox column when enableRowSelection=true
- [ ] BulkActionBar appears with selected count when rows selected
- [ ] BulkApproveDialog shows You are about to approve N cases
- [ ] showBulkProgress shows live toast: Completed: M/N
- [ ] build passes
</success_criteria>