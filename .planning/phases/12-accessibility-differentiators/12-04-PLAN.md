---
phase: 12-accessibility-differentiators
plan: 04
type: execute
wave: 2
depends_on: ['12-02']
files_modified:
  - kapwa-client/src/pages/DashboardPage.tsx
  - kapwa-client/src/routes.tsx
  - kapwa-client/src/components/dashboard/QuickActionPanel.tsx
  - kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx
  - kapwa-client/src/components/dashboard/widgets/MayorWidgets.tsx
  - kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx
  - kapwa-client/src/components/dashboard/widgets/CoordinatorWidgets.tsx
  - kapwa-client/src/components/dashboard/widgets/index.ts
autonomous: true
requirements:
  - ACC-03
must_haves:
  truths:
    - "Role-specific dashboard shows filtered widgets per role (D-05)"
    - "Claimant sees case status timeline, intervention history, Access Card view (D-06)"
    - "Mayor sees aggregate statistics, SLA compliance rates, no PII (D-07)"
    - "Auditor sees audit log viewer, hash-chain verification, consent ledger (D-08)"
    - "Coordinator sees barangay-scoped data (D-09)"
    - "Quick Action Panel on dashboard shows New Intake, Approvals, Search shortcuts"
  artifacts:
    - path: kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx
      provides: Case timeline, intervention history, Access Card link
      exports: ClaimantWidgets
    - path: kapwa-client/src/components/dashboard/widgets/MayorWidgets.tsx
      provides: Aggregate stats, SLA compliance chart, no PII
      exports: MayorWidgets
    - path: kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx
      provides: Audit log viewer, hash-chain verification status, consent ledger summary
      exports: AuditorWidgets
    - path: kapwa-client/src/components/dashboard/widgets/CoordinatorWidgets.tsx
      provides: Barangay-scoped stats, case search
      exports: CoordinatorWidgets
    - path: kapwa-client/src/components/dashboard/QuickActionPanel.tsx
      provides: Dashboard quick action shortcuts (D-19)
      exports: QuickActionPanel
  key_links:
    - from: kapwa-client/src/pages/DashboardPage.tsx
      to: kapwa-client/src/components/dashboard/QuickActionPanel.tsx
      via: renders QuickActionPanel on dashboard
    - from: kapwa-client/src/pages/DashboardPage.tsx
      to: kapwa-client/src/components/dashboard/widgets
      via: conditionally renders role-specific widget based on user.role
---

<objective>
Deliver role-specific dashboards from a single shared page (D-05) with widgets for all 6 roles: social worker (existing dashboard), admin (existing), claimant (D-06), mayor (D-07, no PII), auditor (D-08), and coordinator (D-09, barangay-scoped). Add QuickActionPanel on dashboard per D-19.

Purpose: Replace separate per-role dashboard pages with one unified DashboardPage that conditionally renders role-filtered widgets. This reduces duplication and lets social workers/admins see all role dashboards from one entry point.

Output: Role-specific widget components, QuickActionPanel, refactored DashboardPage.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-accessibility-differentiators/12-CONTEXT.md
@.planning/phases/12-accessibility-differentiators/12-RESEARCH.md
@kapwa-client/src/pages/DashboardPage.tsx
@kapwa-client/src/pages/ClaimantDashboardPage.tsx
@kapwa-client/src/pages/CoordinatorDashboardPage.tsx
@kapwa-client/src/pages/MayorReportsPage.tsx
@kapwa-client/src/pages/AuditorPage.tsx
@kapwa-client/src/routes.tsx
@kapwa-client/src/lib/nav-config.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create role-specific dashboard widgets</name>
  <files>
    kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx
    kapwa-client/src/components/dashboard/widgets/MayorWidgets.tsx
    kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx
    kapwa-client/src/components/dashboard/widgets/CoordinatorWidgets.tsx
    kapwa-client/src/components/dashboard/widgets/index.ts
  </files>
  <action>
    Create individually. Each widget is a self-contained component that fetches its own data via API and renders a card-sized section. All widgets use existing shadcn Card + CardContent, lucide icons, and PageShell-like header pattern. None include bulk operations or SLA timers directly (those are wired in Task 3).

    kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx:
    - Fetches case status, service history, consent data (reusing existing API calls from ClaimantDashboardPage.tsx)
    - Renders: Case Status card with colored badge, Service History list (type, date, amount, status), Access Card quick link
    - Read-only per D-06: no edit buttons, no forms
    - Uses PageShell title/description styling pattern
    - Exports ClaimantWidgets

    kapwa-client/src/components/dashboard/widgets/MayorWidgets.tsx:
    - Fetches aggregate reports from getMayorReports() API
    - Renders: aggregate stat cards (total cases, fund utilization, served today, unique households), case status distribution summary, SLA compliance rate
    - NO PII per D-07: no names, no IDs, no addresses, no phone numbers
    - Exports MayorWidgets

    kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx:
    - Shows hash-chain verification status (all valid / broken), consent ledger summary
    - Reuses existing verification patterns from AuditorPage.tsx
    - Renders: integrity status banner (green check or red x), table summary, re-verify button
    - Exports AuditorWidgets

    kapwa-client/src/components/dashboard/widgets/CoordinatorWidgets.tsx:
    - Fetches barangay-scoped data (consistent with ROL-02, D-09)
    - Renders: case count for their barangay, quick case search, recent entries summary
    - Exports CoordinatorWidgets

    kapwa-client/src/components/dashboard/widgets/index.ts: barrel export of all 4 widgets.
  </action>
  <verify>
    <automated>ls kapwa-client/src/components/dashboard/widgets/</automated>
  </verify>
  <done>All 4 role widgets exist with barrel export.</done>
</task>

<task type="auto">
  <name>Task 2: Create QuickActionPanel and refactor DashboardPage</name>
  <files>
    kapwa-client/src/components/dashboard/QuickActionPanel.tsx
    kapwa-client/src/pages/DashboardPage.tsx
  </files>
  <action>
    Create kapwa-client/src/components/dashboard/QuickActionPanel.tsx:
    - Dashboard quick action shortcuts per D-19
    - Props: none (uses useAuth for role checks)
    - Renders grid of 3 quick action cards:
      - New Intake (FilePlus icon, navigates to /intake) if role in [admin, social_worker, coordinator]
      - Approvals Queue (CheckSquare icon, navigates to /approvals) if role in [admin, social_worker]
      - New Beneficiary (UserPlus icon, navigates to /beneficiaries) if role in [admin, social_worker]
    - Each card: Card with large icon, title, description, onClick navigates
    - Grid: 3 columns on desktop, 1 on mobile
    - Exports QuickActionPanel

    Refactor kapwa-client/src/pages/DashboardPage.tsx:
    - Import useAuth from @/lib/auth-context (may already be indirect)
    - Import QuickActionPanel from @/components/dashboard/QuickActionPanel
    - Import role-specific widgets
    - Add role-conditional rendering after existing stat cards:
      - For social_worker/admin: keep existing dashboard content (stat cards + recent cases table)
      - For claimant: render ClaimantWidgets instead of existing general content
      - For mayor: render MayorWidgets instead of existing general content
      - For auditor: render AuditorWidgets instead of existing general content
      - For coordinator: render CoordinatorWidgets instead of existing general content
    - Add QuickActionPanel BEFORE the stat cards for all worker roles (admin, social_worker, coordinator)
    - Keep existing loading/empty states and PageShell wrapper pattern
    - Do NOT remove existing dashboard content for social_worker/admin roles
    - The /coordinator, /my-dashboard, /reports, /audit-logs routes will remain until Phase 12 completion verification determines if they should redirect or co-exist
  </action>
  <verify>
    <automated>grep -c 'QuickActionPanel\|ClaimantWidgets\|MayorWidgets\|AuditorWidgets\|CoordinatorWidgets' kapwa-client/src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>DashboardPage renders role-specific widgets based on user role. QuickActionPanel shown for worker roles.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SLA timers into dashboard and approval pipeline</name>
  <files>
    kapwa-client/src/pages/DashboardPage.tsx
    kapwa-client/src/routes.tsx
  </files>
  <action>
    In kapwa-client/src/pages/DashboardPage.tsx:
    - If the recentCases table is displayed (social_worker/admin), add SLA column to dashboardCaseColumns
    - Import SlaTimer from @/components/sla/SlaTimer
    - Add cell renderer that shows SlaTimer for each case:
      - Uses case.updatedAt as stageStartedAt proxy (actual stage data depends on backend)
      - Default slaHours from constant (will be configurable per D-16 once backend supports it)
      - When stageStartedAt and slaHours are available, show SlaTimer; otherwise show dash
    - Keep existing columns intact

    In kapwa-client/src/routes.tsx: no route changes needed — the existing /dashboard route serves all roles via the refactored DashboardPage. The existing role-specific routes (/coordinator, /my-dashboard, /reports, /audit-logs) co-exist for backward compatibility (future cleanup can redirect them to /dashboard).
  </action>
  <verify>
    <automated>grep -c 'SlaTimer' kapwa-client/src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>SLA timer column appears in dashboard recent cases table when data available. Routes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role check to widget rendering | Widget rendering depends on user.role from auth context |
| widget data API to display | Each widget fetches role-filtered data from backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Information Disclosure | Dashboard role gate | mitigate | Widget rendering is purely additive -- backend API still enforces ABAC for all data fetches. Frontend role check is UX convenience, not auth boundary |
| T-12-10 | Information Disclosure | Mayor widgets | mitigate | D-07 requires no PII in mayor view. Widgets only display aggregate stats; backend must not include PII in API response for mayor role |
| T-12-SC | Tampering | No new packages | accept | No package installs in this plan
</threat_model>

<verification>
- grep confirms all 5 widgets imported in DashboardPage.tsx
- SlaTimer column added to dashboard data table
- npm run build succeeds
</verification>

<success_criteria>
- [ ] DashboardPage shows social_worker/admin content unchanged (stat cards + recent cases)
- [ ] DashboardPage shows ClaimantWidgets for claimant role
- [ ] DashboardPage shows MayorWidgets (no PII) for mayor role
- [ ] DashboardPage shows AuditorWidgets for auditor role
- [ ] DashboardPage shows CoordinatorWidgets for coordinator role
- [ ] QuickActionPanel renders on dashboard for worker roles
- [ ] SLA timer column visible in cases table
- [ ] build passes
</success_criteria>