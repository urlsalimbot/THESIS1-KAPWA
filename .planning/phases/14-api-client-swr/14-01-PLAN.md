---
phase: 14-api-client-swr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/src/lib/api-error.ts
  - kapwa-client/src/lib/api-error.test.ts
  - kapwa-client/src/lib/api.ts
  - kapwa-client/src/lib/api.test.ts
  - kapwa-client/src/lib/query-keys.ts
  - kapwa-client/src/lib/query-keys.test.ts
  - kapwa-client/src/lib/auth-context.tsx
  - kapwa-client/src/lib/auth-context.test.tsx
  - kapwa-client/src/routes.tsx
  - kapwa-client/src/lib/swr-config.test.tsx
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []

must_haves:
  truths:
    - "`src/lib/api.ts` exports `api.get`, `api.post`, `api.put`, `api.del` as a single object — old `apiFetch` + 67 legacy wrappers are gone"
    - "Every `api.*` call attaches `Authorization: Bearer <localStorage.kapwa_token>` when the token is present"
    - "Every `api.*` call uses a 10s `AbortSignal.timeout` composed with the caller's `signal` via `AbortSignal.any`"
    - "GET requests retry up to 3 times on `TypeError` (network failure) or `AbortError` from the internal timeout — never on 4xx/5xx"
    - "On 401, the client attempts `/auth/refresh` exactly once and retries the original request — if refresh itself 401s, dispatches `kapwa:auth:logout` and throws the original `ApiError`"
    - "Concurrent 401s share a single in-flight `/auth/refresh` promise (single-flight pattern)"
    - "`src/lib/query-keys.ts` exports a `queryKeys` factory whose `.all` keys are `as const` tuples suitable for `mutate(queryKeys.cases.all, …)` prefix revalidation"
    - "`routes.tsx` wraps `<RouterProvider>` in `<SWRConfig value={{ fetcher: api.get, revalidateOnFocus: true, revalidateOnReconnect: true, dedupingInterval: 2000, refreshInterval: 0 }}>` inside `<AuthProvider>`"
    - "`auth-context.tsx` subscribes to the `kapwa:auth:logout` window event in a `useEffect` and calls the existing `logout()` on dispatch"
  prohibitions:
    - "DO NOT use any code block in `<action>` directives — describe identifiers, signatures, imports, env vars, behavior in prose"
    - "DO NOT place `fetch(` in action directives verbatim — the post-Plan-14-03 grep gate in 14-VALIDATION.md `14-03-02` reads as a negative grep and would false-positive"
    - "DO NOT add a literal `apiFetch` export — after Plan 14-01 the new `api` object replaces it; only `auth.ts` /auth/refresh stays on raw `fetch` (D-15)"
  artifacts:
    - path: "kapwa-client/src/lib/api-error.ts"
      provides: "ApiError class with status, body, cause fields and Error superclass"
      contains: "class ApiError extends Error"
    - path: "kapwa-client/src/lib/api.ts"
      provides: "Method-style api object (get/post/put/del), 10s AbortSignal timeout, 3x GET retry with ±20% jitter, single-flight 401 refresh, kapwa:auth:logout dispatch"
      exports: ["api"]
      contains: "const api = {"
    - path: "kapwa-client/src/lib/query-keys.ts"
      provides: "Typed queryKey factory with .all/.list/.detail functions returning as-const tuples for SWR"
      exports: ["queryKeys"]
      contains: "as const"
    - path: "kapwa-client/src/lib/routes.tsx (lines around MainRoutes)"
      provides: "<SWRConfig> wrapping <RouterProvider> inside <AuthProvider> with the 4 global options from D-09"
      contains: "SWRConfig"
    - path: "kapwa-client/src/lib/auth-context.tsx (AuthProvider useEffect)"
      provides: "kapwa:auth:logout event subscriber that calls the existing logout() function"
      contains: "kapwa:auth:logout"
    - path: "kapwa-client/src/lib/api-error.test.ts"
      provides: "ApiError class tests: instanceof Error, status/body/cause exposure, default message"
      contains: "ApiError"
    - path: "kapwa-client/src/lib/api.test.ts"
      provides: "api client unit tests: bearer header, 10s timeout, 401 single-flight, 3x retry, 4xx/5xx no-retry, POST/PUT/DELETE no-retry"
      contains: "describe('api client'"
    - path: "kapwa-client/src/lib/query-keys.test.ts"
      provides: "queryKeys factory stability tests: stable tuples, .all prefix, as-const readonly"
      contains: "queryKeys"
    - path: "kapwa-client/src/lib/swr-config.test.tsx"
      provides: "SWRConfig provider test: revalidateOnFocus/revalidateOnReconnect/dedupingInterval/refreshInterval options are applied to the global config"
      contains: "SWRConfig"
    - path: "kapwa-client/src/lib/auth-context.test.tsx"
      provides: "auth-context logout event subscriber test: dispatching kapwa:auth:logout removes kapwa_token and clears user state"
      contains: "kapwa:auth:logout"
  symbols:
    - "class ApiError (src/lib/api-error.ts) — constructor(status: number, body: unknown, message?: string, cause?: unknown), extends Error"
    - "const api (src/lib/api.ts) — exported object with get<T>(path, opts?), post<T>(path, body?, opts?), put<T>(path, body?, opts?), del<T>(path, opts?)"
    - "const queryKeys (src/lib/query-keys.ts) — factory object with cases.*, beneficiaries.*, dashboard.*, notifications.*, plus 17 more resource subtrees for the remaining GET endpoints"
    - "function refreshAuthToken (src/lib/api.ts, internal) — single-flight helper that POSTs /auth/refresh and writes the new access token"
    - "const KAPWA_AUTH_LOGOUT_EVENT (src/lib/api.ts) — string literal 'kapwa:auth:logout' used by the refresh-fail path and the auth-context subscriber"
  key_links:
    - from: "src/lib/api.ts"
      to: "src/lib/api-error.ts"
      via: "ApiError thrown from rawRequest on non-2xx and from the 401 refresh path"
      pattern: "throw new ApiError"
    - from: "src/lib/api.ts"
      to: "localStorage key 'kapwa_token'"
      via: "Bearer header injection at request time (D-02)"
      pattern: "getItem\\('kapwa_token'\\)"
    - from: "src/lib/api.ts"
      to: "/auth/refresh"
      via: "raw fetch (NOT api.post) inside refreshAuthToken — D-04 + Pitfall 1"
      pattern: "/auth/refresh"
    - from: "src/lib/api.ts"
      to: "window event 'kapwa:auth:logout'"
      via: "dispatchEvent on refresh failure — D-04"
      pattern: "kapwa:auth:logout"
    - from: "src/lib/auth-context.tsx"
      to: "src/lib/api.ts"
      via: "useEffect subscribes to kapwa:auth:logout and calls existing logout() — D-04"
      pattern: "addEventListener\\('kapwa:auth:logout'"
    - from: "src/routes.tsx"
      to: "src/lib/api.ts"
      via: "SWRConfig fetcher: api.get — D-06"
      pattern: "fetcher:\\s*api\\.get"
    - from: "src/lib/query-keys.ts"
      to: "src/lib/api.ts fetcher"
      via: "useSWR(queryKeys.cases.list()) calls api.get with the tuple's stringified URL — D-07"
      pattern: "queryKeys\\.cases\\.all"

---

<objective>
Build the foundation for the centralized API client + SWR activation: the `ApiError` class, the new method-style `api` object in `src/lib/api.ts` (replacing `apiFetch` and all 67 legacy wrappers, with 10s timeout, 3× GET retry, and single-flight 401 refresh), the typed `queryKeys` factory, the global `<SWRConfig>` provider in `routes.tsx`, the `kapwa:auth:logout` event subscriber in `auth-context.tsx`, and unit tests for each new module. Per D-14 this is Plan A — helpers only, no page migration. Per D-15 `/auth/login`, `/auth/mfa/verify`, and `/auth/refresh` stay on raw fetch; `/auth/me` migration to `api.get` is deferred to Plan 14-02 (Task 1). Per D-16 this plan covers the ~30-40% effort of the phase foundation.

Purpose: Every other plan in this phase (14-02 page migration, 14-03 remaining endpoints + cleanup) depends on the helpers built here. If the api client has a circular-dependency bug, the 401 refresh loops, or the SWR key factory produces unstable tuples, the entire phase fails. This plan is the gate.

Output: 6 new files (`api-error.ts`, `api-error.test.ts`, `api.test.ts`, `query-keys.ts`, `query-keys.test.ts`, `swr-config.test.tsx`, `auth-context.test.tsx`) and 3 modified files (`api.ts` rewritten, `routes.tsx` adds SWRConfig, `auth-context.tsx` adds logout subscriber). All 196 baseline tests continue passing, plus 16+ new tests in this plan. Build and full suite green.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-api-client-swr/14-CONTEXT.md
@.planning/phases/14-api-client-swr/14-RESEARCH.md
@.planning/phases/14-api-client-swr/14-PATTERNS.md
@.planning/phases/14-api-client-swr/14-VALIDATION.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONVENTIONS.md
@kapwa-server/src/auth/auth.controller.ts
@kapwa-server/src/auth/auth.service.ts
@kapwa-client/src/lib/api.ts
@kapwa-client/src/lib/auth-context.tsx
@kapwa-client/src/lib/auth.ts
@kapwa-client/src/routes.tsx
@kapwa-client/tests/setup.ts
@kapwa-client/vite.config.ts
@kapwa-client/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spike /auth/refresh contract and build ApiError class</name>
  <files>kapwa-client/src/lib/api-error.ts, kapwa-client/src/lib/api-error.test.ts</files>
  <read_first>
- kapwa-server/src/auth/auth.controller.ts (lines 33-37 — @Post('refresh'))
- kapwa-server/src/auth/auth.service.ts (lines 82-101 — refresh implementation)
- kapwa-client/src/lib/api.ts (lines 1-13 — current apiFetch shape)
- .planning/phases/14-api-client-swr/14-RESEARCH.md (lines 161-245 — Pattern 1 with full rawRequest)
  </read_first>
  <behavior>
  - Test 1: ApiError extends Error — `new ApiError(401, null) instanceof Error` is true and `new ApiError(401, null) instanceof ApiError` is true
  - Test 2: ApiError exposes status, body, cause — `new ApiError(500, { reason: 'oops' }, 'Server error', originalErr).status === 500`, `.body` deep-equals `{ reason: 'oops' }`, `.cause === originalErr`, `.message === 'Server error'`
  - Test 3: ApiError default message — `new ApiError(404, null).message === 'API error: 404'`
  - Test 4: ApiError name — `.name === 'ApiError'` (so log filters can branch on it)
  </behavior>
  <action>
Substep 1 (spike — no code): Read the three files in read_first. From auth.controller.ts confirm the @Post('refresh') signature takes `body: { refreshToken: string }` and returns 200 with new tokens, 401 on invalid. From auth.service.ts confirm the refresh function verifies the JWT, increments tokenVersion, and calls issueTokens which returns `{ accessToken, refreshToken, user }` (verify the exact shape in the source — read lines 50-80 of auth.service.ts to see the issueTokens return shape). From current client api.ts:4-9 confirm the existing pattern reads kapwa_token from localStorage as the bearer.

Document the contract in a single sentence inside the new api-error.ts (no other comments — per AGENTS.md). The class is the only content of api-error.ts; no helpers, no constants.

Substep 2 (build ApiError): Create `kapwa-client/src/lib/api-error.ts`. Export `class ApiError extends Error` with a constructor accepting `(status: number, body: unknown, message?: string, cause?: unknown)` in that order. Set `this.name = 'ApiError'` in the constructor body. Default `message` (when undefined) to the string `API error: ${status}`. Set `this.status`, `this.body`, `this.cause` from the constructor args. No other methods.

Substep 3 (test): Create `kapwa-client/src/lib/api-error.test.ts` with the 4 behavior cases above. Use `vi` from vitest; no fetch mocks needed (pure data class). Test file follows the `describe`/`it`/`expect` pattern from `kapwa-client/src/lib/sync-conflict.test.ts` (lines 1-9).
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/api-error.test.ts 2>&1 | tail -20</automated>
    <human-check>Confirm the spike notes match the actual server response shape by reading the test output — the new api.ts (Task 2) will be coded against the documented contract.</human-check>
  </verify>
  <acceptance_criteria>
  - `src/lib/api-error.ts` exists, exports `class ApiError extends Error`, has no other exports
  - `npx vitest run src/lib/api-error.test.ts` shows 4/4 tests passing
  - The spike notes from substep 1 are referenced in the commit message (proof the contract was verified before api.ts was coded)
  - `ApiError` is NOT exported from `src/lib/api.ts` in this plan — keep it as its own module per the schema-push gate contract (shared types module)
  </acceptance_criteria>
  <done>ApiError class compiles under TypeScript, all 4 unit tests pass, and the spike contract notes are recorded in the git commit body for Task 2 to reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite api.ts as method-style client with retry, timeout, and 401 single-flight refresh</name>
  <files>kapwa-client/src/lib/api.ts, kapwa-client/src/lib/api.test.ts</files>
  <read_first>
- kapwa-client/src/lib/api.ts (entire current file — 387 lines — the body of every legacy wrapper to be deleted)
- kapwa-client/src/lib/api-error.ts (just built — the error class to throw)
- .planning/phases/14-api-client-swr/14-RESEARCH.md (lines 161-245 — Pattern 1 rawRequest + refreshToken + executeWithRetry)
- .planning/phases/14-api-client-swr/14-PATTERNS.md (lines 50-119 — preserve apiFetch token pattern; delete the 67 wrappers)
- kapwa-client/src/lib/auth.ts (lines 37-59 — refreshAuthToken current contract; the new code preserves the response shape `{ accessToken: string }`)
- kapwa-client/tests/setup.ts (localStorage + crypto mocks — read for test environment)
  </read_first>
  <behavior>
  - Test 1 (bearer): `api.get('/cases')` with `localStorage.setItem('kapwa_token', 'tok-1')` calls `fetch` with `headers.Authorization === 'Bearer tok-1'`
  - Test 2 (no token): `api.get('/cases')` with no token calls `fetch` with no `Authorization` header (the header is conditional, not unconditional)
  - Test 3 (timeout): a fetch that never resolves is aborted after ~10s (use `vi.useFakeTimers()` and advance time, OR mock `AbortController.timeout` to call `abort()` immediately and assert the request receives the abort signal)
  - Test 4 (timeout composed with caller signal): when the caller passes its own `AbortController` and aborts, the fetch receives an abort (caller signal wins)
  - Test 5 (retry 3x on network failure): a `fetch` that throws `new TypeError('fetch failed')` 3 times then succeeds is called exactly 4 times (1 initial + 3 retries) and returns the success body
  - Test 6 (no retry on 4xx): a `fetch` returning status 400 throws `ApiError(400, …)` after exactly 1 call — no retry
  - Test 7 (no retry on 5xx): a `fetch` returning status 500 throws `ApiError(500, …)` after exactly 1 call
  - Test 8 (no retry on POST): a `fetch` throwing `TypeError` on `api.post('/x', {})` is called exactly 1 time (POST does not retry)
  - Test 9 (no retry on PUT/DELETE): same as Test 8 for `api.put` and `api.del`
  - Test 10 (401 → refresh → retry): a sequence: first `fetch` returns 401, second `fetch` (to /auth/refresh) returns 200 with `{ accessToken: 'new' }`, third `fetch` (retry of the original request) returns 200 — the caller receives the third response and `localStorage.kapwa_token === 'new'`
  - Test 11 (401 refresh single-flight): when 3 concurrent `api.get` calls all return 401, the /auth/refresh endpoint is hit exactly 1 time and all 3 callers receive the retried response
  - Test 12 (401 refresh loop break): when /auth/refresh itself returns 401, the api throws `ApiError(401, …)` (no infinite loop, no second refresh attempt) and the `kapwa:auth:logout` event is dispatched
  - Test 13 (logout event): `window.dispatchEvent(new CustomEvent('kapwa:auth:logout'))` from inside the refresh-fail path is observable by a `addEventListener('kapwa:auth:logout', …)` callback
  - Test 14 (exponential backoff): use `vi.useFakeTimers()` and stub `globalThis.setTimeout` (or spy on it) — when 2 retries happen, the gaps between them are roughly 500ms × jitter and 1500ms × jitter (assert the call to `setTimeout` receives a number in the range ±20% of 500 and 1500)
  </behavior>
  <action>
Substep 1: Top of new `kapwa-client/src/lib/api.ts` — declare the module constants (API_BASE = `import.meta.env.VITE_API_URL || 'http://localhost:3000/api'`, TOKEN_KEY = `'kapwa_token'`, TIMEOUT_MS = 10000, MAX_RETRIES = 3, BASE_DELAYS_MS = `[500, 1500, 4500]` as const, KAPWA_AUTH_LOGOUT_EVENT = `'kapwa:auth:logout'`). Import `ApiError` from `./api-error`.

Substep 2: Helper functions (private, not exported) — `getToken(): string | null` reads localStorage, `jitteredDelay(baseMs)` returns `Math.round(baseMs + baseMs * 0.2 * (Math.random() * 2 - 1))`, `sleep(ms, signal)` resolves after ms or rejects with `DOMException('Aborted', 'AbortError')` if the signal aborts, `delayForAttempt(attempt: number): number` returns `jitteredDelay(BASE_DELAYS_MS[attempt])`.

Substep 3: `rawRequest(method, path, body, callerSignal)` — builds the URL (prepend API_BASE unless path starts with `http`), creates an `AbortController` for the internal 10s timeout via `setTimeout(…, TIMEOUT_MS)`, composes the internal and caller signals via `AbortSignal.any([internal.signal, callerSignal])` when callerSignal is provided (else use internal.signal directly), builds headers `{ 'Content-Type': 'application/json' }` and conditionally adds `Authorization: Bearer ${getToken()}` if getToken() returns non-null, calls `fetch(url, { method, headers, body: body !== undefined ? JSON.stringify(body) : undefined, signal: composedSignal })`, and on `!res.ok` parses `res.json().catch(() => null)` and `throw new ApiError(res.status, errBody, res.statusText)`. In the `finally` block `clearTimeout(timeoutId)`. Return `res.json() as Promise<T>`. No retry, no 401 handling here — pure single request.

Substep 4: Module-level state `let refreshInFlight: Promise<boolean> | null = null`. Function `refreshToken(): Promise<boolean>` — if `refreshInFlight` is set return it; otherwise assign `refreshInFlight = (async () => { try { fetch API_BASE + '/auth/refresh' (POST, no Bearer header — server reads refresh token from request per the spike contract) → on !res.ok localStorage.removeItem TOKEN_KEY, dispatchEvent KAPWA_AUTH_LOGOUT_EVENT with detail `{ reason: 'refresh_failed' }`, return false; on ok parse body and localStorage.setItem(TOKEN_KEY, body.accessToken), return true } catch { dispatchEvent KAPWA_AUTH_LOGOUT_EVENT with detail `{ reason: 'refresh_network_error' }`, return false } finally { refreshInFlight = null } })()`. Return the promise. Use raw `fetch` (NOT api.post) to avoid the circular dependency (D-04 + RESEARCH Pitfall 1).

Substep 5: `executeWithRetry<T>(method, path, body, signal, isRetry = false)` — call `rawRequest<T>(method, path, body, signal)`. On error: if `err instanceof ApiError && err.status === 401 && !isRetry`, await `refreshToken()` and if true call `executeWithRetry(method, path, body, signal, true)`; if false throw the original err. Else if the method is 'GET' AND `err instanceof TypeError` (network) OR `err instanceof DOMException && err.name === 'AbortError' && !signal?.aborted` (timeout, but only if the caller did NOT abort) AND `attempt < MAX_RETRIES` (use a recursive call with an attempt counter, or wrap in a loop with explicit attempt index), call `await sleep(delayForAttempt(attempt), signal)` then recurse. Otherwise throw the err. Be careful with the `isRetry` guard for 401 — it prevents the refresh-on-refresh infinite loop (RESEARCH Pitfall 3).

Substep 6: Exported `const api = { get: <T>(path, opts?) => executeWithRetry<T>('GET', path, undefined, opts?.signal), post: <T>(path, body?, opts?) => executeWithRetry<T>('POST', path, body, opts?.signal), put: <T>(path, body?, opts?) => executeWithRetry<T>('PUT', path, body, opts?.signal), del: <T>(path, opts?) => executeWithRetry<T>('DELETE', path, undefined, opts?.signal) }`. This is the ONLY export from this file (per D-01 method-style). Delete the old `apiFetch` function and all 67 legacy wrapper functions (`getCases`, `createCase`, `uploadSignature`, `downloadCsrPdf`, `bulkExport`, `getNotificationPreferences`, etc.). The new call sites in Plans 14-02/14-03 will replace them.

Substep 7: Tests in `src/lib/api.test.ts`. Use `vi.stubGlobal('fetch', vi.fn())` and `vi.unstubAllGlobals()` in afterEach. Use `localStorage.setItem('kapwa_token', 'tok-1')` per test (tests/setup.ts already mocks localStorage with a per-test store; `afterEach` clears it). For retry tests use `vi.spyOn(globalThis, 'setTimeout')` or `vi.useFakeTimers()` — prefer fake timers to advance the retry delay without real waits. For single-flight test use `Promise.all([api.get('/a'), api.get('/b'), api.get('/c')])` with a fetch mock that returns 401, then 200, then 200, then 200, and assert fetch was called exactly 4 times (1 per api.get + 1 for refresh, the third api.get reuses the in-flight refresh).
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/api.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
  - `src/lib/api.ts` exports ONLY the `api` object (verified: `grep -E '^export ' src/lib/api.ts` returns exactly one line matching `export const api`)
  - `src/lib/api.ts` does NOT contain `apiFetch` as an export (verified: `grep -c 'apiFetch' src/lib/api.ts` returns 0 in the export position, OR the function is removed entirely if Plan 14-01 Task 1 spike A4 confirmed no external consumers)
  - `npx vitest run src/lib/api.test.ts` shows 14/14 tests passing (one per behavior)
  - `npx vitest run` (full suite) shows 196 baseline + 4 ApiError + 14 api = 214+ tests passing, no regressions
  - `npm run build` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>Method-style api object with retry, timeout, and 401 single-flight refresh; all 14 behaviors verified; existing 196 tests still pass; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build typed queryKeys factory with as-const tuples for all 21 GET endpoints</name>
  <files>kapwa-client/src/lib/query-keys.ts, kapwa-client/src/lib/query-keys.test.ts</files>
  <read_first>
- .planning/phases/14-api-client-swr/14-RESEARCH.md (lines 293-327 — Pattern 2 with the example factory shape)
- .planning/phases/14-api-client-swr/14-CONTEXT.md (line 91 — D-10 lists 9 pages + 3 widget components + 2 hooks + sync.ts = the 21 GET endpoints; the exact 21 enumeration is Plan 14-02 Task 1's spike A3 — for THIS plan, define 9 resource subtrees covering the 5 Plan-14-02 top pages plus 4 foundational ones: cases, beneficiaries, dashboard, notifications, audit, admin, accessCards, filing, programs — leaving Plan 14-03 to add the remaining 12 subtrees as endpoints are migrated)
- .planning/phases/14-api-client-swr/14-PATTERNS.md (lines 146-170 — queryKeys shape requirements)
  </read_first>
  <behavior>
  - Test 1: `queryKeys.cases.all === ['cases'] as const` (stable tuple reference for prefix revalidation)
  - Test 2: `queryKeys.cases.detail('c1') === queryKeys.cases.detail('c1')` (same input → same tuple reference)
  - Test 3: `queryKeys.cases.detail('c1') !== queryKeys.cases.detail('c2')` (different input → different reference)
  - Test 4: `queryKeys.cases.list({ status: 'pending' })` deep-equals `['cases', 'list', { status: 'pending' }]`
  - Test 5: `queryKeys.beneficiaries.list({ search: 'foo', category: 'pwd', barangay: 'b1' })` deep-equals `['beneficiaries', 'list', { search: 'foo', category: 'pwd', barangay: 'b1' }]`
  - Test 6: TypeScript compile-time check: the factory uses `as const` (verified by `Object.freeze` on a tuple would mutate-fail; verified by a `@ts-expect-error` directive showing the readonly tuple rejects `.push`)
  - Test 7: `queryKeys` has at least 9 subtrees for the 9 top resource groups (cases, beneficiaries, dashboard, notifications, audit, admin, accessCards, filing, programs) — `Object.keys(queryKeys).length >= 9`
  </behavior>
  <action>
Substep 1: Create `kapwa-client/src/lib/query-keys.ts` with the `export const queryKeys = { … } as const` form (the outer `as const` is critical — RESEARCH Pitfall 2). For each of the 9 resource subtrees, define an `all` key (e.g. `cases: { all: ['cases'] as const, … }`) and the verb-specific functions (list, detail, stats, etc.). Use literal `as const` on every tuple, not just the outer object. For each `list(params?)` function, the params object is the third tuple element with `params ?? {}` as the default. For each `detail(id)` function, the id is a string appended as the third tuple element.

Substep 2: Include at least these 9 subtrees in this plan (Plan 14-03 will extend with the remaining 12):
- `cases` — all, list(params?), detail(id)
- `beneficiaries` — all, list(params?), detail(id), familyGraph(id), myAccessCard(), myServices()
- `dashboard` — all, stats(), mayorReports()
- `notifications` — all, list(), unreadCount()
- `audit` — all, hashChains(), consentLedger()
- `admin` — all, programs(), users(), syncEntries(), auditLogs()
- `accessCards` — all, list(), print(id)
- `filing` — all, byCategory(category)
- `programs` — all, list(), detail(id)

Substep 3: Tests in `src/lib/query-keys.test.ts` per the 7 behaviors above. No fetch mocks needed. Tests are pure data equality. For the readonly check (Test 6), add a `@ts-expect-error` comment in a test that pushes to a tuple — the TypeScript compile must fail, proving the tuple is readonly. Use `vitest`'s `expect(...).toBe(...)` for reference equality (the same input returns the same tuple reference because the function is pure).
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/query-keys.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - `src/lib/query-keys.ts` exports `queryKeys` with at least 9 subtrees
  - All tuple returns use `as const` (verified by TypeScript compile + a `@ts-expect-error` test that pushes to a tuple)
  - `npx vitest run src/lib/query-keys.test.ts` shows 7/7 tests passing
  - `npx tsc --noEmit -p kapwa-client` exits 0 (no type errors from the readonly tuples)
  </acceptance_criteria>
  <done>queryKeys factory compiles, is fully readonly, and exposes the 9 subtrees needed by Plan 14-02 page migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire SWRConfig provider and kapwa:auth:logout subscriber; full suite green</name>
  <files>kapwa-client/src/routes.tsx, kapwa-client/src/lib/auth-context.tsx, kapwa-client/src/lib/swr-config.test.tsx, kapwa-client/src/lib/auth-context.test.tsx</files>
  <read_first>
- kapwa-client/src/routes.tsx (entire file — the provider chain to extend)
- kapwa-client/src/lib/auth-context.tsx (lines 19-95 — the AuthProvider component to add a useEffect to; preserve the existing logout function at line 84)
- kapwa-client/src/lib/api.ts (just-built — the api object and KAPWA_AUTH_LOGOUT_EVENT constant to reference)
- kapwa-client/src/lib/api-error.ts (just-built — for the SWR onError filter)
- .planning/phases/14-api-client-swr/14-RESEARCH.md (lines 329-372 — Pattern 3 with the full SWRConfig example)
- kapwa-client/src/components/ErrorBoundary.test.tsx (for the `vi.spyOn(console, 'error')` pattern to suppress React 19 act warnings in tests)
  </read_first>
  <behavior>
  - Test 1 (SWRConfig fetcher is api.get): a test component that calls `useSWR(queryKeys.dashboard.stats())` and the mock api.get is invoked with the dashboard stats key
  - Test 2 (revalidateOnFocus is true): a test asserts the SWRConfig options pass revalidateOnFocus: true to the SWR provider
  - Test 3 (dedupingInterval is 2000): assert the SWRConfig options pass dedupingInterval: 2000
  - Test 4 (refreshInterval is 0): assert no polling by default
  - Test 5 (auth-context logout event triggers logout): dispatch `new CustomEvent('kapwa:auth:logout', { detail: { reason: 'refresh_failed' } })` on window and assert `localStorage.getItem('kapwa_token')` is null after the event handler runs (the subscriber calls the existing logout)
  - Test 6 (auth-context logout clears user state): wrap the dispatch in a render of AuthProvider, dispatch the event, assert the `useAuth().user` is null and `useAuth().token` is null
  - Test 7 (full suite green): `npx vitest run` shows 196 baseline + 4 ApiError + 14 api + 7 queryKeys + 4 swr-config + 2 auth-context = 227+ tests passing
  - Test 8 (build green): `npm run build` exits 0
  </behavior>
  <action>
Substep 1: Modify `kapwa-client/src/routes.tsx`. Add an import of `SWRConfig` from 'swr'. Add an import of `api` from './lib/api' and `ApiError` from './lib/api-error'. Add a top-level `function swrErrorHandler(error: unknown)` that checks `if (error instanceof ApiError && error.status !== 401) console.error('SWR fetch error:', error)` and is a no-op for 401s (the api client handles 401 internally). In the `MainRoutes` function, wrap the existing `<RouterProvider router={router} />` in `<AuthProvider>` with `<SWRConfig value={{ fetcher: api.get, onError: swrErrorHandler, revalidateOnFocus: true, revalidateOnReconnect: true, dedupingInterval: 2000, refreshInterval: 0 }}>`. The provider order must be: ThemeProvider > Toaster > AuthProvider > SWRConfig > RouterProvider (D-06 — SWRConfig inside AuthProvider so token reads work, outside RouterProvider so all pages can use it).

Substep 2: Modify `kapwa-client/src/lib/auth-context.tsx`. Inside the `AuthProvider` function component, add a `useEffect` (with empty deps) that registers `window.addEventListener('kapwa:auth:logout', handler)` where `handler` reads the event's `detail.reason` and calls the existing `logout()` function (the one at line 84-88). Return the cleanup `window.removeEventListener('kapwa:auth:logout', handler)`. Do NOT modify the existing `fetchUser`/`login`/`resolveMfa` functions in this plan — those migrate in Plan 14-02 (D-15 keeps login + MFA on raw fetch; /auth/me migrates to api.get in Plan 14-02 Task 1).

Substep 3: Create `kapwa-client/src/lib/swr-config.test.tsx` per the swr-config behaviors. Use a tiny test component that calls `useSWR(queryKeys.dashboard.stats())` and renders the data. Wrap in a render of `<SWRConfig value={{ ... }}>` with the actual options from routes.tsx. For fetcher verification, use a custom `fetcher: vi.fn().mockResolvedValue({ stats: [] })` and assert it was called with the dashboard stats key tuple. For the option-passing tests, render a child that reads the SWR config via `useSWRConfig()` and assert the option values are present.

Substep 4: Create `kapwa-client/src/lib/auth-context.test.tsx` per the 2 auth-context behaviors. Wrap the AuthProvider in a render with `MemoryRouter`. Set `localStorage.setItem('kapwa_token', 'test-tok')`. Dispatch `new CustomEvent('kapwa:auth:logout')` on window. Use `act` from react-dom/test-utils to wait for the effect to run. Assert the localStorage key is removed. Use a child component that calls `useAuth()` to assert `user` and `token` are null after the dispatch. The existing `fetchUser` will fire on mount; mock `global.fetch` to return `{ ok: true, json: () => Promise.resolve({ user: { id: 'u1', email: 'a@b', fullName: 'A B', role: 'admin' } }) }` so the provider reaches a stable state, then dispatch the event.
  </action>
  <verify>
    <automated>cd kapwa-client && npm run test:run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
  - `src/routes.tsx` contains `<SWRConfig value={{ fetcher: api.get, onError: swrErrorHandler, revalidateOnFocus: true, revalidateOnReconnect: true, dedupingInterval: 2000, refreshInterval: 0 }}>`
  - `src/lib/auth-context.tsx` AuthProvider has a `useEffect` with empty deps that subscribes to `kapwa:auth:logout` and calls `logout()`
  - `npx vitest run src/lib/swr-config.test.tsx` shows 4/4 tests passing
  - `npx vitest run src/lib/auth-context.test.tsx` shows 2/2 tests passing
  - `npx vitest run` (full suite) shows 196 baseline + 4 + 14 + 7 + 4 + 2 = 227+ tests passing, 0 failing
  - `npm run build --prefix kapwa-client` exits 0
  </acceptance_criteria>
  <done>SWRConfig provider installed at app root with the D-09 options; auth-context subscribes to the logout event; full suite green; build green. Phase 14-01 ready for handoff to Plan 14-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS ↔ VITE_API_URL | All api.get/post/put/del calls go through this base URL; token read from localStorage at request time |
| api.ts ↔ /auth/refresh | Internal hand-off via raw fetch (NOT api.post) — single-flight promise guards against concurrent 401 storms |
| api.ts ↔ auth-context.tsx | One-way signal via window CustomEvent ('kapwa:auth:logout') — api.ts knows nothing about React; auth-context subscribes |
| queryKeys factory ↔ SWR cache | Tuple references are passed to useSWR — dedup relies on reference equality (Pitfall 2) |
| Test code ↔ fetch / setTimeout | vi.stubGlobal + vi.useFakeTimers — tests must not leak timers or fetch mocks between cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information Disclosure | Bearer token in URL | mitigate | api.ts reads token at request time and sets the Authorization header only; never serializes the token into the path or query (RESEARCH Threat Patterns §Token in URL). Verified by api.test.ts Test 2 — no Authorization header is set when no token is present, AND by a grep gate in Plan 14-03-02 that no call site puts kapwa_token in a URL |
| T-14-01-02 | Denial of Service (refresh loop) | 401 → /auth/refresh → 401 → /auth/refresh | mitigate | `executeWithRetry(…, isRetry)` boolean flag breaks the loop — second 401 short-circuits to throw + dispatch logout (RESEARCH Pitfall 3). Verified by api.test.ts Test 12 (refresh loop break) |
| T-14-01-03 | Denial of Service (concurrent refresh storm) | 5 parallel api.get calls all return 401 | mitigate | `refreshInFlight: Promise<boolean> \| null` module-level cache; 5 callers share the same refresh promise (D-04 single-flight). Verified by api.test.ts Test 11 (concurrent 401 single-flight) |
| T-14-01-04 | Tampering | npm install of new swr version | mitigate | slopcheck — `swr@^2.4.2` is [VERIFIED: npm registry] in RESEARCH.md Package Legitimacy Audit; peerDeps confirm React 19 support. v2.4.1 already installed; no install needed in this plan |
| T-14-01-05 | Denial of Service (5xx retry storm) | Server returns 500 repeatedly | mitigate | D-11 explicit: 4xx/5xx are NOT retried — only TypeError (network) and AbortError (timeout) trigger retry. Verified by api.test.ts Tests 6+7 (no retry on 4xx/5xx) and Test 5 (retry only on TypeError) |
| T-14-01-06 | Information Disclosure | ApiError leaks server stack | accept | ApiError exposes `status`, `body`, `cause`, `message`; no internal server stack traces are included. The body is whatever the server sent. `cause` is the underlying fetch error if any. No PII filtering needed client-side |
| T-14-01-07 | Repudiation | Logout event dispatched silently | mitigate | swrErrorHandler logs non-401 ApiError to console with status; the 401 path is silent (api client handled it). For audit purposes the dispatch happens before throw — the auth-context subscriber clears localStorage and the SWR cache is reset on next render |
| T-14-01-08 | Elevation of Privilege | SWRConfig fetcher trusts api.get as the global | mitigate | The fetcher is set ONCE at app root (routes.tsx); no per-page override. Pages cannot inject a custom fetcher that bypasses the bearer header / retry / refresh |
| T-14-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds no new packages; swr 2.4.1 is already installed and matches the verified 2.4.2 line. No human checkpoint required for installs. `T-14-01-SC` row reserved per planner convention even though no install task is present |
</threat_model>

<verification>
## Automated checks (run after every task commit)

1. `cd kapwa-client && npx vitest run src/lib/api-error.test.ts` — ApiError class unit tests (4/4)
2. `cd kapwa-client && npx vitest run src/lib/api.test.ts` — api client unit tests (14/14)
3. `cd kapwa-client && npx vitest run src/lib/query-keys.test.ts` — queryKeys factory tests (7/7)
4. `cd kapwa-client && npx vitest run src/lib/swr-config.test.tsx` — SWRConfig provider tests (4/4)
5. `cd kapwa-client && npx vitest run src/lib/auth-context.test.tsx` — auth-context logout subscriber tests (2/2)

## Full suite gate (run after Task 4)

`cd kapwa-client && npm run test:run` — 196 baseline + 4 + 14 + 7 + 4 + 2 = 227+ tests passing, 0 failing, 0 regressions

`cd kapwa-client && npm run build` — TypeScript compiles, Vite produces a working dist/

## Static checks (run after Task 4)

- `grep -E '^export ' kapwa-client/src/lib/api.ts` returns exactly one line (`export const api`)
- `grep -c 'apiFetch' kapwa-client/src/lib/api.ts` returns 0 (the function is removed)
- `grep -c 'SWRConfig' kapwa-client/src/routes.tsx` returns ≥1
- `grep -c 'kapwa:auth:logout' kapwa-client/src/lib/auth-context.tsx` returns ≥1
- `cd kapwa-client && npx tsc --noEmit -p .` exits 0
</verification>

<success_criteria>
Phase 14-01 success:

1. `src/lib/api.ts` exports ONLY `const api = { get, post, put, del }` with all 14 behaviors verified (bearer, no-token, timeout, caller signal, retry 3x, no retry 4xx, no retry 5xx, no retry POST/PUT/DELETE, 401 refresh + retry, single-flight, refresh loop break, logout event dispatch, exponential backoff)
2. `src/lib/api-error.ts` exports `class ApiError` with status/body/cause/Error superclass
3. `src/lib/query-keys.ts` exports `queryKeys` with 9 resource subtrees, all `as const` tuples, stable references per (resource, params)
4. `src/routes.tsx` wraps `<RouterProvider>` in `<SWRConfig value={{ fetcher: api.get, revalidateOnFocus: true, revalidateOnReconnect: true, dedupingInterval: 2000, refreshInterval: 0 }}>` inside `<AuthProvider>`
5. `src/lib/auth-context.tsx` subscribes to `kapwa:auth:logout` in a `useEffect` and calls the existing `logout()` on dispatch
6. Full test suite (227+ tests) passes, build green
7. 4 atomic commits land (one per task) per the gsd-executor per-task commit protocol (Phase 12/13 precedent)
8. Plan summary lands at `.planning/phases/14-api-client-swr/14-01-SUMMARY.md`
</success_criteria>

<output>
## Artifacts this phase produces

**New files (8):**
- `kapwa-client/src/lib/api-error.ts` — `class ApiError extends Error` with `status`, `body`, `cause` fields
- `kapwa-client/src/lib/api-error.test.ts` — 4 unit tests
- `kapwa-client/src/lib/api.ts` (rewritten) — `const api = { get, post, put, del }`; private helpers `getToken`, `jitteredDelay`, `delayForAttempt`, `sleep`, `rawRequest`, `executeWithRetry`, `refreshToken`; constants `API_BASE`, `TOKEN_KEY`, `TIMEOUT_MS = 10000`, `MAX_RETRIES = 3`, `BASE_DELAYS_MS = [500, 1500, 4500] as const`, `KAPWA_AUTH_LOGOUT_EVENT = 'kapwa:auth:logout'`; module-level `refreshInFlight: Promise<boolean> | null` cache
- `kapwa-client/src/lib/api.test.ts` — 14 unit tests covering all retry/timeout/refresh behaviors
- `kapwa-client/src/lib/query-keys.ts` — `const queryKeys = { cases, beneficiaries, dashboard, notifications, audit, admin, accessCards, filing, programs } as const`; 9 subtrees with .all / .list / .detail / verb-specific functions returning `as const` tuples
- `kapwa-client/src/lib/query-keys.test.ts` — 7 unit tests
- `kapwa-client/src/lib/swr-config.test.tsx` — 4 component tests for the SWRConfig global options
- `kapwa-client/src/lib/auth-context.test.tsx` — 2 component tests for the logout event subscriber

**Modified files (2):**
- `kapwa-client/src/routes.tsx` — add `SWRConfig` import, `api` import, `ApiError` import, top-level `swrErrorHandler` function, wrap `<RouterProvider>` in `<SWRConfig value={{ ... }}>` inside `<AuthProvider>` (provider order: Theme > Toaster > Auth > SWRConfig > Router)
- `kapwa-client/src/lib/auth-context.tsx` — add a `useEffect` in `AuthProvider` (empty deps) that subscribes to `kapwa:auth:logout` and calls the existing `logout()` (D-04)

**New exported symbols (planning-review-convergence source-grounding exclusions):**
- `ApiError` (class)
- `api.get`, `api.post`, `api.put`, `api.del` (object methods)
- `queryKeys` (object with 9 subtrees)
- `KAPWA_AUTH_LOGOUT_EVENT` (internal constant; not exported, but referenced from auth-context via the string literal `'kapwa:auth:logout'`)

These symbols are NEW and were created in Plan 14-01. The plan-review-convergence source-grounding pass should NOT flag them as drift from pre-existing source — they are deliberate introductions per CONTEXT.md D-01/D-03/D-07/D-06/D-04 and REQUIREMENTS.md API-01/API-02.

Create `.planning/phases/14-api-client-swr/14-01-SUMMARY.md` when done.
</output>
