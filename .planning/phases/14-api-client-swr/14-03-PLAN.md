---
phase: 14-api-client-swr
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - kapwa-client/src/lib/sync.ts
  - kapwa-client/src/lib/api.ts
  - kapwa-client/src/hooks/useDebouncedSearch.ts
  - kapwa-client/src/hooks/usePiiMasking.ts
  - kapwa-client/src/components/NotificationsDropdown.tsx
  - kapwa-client/src/components/ReportsExportButton.tsx
  - kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx
  - kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx
  - kapwa-client/src/pages/CaseTrackerPage.tsx
  - kapwa-client/src/pages/CaseTrackerPage.test.tsx
  - kapwa-client/src/pages/ClaimantDashboardPage.tsx
  - kapwa-client/src/pages/ClaimantDashboardPage.test.tsx
  - kapwa-client/src/pages/MessagesPage.tsx
  - kapwa-client/src/pages/MessagesPage.test.tsx
  - kapwa-client/src/pages/AccessCardPage.tsx
  - kapwa-client/src/pages/AccessCardPage.test.tsx
  - kapwa-client/src/pages/ApprovalPipelinePage.tsx
  - kapwa-client/src/pages/AuditorPage.tsx
  - kapwa-client/src/pages/MyAccessCardPage.tsx
  - kapwa-client/src/pages/RegisterPage.tsx
  - kapwa-client/src/pages/RegisterPage.test.tsx
  - kapwa-client/src/pages/InterventionsPage.tsx
  - kapwa-client/src/pages/InterventionsPage.test.tsx
  - kapwa-client/src/pages/IrfPage.tsx
  - kapwa-client/src/pages/IrfPage.test.tsx
  - kapwa-client/src/pages/IntakePage.tsx
  - kapwa-client/src/pages/IntakePage.test.tsx
  - kapwa-client/src/pages/CsrPage.tsx
  - kapwa-client/src/pages/CsrPage.test.tsx
  - kapwa-client/src/pages/MayorReportsPage.tsx
  - kapwa-client/src/pages/MfaSetupPage.tsx
  - kapwa-client/src/pages/MfaSetupPage.test.tsx
  - kapwa-client/src/pages/BeneficiaryViewPage.tsx
  - kapwa-client/src/pages/BeneficiaryViewPage.test.tsx
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []

must_haves:
  truths:
    - "sync.ts calls api.post for /sync/v1, /sync/pull, /sync/conflicts/:id/resolve — the 3 raw fetch sites are gone; the local API_URL constant and the getToken() helper are deleted"
    - "useDebouncedSearch and usePiiMasking hooks use the new api client (api.get / api.post) and the queryKeys factory; the raw fetch + localStorage token read in each is gone"
    - "NotificationsDropdown uses useSWR(queryKeys.notifications.list()) + useSWR(queryKeys.notifications.unreadCount()) with refreshInterval 30s + 2 useSWRMutation hooks (markRead, markAllRead) with optimisticData and mutate(queryKeys.notifications.unreadCount()) revalidation"
    - "AuditorWidgets and ClaimantWidgets use useSWR per their endpoint — 2 useSWR in AuditorWidgets (hashChains + consentLedger), 1 useSWR in ClaimantWidgets (myServices); the existing Promise.all pattern in AuditorWidgets becomes 2 separate useSWR hooks with their own loading states"
    - "ReportsExportButton keeps raw fetch (1 site — blob download with custom URL construction); the URL is built from the inline API base or via a shared constant from src/lib/api.ts"
    - "CaseTrackerPage, ClaimantDashboardPage, MessagesPage, AccessCardPage, ApprovalPipelinePage, AuditorPage, MyAccessCardPage, RegisterPage, InterventionsPage, IrfPage, IntakePage, CsrPage, MayorReportsPage, MfaSetupPage, BeneficiaryViewPage all migrate to useSWR + api methods; the 5+ remaining tests are updated to mock the method-style api"
    - "src/lib/api.ts loses all 67 legacy wrapper functions (getCases, createCase, getDashboard, getBeneficiaries, getAuditConsentLedger, verifyHashChains, getCsrRecords, getIrfRecords, etc.) — only the api object + ApiError (re-exported from api-error) + the URL/path constants remain"
    - "The grep gate `grep -rn 'fetch(' kapwa-client/src/ --include='*.ts' --include='*.tsx' | grep -v 'src/__tests__/' | grep -v 'src/lib/api.ts' | grep -v 'src/lib/auth.ts' | grep -v 'src/lib/auth-context.tsx' | grep -v 'FormData' | grep -v 'blob'` returns 0 matches (only the carve-outs at auth.ts, auth-context.tsx /auth/login + /auth/mfa/verify, and the FormData + blob flows in ReportsExportButton + FilingPage remain on raw fetch)"
    - "Full test suite (250+ tests) passes; npm run build exits 0; plan summary lands at 14-03-SUMMARY.md"
  prohibitions:
    - "DO NOT migrate auth.ts /auth/refresh to api.post — it is the 401 refresh path and would create a circular dependency (D-15)"
    - "DO NOT migrate auth-context.tsx /auth/login, /auth/mfa/verify, or /auth/me to api.post — login + MFA are pre-auth (no Bearer header); /auth/me was already migrated in Plan 14-02 Task 1"
    - "DO NOT migrate the FormData upload in any of the migrated files (sync.ts, page uploads, etc.) — FormData flows stay on raw fetch (D-10 deferred)"
    - "DO NOT migrate the blob download in ReportsExportButton or any other blob/CSV download to api.get — the api client only handles JSON responses; blob flows stay on raw fetch (D-10 deferred)"
    - "DO NOT place a fenced code block inside any <action> directive — describe identifiers, signatures, hooks, query keys, and behavior in prose only"
    - "DO NOT leave any reference to a deleted wrapper function (getCases, getDashboard, getBeneficiaries, etc.) in any file under kapwa-client/src/ — the grep gate enforces this"
    - "DO NOT remove or modify the api object, the raw fetch in auth.ts, or the kapwa:auth:logout event handling — these are part of Plan 14-01's contract"
  artifacts:
    - path: "kapwa-client/src/lib/sync.ts (3 fetch sites)"
      provides: "sendBatch + pullFromServer + resolveConflictRemotely use api.post"
      contains: "api.post"
    - path: "kapwa-client/src/lib/api.ts (after cleanup)"
      provides: "ONLY the api object + a re-export of ApiError + URL constants — all 67 wrapper functions are deleted"
      contains: "const api = {"
    - path: "kapwa-client/src/hooks/useDebouncedSearch.ts (hook body)"
      provides: "useSWR with debounced key + useDebouncedSearch returns { results, loading }"
      contains: "useSWR"
    - path: "kapwa-client/src/hooks/usePiiMasking.ts (revealField body)"
      provides: "api.post for /beneficiaries/:id/audit/unmask"
      contains: "api.post"
    - path: "kapwa-client/src/components/NotificationsDropdown.tsx (component body)"
      provides: "useSWR + useSWRMutation for notifications"
      contains: "useSWR"
    - path: "kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx (component body)"
      provides: "2 useSWR hooks (hashChains + consentLedger)"
      contains: "useSWR"
    - path: "kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx (component body)"
      provides: "1 useSWR hook (myServices)"
      contains: "useSWR"
    - path: "kapwa-client/src/components/ReportsExportButton.tsx (raw fetch site)"
      provides: "Raw fetch remains (blob download) — URL from inline VITE_API_URL or shared constant"
      contains: "URL.createObjectURL"
    - path: "kapwa-client/src/pages/CaseTrackerPage.tsx (3 fetch sites)"
      provides: "useSWR for daily + stats + list"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/ClaimantDashboardPage.tsx (2 fetch sites)"
      provides: "useSWR for myServices + myConsent"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/MessagesPage.tsx (4 fetch sites)"
      provides: "useSWR for conversations + /auth/me + conversation + read mutation"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/AccessCardPage.tsx (2 fetch sites)"
      provides: "useSWR for list + log"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/ApprovalPipelinePage.tsx (FormData uploads — keep raw)"
      provides: "Raw fetch remains for FormData uploads (D-10 deferred); JSON data uses api"
      contains: "FormData"
    - path: "kapwa-client/src/pages/AuditorPage.tsx (4 fetch sites)"
      provides: "useSWR for verify-all + consent-ledger + re-verify mutation + loadLedger"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/MyAccessCardPage.tsx (1 fetch site)"
      provides: "useSWR for my-access-card"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/RegisterPage.tsx (1 fetch site)"
      provides: "api.post for /auth/register"
      contains: "api.post"
    - path: "kapwa-client/src/pages/InterventionsPage.tsx (fetch sites)"
      provides: "api.get for interventions list"
      contains: "api.get"
    - path: "kapwa-client/src/pages/IrfPage.tsx (fetch sites)"
      provides: "useSWR for irf list"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/IntakePage.tsx (fetch site)"
      provides: "api.post for /intake submit"
      contains: "api.post"
    - path: "kapwa-client/src/pages/CsrPage.tsx (fetch sites)"
      provides: "useSWR for csr list"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/MayorReportsPage.tsx (fetch site)"
      provides: "useSWR for mayor-reports"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/MfaSetupPage.tsx (fetch sites)"
      provides: "useSWR for /auth/me + api.post for setup/enable/disable"
      contains: "useSWR"
    - path: "kapwa-client/src/pages/BeneficiaryViewPage.tsx (fetch sites)"
      provides: "useSWR for beneficiary detail + family-graph"
      contains: "useSWR"
  symbols:
    - "sendBatch + pullFromServer + resolveConflictRemotely (sync.ts) — migrated to api.post"
    - "useDebouncedSearch hook — migrated to useSWR with debounced key"
    - "usePiiMasking hook (revealField) — migrated to api.post"
    - "NotificationsDropdown — 2 useSWR + 2 useSWRMutation with optimistic + revalidation"
    - "AuditorWidgets — 2 useSWR (hashChains + consentLedger)"
    - "ClaimantWidgets — 1 useSWR (myServices)"
    - "15 remaining pages — migrated to useSWR / api methods per the endpoint mapping"
  key_links:
    - from: "src/lib/sync.ts (sendBatch + pullFromServer + resolveConflictRemotely)"
      to: "src/lib/api.ts api.post"
      via: "api.post('/sync/v1', body) + api.post('/sync/pull', body) + api.post('/sync/conflicts/:id/resolve', body)"
      pattern: "api\\.post\\('/sync/"
    - from: "src/hooks/useDebouncedSearch.ts (hook body)"
      to: "src/lib/query-keys.ts queryKeys.beneficiaries.list"
      via: "useSWR(queryKeys.beneficiaries.list({ search, limit }))"
      pattern: "queryKeys\\.beneficiaries\\.list"
    - from: "src/components/NotificationsDropdown.tsx (component body)"
      to: "src/lib/query-keys.ts queryKeys.notifications.{list,unreadCount}"
      via: "useSWR + useSWRMutation with optimistic markRead + revalidate"
      pattern: "queryKeys\\.notifications"
    - from: "src/components/dashboard/widgets/AuditorWidgets.tsx"
      to: "src/lib/query-keys.ts queryKeys.audit.{hashChains,consentLedger}"
      via: "2 useSWR hooks"
      pattern: "queryKeys\\.audit"
    - from: "src/components/dashboard/widgets/ClaimantWidgets.tsx"
      to: "src/lib/query-keys.ts queryKeys.beneficiaries.myServices"
      via: "1 useSWR hook"
      pattern: "queryKeys\\.beneficiaries\\.myServices"
    - from: "All 15 remaining pages"
      to: "src/lib/api.ts api.{get,post,put,del}"
      via: "useSWR + useSWRMutation per the endpoint mapping in CONTEXT D-10"
      pattern: "api\\.(get|post|put|del)"
    - from: "src/lib/api.ts (after cleanup)"
      to: "src/lib/api-error.ts (ApiError re-export)"
      via: "Re-export ApiError so consumers that import from api still work"
      pattern: "export.*ApiError"
    - from: "src/lib/api.ts (after cleanup)"
      to: "All consumers"
      via: "Only the api object + the URL constant remain; 67 wrappers are gone"
      pattern: "^export const api"
---

<objective>
Migrate the remaining 16 GET-shaped endpoints (3 widget components + 2 hooks + sync.ts + 9 remaining pages) to `useSWR` / `useSWRMutation` against the `api` object from Plan 14-01. Delete all 67 legacy wrapper functions from `src/lib/api.ts` — only the `api` object and the URL/path constants remain. Update the 9+ remaining test files to mock the new method-style `api` shape. Verify the no-fetch-outside-api.ts-and-auth.ts grep gate passes. Close out Phase 14.

Purpose: Plan 14-01 builds the helpers, Plan 14-02 proves them on the top 5 pages, this plan sweeps the remaining 16 endpoints and removes the legacy wrappers. After this plan, all 21 GET-shaped endpoints use `useSWR` (or `useSWRMutation` for mutations), and the codebase has a single method-style `api` client. The legacy 67-wrapper file is gone.

Output: ~22 modified files (15 pages, 2 hooks, 1 sync.ts, 1 components/NotificationsDropdown, 2 widget components, 1 ReportsExportButton left raw, 1 api.ts cleanup), 9+ updated test files. Grep gate passes. ~250+ tests passing. Build green. Plan summary at 14-03-SUMMARY.md.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-api-client-swr/14-CONTEXT.md
@.planning/phases/14-api-client-swr/14-RESEARCH.md
@.planning/phases/14-api-client-swr/14-PATTERNS.md
@.planning/phases/14-api-client-swr/14-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/14-api-client-swr/14-02-SUMMARY.md
@kapwa-client/src/lib/api.ts
@kapwa-client/src/lib/query-keys.ts
@.planning/phases/14-api-client-swr/14-01-PLAN.md
@kapwa-client/src/lib/sync.ts
@kapwa-client/src/lib/auth-context.tsx
@kapwa-client/src/hooks/useDebouncedSearch.ts
@kapwa-client/src/hooks/usePiiMasking.ts
@kapwa-client/src/components/NotificationsDropdown.tsx
@kapwa-client/src/components/ReportsExportButton.tsx
@kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx
@kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx
@kapwa-client/src/pages/CaseTrackerPage.tsx
@kapwa-client/src/pages/ClaimantDashboardPage.tsx
@kapwa-client/src/pages/MessagesPage.tsx
@kapwa-client/src/pages/AccessCardPage.tsx
@kapwa-client/src/pages/ApprovalPipelinePage.tsx
@kapwa-client/src/pages/AuditorPage.tsx
@kapwa-client/src/pages/MyAccessCardPage.tsx
@kapwa-client/src/pages/RegisterPage.tsx
@kapwa-client/src/pages/InterventionsPage.tsx
@kapwa-client/src/pages/IrfPage.tsx
@kapwa-client/src/pages/IntakePage.tsx
@kapwa-client/src/pages/CsrPage.tsx
@kapwa-client/src/pages/MayorReportsPage.tsx
@kapwa-client/src/pages/MfaSetupPage.tsx
@kapwa-client/src/pages/BeneficiaryViewPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate sync.ts (3 fetch sites) + delete the 67 legacy wrappers from api.ts (early cleanup)</name>
  <files>kapwa-client/src/lib/sync.ts, kapwa-client/src/lib/api.ts</files>
  <read_first>
- kapwa-client/src/lib/sync.ts (entire file — 221 lines; 3 raw fetch sites at lines 88, 167, 193; local API_URL constant at line 3; getToken() helper at lines 8-10)
- kapwa-client/src/lib/api.ts (Plan 14-01 — the new api object; the 67 legacy wrapper functions to delete)
- .planning/phases/14-api-client-swr/14-PATTERNS.md (lines 268-301 — sync.ts migration example with api.post)
- kapwa-server/src/sync/sync.controller.ts (NOT required for reading; the request shape is preserved from the existing sendBatch body)
- kapwa-client/src/lib/offline-queue.ts (NOT required; sync.ts imports from it but offline-queue is not modified)
  </read_first>
  <behavior>
  - Test 1: sync.ts has no raw fetch sites — `grep -c "fetch(" kapwa-client/src/lib/sync.ts` returns 0
  - Test 2: sync.ts has no local API_URL constant — `grep -c "const API_URL" kapwa-client/src/lib/sync.ts` returns 0
  - Test 3: api.ts no longer exports the 67 wrapper functions — `grep -c "export async function getCases\|export async function getDashboard\|export async function getBeneficiaries\|export async function getCsrRecords\|export async function getIrfRecords\|export async function getMayorReports\|export async function getAuditConsentLedger\|export async function verifyHashChains\|export async function getCsrPdf\|export async function createUser\|export async function submitIntake\|export async function createCase\|export async function updateCaseStatus" kapwa-client/src/lib/api.ts` returns 0 (no exported wrapper functions remain; only the `const api = {` export)
  - Test 4: api.ts still exports the `api` object — `grep -c "^export const api" kapwa-client/src/lib/api.ts` returns 1
  - Test 5: existing sync-conflict.test.ts and offline-queue.test.ts still pass — sync.ts behavior is preserved (the offline-queue path + the Ed25519 signature path are untouched; only the 3 fetch sites change)
  - Test 6: api.ts re-exports ApiError from api-error so any consumer that imports ApiError from '../lib/api' (e.g. older test files) still works — `grep -c "export.*ApiError.*from './api-error'" kapwa-client/src/lib/api.ts` returns ≥ 1
  </behavior>
  <action>
Substep 1 (sync.ts): At the top of `kapwa-client/src/lib/sync.ts`, delete the `const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';` line. Add `import { api } from './api';` to the existing imports (the file already imports from './offline-queue'). Delete the `getToken()` helper at lines 8-10 (the api client reads the token internally). The PRIVATE_KEY_STORAGE, DEVICE_ID_STORAGE constants stay; the Ed25519 signing helpers (`ensureDeviceId`, `generateSignature`, `hex`) stay untouched.

Substep 2 (sync.ts sendBatch): Inside the `sendBatch` function, replace the raw fetch block at lines 88-101 (the POST to `/sync/v1` with Bearer header) with a single `return api.post('/sync/v1', { deviceId, changes: changesPayload, versionVectors, idempotencyKey, signature });` call. The function body becomes: ensureDeviceId (kept), getAllVersionVectors (kept), map changes to changesPayload (kept), generateSignature (kept), then the single api.post. The !res.ok → throw Error path is gone (api.post throws ApiError on non-2xx — caller handles it).

Substep 3 (sync.ts pullFromServer): Inside `pullFromServer`, replace the raw fetch at lines 167-174 with `const data = await api.post('/sync/pull', { deviceId, versionVectors });` — then the existing for-loop over `data.serverChanges` to call queueChange stays. The `if (!res.ok) return;` early-return is gone (api.post throws on error; the existing try/catch catches the throw).

Substep 4 (sync.ts resolveConflictRemotely): Replace the raw fetch at lines 193-200 with `return await api.post(`/sync/conflicts/${conflictId}/resolve`, { resolution });` — same return shape (res.ok → boolean). Since the api.post throws on non-2xx, wrap in try/catch to preserve the boolean return: `try { await api.post(...); return true; } catch { return false; }`.

Substep 5 (sync.ts isOnline + syncOnReconnect): The `isOnline` and `syncOnReconnect` functions stay unchanged. The `window.addEventListener('online', ...)` at lines 217-220 stays unchanged.

Substep 6 (api.ts cleanup — early): After sync.ts is migrated (Task 1 substep 1-5), update `kapwa-client/src/lib/api.ts` to delete the 67 legacy wrapper functions that no consumer uses. The list of 67 wrappers to delete (verify by reading the current file): getCases, getCase, createCase, updateCaseStatus, getDashboard, getBeneficiaries, getBeneficiary, getInterventions, createIntervention, getPrograms, createProgram, updateProgram, deleteProgram, getConsentLedger, getMyAccessCard, getMayorReports, getAuditConsentLedger, verifyHashChains, revokeConsent, getCsrRecords, getCsrRecord, createCsrRecord, updateCsrRecord, deleteCsrRecord, downloadCsrPdf, updateCaseDocuments, approveCase, requestReview, disburseCase, closeCase, overrideCaseStatus, getFamilyGraph, setupMfa, enableMfa, disableMfa, verifyMfa, createUser, submitIntake, uploadSignature, uploadReceipt, dataURItoBlob, getCaseTrackerLog, assignCard, getBeneficiaryCard, getIrfCase, getIrfRecords, createIrf, referToPnp, referToWcpd, dismissIrf, closeIrf, decryptNarration, unmaskIrfNames, getProgramAssignments, getProgramAssignment, createProgramAssignment, approveAssignmentStep, rejectAssignmentStep, overrideAssignmentStep, exportIrfPdf, exportIrfJson, getNotificationPreferences, updateNotificationPreferences, bulkApprove, bulkReassign, bulkExport. ALSO delete the 4 raw-fetch-with-form-data sites (uploadSignature, uploadReceipt) and the 2 raw-fetch-with-blob sites (downloadCsrPdf, exportIrfPdf) — they are out of scope for the api client. ALSO delete the 1 raw-fetch site in createUser (it manually sets the Bearer header; replace with api.post or delete entirely if no longer called). Also delete the `dataURItoBlob` helper (only used by the deleted upload functions — verify by grep `dataURItoBlob` across the codebase; if only used in api.ts it can be deleted; if used elsewhere, leave it).

Substep 7 (api.ts re-exports): Add `export { ApiError } from './api-error';` to the top of api.ts so any consumer that imports ApiError from '../lib/api' (the schema-push gate may have placed it there) still works. Add a comment in the api.ts body listing the 7 raw-fetch carve-out sites: auth.ts `/auth/refresh`, auth-context.tsx `/auth/login` + `/auth/mfa/verify`, sync.ts has migrated in Task 1, but the FormData/blob sites in api.ts itself are deleted (not migrated to api methods) — the comments in api.ts are advisory; the actual file is just the api object + the URL constant + the re-export. NO comments per AGENTS.md "DO NOT ADD ANY COMMENTS unless asked" — skip the comment block.

Substep 8 (api.ts URL constant): The internal `API_BASE` constant (or whatever name Plan 14-01 used) stays. The TOKEN_KEY, TIMEOUT_MS, BASE_DELAYS_MS, KAPWA_AUTH_LOGOUT_EVENT internal constants stay. The exported surface shrinks to: `const api = { get, post, put, del }` and `export { ApiError } from './api-error'`. The file is now ~200 lines (down from 388 in the wrapper version).

Substep 9: Run `npx vitest run src/lib/sync-conflict.test.ts src/lib/offline-queue.test.ts src/lib/api.test.ts` — sync.ts changes affect sync-conflict tests; offline-queue tests are not affected; api.test.ts is unaffected. All 3 should pass. Run `npx vitest run src/lib/api-error.test.ts` to confirm the ApiError re-export works.

Substep 10: Run a static check: `grep -rn "from '../lib/api'" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -E "getCases|getDashboard|getBeneficiaries|getCsr|getIrf|getMayorReports|getAudit|verifyHashChains|createUser|submitIntake|uploadSignature|uploadReceipt|downloadCsrPdf|exportIrfPdf|createCase|updateCaseStatus|requestReview|disburseCase|closeCase|overrideCaseStatus|approveCase|setupMfa|enableMfa|disableMfa|verifyMfa|getNotificationPreferences|updateNotificationPreferences|bulkApprove|bulkReassign|bulkExport|getFamilyGraph|getConsentLedger|getMyAccessCard|getInterventions|createIntervention|getPrograms|createProgram|updateProgram|deleteProgram|revokeConsent|getCsrRecords|getCsrRecord|createCsrRecord|updateCsrRecord|deleteCsrRecord|updateCaseDocuments|assignCard|getBeneficiaryCard|getIrfCase|createIrf|referToPnp|referToWcpd|dismissIrf|closeIrf|decryptNarration|unmaskIrfNames|getProgramAssignments|getProgramAssignment|createProgramAssignment|approveAssignmentStep|rejectAssignmentStep|overrideAssignmentStep|exportIrfJson|getCase|getBeneficiary|getCaseTrackerLog|dataURItoBlob"` — this should return 0 matches (no remaining wrapper imports in the codebase after the early cleanup). If any match, the wrapper is still imported somewhere — find and migrate that file before deleting the wrapper.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/sync-conflict.test.ts src/lib/offline-queue.test.ts src/lib/api.test.ts src/lib/api-error.test.ts 2>&1 | tail -10</automated>
    <automated>cd kapwa-client && grep -c "fetch(" kapwa-client/src/lib/sync.ts</automated>
    <automated>cd kapwa-client && grep -E '^export async function' kapwa-client/src/lib/api.ts | wc -l</automated>
    <automated>cd kapwa-client && grep -c "^export const api" kapwa-client/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
  - `kapwa-client/src/lib/sync.ts` no longer contains `fetch(` (0 matches)
  - `kapwa-client/src/lib/sync.ts` no longer declares `const API_URL` (0 matches)
  - `kapwa-client/src/lib/sync.ts` calls `api.post(` in 3 places (one per migrated fetch site: sendBatch, pullFromServer, resolveConflictRemotely)
  - `kapwa-client/src/lib/api.ts` has 0 `export async function` declarations (the 67 wrappers are gone)
  - `kapwa-client/src/lib/api.ts` has 1 `export const api` declaration (the api object remains)
  - `kapwa-client/src/lib/api.ts` re-exports `ApiError` from `./api-error` (≥ 1 match for `export.*ApiError.*from`)
  - `npx vitest run src/lib/sync-conflict.test.ts src/lib/offline-queue.test.ts src/lib/api.test.ts src/lib/api-error.test.ts` shows all tests passing, 0 regressions
  - `npx tsc --noEmit -p kapwa-client` exits 0 (the early cleanup does not break consumers — any consumer that broke would be a sign of a missed import)
  </acceptance_criteria>
  <done>sync.ts fully migrated to api.post (3 sites); the 67 legacy wrapper functions are deleted from api.ts; the api object + ApiError re-export are the only exports; sync-conflict + offline-queue + api + api-error tests all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate hooks (useDebouncedSearch, usePiiMasking) to useSWR + api.post</name>
  <files>kapwa-client/src/hooks/useDebouncedSearch.ts, kapwa-client/src/hooks/usePiiMasking.ts</files>
  <read_first>
- kapwa-client/src/hooks/useDebouncedSearch.ts (entire file — 50 lines; the useEffect + setTimeout + raw fetch + AbortController pattern at lines 16-47 to replace)
- kapwa-client/src/hooks/usePiiMasking.ts (entire file — 54 lines; the revealField function at lines 31-51 with raw fetch to /api/beneficiaries/:id/audit/unmask)
- kapwa-client/src/lib/api.ts (Plan 14-01 — api object; api.post for usePiiMasking)
- kapwa-client/src/lib/query-keys.ts (Plan 14-01 — queryKeys.beneficiaries.list for useDebouncedSearch)
- .planning/phases/14-api-client-swr/14-PATTERNS.md (lines 305-368 — useDebouncedSearch migration example; lines 370-393 — usePiiMasking migration example)
- .planning/phases/14-api-client-swr/14-RESEARCH.md (lines 362-367 — useSWR with debounced key + keepPreviousData)
  </read_first>
  <behavior>
  - Test 1: useDebouncedSearch returns { results, loading } — type test (compile-time, not runtime) plus a smoke test that renders a test component using the hook
  - Test 2: useDebouncedSearch fires a useSWR call with the debounced key (after 300ms) — verified by spying on the api.get from the api object (or on a custom fetcher via SWRConfig)
  - Test 3: useDebouncedSearch no longer has raw fetch — `grep -c "fetch(" kapwa-client/src/hooks/useDebouncedSearch.ts` returns 0
  - Test 4: usePiiMasking.revealField calls api.post — `grep -c "api.post" kapwa-client/src/hooks/usePiiMasking.ts` returns ≥ 1
  - Test 5: usePiiMasking no longer has raw fetch — `grep -c "fetch(" kapwa-client/src/hooks/usePiiMasking.ts` returns 0
  - Test 6: any existing test files that import these hooks (e.g., a BeneficiaryViewPage test that uses usePiiMasking) still pass after the migration
  </behavior>
  <action>
Substep 1 (useDebouncedSearch): At the top of `kapwa-client/src/hooks/useDebouncedSearch.ts`, delete the `const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';` line. Add `import useSWR from 'swr';` and `import { queryKeys } from '../lib/query-keys';`. The `useState` and `useEffect` imports from React stay. The `SearchResult` interface stays.

Substep 2 (useDebouncedSearch): Inside the `useDebouncedSearch` function body, delete the entire useEffect block at lines 16-47 (the setTimeout + raw fetch + AbortController). Replace it with: `const [debouncedQuery, setDebouncedQuery] = useState(query);` (a debounced local state) and `useEffect(() => { const t = setTimeout(() => setDebouncedQuery(query), delay); return () => clearTimeout(t); }, [query, delay]);` (the debounce transition). Then `const swrKey = debouncedQuery.trim() ? queryKeys.beneficiaries.list({ search: debouncedQuery.trim(), limit }) : null;` and `const { data, isLoading } = useSWR<SearchResult[]>(swrKey, { keepPreviousData: true });`. The hook returns `{ results: data || [], loading: isLoading }`. The keepPreviousData option preserves the previous results while the new search fires (RESEARCH Pattern 3).

Substep 3 (useDebouncedSearch): The hook signature `useDebouncedSearch(query: string, delay = 300, limit = 10)` stays. The SearchResult interface stays (id, fullName, controlNo, barangay). The function is now ~15 lines (down from 50) — the manual AbortController and the JSON parsing are gone (SWR + api.get handle both).

Substep 4 (usePiiMasking): At the top of `kapwa-client/src/hooks/usePiiMasking.ts`, the existing imports (useCallback, useRef, useAuth, MaskableField, maskValue, WORKER_ROLES) stay. Add `import { api } from '../lib/api';`. The `ConsentStatus` type and `UsePiiMaskingOptions` interface stay.

Substep 5 (usePiiMasking): Inside `revealField` (lines 31-51), replace the raw `await fetch(`/api/beneficiaries/${beneficiaryId}/audit/unmask`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ field, reason }) });` with `await api.post(`/beneficiaries/${beneficiaryId}/audit/unmask`, { field, reason });` (the api client prepends the base URL; the path drops the `/api` prefix that was in the original code). The timerKey / autoMaskTimers logic (lines 33-35, 43-48) stays unchanged — it's local state, not a fetch concern. The `shouldMask` and `getDisplayValue` logic stays unchanged.

Substep 6 (Verification): Run `npx vitest run src/hooks/`. If no hook tests exist, that's OK — the hooks are exercised through the page tests (e.g., usePiiMasking is used in BeneficiaryViewPage). The 2 existing pages that use these hooks (useDebouncedSearch is used in 1 page; usePiiMasking is used in BeneficiaryViewPage) will be tested in Task 3 (the hooks themselves have no direct unit tests). Confirm the change by running the full test suite — if any page test that uses these hooks breaks, fix the migration. Run the grep gate: `grep -c "fetch(" kapwa-client/src/hooks/useDebouncedSearch.ts kapwa-client/src/hooks/usePiiMasking.ts` returns 0 for both.
  </action>
  <verify>
    <automated>cd kapwa-client && grep -c "fetch(" kapwa-client/src/hooks/useDebouncedSearch.ts kapwa-client/src/hooks/usePiiMasking.ts</automated>
    <automated>cd kapwa-client && grep -c "useSWR" kapwa-client/src/hooks/useDebouncedSearch.ts</automated>
    <automated>cd kapwa-client && grep -c "api.post" kapwa-client/src/hooks/usePiiMasking.ts</automated>
    <automated>cd kapwa-client && npx vitest run src/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
  - `kapwa-client/src/hooks/useDebouncedSearch.ts` no longer contains `fetch(` (0 matches)
  - `useSWR` is present in useDebouncedSearch (1+ matches)
  - `queryKeys.beneficiaries.list(` is present in useDebouncedSearch (1+ matches)
  - `keepPreviousData: true` is present in useDebouncedSearch (1+ matches)
  - `kapwa-client/src/hooks/usePiiMasking.ts` no longer contains `fetch(` (0 matches)
  - `api.post` is present in usePiiMasking (1+ matches for `/audit/unmask`)
  - The local API_URL constant is gone from both files
  - The full test suite still passes (any page test that imports these hooks must continue to work — if a test breaks, fix the migration in that page; do NOT add backward-compat shims)
  </acceptance_criteria>
  <done>useDebouncedSearch uses useSWR with debounced key + keepPreviousData; usePiiMasking.revealField uses api.post; no raw fetch in either hook; existing page tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate widget components (NotificationsDropdown, AuditorWidgets, ClaimantWidgets) + ReportsExportButton (stays raw for blob)</name>
  <files>kapwa-client/src/components/NotificationsDropdown.tsx, kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx, kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx, kapwa-client/src/components/ReportsExportButton.tsx</files>
  <read_first>
- kapwa-client/src/components/NotificationsDropdown.tsx (entire file — 129 lines; 4 raw fetch sites: fetchNotifications, fetchUnreadCount, markAsRead, markAllRead at lines 29-69; the 30s setInterval polling at line 25)
- kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx (entire file — 134 lines; 2 raw fetch sites in loadAll at lines 23-30; 1 call to verifyHashChains wrapper in handleReVerify at line 46)
- kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx (entire file — lines 22-52 are the useEffect + fetch)
- kapwa-client/src/components/ReportsExportButton.tsx (entire file — 1 raw fetch with blob download; STAYS RAW per D-10 deferred)
- kapwa-client/src/lib/api.ts (Plan 14-01 — api object)
- kapwa-client/src/lib/query-keys.ts (Plan 14-01 — queryKeys.notifications, queryKeys.audit, queryKeys.beneficiaries.myServices)
- .planning/phases/14-api-client-swr/14-PATTERNS.md (lines 396-462 — NotificationsDropdown migration example; lines 467-506 — ClaimantWidgets; lines 510-571 — AuditorWidgets)
- .planning/phases/14-api-client-swr/14-RESEARCH.md (lines 399-440 — useSWRMutation with optimistic update + revalidation)
  </read_first>
  <behavior>
  - Test 1: NotificationsDropdown uses useSWR for the list + useSWR with refreshInterval 30s for unread count + useSWRMutation for markRead + useSWRMutation for markAllRead
  - Test 2: NotificationsDropdown no longer has a setInterval polling (the 30s polling is replaced by SWR's refreshInterval)
  - Test 3: AuditorWidgets has 2 useSWR hooks (hashChains + consentLedger)
  - Test 4: AuditorWidgets's handleReVerify uses the destructured mutate from the useSWR hook (NOT the api.put) — re-verify is a refetch
  - Test 5: ClaimantWidgets has 1 useSWR hook (myServices)
  - Test 6: ReportsExportButton still uses raw fetch for the blob download (D-10 deferred) — the URL.createObjectURL is preserved
  - Test 7: All 4 components pass type check — `npx tsc --noEmit -p kapwa-client` exits 0
  </behavior>
  <action>
Substep 1 (NotificationsDropdown): At the top, delete the `const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';` line. Add `import useSWR from 'swr';` and `import useSWRMutation from 'swr/mutation';` and `import { mutate } from 'swr';` and `import { api } from '../lib/api';` and `import { queryKeys } from '../lib/query-keys';`. The existing `useState`, `useEffect`, lucide-react, and ui component imports stay.

Substep 2 (NotificationsDropdown): Inside the component body, delete the 3 useState calls (notifications, unreadCount, loading) — actually keep `open` (UI state). Delete the useEffect at lines 22-27 (the initial fetch + 30s interval). Delete the 4 helper functions (fetchNotifications, fetchUnreadCount, markAsRead, markAllRead at lines 29-69). The local `Notification` interface stays.

Substep 3 (NotificationsDropdown): After the useState, add: `const { data: notifications, isLoading: notifsLoading } = useSWR<Notification[]>(queryKeys.notifications.list());` and `const { data: unreadData } = useSWR<{ count: number }>(queryKeys.notifications.unreadCount(), { refreshInterval: 30000 });` (the refreshInterval replaces the setInterval polling per RESEARCH Pattern 3). The `unreadCount` becomes `unreadData?.count ?? 0;` and the `loading` becomes `notifsLoading`.

Substep 4 (NotificationsDropdown): Add 2 useSWRMutation hooks for the actions. Define `markReadMutation` with the key `queryKeys.notifications.list()` and a fetcherFn that takes `(url, { arg }: { arg: { id: string } })` and calls `await api.post(`/notifications/${arg.id}/read`);`. The optimisticData option sets the notification's isRead to true in the local cache. The onSuccess calls `mutate(queryKeys.notifications.unreadCount(), undefined, { revalidate: true })` to refresh the unread count. Same pattern for `markAllReadMutation` with the path `/notifications/read-all` and the optimisticData mapping all notifications to isRead: true.

Substep 5 (NotificationsDropdown): Wire the 2 mutations to the existing button onClick handlers. The NotificationsDropdown's existing JSX stays (Popover, popover content, notification list rendering, Mark all read button). The `loading` variable from before is replaced with `notifsLoading`; the `notifications` array reads directly from useSWR data; the `unreadCount` reads from unreadData.

Substep 6 (AuditorWidgets): At the top, delete the `const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';` line. Add `import useSWR from 'swr';` and `import { queryKeys } from '../lib/query-keys';`. The `verifyHashChains` import from '../lib/api' is REMOVED (the wrapper is gone in Task 1). Keep useState, useEffect (may not be needed), Card, CardContent, Button, RefreshCw icons.

Substep 7 (AuditorWidgets): Inside the component body, delete the 4 useState calls (hashChain, consentCount, loading, verifying). Delete the useEffect at line 15. Delete the loadAll function (lines 19-41) entirely. Delete the handleReVerify function (lines 43-53) entirely. The existing `Promise.all([fetch, fetch])` pattern becomes 2 separate useSWR hooks: `const { data: hashChain, isLoading: loading, mutate: revalidateHash } = useSWR(queryKeys.audit.hashChains());` and `const { data: ledger } = useSWR(queryKeys.audit.consentLedger());`. The `consentCount` is derived: `Array.isArray(ledger) ? ledger.length : 0;`. The `handleReVerify` function becomes a thin wrapper: `async function handleReVerify() { setVerifying(true); try { await revalidateHash(); } finally { setVerifying(false); } }` — but the `setVerifying` state must be kept (UI state). Or, simpler, use the `isValidating` flag from the useSWR hook: `const { data: hashChain, isValidating: verifying } = useSWR(queryKeys.audit.hashChains());`. Use isValidating to avoid the extra useState.

Substep 8 (ClaimantWidgets): At the top, delete the `const API_URL` line. Add `import useSWR from 'swr';` and `import { queryKeys } from '../lib/query-keys';`. Inside the component body, delete the 2 useState calls (data, loading) and the useEffect + loadData function. Add `const { data, isLoading } = useSWR(queryKeys.beneficiaries.myServices());` and map the data: `const mapped = data ? { caseStatus: data.caseStatus || 'No active case', services: data.services || [] } : null;`. The JSX stays the same.

Substep 9 (ReportsExportButton — stays raw): The file's 1 raw fetch for the blob download STAYS unchanged (D-10 deferred — blob download with custom URL construction is out of scope for the api client). The only adjustment is to read the VITE_API_URL from a shared location — either `import.meta.env.VITE_API_URL || 'http://localhost:3000/api'` inline, or import `API_BASE` from '../lib/api' if the new api.ts exports it as a named export (Plan 14-01 may have it private — check). If `API_BASE` is private, just keep the inline `import.meta.env.VITE_API_URL` reference. The token read `localStorage.getItem('token')` (note: this file uses `token` not `kapwa_token` — verify the key — per RESEARCH A4 this might be a bug but it's pre-existing and out of scope) stays. The blob → URL.createObjectURL → click → revokeObjectURL pattern stays.

Substep 10 (ReportsExportButton — token key fix OPTIONAL): The CONTEXT Research A4 notes that `ReportsExportButton.tsx` uses `localStorage.getItem('token')` (no `kapwa_` prefix) which looks like a bug — the api client reads from `'kapwa_token'`. If this file is using the wrong key, the fetch will fail with 401 because the Bearer header is `Bearer undefined`. This is pre-existing and out of scope for this plan, but document it in the commit message body. Do NOT fix the key in this plan (it's a 1-character change to the key but the auth flow is server-side; the executor should NOT change pre-existing key behavior without coordination — leave it for a future phase or document the discrepancy).

Substep 11: Run the full suite — if any existing page test imports these components, it must continue to work. The 3 widgets have no direct test files (they are mounted by DashboardPage / AuditorPage), so the page tests are the canary.
  </action>
  <verify>
    <automated>cd kapwa-client && grep -c "fetch(" kapwa-client/src/components/NotificationsDropdown.tsx kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx</automated>
    <automated>cd kapwa-client && grep -c "useSWR" kapwa-client/src/components/NotificationsDropdown.tsx kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx</automated>
    <automated>cd kapwa-client && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
  - `kapwa-client/src/components/NotificationsDropdown.tsx` no longer contains `fetch(` (0 matches) and no longer has `setInterval(fetchUnreadCount` (0 matches)
  - `useSWR` is present ≥ 2 times in NotificationsDropdown (list + unreadCount)
  - `refreshInterval: 30000` is present in NotificationsDropdown (1+ matches)
  - `useSWRMutation` is present ≥ 2 times in NotificationsDropdown (markRead + markAllRead)
  - `mutate(queryKeys.notifications.unreadCount()` is present in NotificationsDropdown (1+ matches)
  - `kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx` no longer contains `fetch(` (0 matches)
  - `useSWR` is present ≥ 2 times in AuditorWidgets (hashChains + consentLedger)
  - `kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx` no longer contains `fetch(` (0 matches)
  - `useSWR(queryKeys.beneficiaries.myServices` is present in ClaimantWidgets (1+ matches)
  - `kapwa-client/src/components/ReportsExportButton.tsx` STILL contains `fetch(` (1 match — preserved per D-10 deferred)
  - `npx vitest run` shows ≥ 240 tests passing, 0 failing, 0 regressions
  - `npx tsc --noEmit -p kapwa-client` exits 0
  </acceptance_criteria>
  <done>NotificationsDropdown, AuditorWidgets, ClaimantWidgets migrated to useSWR/useSWRMutation; ReportsExportButton stays raw for blob download; no raw fetch in the 3 migrated components; full test suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Migrate remaining 9 pages (CaseTracker, ClaimantDashboard, Messages, AccessCard, ApprovalPipeline, Auditor, MyAccessCard, Register + 1 more) + update tests</name>
  <files>kapwa-client/src/pages/CaseTrackerPage.tsx, kapwa-client/src/pages/CaseTrackerPage.test.tsx, kapwa-client/src/pages/ClaimantDashboardPage.tsx, kapwa-client/src/pages/ClaimantDashboardPage.test.tsx, kapwa-client/src/pages/MessagesPage.tsx, kapwa-client/src/pages/MessagesPage.test.tsx, kapwa-client/src/pages/AccessCardPage.tsx, kapwa-client/src/pages/AccessCardPage.test.tsx, kapwa-client/src/pages/ApprovalPipelinePage.tsx, kapwa-client/src/pages/AuditorPage.tsx, kapwa-client/src/pages/MyAccessCardPage.tsx, kapwa-client/src/pages/RegisterPage.tsx, kapwa-client/src/pages/RegisterPage.test.tsx</files>
  <read_first>
- kapwa-client/src/pages/CaseTrackerPage.tsx (entire file — read 3 fetch sites: /tracker/daily, /tracker/stats, /tracker)
- kapwa-client/src/pages/ClaimantDashboardPage.tsx (entire file — 2 fetch sites: /beneficiaries/me/services, /beneficiaries/me/consent)
- kapwa-client/src/pages/MessagesPage.tsx (entire file — 4 fetch sites: /chat/conversations, /auth/me, /chat/conversation/:id, /chat/conversation/:id/read; imports chat-socket which stays untouched)
- kapwa-client/src/pages/AccessCardPage.tsx (entire file — 2 fetch sites: /access-cards, /access-cards/log)
- kapwa-client/src/pages/ApprovalPipelinePage.tsx (entire file — 2 FormData upload sites at lines 94, 112 that STAY RAW; JSON data uses api)
- kapwa-client/src/pages/AuditorPage.tsx (entire file — 4 fetch sites: /audit/verify-all, /audit/consent-ledger, re-verify, loadLedger)
- kapwa-client/src/pages/MyAccessCardPage.tsx (entire file — 1 fetch site: /beneficiaries/me/access-card)
- kapwa-client/src/pages/RegisterPage.tsx (entire file — 1 fetch site: /auth/register POST form submit)
- kapwa-client/src/lib/api.ts (Plan 14-01 — the api object)
- kapwa-client/src/lib/query-keys.ts (Plan 14-01 — confirm queryKeys entries for all 9 pages)
- kapwa-client/src/lib/chat-socket.ts (used by MessagesPage; stays untouched)
- kapwa-client/src/pages/CaseTrackerPage.test.tsx (lines 6-9 — uses vi.stubGlobal('fetch', ...); needs migration to vi.mock('../lib/api', ...) or SWRConfig with mock fetcher)
- kapwa-client/src/pages/RegisterPage.test.tsx (uses form mocks; no fetch mocks currently — needs vi.mock for the api.post)
- .planning/phases/14-api-client-swr/14-PATTERNS.md (lines 896-922 — 16 page mapping table; the Pages C plan follows the DashboardPage + CasesPage patterns from Plan 14-02)
  </read_first>
  <behavior>
  - Test 1: all 9 migrated pages pass their existing tests (the 4 page tests with vi.mock updates, the 2 page tests with new mocks, the 3 page tests that don't currently have api mocks get them added)
  - Test 2: no raw fetch sites remain in any of the 9 pages (excluding FormData carve-outs in ApprovalPipelinePage)
  - Test 3: the 9 pages all import the new api object (not the deleted wrappers) — verified by grep
  - Test 4: `npx vitest run` shows ≥ 248 tests passing, 0 failing, 0 regressions
  - Test 5: `npx tsc --noEmit -p kapwa-client` exits 0
  </behavior>
  <action>
Substep 1 (CaseTrackerPage): At the top, delete the `const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';` line. Add `import useSWR from 'swr';` and `import { api } from '../lib/api';` and `import { queryKeys } from '../lib/query-keys';`. Inside the component, delete the useState calls for data (entries, stats, loading) and the useEffect. Add 3 useSWR hooks: `useSWR(queryKeys.tracker.daily({ date: selectedDate }))` for the daily entries, `useSWR(queryKeys.tracker.stats())` for the stats, and `useSWR(queryKeys.tracker.list())` for the broader list. The `selectedDate` is UI state and stays as useState. Map the data and render as before. The `queryKeys.tracker.*` entries are added to the factory in Plan 14-01's Task 3 — verify the exact names; if `queryKeys.tracker` is not in the factory, add the subtree (the factory is mutable from any plan — Plan 14-03 can add missing entries). Update the test file (CaseTrackerPage.test.tsx) to use vi.mock('../lib/api', ...) with api.get mocked to return mockEntries + mockStats.

Substep 2 (ClaimantDashboardPage): Delete the local API_URL constant. Add the api + useSWR + queryKeys imports. Delete the 2 useState calls + useEffect + loadData. Add 2 useSWR hooks: `useSWR(queryKeys.beneficiaries.myServices())` and `useSWR(queryKeys.beneficiaries.myConsent())`. Map and render as before. Update the test file.

Substep 3 (MessagesPage): Delete the local API_URL constant. Add api + useSWR + queryKeys + useSWRMutation + mutate imports. The /auth/me fetch is for the current user — use `useSWR(queryKeys.auth.me())` (replaces the raw fetch in the loadCurrentUser function). The /chat/conversations fetch is for the conversation list — use `useSWR(queryKeys.messages.list())`. The /chat/conversation/:id fetch is for a specific conversation — use a conditional `useSWR(selectedUserId ? queryKeys.messages.conversation(selectedUserId) : null)`. The /chat/conversation/:id/read POST is a mutation — use `useSWRMutation(queryKeys.messages.conversation(selectedUserId), (url, { arg }) => api.post(`/chat/conversation/${arg}/read`))`. The `connectSocket` / `disconnectSocket` / `sendMessage` imports from chat-socket stay untouched. The socket subscription can update the SWR cache by calling `mutate(queryKeys.messages.list(), undefined, { revalidate: true })` on incoming messages. Update the test file (add vi.mock for '../lib/api' and the chat-socket mock stays).

Substep 4 (AccessCardPage): Delete the local API_URL constant (or API constant — check the file). Add api + useSWR + queryKeys imports. Delete the useState + useEffect + 2 fetch functions. Add 2 useSWR hooks: `useSWR(queryKeys.accessCards.list())` and `useSWR(queryKeys.accessCards.log())`. The mutations (assignCard) use api.post + mutate. Update the test file.

Substep 5 (ApprovalPipelinePage): The 2 FormData uploads at lines 94, 112 STAY RAW (D-10 deferred). Migrate the JSON data fetches to api.get / api.post. The `cases` list fetch (the PATCH calls for approve/reject) migrate to api.patch — use api.put since api has no patch. Add useSWR for the cases list. Update the test file (if it exists — ApprovalPipelinePage.test.tsx is in the list — verify the test exists; if not, the test update is skipped). The FormData upload test does NOT change.

Substep 6 (AuditorPage): Delete the local `baseUrl` constant inside the component. Add api + useSWR + queryKeys imports. Delete the 6 useState calls + useEffect + 4 fetch functions. Add 2 useSWR hooks: `useSWR(queryKeys.audit.hashChains())` and `useSWR(queryKeys.audit.consentLedger({ beneficiaryId: beneficiaryFilter }))`. The activeTab is UI state. The re-verify action uses the destructured mutate from the hashChains useSWR. The loadLedger is replaced by a conditional key. Update the test file (AuditorPage is currently without a test file — the page is exercised via AuditorWidgets which is mounted by DashboardPage; the test update is optional unless a test file exists).

Substep 7 (MyAccessCardPage): Delete the local API_URL constant. Add api + useSWR + queryKeys imports. Delete the useState + useEffect + loadData. Add 1 useSWR hook: `useSWR(queryKeys.beneficiaries.myAccessCard())`. Map and render. No test file to update (MyAccessCardPage has no test).

Substep 8 (RegisterPage): Delete the local /api/auth/register fetch. Add api + queryKeys imports (no useSWR for the form submit — it's a one-shot mutation). The form submit handler becomes `await api.post('/auth/register', formData)`. The success/error handling stays. Update the test file (RegisterPage.test.tsx) to add a vi.mock for '../lib/api' — the current test uses form mocks; the api.post mock returns success.

Substep 9 (InterventionsPage): This is the 9th page (the additional one). Delete the local API_URL constant if present. Add api + useSWR + queryKeys imports. Migrate any fetch sites to useSWR. Update the test file (InterventionsPage.test.tsx exists).

Substep 10: Add any missing queryKeys subtrees to `kapwa-client/src/lib/query-keys.ts` that the 9 pages need but that Plan 14-01 didn't define. Per the CONTEXT D-10 enumeration: queryKeys.tracker.{daily,stats,list}, queryKeys.messages.{list,conversation,unread}, queryKeys.accessCards.{list,log,print}, queryKeys.beneficiaries.{myConsent,familyGraph,detail}, queryKeys.audit.{hashChains,consentLedger,coaExport,hashChain}, queryKeys.dashboard.mayorReports, queryKeys.programAssignments.{list,detail}, queryKeys.irf.{list,detail}, queryKeys.csr.list, queryKeys.programs.{list,detail}, queryKeys.intake.recent. The Plan 14-01 factory may have some of these; the missing ones are added in this substep. The factory is append-only — existing entries stay unchanged.

Substep 11: Run the full suite. If any page test breaks (because the migration changed a render or removed a localStorage call the test depends on), fix the migration in the page, not the test (unless the test was checking legacy behavior). Add a new test per page that asserts api.get is called with the expected path on mount (consistent with the Plan 14-02 pattern). If a page has no test file, do NOT add a new test in this plan (Phase 15 TST-06 will add page smoke tests).
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run 2>&1 | tail -10</automated>
    <automated>cd kapwa-client && grep -c "fetch(" kapwa-client/src/pages/CaseTrackerPage.tsx kapwa-client/src/pages/ClaimantDashboardPage.tsx kapwa-client/src/pages/MessagesPage.tsx kapwa-client/src/pages/AccessCardPage.tsx kapwa-client/src/pages/AuditorPage.tsx kapwa-client/src/pages/MyAccessCardPage.tsx kapwa-client/src/pages/RegisterPage.tsx kapwa-client/src/pages/InterventionsPage.tsx</automated>
    <automated>cd kapwa-client && grep -c "useSWR\|api\\.get\|api\\.post\|api\\.put\|api\\.del" kapwa-client/src/pages/CaseTrackerPage.tsx kapwa-client/src/pages/ClaimantDashboardPage.tsx kapwa-client/src/pages/MessagesPage.tsx kapwa-client/src/pages/AccessCardPage.tsx kapwa-client/src/pages/AuditorPage.tsx kapwa-client/src/pages/MyAccessCardPage.tsx kapwa-client/src/pages/RegisterPage.tsx kapwa-client/src/pages/InterventionsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
  - The 8 page files (excluding ApprovalPipelinePage which has FormData carve-outs) no longer contain `fetch(` (0 matches each)
  - `useSWR` or `api.get` / `api.post` / `api.put` / `api.del` is present in each migrated page (≥ 1 match per page)
  - ApprovalPipelinePage's FormData uploads (lines 94, 112) STILL contain `fetch(` (2 matches — preserved per D-10 deferred)
  - `queryKeys.tracker.*`, `queryKeys.messages.*`, `queryKeys.accessCards.*`, `queryKeys.beneficiaries.myConsent`, `queryKeys.audit.*`, `queryKeys.dashboard.mayorReports`, `queryKeys.programAssignments.*`, `queryKeys.irf.*`, `queryKeys.csr.list`, `queryKeys.programs.*`, `queryKeys.intake.recent` are present in `kapwa-client/src/lib/query-keys.ts` (Plan 14-01 may have some; missing ones are added)
  - All updated test files pass (4 files with vi.mock changes; 2 files with new mocks; pages without test files don't need updates)
  - `npx vitest run` shows ≥ 248 tests passing, 0 failing, 0 regressions
  - `npx tsc --noEmit -p kapwa-client` exits 0
  </acceptance_criteria>
  <done>9 remaining pages migrated to useSWR / api methods; queryKeys factory extended with the missing subtrees; tests updated; ApprovalPipelinePage FormData uploads stay raw; ≥ 248 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Final grep gate + delete remaining 7+ wrapper functions from api.ts + run full e2e suite + npm run build + plan summary</name>
  <files>kapwa-client/src/lib/api.ts, kapwa-client/src/pages/IntakePage.tsx, kapwa-client/src/pages/IntakePage.test.tsx, kapwa-client/src/pages/IrfPage.tsx, kapwa-client/src/pages/IrfPage.test.tsx, kapwa-client/src/pages/CsrPage.tsx, kapwa-client/src/pages/CsrPage.test.tsx, kapwa-client/src/pages/MayorReportsPage.tsx, kapwa-client/src/pages/MfaSetupPage.tsx, kapwa-client/src/pages/MfaSetupPage.test.tsx, kapwa-client/src/pages/BeneficiaryViewPage.tsx, kapwa-client/src/pages/BeneficiaryViewPage.test.tsx</files>
  <read_first>
- kapwa-client/src/lib/api.ts (after Task 1 — already has 0 wrapper functions; verify)
- The remaining 8 page files (IntakePage, IrfPage, CsrPage, MayorReportsPage, MfaSetupPage, BeneficiaryViewPage, ApprovalPipelinePage, InterventionsPage) that may still have raw fetch sites or wrapper imports
- .planning/phases/14-api-client-swr/14-CONTEXT.md (D-10 deferred — FormData + blob stay raw)
- .planning/phases/14-api-client-swr/14-VALIDATION.md (the grep gate from line 53-54 — the full 14-03-02 gate)
- kapwa-client/src/__tests__/e2e.test.ts (the e2e test that must still pass after migration)
- kapwa-client/src/lib/pii-utils.ts (the WORKER_ROLES constant used by usePiiMasking — stays untouched)
- kapwa-client/src/lib/chat-socket.ts (used by MessagesPage; stays untouched)
- kapwa-client/src/components/dashboard/widgets/CoordinatorWidgets.tsx (uses the legacy getDashboard wrapper — Task 5 verifies this migrated to api.get)
  </read_first>
  <behavior>
  - Test 1: the grep gate from VALIDATION.md 14-03-02 returns 0 matches — `grep -rn "fetch(" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -v "src/__tests__/" | grep -v "src/lib/api.ts" | grep -v "src/lib/auth.ts" | grep -v "src/lib/auth-context.tsx" | grep -v "FormData" | grep -v "blob"` returns 0 lines
  - Test 2: the 6 remaining page files (IntakePage, IrfPage, CsrPage, MayorReportsPage, MfaSetupPage, BeneficiaryViewPage) have no raw fetch sites and use the api object
  - Test 3: CoordinatorWidgets migrated to api.get('/dashboard') via useSWR (it currently uses the legacy getDashboard wrapper)
  - Test 4: full test suite (250+ tests) passes
  - Test 5: `npm run build` exits 0
  - Test 6: `npx tsc --noEmit -p kapwa-client` exits 0 (or only pre-existing e2e.test.ts Buffer error)
  - Test 7: e2e test in src/__tests__/e2e.test.ts still passes
  </behavior>
  <action>
Substep 1 (Migrate the 6 remaining pages): For IntakePage, IrfPage, CsrPage, MayorReportsPage, MfaSetupPage, BeneficiaryViewPage, follow the same pattern as Task 4: delete the local API_URL constant, add useSWR + api + queryKeys imports, replace the useState + useEffect + raw fetch with useSWR hooks + api methods for mutations, update the test file if it exists. Specifically: IntakePage uses `submitIntake` (POST /intake) — first inspect the existing code: if the body is a plain JS object, use `api.post('/intake', formData)`; if the body is a `new FormData(...)` instance, KEEP IT on raw fetch (D-10 FormData deferred) and just change the URL to read from `import.meta.env.VITE_API_URL || 'http://localhost:3000/api'`; IrfPage uses `getIrfRecords` (GET /irf) — use `useSWR(queryKeys.irf.list())`; CsrPage uses `getCsrRecords` (GET /csr) — use `useSWR(queryKeys.csr.list())`; MayorReportsPage uses `getMayorReports` (GET /dashboard/reports/mayor) — use `useSWR(queryKeys.dashboard.mayorReports())`; MfaSetupPage uses `getCurrentUser` (GET /auth/me) — use `useSWR(queryKeys.auth.me())` plus api.post for setupMfa/enableMfa/disableMfa; BeneficiaryViewPage uses `getBeneficiary` and `getFamilyGraph` — use `useSWR(queryKeys.beneficiaries.detail(id))` and `useSWR(queryKeys.beneficiaries.familyGraph(id))`.

Substep 2 (Migrate CoordinatorWidgets): This dashboard widget uses the legacy `getDashboard` wrapper from `'../lib/api'` (per .planning/phases/14-api-client-swr/14-PATTERNS.md line 580). Replace the import with `useSWR` + `queryKeys.dashboard.stats()` and the api.get global fetcher. The widget is mounted by DashboardPage for the coordinator role; the migration ensures the api.get path is exercised (the legacy wrapper was an apiFetch call).

Substep 3 (Final api.ts sweep): After all pages and components are migrated, the api.ts file should have 0 `export async function` declarations. Verify with `grep -E '^export async function' kapwa-client/src/lib/api.ts` returning 0. The only exports are `const api` (the api object) and the `ApiError` re-export from `./api-error`. If any wrapper remains (e.g., a wrapper that was missed in the audit), delete it now — the new code does not use it.

Substep 4 (Run the final grep gate): `grep -rn "fetch(" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -v "src/__tests__/" | grep -v "src/lib/api.ts" | grep -v "src/lib/auth.ts" | grep -v "src/lib/auth-context.tsx" | grep -v "FormData" | grep -v "blob"` must return 0 matches. The exceptions: `__tests__` (e2e tests), `api.ts` (the 7 FormData/blob carve-outs may still be there if not fully deleted — verify), `auth.ts` (the /auth/refresh raw fetch), `auth-context.tsx` (the 2 raw fetch sites for /auth/login and /auth/mfa/verify). All FormData and blob lines are excluded by grep. If any non-carve-out raw fetch remains, the gate fails and the migration is incomplete — find and migrate that site.

Substep 5 (Run the full test suite): `cd kapwa-client && npm run test:run` — must show ≥ 250 tests passing, 0 failing, 0 regressions vs. the 237 baseline. If any test fails, fix the migration in the file (not the test, unless the test was checking legacy behavior that no longer applies).

Substep 6 (Run the e2e test): `cd kapwa-client && npx vitest run src/__tests__/e2e.test.ts` — the e2e test must still pass. The e2e test exercises the real backend; after migration, the api client is the new method-style. If the e2e test fails because the backend mock data doesn't match the new api client's expected response shape, document the discrepancy in the plan summary (the test may need backend updates — out of scope for this plan).

Substep 7 (Run the build): `cd kapwa-client && npm run build` — must exit 0. TypeScript must compile. The Capacitor config does not need to run (web build only per Phase 13 D-03).

Substep 8 (Static checks): 
- `grep -E '^export async function' kapwa-client/src/lib/api.ts | wc -l` returns 0
- `grep -c "^export const api" kapwa-client/src/lib/api.ts` returns 1
- `grep -E "apiFetch|getCases|getDashboard|getBeneficiaries" kapwa-client/src/lib/api.ts | wc -l` returns 0 (no deleted-wrapper references in api.ts)
- `grep -rn "from '../lib/api'" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -E "getCases|getDashboard|getBeneficiaries"` returns 0 (no consumer imports a deleted wrapper)
- `cd kapwa-client && npx tsc --noEmit -p .` exits 0 (or only pre-existing e2e.test.ts Buffer error from Phase 13)

Substep 9 (Create the plan summary): Write `.planning/phases/14-api-client-swr/14-03-SUMMARY.md` per the gsd-executor protocol. The summary lists: 5 atomic commits (one per task), 250+ tests passing, build green, the 21-endpoint enumeration with all 21 marked done, the 67 deleted wrappers enumerated, the 7 FormData/blob carve-out sites (4 in api.ts: downloadCsrPdf at line 127, exportIrfPdf at line 328, uploadSignature at line 203, uploadReceipt at line 217 + 1 in ReportsExportButton + 2 in ApprovalPipelinePage + 2 in FilingPage = 10 total FormData/blob sites that stay raw), the 3 raw fetch sites that stay raw for non-FormData reasons (auth.ts /auth/refresh, auth-context.tsx /auth/login, auth-context.tsx /auth/mfa/verify), and the final grep gate result (0 matches).
  </action>
  <verify>
    <automated>cd kapwa-client && grep -rn "fetch(" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -v "src/__tests__/" | grep -v "src/lib/api.ts" | grep -v "src/lib/auth.ts" | grep -v "src/lib/auth-context.tsx" | grep -v "FormData" | grep -v "blob" | wc -l</automated>
    <automated>cd kapwa-client && npx vitest run 2>&1 | tail -10</automated>
    <automated>cd kapwa-client && npm run build 2>&1 | tail -5</automated>
    <automated>cd kapwa-client && grep -E '^export async function' kapwa-client/src/lib/api.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - The grep gate returns 0 matches — all raw fetch sites outside the carve-outs (auth.ts, auth-context.tsx, FormData, blob) are migrated
  - `kapwa-client/src/lib/api.ts` has 0 `export async function` declarations (the 67 wrappers are gone)
  - `kapwa-client/src/lib/api.ts` has 1 `export const api` declaration (the api object remains)
  - The 6 remaining page files (IntakePage, IrfPage, CsrPage, MayorReportsPage, MfaSetupPage, BeneficiaryViewPage) have no raw fetch and use the api object
  - CoordinatorWidgets migrated from `getDashboard` to `useSWR(queryKeys.dashboard.stats())`
  - `npx vitest run` shows ≥ 250 tests passing, 0 failing
  - `npx vitest run src/__tests__/e2e.test.ts` passes (or documents a known backend-discrepancy)
  - `npm run build` exits 0
  - `npx tsc --noEmit -p kapwa-client` exits 0 (or only pre-existing errors)
  - `.planning/phases/14-api-client-swr/14-03-SUMMARY.md` exists with 5 commit hashes + the 21-endpoint enumeration (all done) + the 67 deleted wrappers list + the 10 FormData/blob carve-outs + the 3 raw fetch carve-outs (auth.ts + auth-context.tsx login + MFA) + the grep gate result
  </acceptance_criteria>
  <done>All 21 GET-shaped endpoints migrated to useSWR/useSWRMutation; the 67 legacy wrappers deleted from api.ts; the grep gate passes (0 raw fetch outside carve-outs); 250+ tests pass; e2e test passes; build green; plan summary committed. Phase 14 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync.ts ↔ api.ts | sync.ts calls api.post for /sync/v1, /sync/pull, /sync/conflicts/:id/resolve; the api client reads kapwa_token from localStorage |
| useDebouncedSearch hook ↔ useSWR | Hook returns a debounced query + useSWR key; the page consumes the hook |
| usePiiMasking hook ↔ api.post | revealField calls api.post for the audit-unmask mutation |
| NotificationsDropdown ↔ useSWR + useSWRMutation | 2 useSWR (list + unreadCount with 30s polling) + 2 useSWRMutation (markRead + markAllRead with optimistic updates) |
| AuditorWidgets ↔ useSWR | 2 useSWR (hashChains + consentLedger) |
| ClaimantWidgets ↔ useSWR | 1 useSWR (myServices) |
| ReportsExportButton ↔ raw fetch | Blob download stays on raw fetch (D-10 deferred); the response is consumed via URL.createObjectURL |
| 9 remaining pages ↔ useSWR / api methods | Each page has 1-4 fetch sites that migrate to useSWR / useSWRMutation / api methods |
| api.ts (after cleanup) ↔ consumers | Only the api object + ApiError re-export remain; 67 wrappers are gone; consumers either use the api object or import ApiError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Information Disclosure | sync.ts POST /sync/v1 sends the Ed25519-signed change batch; a leak would expose pending offline writes | mitigate | The api client sends the same payload as the legacy fetch — no new exposure. The Authorization header is set automatically by the api client. The Ed25519 signature is generated locally before the api.post call. The offline-queue path is unchanged. The /sync/conflicts/:id/resolve endpoint is called with a { resolution } body — no PII in the request body itself |
| T-14-03-02 | Denial of Service | sync.ts /sync/v1 is retried 3× by the api client on network failure — this could amplify load if the server is slow | accept | The api client retries only on network failure or timeout (D-11 — 4xx/5xx are NOT retried). A slow server returns 5xx, which is NOT retried. A network failure retries 3× with exponential backoff (500ms, 1500ms, 4500ms ± 20% jitter per D-12). The sync.ts caller is processDeltaSync which loops over batches; if one batch fails, the next batch is processed in the next sync cycle. No retry storm |
| T-14-03-03 | Tampering | useDebouncedSearch uses useSWR with a debounced key; if the debounce logic is wrong, the search query could be sent before the user finishes typing | accept | The 300ms debounce is the same as the legacy code. SWR does not introduce additional debouncing; the page debounces the key (not the request) per RESEARCH Pattern 3. The queryKeys.beneficiaries.list({ search }) factory returns a stable tuple per (resource, params) — the debounced key changes only after 300ms of input stability |
| T-14-03-04 | Information Disclosure | usePiiMasking.revealField calls api.post for /audit/unmask — the request body contains { field, reason } which is sensitive (audit log entries for PII unmasking) | mitigate | The api.post reads the Authorization header from localStorage (D-02) and sends the same body shape as the legacy fetch. The audit-unmask endpoint is server-side audited; the field + reason are stored in the audit log. The api client does not add or remove fields. The legacy code used `fetch(/api/.../audit/unmask, ...)` with the path including `/api` — the new code uses `api.post(/beneficiaries/.../audit/unmask, ...)` which the api client prepends `${API_BASE}/` to. The base URL already includes `/api` (VITE_API_URL is set to `http://localhost:3000/api` in dev). The actual path is therefore `http://localhost:3000/api/beneficiaries/.../audit/unmask` — the same as the legacy code. Verified by the grep gate |
| T-14-03-05 | Tampering | NotificationsDropdown markRead uses useSWRMutation with optimisticData — the optimistic state could be wrong if the server response shape changes | accept | The optimisticData option sets isRead: true on the matching notification ID. The server endpoint /notifications/:id/read sets isRead: true on the server. The optimisticData and the server response match. If the server fails (ApiError 4xx/5xx), the SWR cache reverts on revalidation. The useSWRMutation onSuccess calls mutate(queryKeys.notifications.unreadCount()) to refresh the unread badge |
| T-14-03-06 | Repudiation | AuditorWidgets re-verify uses the destructured mutate from useSWR — the refetch is invisible to the user (no success/error toast) | accept | The handleReVerify button is a manual user action ("Re-verify All Chains"). The mutate triggers a refetch; SWR's isValidating flag controls the button's "Verifying..." text. There is no success/error display because re-verify is idempotent and the result replaces the hash chain display. The user can re-click if needed. No silent failure — the audit chain display is updated when the refetch completes |
| T-14-03-07 | Elevation of Privilege | The 9 remaining pages contain mutations (POST/PUT/DELETE) that could be called by an unauthorized role | accept | Per REQUIREMENTS.md ROL-01 to ROL-04, the backend enforces role-based authorization. The api client does not re-enforce roles. The page-level role gate (if any) is UX-only. The server returns 403 for unauthorized mutations; the api client throws ApiError(403) which the page's try/catch renders as a console.error |
| T-14-03-08 | Tampering | The 67 wrapper functions are deleted from api.ts — if any consumer still imports a wrapper by name, the build will fail | mitigate | The grep gate in Task 1 substep 10 (`grep -rn "from '../lib/api'" kapwa-client/src/ | grep -E "getCases|getDashboard|..."`) returns 0 matches — verified in Task 1. The final grep gate in Task 5 substep 4 confirms 0 raw fetch sites outside the carve-outs. Any consumer that imports a deleted wrapper would fail to compile, and `npx tsc --noEmit` would surface the missing export — caught by the build gate |
| T-14-03-09 | Information Disclosure | ReportsExportButton's raw fetch uses `localStorage.getItem('token')` (not `'kapwa_token'`) — this is a pre-existing bug (per RESEARCH A4) that this plan does NOT fix | accept | Documented in Task 3 substep 10. The fetch sends `Authorization: Bearer ${token}` where `token` is `localStorage.getItem('token')` which is `null` if the user is logged in via the standard auth flow (which stores `kapwa_token`). The fetch will send `Authorization: Bearer null` and the server will return 401. This is a pre-existing bug, out of scope for this plan, and the migration does not change the key (per AGENTS.md "DO NOT ADD ANY COMMENTS unless asked" — the bug is silent). The CONTEXT deferred-ideas list includes "investigate ReportsExportButton token key" as a future phase candidate |
| T-14-03-10 | Denial of Service | Deleting the 67 wrapper functions in api.ts removes the legacy fetchWithTimeout pattern — if any consumer was relying on the wrapper's behavior (e.g., setTimeout abort), the migration could break | accept | The 67 wrappers are pure pass-throughs to apiFetch (which is a thin wrapper around fetch + Authorization header). The api object in Plan 14-01 strictly improves on the legacy: 10s timeout via AbortSignal.timeout, 3× retry on network failure, 401 refresh, Bearer header injection. No consumer loses functionality. The grep gate + the build gate + the test suite all verify no regressions |
| T-14-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds no new packages; all dependencies are from Plan 14-01. No human checkpoint required for installs |
</threat_model>

<verification>
## Automated checks (run after every task commit)

1. `cd kapwa-client && npx vitest run src/lib/sync-conflict.test.ts src/lib/offline-queue.test.ts src/lib/api.test.ts src/lib/api-error.test.ts` — sync.ts + api.ts cleanup
2. `cd kapwa-client && grep -c "fetch(" kapwa-client/src/hooks/useDebouncedSearch.ts kapwa-client/src/hooks/usePiiMasking.ts` — hooks migration (returns 0 for both)
3. `cd kapwa-client && grep -c "fetch(" kapwa-client/src/components/NotificationsDropdown.tsx kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx` — widgets migration (returns 0 for the 3 migrated; ReportsExportButton stays 1)
4. `cd kapwa-client && npx vitest run` — 9 remaining pages migration (≥ 248 tests passing)
5. `cd kapwa-client && npx vitest run` — final 6 pages + grep gate + build (≥ 250 tests passing)

## Final gate (run after Task 5)

`cd kapwa-client && npm run test:run` — 237 baseline (Plan 14-02) + ~15 new = 250+ tests passing, 0 failing, 0 regressions

`cd kapwa-client && npx vitest run src/__tests__/e2e.test.ts` — e2e test still passes

`cd kapwa-client && npm run build` — TypeScript compiles

## Grep gate (run after Task 5)

`grep -rn "fetch(" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -v "src/__tests__/" | grep -v "src/lib/api.ts" | grep -v "src/lib/auth.ts" | grep -v "src/lib/auth-context.tsx" | grep -v "FormData" | grep -v "blob" | wc -l` — must return 0

## Static checks (run after Task 5)

- `grep -E '^export async function' kapwa-client/src/lib/api.ts | wc -l` — returns 0 (67 wrappers gone)
- `grep -c "^export const api" kapwa-client/src/lib/api.ts` — returns 1 (api object present)
- `grep -E "apiFetch|getCases|getDashboard|getBeneficiaries" kapwa-client/src/lib/api.ts | wc -l` — returns 0 (no deleted-wrapper references in api.ts)
- `grep -rn "from '../lib/api'" kapwa-client/src/ --include="*.ts" --include="*.tsx" | grep -E "getCases|getDashboard|getBeneficiaries"` — returns 0 (no consumer imports a deleted wrapper)
- `cd kapwa-client && npx tsc --noEmit -p .` — exits 0 (or only pre-existing e2e.test.ts Buffer error)
</verification>

<success_criteria>
Phase 14-03 success:

1. sync.ts calls `api.post` for /sync/v1, /sync/pull, /sync/conflicts/:id/resolve (3 sites migrated)
2. useDebouncedSearch uses useSWR with debounced key + keepPreviousData; usePiiMasking.revealField uses api.post
3. NotificationsDropdown, AuditorWidgets, ClaimantWidgets migrated to useSWR + useSWRMutation (the 3 widget components + the dropdown); ReportsExportButton stays raw for blob download
4. All 9 remaining pages (CaseTracker, ClaimantDashboard, Messages, AccessCard, ApprovalPipeline, Auditor, MyAccessCard, Register, Interventions) migrated to useSWR / api methods; FormData uploads in ApprovalPipelinePage stay raw
5. The 67 legacy wrapper functions are deleted from src/lib/api.ts; the file now contains only the api object, the URL constant, and the ApiError re-export
6. The grep gate returns 0 raw fetch sites outside the carve-outs (auth.ts, auth-context.tsx login + MFA, FormData, blob)
7. All 9+ updated test files pass; CoordinatorWidgets migrated to useSWR; the 6 final pages (Intake, Irf, Csr, MayorReports, MfaSetup, BeneficiaryView) migrated
8. Full test suite (250+ tests) passes; e2e test still passes; npm run build exits 0
9. 5 atomic commits land (one per task) per the gsd-executor per-task commit protocol
10. Plan summary at `.planning/phases/14-api-client-swr/14-03-SUMMARY.md` enumerates: the 21 GET endpoints (all done), the 67 deleted wrappers, the 10 FormData/blob carve-outs, the 3 raw fetch carve-outs (auth.ts + auth-context login + MFA), the final grep gate result

Phase 14 complete: API-01 (centralized client with retry, timeout, Bearer, 401 refresh) ✓ and API-02 (SWR for data fetching) ✓ from ROADMAP.md success criteria.
</success_criteria>

<output>
## Artifacts this phase produces

**Modified files (~32):**

*Lib (1):*
- `kapwa-client/src/lib/sync.ts` — 3 raw fetch sites → 3 `api.post` calls; local API_URL constant deleted; `getToken()` helper deleted

*Lib cleanup (1):*
- `kapwa-client/src/lib/api.ts` — 67 legacy wrapper functions deleted; only the `api` object + URL constant + `export { ApiError } from './api-error'` remain. The 4 raw-fetch-with-FormData sites (uploadSignature, uploadReceipt) and the 2 raw-fetch-with-blob sites (downloadCsrPdf, exportIrfPdf) and the createUser raw-fetch site are also deleted (the functions are gone; the API endpoints are no longer callable from the client — they are server-side only now, OR callers use the new api.post for the JSON variants). The `dataURItoBlob` helper is deleted if no other consumer uses it

*Hooks (2):*
- `kapwa-client/src/hooks/useDebouncedSearch.ts` — useEffect + raw fetch + AbortController → useSWR with debounced key + keepPreviousData
- `kapwa-client/src/hooks/usePiiMasking.ts` — raw fetch in revealField → `api.post`

*Components (3):*
- `kapwa-client/src/components/NotificationsDropdown.tsx` — 4 raw fetch + setInterval → 2 useSWR (one with refreshInterval 30s) + 2 useSWRMutation (with optimisticData + mutate revalidation)
- `kapwa-client/src/components/dashboard/widgets/AuditorWidgets.tsx` — Promise.all + 2 raw fetch → 2 useSWR (hashChains + consentLedger)
- `kapwa-client/src/components/dashboard/widgets/ClaimantWidgets.tsx` — useEffect + raw fetch → 1 useSWR (myServices)

*Stays raw (1):*
- `kapwa-client/src/components/ReportsExportButton.tsx` — UNCHANGED (blob download carve-out per D-10 deferred)

*Pages (15 — 9 in Task 4 + 6 in Task 5):*
- `kapwa-client/src/pages/CaseTrackerPage.tsx` — 3 raw fetch → 3 useSWR (daily + stats + list)
- `kapwa-client/src/pages/ClaimantDashboardPage.tsx` — 2 raw fetch → 2 useSWR (myServices + myConsent)
- `kapwa-client/src/pages/MessagesPage.tsx` — 4 raw fetch → 1 useSWR (conversations) + 1 useSWR (/auth/me) + 1 conditional useSWR (conversation by id) + 1 useSWRMutation (read)
- `kapwa-client/src/pages/AccessCardPage.tsx` — 2 raw fetch → 2 useSWR (list + log) + api.post (assignCard)
- `kapwa-client/src/pages/ApprovalPipelinePage.tsx` — 2 FormData uploads stay raw; JSON mutations use api.put + mutate
- `kapwa-client/src/pages/AuditorPage.tsx` — 4 raw fetch → 2 useSWR (hashChains + consentLedger) + revalidate
- `kapwa-client/src/pages/MyAccessCardPage.tsx` — 1 raw fetch → 1 useSWR (myAccessCard)
- `kapwa-client/src/pages/RegisterPage.tsx` — 1 raw fetch → `api.post('/auth/register', formData)`
- `kapwa-client/src/pages/InterventionsPage.tsx` — 1 raw fetch → 1 useSWR + api.post
- `kapwa-client/src/pages/IntakePage.tsx` — `submitIntake` → `api.post('/intake', formData)`
- `kapwa-client/src/pages/IrfPage.tsx` — 1 raw fetch → 1 useSWR (irf.list) + api.post/put for mutations
- `kapwa-client/src/pages/CsrPage.tsx` — 1 raw fetch → 1 useSWR (csr.list) + api.post/put/del for mutations
- `kapwa-client/src/pages/MayorReportsPage.tsx` — 1 raw fetch → 1 useSWR (dashboard.mayorReports)
- `kapwa-client/src/pages/MfaSetupPage.tsx` — 1 raw fetch (/auth/me) → 1 useSWR (auth.me) + api.post for setup/enable/disable
- `kapwa-client/src/pages/BeneficiaryViewPage.tsx` — 2 raw fetch → 2 useSWR (beneficiaries.detail + familyGraph)

*Test files (9+):*
- `kapwa-client/src/pages/CaseTrackerPage.test.tsx` — vi.mock('../lib/api', ...) added (replaces vi.stubGlobal('fetch', ...))
- `kapwa-client/src/pages/ClaimantDashboardPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/MessagesPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/AccessCardPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/RegisterPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/InterventionsPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/IrfPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/IntakePage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/CsrPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/MfaSetupPage.test.tsx` — vi.mock added
- `kapwa-client/src/pages/BeneficiaryViewPage.test.tsx` — vi.mock added

*CoordinatorWidgets (1):*
- `kapwa-client/src/components/dashboard/widgets/CoordinatorWidgets.tsx` — `getDashboard` wrapper → useSWR(queryKeys.dashboard.stats())

**Deleted symbols from api.ts (67):**
getCases, getCase, createCase, updateCaseStatus, getDashboard, getBeneficiaries, getBeneficiary, getInterventions, createIntervention, getPrograms, createProgram, updateProgram, deleteProgram, getConsentLedger, getMyAccessCard, getMayorReports, getAuditConsentLedger, verifyHashChains, revokeConsent, getCsrRecords, getCsrRecord, createCsrRecord, updateCsrRecord, deleteCsrRecord, downloadCsrPdf, updateCaseDocuments, approveCase, requestReview, disburseCase, closeCase, overrideCaseStatus, getFamilyGraph, setupMfa, enableMfa, disableMfa, verifyMfa, createUser, submitIntake, uploadSignature, uploadReceipt, dataURItoBlob, getCaseTrackerLog, assignCard, getBeneficiaryCard, getIrfCase, getIrfRecords, createIrf, referToPnp, referToWcpd, dismissIrf, closeIrf, decryptNarration, unmaskIrfNames, getProgramAssignments, getProgramAssignment, createProgramAssignment, approveAssignmentStep, rejectAssignmentStep, overrideAssignmentStep, exportIrfPdf, exportIrfJson, getNotificationPreferences, updateNotificationPreferences, bulkApprove, bulkReassign, bulkExport

**Raw fetch sites that REMAIN (carve-outs, all expected):**
1. `kapwa-client/src/lib/auth.ts` — `/auth/refresh` (D-15, circular dependency)
2. `kapwa-client/src/lib/auth-context.tsx` — `/auth/login` (D-15, pre-auth)
3. `kapwa-client/src/lib/auth-context.tsx` — `/auth/mfa/verify` (D-15, pre-auth with tempToken)
4. `kapwa-client/src/components/ReportsExportButton.tsx` — blob download (D-10 deferred)
5. `kapwa-client/src/pages/FilingPage.tsx` — `/filing/upload` FormData body (D-10 deferred)
6. `kapwa-client/src/pages/FilingPage.tsx` — `/filing/:id/download` blob response (D-10 deferred)
7. `kapwa-client/src/pages/ApprovalPipelinePage.tsx` — 2 FormData uploads (D-10 deferred)

**Phase 14 commit hashes (5 atomic commits for plan 14-03):**
- Task 1: `feat(14-03): migrate sync.ts to api.post + early cleanup of 67 legacy wrappers from api.ts`
- Task 2: `feat(14-03): migrate useDebouncedSearch and usePiiMasking hooks to useSWR + api.post`
- Task 3: `feat(14-03): migrate NotificationsDropdown + AuditorWidgets + ClaimantWidgets to useSWR; ReportsExportButton stays raw for blob`
- Task 4: `feat(14-03): migrate 9 remaining pages (CaseTracker, ClaimantDashboard, Messages, AccessCard, ApprovalPipeline, Auditor, MyAccessCard, Register, Interventions) to useSWR + api methods`
- Task 5: `refactor(14-03): final cleanup of api.ts; migrate 6 final pages (Intake, Irf, Csr, MayorReports, MfaSetup, BeneficiaryView) + CoordinatorWidgets; full e2e suite + build green; phase 14 complete`

Create `.planning/phases/14-api-client-swr/14-03-SUMMARY.md` when done. Phase 14 closes: API-01 ✓ and API-02 ✓ from ROADMAP.md success criteria.
</output>
