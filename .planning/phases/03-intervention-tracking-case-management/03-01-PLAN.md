---
phase: 03-intervention-tracking-case-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-server/package.json
  - kapwa-server/src/database/migrations/20260622000001-CaseFsmLifecycle.ts
  - kapwa-server/src/cases/cases.service.ts
  - kapwa-server/src/cases/cases.controller.ts
  - kapwa-server/src/cases/case-history.entity.ts
  - kapwa-server/src/cases/dto/cases.zod.ts
  - kapwa-server/src/cases/cases.module.ts
  - kapwa-client/src/lib/api.ts
  - kapwa-client/src/pages/CasesPage.tsx
autonomous: true
requirements:
  - INT-02
  - CON-03
must_haves:
  truths:
    - "Social worker can move a pending case to in_review via Request Review button"
    - "Admin can approve an in_review case to approved"
    - "Admin can disburse an approved case to disbursed"
    - "Admin or social worker can close a disbursed case"
    - "Admin can override case status to any state with mandatory reason"
    - "Override transitions are flagged as 'override' in CaseHistory for audit"
    - "Role-gated transitions enforce separation: SW initiates, Admin approves/disburses"
    - "No backward transitions are allowed (e.g., approved back to pending)"
  artifacts:
    - path: "kapwa-server/src/database/migrations/20260622000001-CaseFsmLifecycle.ts"
      provides: "btree_gist extension + case_history transition_type/override_reason columns"
      contains: "CREATE EXTENSION IF NOT EXISTS btree_gist"
    - path: "kapwa-server/src/cases/cases.service.ts"
      provides: "Dedicated per-gate FSM methods with strict sequential transitions"
      exports: ["requestReview", "disburse", "close", "overrideStatus"]
    - path: "kapwa-server/src/cases/cases.controller.ts"
      provides: "Per-gate PATCH endpoints with @Roles decorator"
      exports: ["PATCH :id/request-review", "PATCH :id/disburse", "PATCH :id/close", "PATCH :id/override-status"]
    - path: "kapwa-server/src/cases/case-history.entity.ts"
      provides: "Override audit trail fields"
      contains: "transitionType: 'standard' | 'override'"
    - path: "kapwa-server/src/cases/dto/cases.zod.ts"
      provides: "OverrideStatusSchema with mandatory reason, DisburseSchema"
      exports: ["OverrideStatusSchema", "DisburseSchema"]
    - path: "kapwa-client/src/lib/api.ts"
      provides: "FSM transition API client functions"
      exports: ["requestReview", "disburseCase", "closeCase", "overrideCaseStatus"]
    - path: "kapwa-client/src/pages/CasesPage.tsx"
      provides: "Role-specific action buttons per case status"
      exports: ["CasesPage"]
  key_links:
    - from: "kapwa-client/src/pages/CasesPage.tsx"
      to: "kapwa-client/src/lib/api.ts"
      via: "Calls requestReview(), disburseCase(), closeCase(), overrideCaseStatus()"
      pattern: "requestReview|disburseCase|closeCase|overrideCaseStatus"
    - from: "kapwa-server/src/cases/cases.controller.ts"
      to: "kapwa-server/src/cases/cases.service.ts"
      via: "Delegates to service for each per-gate transition"
      pattern: "this.casesService\\.(requestReview|disburse|close|overrideStatus)"
    - from: "kapwa-server/src/cases/cases.service.ts"
      to: "kapwa-server/src/cases/case-history.entity.ts"
      via: "Logs override transitions with transitionType and overrideReason"
      pattern: "logHistory.*transitionType"
---

<objective>
Deliver the strict 3-gate FSM lifecycle for cases — social worker initiates review, admin approves, admin disburses, admin/SW closes, with admin override for emergencies.

Purpose: This is the core case workflow that all downstream Phase 3 features (interventions, tracker, SLA) depend on. Without strict FSM transitions, staff cannot move cases through the approval pipeline.

Output: DB migration + server per-gate endpoints + client action buttons on CasesPage
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intervention-tracking-case-management/03-CONTEXT.md
@.planning/phases/03-intervention-tracking-case-management/03-RESEARCH.md
@.planning/phases/03-intervention-tracking-case-management/03-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
<name>Task 1: FSM lifecycle end-to-end tests</name>
<files>
  kapwa-server/src/cases/cases.service.spec.ts
</files>
<behavior>
  - Test 1: requestReview moves case from pending to in_review when role is social_worker
  - Test 2: approve moves case from in_review to approved when role is admin
  - Test 3: disburse moves case from approved to disbursed when role is admin
  - Test 4: close moves case from disbursed to closed when role is admin or social_worker
  - Test 5: overrideStatus moves case from any state to any other state with mandatory reason
  - Test 6: overrideStatus throws if reason is empty
  - Test 7: backward transitions (e.g., approved → in_review) throw BadRequestException
  - Test 8: social_worker cannot call approve() — throws ForbiddenException
  - Test 9: admin cannot call requestReview() — throws ForbiddenException
  - Test 10: CaseHistory entry for override has transitionType='override' and overrideReason set
</behavior>
<action>
Create kapwa-server/src/cases/cases.service.spec.ts with the above test cases. Follow existing Jest patterns in the project. Use a mocked repository pattern (not e2e) — mock CaseRepo and HistoryRepo from typeorm. Import Case, CaseStatus from case.entity, CaseHistory from case-history.entity. Instantiate CasesService with mocked repos. Each test should: (1) set up a case with known status, (2) call the method with appropriate role, (3) assert the outcome (status change or exception). For the override audit test, verify the historyRepo.save was called with transitionType: 'override' and overrideReason matching the input. Run `cd kapwa-server && npx jest --no-coverage --force-exit cases.service --testNamePattern="FSM|override|backward|role"` to verify failing tests (RED phase).
</action>
<verify>
<automated>cd kapwa-server && npx jest --no-coverage --force-exit cases.service --testNamePattern="FSM|override|backward|role" 2>&1 | head -30</automated>
</verify>
<done>
10 tests exist in cases.service.spec.ts covering all FSM transitions, override audit, backward rejection, and role enforcement. Tests fail because methods don't exist yet.
</done>
</task>

<task type="feature">
<name>Task 2: DB migration + Server per-gate FSM endpoints</name>
<files>
  kapwa-server/package.json
  kapwa-server/src/database/migrations/20260622000001-CaseFsmLifecycle.ts
  kapwa-server/src/cases/case-history.entity.ts
  kapwa-server/src/cases/dto/cases.zod.ts
  kapwa-server/src/cases/cases.service.ts
  kapwa-server/src/cases/cases.controller.ts
  kapwa-server/src/cases/cases.module.ts
</files>
<action>
This task creates the DB migration and extends server code to implement per-gate FSM with strict 3-gate separation per D-01, D-09, D-10, D-11, D-06.

STEP 1 — Create migration file as 20260622000001-CaseFsmLifecycle.ts:
- Import MigrationInterface, QueryRunner from typeorm
- name = 'CaseFsmLifecycle20260622000001'
- up(): run `CREATE EXTENSION IF NOT EXISTS btree_gist` (needed for future exclusion constraint), then `ALTER TABLE case_history ADD COLUMN IF NOT EXISTS transition_type VARCHAR DEFAULT 'standard'`, then `ALTER TABLE case_history ADD COLUMN IF NOT EXISTS override_reason VARCHAR`. Note: btree_gist extension is created here (defense-in-depth dependency) even though the exclusion constraint on interventions is added in Plan 2. 
- down(): reverse the column additions and drop extension if safe (use IF EXISTS).

STEP 2 — Extend case-history.entity.ts per PATTERNS.md analog (lines 187-225):
- Add @Column({ name: 'transition_type', default: 'standard' }) transitionType: 'standard' | 'override';
- Add @Column({ name: 'override_reason', nullable: true }) overrideReason?: string;
- Leave all existing fields unchanged.

STEP 3 — Extend dto/cases.zod.ts per PATTERNS.md analog (lines 230-268):
- Add export const OverrideStatusSchema = z.object({ status: z.nativeEnum(CaseStatus), reason: z.string().min(1, 'Override reason is required') });
- Add export const DisburseSchema = z.object({ status: z.nativeEnum(CaseStatus) }); (signature optional in disburse)
- Add type exports: OverrideStatusInput, DisburseInput
- Import OverrideStatusInput from the existing patterns — match existing style

STEP 4 — Extend cases.service.ts per PATTERNS.md analog (lines 36-99):
ADD four new methods:

async requestReview(id: string, userRole?: string):
- findById, verify status is PENDING (throw BadRequestException otherwise)
- Verify userRole is 'social_worker' (not admin, per D-01 strict separation). throw ForbiddenException otherwise.
- c.status = CaseStatus.IN_REVIEW; save; logHistory with `transitionType: 'standard'`
- Return updated case

async disburse(id: string, newStatus: CaseStatus, userRole?: string):
- findById, verify status is APPROVED (throw BadRequestException if not)
- Verify userRole is 'admin' (throw ForbiddenException for others, per D-01)
- c.status = CaseStatus.DISBURSED; save; logHistory with remarks 'Disbursed by admin'
- Trigger beneficiary notification per existing pattern (lines 136-144)
- Return updated case

async close(id: string, newStatus: CaseStatus, userRole?: string):
- findById, verify status is DISBURSED (throw BadRequestException if not)
- Allow roles ['admin', 'social_worker'] per D-01
- c.status = CaseStatus.CLOSED; save; logHistory
- Return updated case

async overrideStatus(id: string, targetStatus: CaseStatus, reason: string, userRole?: string):
- findById (any status allowed, per D-10)
- Verify role is 'admin' per D-10
- Reason must be non-empty string (throw BadRequestException if empty, per D-06, D-10)
- oldStatus = c.status; c.status = targetStatus; save
- logHistory with transitionType: 'override' and overrideReason: reason per D-10
- Return updated case

Do NOT modify the existing updateStatus() or approve() methods — keep backward compatibility per RESEARCH.md "Keep for backward compatibility" note.

STEP 5 — Extend cases.controller.ts per PATTERNS.md analog (lines 120-175):
ADD four new endpoints after existing @Patch(':id/approve'):

@Patch(':id/request-review')
@Roles('social_worker')  // per D-01 — only social worker initiates review
async requestReview(@Param('id') id: string, @Request() req: AuthenticatedRequest)

@Patch(':id/disburse')
@Roles('admin')  // per D-01 — only admin disburses
async disburse(@Param('id') id: string, @Body(new ZodPipe(DisburseSchema)) body, @Request() req: AuthenticatedRequest)

@Patch(':id/close')
@Roles('admin', 'social_worker')  // per D-01
async close(@Param('id') id: string, @Request() req: AuthenticatedRequest)

@Patch(':id/override-status')
@Roles('admin')  // per D-10
async overrideStatus(@Param('id') id: string, @Body(new ZodPipe(OverrideStatusSchema)) body, @Request() req: AuthenticatedRequest)

Import the new Zod schemas from dto/cases.zod.

STEP 6 — cases.module.ts: Verify CasesModule imports TypeOrmModule.forFeature([Case, CaseHistory]) — no changes needed unless something is missing. Add any missing imports.

STEP 7 — Check package.json — no changes needed (all dependencies exist except @nestjs/schedule which belongs to Plan 3).
</action>
<verify>
<automated>cd kapwa-server && npx jest --no-coverage --force-exit --passWithNoTests cases 2>&1 | tail -10</automated>
</verify>
<done>
Migration creates btree_gist extension and case_history columns. 4 new service methods (requestReview, disburse, close, overrideStatus) with strict role gating. 4 new controller endpoints with @Roles decorators. Task 1 tests now pass (GREEN).
</done>
</task>

<task type="feature">
<name>Task 3: Client CasesPage — role-specific FSM action buttons</name>
<files>
  kapwa-client/src/lib/api.ts
  kapwa-client/src/pages/CasesPage.tsx
</files>
<action>
STEP 1 — Extend api.ts per PATTERNS.md analog (lines 848-892):
Add the following new functions after the existing approveCase function:

export async function requestReview(id: string) {
  return apiFetch(`/cases/${id}/request-review`, { method: 'PATCH' });
}

export async function disburseCase(id: string) {
  return apiFetch(`/cases/${id}/disburse`, { method: 'PATCH', body: JSON.stringify({ status: 'disbursed' }) });
}

export async function closeCase(id: string) {
  return apiFetch(`/cases/${id}/close`, { method: 'PATCH' });
}

export async function overrideCaseStatus(id: string, status: string, reason: string) {
  return apiFetch(`/cases/${id}/override-status`, { method: 'PATCH', body: JSON.stringify({ status, reason }) });
}

STEP 2 — Extend CasesPage.tsx per PATTERNS.md analog (lines 769-792):
The existing CasesPage renders a table of case data but does NOT show case status or action buttons. Enhance it to:

2a. Update the CaseRow interface to include: id, status, controlNo fields from the API response
2b. In loadCases(), map the case status from the API (c.status as string) 
2c. Add a new "Status" column after the "Category" column showing a status badge
2d. Add an "Actions" column at the end with role-specific action buttons based on case.status
2e. Buttons per the PATTERNS.md pattern:
  - status === 'pending_assessment' + role === 'social_worker': "Request Review" button
  - status === 'in_review' + role === 'admin': "Approve" button
  - status === 'approved' + role === 'admin': "Disburse" button
  - status === 'disbursed' + role in ['admin','social_worker']: "Close" button
2f. The user's role must be injected. Since the existing CasesPage doesn't track user role, add a simple mechanism — either read from localStorage('kapwa_role') or from the JWT token. Use: `const role = localStorage.getItem('kapwa_role') || '';` as a quick approach per D-04 "the agent's Discretion" on UI design.
2g. Each button calls the corresponding api.ts function and reloads cases on success
2h. Handle errors with a simple alert or console.error per existing pattern
2i. Add styling for status badges following existing project patterns (e.g., badge-age, badge-category classes)
2j. Add a loading state for each action button (disable during the API call to prevent double-clicks)

Status badge colors (per the agent's Discretion):
- pending_assessment: yellow/amber badge
- in_review: blue badge
- approved: green badge
- disbursed: purple badge
- closed: gray badge

Use existing CSS classes from index.css where possible; add inline styles or additional class mappings in the component for status badges if no existing classes exist.

Keep ALL existing functionality (CSV export, search, filters, pagination) intact — only ADD the Status column and Actions column.
</action>
<verify>
<automated>cd kapwa-client && npx vitest --passWithNoTests --silent 2>&1 | tail -5</automated>
</verify>
<done>
CasesPage shows case status badges and role-appropriate action buttons. Social worker sees "Request Review" on pending cases. Admin sees "Approve" on in_review, "Disburse" on approved, "Close" on disbursed. api.ts exports 4 new FSM functions. All existing functionality preserved.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (FSM transitions) | Authenticated requests with JWT cross the boundary — role validation is enforced server-side |
| Admin override endpoint | Highest-risk path — mandatory reason + audit trail mitigate abuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | CasesController per-gate endpoints | mitigate | @Roles() decorator restricts each transition to authorized role per D-01 |
| T-03-02 | Tampering | overrideStatus() | mitigate | Requires mandatory reason (zod min(1)) + audit trail with transitionType='override' in CaseHistory per D-10 |
| T-03-03 | Information Disclosure | Error messages (invalid transitions) | accept | Error messages are role-agnostic — no sensitive data leakage |
| T-03-04 | Elevation of Privilege | Client-side role check bypass | mitigate | Server re-validates role in every service method using req.user.role — client-side buttons are UX-only |
| T-03-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. Run `cd kapwa-server && npx jest --no-coverage --force-exit cases 2>&1` — all cases tests pass
2. Run `cd kapwa-client && npx vitest --passWithNoTests --silent 2>&1` — client tests pass
3. Verify migration runs: `cd kapwa-server && npx typeorm migration:run -d src/database/ormconfig.ts 2>&1` (or check that migration file syntax is correct)
</verification>

<success_criteria>
- All 4 per-gate endpoints respond with correct status transitions
- Role enforcement blocks unauthorized transitions (social_worker cannot approve)
- Override endpoint requires reason and records override in CaseHistory
- CasesPage shows status badges and role-specific action buttons
- Backward transitions throw 400 errors
- No existing functionality broken
</success_criteria>

<output>
Create `.planning/phases/03-intervention-tracking-case-management/03-01-SUMMARY.md` when done
</output>
