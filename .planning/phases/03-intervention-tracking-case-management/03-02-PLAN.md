---
phase: 03-intervention-tracking-case-management
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - kapwa-server/src/database/migrations/20260622000002-InterventionFields.ts
  - kapwa-server/src/interventions/intervention.entity.ts
  - kapwa-server/src/interventions/interventions.service.ts
  - kapwa-server/src/interventions/interventions.controller.ts
  - kapwa-server/src/interventions/dto/interventions.zod.ts
  - kapwa-client/src/lib/api.ts
  - kapwa-client/src/pages/InterventionsPage.tsx
  - kapwa-client/src/pages/BeneficiaryViewPage.tsx
autonomous: true
requirements:
  - INT-01
  - INT-03
  - INT-04
  - INT-05
  - INT-08
must_haves:
  truths:
    - "Staff can create an intervention only when case status is 'disbursed'"
    - "Worker signature is mandatory but deferrable — intervention created with signatures_pending status if missing"
    - "Client receipt/liquidation scan can be uploaded separately after intervention creation"
    - "Duplicate intervention (same household + type within 30 days) returns HTTP 409"
    - "Fund source is tagged on each intervention (Regular/PDAF/Legislative/Donation)"
    - "Signature upload via MinIO returns a presigned URL stored on the intervention"
    - "Canvas-based SignaturePad.tsx can be used to capture signatures client-side"
  artifacts:
    - path: "kapwa-server/src/database/migrations/20260622000002-InterventionFields.ts"
      provides: "Exclusion constraint + household_id + signature fields + signature_status enum on interventions"
      contains: "EXCLUDE USING gist"
    - path: "kapwa-server/src/interventions/intervention.entity.ts"
      provides: "Extended Intervention entity with SignatureStatus enum, householdId, clientSignatureUrl, clientReceiptUrl"
      exports: ["SignatureStatus"]
    - path: "kapwa-server/src/interventions/interventions.service.ts"
      provides: "Deferrable signature check, household_id population, 409 duplicate response"
      exports: ["create (enhanced)", "uploadSignature", "uploadReceipt"]
    - path: "kapwa-server/src/interventions/interventions.controller.ts"
      provides: "Signature/receipt upload endpoints"
      exports: ["POST upload-signature", "POST upload-receipt"]
    - path: "kapwa-server/src/interventions/dto/interventions.zod.ts"
      provides: "Extended CreateInterventionSchema with optional sig fields"
      exports: ["CreateInterventionSchema (enhanced)"]
    - path: "kapwa-client/src/lib/api.ts"
      provides: "Signature upload, receipt upload functions"
      exports: ["uploadSignature", "uploadReceipt"]
    - path: "kapwa-client/src/pages/InterventionsPage.tsx"
      provides: "Intervention form with SignaturePad, signature status display"
      exports: ["InterventionsPage"]
    - path: "kapwa-client/src/pages/BeneficiaryViewPage.tsx"
      provides: "Intervention creation form with signature capture for disbursed cases"
      exports: ["BeneficiaryViewPage (enhanced)"]
  key_links:
    - from: "kapwa-client/src/pages/InterventionsPage.tsx"
      to: "kapwa-server/src/interventions/interventions.controller.ts"
      via: "POST /interventions with signature URLs"
      pattern: "createIntervention|uploadSignature"
    - from: "kapwa-server/src/interventions/interventions.service.ts"
      to: "kapwa-server/src/minio/minio.service.ts"
      via: "Uploads signature blobs to MinIO buckets (worker-signatures, client-receipts)"
      pattern: "minioService\\.uploadFile"
    - from: "kapwa-server/src/interventions/interventions.service.ts"
      to: "kapwa-server/src/database/migrations/20260622000002-InterventionFields.ts"
      via: "Exclusion constraint prevents race-condition duplicates"
      pattern: "no_duplicate_intervention_30d"
---

<objective>
Deliver post-disbursement intervention logging with mandatory (but deferrable) signatures, MinIO-based signature upload, duplicate detection (service + DB exclusion constraint), and fund source tracking.

Purpose: Staff can record social services delivered (FA/C/CSR/R/H/HV) after a case is disbursed. Signatures are captured via canvas upload to MinIO. Duplicate interventions within 30 days are blocked at both service and DB levels. Fund source is tagged for reporting.

Output: DB migration (exclusion constraint + intervention fields) + enhanced server intervention service + client intervention form with SignaturePad
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intervention-tracking-case-management/03-CONTEXT.md
@.planning/phases/03-intervention-tracking-case-management/03-RESEARCH.md
@.planning/phases/03-intervention-tracking-case-management/03-PATTERNS.md
@.planning/phases/03-intervention-tracking-case-management/03-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
<name>Task 1: Intervention creation tests — signatures, duplicate, deferrable state</name>
<files>
  kapwa-server/src/interventions/interventions.service.spec.ts
</files>
<behavior>
  - Test 1: create() throws BadRequestException if case status is not disbursed (INT-01)
  - Test 2: create() succeeds with workerSignatureUrl, returns intervention with signatures_collected status
  - Test 3: create() succeeds without workerSignatureUrl but with signatureStatus='signatures_pending' (D-15 deferrable)
  - Test 4: create() throws BadRequestException with HTTP 409 equivalent if duplicate exists within 30 days (INT-03, D-12)
  - Test 5: create() stores fundSource when provided (INT-08)
  - Test 6: create() defaults fundSource to REGULAR when not provided
  - Test 7: create() populates householdId from case→beneficiary→household JOIN
  - Test 8: create() still requires access_card_code ("No Card = No Voucher" guard)
</behavior>
<action>
Create kapwa-server/src/interventions/interventions.service.spec.ts. Mock InterventionRepo and CaseRepo. Follow existing Jest patterns in the project. Use the existing InterventionsService pattern (the real create() method already exists — tests target the enhanced version). Import Intervention, InterventionType, FundSource from entity. Mock Case entity with known status.

For the "409 equivalent" test: the service should throw the existing BadRequestException but the controller should convert it to a 409. Test the service-level check — verify the error message contains "Duplicate" or "duplicate". The actual status code conversion happens in the controller layer (use @HttpCode or exception filter) — mark this test as documenting the expected behavior.

Run `cd kapwa-server && npx jest --no-coverage --force-exit interventions.service --testNamePattern="should throw|should succeed|should store|should default|should populate"` to confirm failing tests exist.
</action>
<verify>
<automated>cd kapwa-server && npx jest --no-coverage --force-exit interventions.service --testNamePattern="should throw|should succeed|should store|should default|should populate" 2>&1 | head -20</automated>
</verify>
<done>
8 test cases cover deferrable signature, duplicate detection (409), fund source tagging, household_id population, and "No Card = No Voucher" guard. Tests fail because enhanced service methods not yet implemented.
</done>
</task>

<task type="feature">
<name>Task 2: Server — intervention entity enhancements + migration + service enhancements</name>
<files>
  kapwa-server/src/database/migrations/20260622000002-InterventionFields.ts
  kapwa-server/src/interventions/intervention.entity.ts
  kapwa-server/src/interventions/interventions.service.ts
  kapwa-server/src/interventions/interventions.controller.ts
  kapwa-server/src/interventions/dto/interventions.zod.ts
  kapwa-server/src/interventions/interventions.module.ts
</files>
<action>
STEP 1 — Create migration 20260622000002-InterventionFields.ts per PATTERNS.md analog (lines 697-763):
up() must run in order:
1. Check btree_gist extension (CREATE EXTENSION IF NOT EXISTS btree_gist — idempotent)
2. ALTER TABLE interventions ADD COLUMN IF NOT EXISTS household_id UUID (denormalized for exclusion constraint per RESEARCH.md Pitfall 1 approach 1)
3. Backfill household_id via UPDATE JOIN (see PATTERNS.md lines 714-723 for exact SQL)
4. CREATE TYPE "public"."intervention_signature_status_enum" AS ENUM('signatures_pending', 'signatures_collected')
5. ALTER TABLE interventions ADD COLUMN IF NOT EXISTS client_signature_url VARCHAR
6. ALTER TABLE interventions ADD COLUMN IF NOT EXISTS client_receipt_url VARCHAR
7. ALTER TABLE interventions ADD COLUMN IF NOT EXISTS signature_status "public"."intervention_signature_status_enum" DEFAULT 'signatures_collected'
8. ALTER TABLE interventions ADD CONSTRAINT no_duplicate_intervention_30d EXCLUDE USING gist (household_id WITH =, intervention_type WITH =, daterange(service_date, service_date + interval '30 days') WITH &&)
9. ALTER TABLE case_tracker_log ADD COLUMN IF NOT EXISTS tracker_id VARCHAR UNIQUE (shared with Plan 3 — added here for dependency ordering)
down(): reverse all changes — drop constraint, drop columns, drop type.

STEP 2 — Extend intervention.entity.ts per PATTERNS.md analog (lines 273-357):
- Add SignatureStatus enum with PENDING = 'signatures_pending', COLLECTED = 'signatures_collected' (export it)
- Add @Column({ name: 'household_id', nullable: true }) householdId?: string;
- Add @Column({ name: 'client_signature_url', nullable: true }) clientSignatureUrl?: string;
- Add @Column({ name: 'client_receipt_url', nullable: true }) clientReceiptUrl?: string;
- Add @Column({ name: 'signature_status', type: 'enum', enum: SignatureStatus, default: SignatureStatus.COLLECTED }) signatureStatus!: SignatureStatus;
- Keep all existing fields unchanged.

STEP 3 — Extend dto/interventions.zod.ts per PATTERNS.md analog (lines 469-493):
- Import SignatureStatus enum
- Add to CreateInterventionSchema: clientSignatureUrl: z.string().url().optional(), signatureStatus: z.nativeEnum(SignatureStatus).optional(), clientReceiptUrl: z.string().url().optional()
- Keep existing required workerSignatureUrl as required per D-15 (but service will allow deferral)

STEP 4 — Extend interventions.service.ts per PATTERNS.md analog (lines 361-443):
Changes to create() method:

4a. Change duplicate detection response from BadRequestException to throw new ConflictException (import from @nestjs/common) — this returns HTTP 409 per D-12. Import ConflictException alongside existing exceptions.

4b. Change worker signature check from blocking (existing lines 51-53 throw error if missing) to deferrable: if !data.workerSignatureUrl, set signatureStatus to SignatureStatus.PENDING and skip the throw. If workerSignatureUrl IS provided, set signatureStatus to SignatureStatus.COLLECTED (or use the provided signatureStatus override). Per D-15.

4c. Add household_id population before hash chain creation per PATTERNS.md lines 430-442: query households table via case→beneficiary→household JOIN.

4d. In the interventionRepo.create() call, add: householdId, clientSignatureUrl, clientReceiptUrl, signatureStatus from data.

Add two new methods:

async uploadSignature(fileBuffer: Buffer, fileName: string, mimeType: string): Promise<string>:
- Accept the MinioService via constructor injection (add private minioService: MinioService to constructor)
- Delegate to this.minioService.uploadFile('worker-signatures', fileName, fileBuffer, mimeType)
- Return the presigned URL

async uploadReceipt(fileBuffer: Buffer, fileName: string, mimeType: string): Promise<string>:
- Delegate to this.minioService.uploadFile('client-receipts', fileName, fileBuffer, mimeType)
- Return the presigned URL

Inject MinioService into constructor: add private minioService: MinioService after existing dependencies. Import MinioService from '../minio/minio.service'. Add to module providers if needed.

STEP 5 — Extend interventions.module.ts:
- Add MinioService to the providers array (or verify it's available via global module)
- If MinioModule is not globally registered, import it: `MinioModule`

STEP 6 — Extend interventions.controller.ts per PATTERNS.md analog (lines 448-465):
Add two new POST endpoints:

@Post('upload-signature')
@Roles('admin', 'social_worker')
@UseInterceptors(FileInterceptor('file'))
async uploadSignature(@UploadedFile() file: any) {
  if (!file) throw new BadRequestException('File is required');
  const url = await this.intService.uploadSignature(file.buffer, `${Date.now()}-${file.originalname}`, file.mimetype);
  return { url };
}

@Post('upload-receipt')
@Roles('admin', 'social_worker')
@UseInterceptors(FileInterceptor('file'))
async uploadReceipt(@UploadedFile() file: any) {
  if (!file) throw new BadRequestException('File is required');
  const url = await this.intService.uploadReceipt(file.buffer, `${Date.now()}-${file.originalname}`, file.mimetype);
  return { url };
}

Import FileInterceptor from '@nestjs/platform-express', UploadedFile from '@nestjs/common'.
</action>
<verify>
<automated>cd kapwa-server && npx jest --no-coverage --force-exit interventions 2>&1 | tail -10</automated>
</verify>
<done>
Migration adds btree_gist (idempotent), household_id, signature fields, exclusion constraint, tracker_id. Intervention entity extended with 4 new fields. Service create() allows deferrable signatures, populates householdId, throws ConflictException (409) on duplicate. Two new upload endpoints delegate to MinIO. Task 1 tests now pass.
</done>
</task>

<task type="feature">
<name>Task 3: Client — intervention form with SignaturePad + MinIO upload + signature status</name>
<files>
  kapwa-client/src/lib/api.ts
  kapwa-client/src/pages/InterventionsPage.tsx
  kapwa-client/src/pages/BeneficiaryViewPage.tsx
</files>
<action>
STEP 1 — Extend api.ts per PATTERNS.md analog (lines 848-891):
Add the following functions after existing functions:

export async function uploadSignature(file: Blob, fileName: string): Promise<string> {
  const token = localStorage.getItem('kapwa_token');
  const formData = new FormData();
  formData.append('file', file, fileName);
  const res = await fetch(`${API}/interventions/upload-signature`, {
    method: 'POST',
    headers: { Authorization: `Bearer ${token}` },
    body: formData,
  });
  if (!res.ok) throw new Error('Signature upload failed');
  const data = await res.json();
  return data.url;
}

export async function uploadReceipt(file: Blob, fileName: string): Promise<string> {
  const token = localStorage.getItem('kapwa_token');
  const formData = new FormData();
  formData.append('file', file, fileName);
  const res = await fetch(`${API}/interventions/upload-receipt`, {
    method: 'POST',
    headers: { Authorization: `Bearer ${token}` },
    body: formData,
  });
  if (!res.ok) throw new Error('Receipt upload failed');
  const data = await res.json();
  return data.url;
}

export function dataURItoBlob(dataUrl: string): Blob {
  const [header, base64] = dataUrl.split(',');
  const mime = header.match(/:(.*?);/)?.[1] || 'image/png';
  const binary = atob(base64);
  const array = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) array[i] = binary.charCodeAt(i);
  return new Blob([array], { type: mime });
}

STEP 2 — Extend InterventionsPage.tsx:
The existing page has a minimal create form. Enhance it per PATTERNS.md analog (lines 797-812):

2a. Add a "Signature" column to the interventions table showing a signature status badge: if signatureStatus === 'signatures_pending' show a pending badge (yellow), otherwise show a "Signed" badge (green). Use the existing intervention data mapping — add signatureStatus and clientReceiptUrl to the Intervention interface.

2b. After the table, if showForm is true, render the enhanced form with:
  - caseId input (text, required)
  - Intervention type dropdown (FA/C/CSR/R/H/HV)
  - Amount input (number)
  - Fund source dropdown (Regular/PDAF/Legislative/Donation)
  - Agency input (text)
  - Worker Signature section: embed SignaturePad.tsx component (import from '../components/forms/SignaturePad'). On save, convert dataUrl to Blob via dataURItoBlob(), upload via uploadSignature(), store returned URL in form state.
  - Client Receipt section: file input for photo upload → upload via uploadReceipt() → store URL
  - Submit button that calls createIntervention() with all fields

2c. Import SignaturePad: `import { SignaturePad } from '../components/forms/SignaturePad';` — check actual export name from the file first.

STEP 3 — Extend BeneficiaryViewPage.tsx:
The existing page shows beneficiary detail with cases and interventions. Enhance it to support intervention creation for disbursed cases. After reading the existing file:

3a. Add a "Log Intervention" button that appears when the beneficiary has a case with status 'disbursed'. Show it in the Cases section.

3b. When clicked, show an inline intervention form with:
  - Intervention type dropdown
  - Amount input
  - Fund source dropdown
  - SignaturePad for worker signature
  - File upload for client receipt
  - Submit button

3c. Read more of the existing BeneficiaryViewPage.tsx to find the case rendering section and add the button/form there. Follow existing UI patterns (lucide icons, color scheme #2E5C8A).

For both pages: use the existing index.css classes and project color conventions. Handle errors gracefully with user-facing messages.
</action>
<verify>
<automated>cd kapwa-client && npx vitest --passWithNoTests --silent 2>&1 | tail -5</automated>
</verify>
<done>
api.ts exports uploadSignature, uploadReceipt, dataURItoBlob. InterventionsPage shows signature status badges and enhanced creation form with SignaturePad. BeneficiaryViewPage shows "Log Intervention" button on disbursed cases with inline form. Signature data flows: canvas → dataUrl → Blob → MinIO upload → URL stored on intervention.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → MinIO upload | Large binary uploads cross the internet boundary — size limits enforced by FileInterceptor |
| client → API (intervention create) | Intervention payload with signature URLs crosses — validation via ZodPipe |
| MinIO presigned URL | URL is stored in DB and rendered in UI — URL expires after 24h |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Duplicate detection bypass (race condition) | mitigate | Defense-in-depth: service-level check (line 44-47) + DB exclusion constraint (migration step 8) per D-13 |
| T-03-06 | Information Disclosure | Duplicate check error details (SQL constraint violation) | mitigate | Catch 23P01 and return generic 409 — never expose raw constraint error per RESEARCH.md Pitfall 6 |
| T-03-07 | Spoofing | Signature URL manipulation | mitigate | URLs are opaque references; Presigned MinIO URLs expire after 24h per minio.service.ts. Server does NOT re-validate URL content. |
| T-03-08 | Tampering | Offline queue bypass for approval/disbursement | accept | D-04 explicitly blocks approval/disbursement offline — only pending→in_review is allowed offline |
| T-03-09 | Tampering | Duplicate check change from 400 to 409 | mitigate | Existing BadRequestException(400) changed to ConflictException(409) per D-12. Handled in exception filter. |
| T-03-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `cd kapwa-server && npx jest --no-coverage --force-exit interventions 2>&1` — all intervention tests pass
2. `cd kapwa-client && npx vitest --passWithNoTests --silent 2>&1` — client tests pass  
3. Verify migration syntax: `cd kapwa-server && npx ts-node -e "import('./src/database/migrations/20260622000002-InterventionFields.ts')"` or just check for TypeScript compilation
4. Spot-check: `grep -c "ConflictException" kapwa-server/src/interventions/interventions.service.ts` should be >= 1
5. Spot-check: `grep -c "no_duplicate_intervention_30d" kapwa-server/src/database/migrations/20260622000002-InterventionFields.ts` should be 1
</verification>

<success_criteria>
- Intervention creation blocked if case not disbursed (INT-01)
- Duplicate household+type within 30 days returns 409, not 400 (INT-03, D-12)
- Worker signature can be deferred — intervention created with signatures_pending status (INT-04, D-15)
- Client receipt URL can be stored on intervention (INT-05)
- Fund source tagged on each intervention (INT-08, D-03)
- Signature upload endpoints store to MinIO and return presigned URL
- Client intervention form includes SignaturePad.tsx canvas capture
- No existing functionality broken
</success_criteria>

<output>
Create `.planning/phases/03-intervention-tracking-case-management/03-02-SUMMARY.md` when done
</output>
