---
phase: 04
slug: access-card-system
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-22
---

# Phase 04 — UI Design Contract

> Visual and interaction contract for Access Card System frontend. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom CSS + Tailwind CSS 3.4) |
| Icon library | lucide-react ^1.14.0 |
| Font | Body: Inter (300-700), Headings: Plus Jakarta Sans (700) |

**Source:** `kapwa-client/src/index.css` established the design language across Phases 1-3. Phase 4 uses the existing system — no new design tokens introduced.

---

## Spacing Scale

Declared values (Tailwind CSS default spacing scale — multiples of 4 with 12px exception):

| Token | Value | Usage |
|-------|-------|-------|
| 1 | 4px | Icon gaps, compact label padding (badge px-2 py-0.5) |
| 2 | 8px | Compact element spacing (filter-pill gap, compact form padding) |
| 3 | 12px | Default inline padding (px-3, py-3), gap-3 for compact grids |
| 4 | 16px | Default element spacing (p-4, gap-4), card body padding |
| 6 | 24px | Section padding (p-6, mb-6), main content area padding |
| 8 | 32px | Major section breaks, page-level spacing (my-8, p-8) |
| 12 | 48px | Wide layout gaps between major page sections |
| 16 | 64px | Page max-width gutters, top-level container padding |

**Exceptions:** 12px (Tailwind `3` / `gap-3` / `px-3`) is used extensively in the codebase for inline horizontal padding and compact grid gaps — this deviates from the strict 4px scale but is standard Tailwind spacing. All spacings derive from Tailwind's default `theme.spacing` scale.

**Touch targets:** Icon buttons are 40×40px minimum (`.icon-btn { width: 40px; height: 40px; }`). Form inputs use 8px 12px padding for comfortable tap targets. No exceptions needed for this phase.

---

## Typography

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Body | 14px | 400 (Regular) | 1.5 (20px) | Inter |
| Label | 12px | 600 (Semibold) | 1.5 (18px) | Inter |
| Heading | 16px | 700 (Bold) | 1.5 (24px) | Plus Jakarta Sans |
| Display | 24px | 700 (Bold) | 1.33 (32px) | Plus Jakarta Sans |

**Notes:**
- Label uses `text-transform: uppercase` with `letter-spacing: 0.5px` (see `.text-style-label`, `.text-style-label-upper`)
- Table header text uses 12px / 600 / uppercase (see `.table th`)
- Monospace (`font-mono`) reserved for ID display — card codes (`NORZ-AC-YYYY-####`), UUIDs, sequence numbers
- Table cell body uses 14px (standard body) with no explicit line-height (inherits 1.5)
- Muted/secondary body text uses `color: #707070` at 14px

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F9F9FD` | Page background, layout backgrounds |
| Secondary (30%) | `#FFFFFF` | Cards, sidebar, header, table containers, form sections |
| Accent (10%) | `#2E5C8A` | Primary buttons, brand elements, active nav link, link text, municipal seal, progress indicators |
| Accent hover | `#1E3D5E` | Primary button hover, link hover |
| Accent light | `#E8F0F7` | Active nav background, category badge background, active table row hover |
| Border | `#E0E1E3` / `#E2E8F0` | Card borders, table borders, sidebar border, input borders |
| Body text | `#1A1A1A` | Primary text, table cell text, page title |
| Muted text | `#707070` | Secondary text, table header, placeholder, labels |
| Input background | `#F8FAFC` | Form input, select, search input backgrounds |
| Destructive | `#DC3545` | Notification dot, error states (red-500 for delete actions) |
| Success | `#155724` | Success messages, "Active" status, "Signed" badge |
| Warning | `#856404` | Warning messages, "Pending" status, "Overdue" badges |
| Error bg | `#FFF3CD` | Alert/warning background, error message background |
| Success bg | `#D4EDDA` | Success message background, approved status |
| Destructive bg | `#F8D7DA` | Error/destructive message area background |

**Accent reserved for:** Primary action buttons (`.btn-primary`), active navigation link (`.nav-link.active`), brand/municipal seal, link text (`<a>`), status badges (`.badge-category`), icon color in section headers. **Not used for:** Borders, backgrounds, secondary buttons, disabled elements, or decorative elements.

**Phase 4-specific color usage:**
- `#2E5C8A` — "Generate & Assign Card" / "Print Card" / "Reprint" primary buttons
- `#FFF3CD` + `#856404` — No Card warning banner (soft warning per D-03)
- `#E8F0F7` — Printable card view informational banner background
- `#D4EDDA` + `#155724` — Card assignment success confirmation
- `#DC3545` — Reprint identity verification `window.confirm()` is browser-native (no custom styling)
- Table row `#F5F5F5` — Zebra striping (even rows), `#E8F0F7` — Row hover
- Card code display: `font-mono, font-medium` at `12px` text size

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (generate) | **Generate & Assign Card** — shown as button on beneficiary detail page and on Access Card management page |
| Primary CTA (print) | **Print Card** — triggers `window.print()` on the printable card view |
| Primary CTA (reprint) | **Reprint Card** — opens identity verification before navigation to print |
| Empty state (no card) | **No Access Card** — "This beneficiary has no Access Card. Generate and assign one to enable card-based tracking." |
| Empty state (service log) | **No Services Logged** — "This card's service log is empty. Services will appear here once interventions are logged for this beneficiary." |
| No Card warning (soft) | **No Access Card Found** — "This beneficiary has no active Access Card. Interventions can still be logged by enabling override. Click 'Log Anyway' to proceed without a card." |
| No Card warning (override CTA) | **Log Anyway** — secondary button on the No Card warning banner / checkbox label in intervention form |
| Error state (api failure) | **Failed to load card data** — "Could not retrieve Access Card information. Check your connection and try again." |
| Destructive action (reprint confirmation) | **Reprint Access Card** — "Reprint card for {beneficiary_name}? Current code: {NORZ-AC-####} will remain valid. Verify claimant identity before proceeding." (displayed via `window.confirm()`) |
| Assign confirmation (success) | **Card Assigned** — "Access Card {code} has been assigned to {beneficiary_name}." (shown as green success banner) |
| Card print header | **MSWDO Norzagaray — Access Card** — page title on printable card view |
| Loading state | **Loading card data...** — shown while fetching card or beneficiary data |

### Interaction copy notes:
- No Card override uses `overrideNoCardCheck: boolean` in API request body — client UI labels this as "Beneficiary has no Access Card — proceed anyway?"
- Reprint identity verification uses `window.confirm()` with beneficiary name + current card code — no custom modal per D-04 (simple confirmation matches physical office workflow)
- The empty service log uses "No services logged yet" — not "No data" — to imply action expected
- The "Card Assigned" success message stays visible for 3 seconds or until user navigation

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no shadcn or third-party registries in use |

The project does not use shadcn/ui. All components use custom CSS (.css in `kapwa-client/src/index.css`) with Tailwind CSS 3.4 utility classes and lucide-react icons. No third-party UI registries are declared. No registry vetting required.

---

## Phase 4-Specific UI Components

### 1. Generate & Assign Card UI (AC-01)
- **Where:** BeneficiaryViewPage (`/beneficiaries/:id`) — new button in the personal info card section AND AccessCardPage (`/access-cards`)
- **Interaction:** Pick beneficiary (via existing beneficiary selector) → click "Generate & Assign Card" → POST `/access-cards/assign/:beneficiaryId` → show `{ accessCardCode }` in green success banner
- **States:** Idle (default), Loading (button shows "Generating..."), Success (green banner with code), Error (red banner with API error)
- **Role gate:** `@Roles('admin')` — only Admin can assign cards

### 2. No Card Warning Overlay (AC-04, per D-03)
- **Where:** InterventionsPage / BeneficiaryViewPage intervention form
- **Interaction:** When `createIntervention` returns warning (beneficiary has no card) → yellow warning banner shown at top of form with override checkbox "Beneficiary has no Access Card — proceed anyway?"
- **States:** No card detected (warning banner visible), Override checked (request sends `overrideNoCardCheck: true`), Override not checked (blocked with 400 error — user must check override)
- **Visual:** Yellow/amber banner `#FFF3CD` background with `#856404` text, alert icon from lucide

### 3. Printable Card View (AC-02, per D-06)
- **Where:** Dedicated route `/beneficiary/:id/card/print` (AccessCardPrintView)
- **Interaction:** Shows card on screen → click "Print Card" → `window.print()` with `@media print` CSS
- **Layout:** 
  - Screen: card preview with print button, beneficiary info, service log table
  - Print: A4 portrait, 15mm margins, white background, monospace card code at 18pt, service log table with borders
- **States:** Loading, Loaded (card data visible), No card found (empty state), Print dialog open (browser native)

### 4. Reprint Verification (AC-03, per D-04)
- **Where:** BeneficiaryViewPage — "Reprint Card" button when beneficiary already has `access_card_code`
- **Interaction:** Click "Reprint Card" → `window.confirm()` showing beneficiary name + current card code + "Verify claimant identity" → if confirmed, navigate to `/beneficiary/:id/card/print`
- **States:** Card exists (reprint button visible), No card (button hidden), Confirmation dialog open (browser native)

### 5. Admin Card Management (AC-01, AC-02)
- **Where:** AccessCardPage (`/access-cards`)
- **Interaction:** Search beneficiary → assign card → view card service log → reprint / print
- **States:** Loading, Table loaded, Search active, Empty (no beneficiaries), Error

---

## Existing Routes & Navigation

| Route | Component | Role Access | Phase 4 Changes |
|-------|-----------|-------------|-----------------|
| `/access-cards` | AccessCardPage | admin, social_worker | REWRITE: replace generate/log/search UI with assign workflow + service log table |
| `/beneficiaries/:id` | BeneficiaryViewPage | admin, social_worker | EXTEND: add "Generate & Assign Card" / "Reprint Card" buttons in beneficiary profile |
| `/beneficiary/:id/card/print` | AccessCardPrintView (NEW) | admin, social_worker | NEW: printable card view route |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
