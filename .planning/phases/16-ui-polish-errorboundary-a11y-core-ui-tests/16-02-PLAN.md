---
phase: 16-ui-polish-errorboundary-a11y-core-ui-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - kapwa-client/src/components/Layout.test.tsx
  - kapwa-client/src/components/Topbar.test.tsx
  - kapwa-client/src/components/Sidebar.test.tsx
  - kapwa-client/src/components/ProtectedRoute.test.tsx
autonomous: true
requirements:
  - TST-05
  - A11Y-01
user_setup: []

must_haves:
  truths:
    - "Layout renders a `<main id=\"main-content\">` element (the Outlet mount point)"
    - "Layout renders the SkipToContent anchor link as the first focusable element (A11Y-01 verified)"
    - "Layout's `<Outlet>` is wrapped in `<ErrorBoundary>` (the offline UI restored in 16-01 applies to every page)"
    - "Layout shows the AriaLiveRegion offline message when `navigator.onLine === false`"
    - "Layout has zero axe violations (WCAG 2 A + AA)"
    - "Topbar renders a `<header>` element without crashing"
    - "Topbar's user dropdown / logout button is wired (renders for a social_worker mock)"
    - "Topbar's role-gated buttons (New Intake, Approvals Queue) appear for social_worker role and not for claimant role"
    - "Topbar has zero axe violations"
    - "Sidebar renders a nav container without crashing"
    - "Sidebar's role-gated nav groups appear for the social_worker role"
    - "Sidebar's active link has the `bg-muted` highlight class when the URL matches"
    - "ProtectedRoute redirects to `/login` when there is no `kapwa_token` in localStorage"
    - "ProtectedRoute renders children when the user is authenticated and the role is in the `roles` prop"
    - "ProtectedRoute shows 'Verifying access…' while the async `getCurrentUser()` is in flight"
    - "ProtectedRoute redirects to the role-specific page (e.g., `/dashboard` for social_worker) when the role does not match"
  prohibitions:
    must_not:
      - "Mock the child components (Sidebar, Topbar, SyncStatusBanner, BottomNav) in Layout.test.tsx — render the real Layout subtree so axe sees the real DOM"
      - "Add `axe()` to ProtectedRoute.test.tsx — it has no DOM output of its own (it just decides what to render per D-06)"
      - "Mock `getCurrentUser` to throw — use a resolved promise to avoid unhandled rejection noise in the test output"
      - "Use `axe(document.body)` — always use `axe(container)` scoped to the rendered subtree (RESEARCH.md anti-pattern)"
      - "Inline-import `vitest-axe/extend-expect` in any of these test files — 16-01 wired it globally in tests/setup.ts; the matcher is available everywhere"
      - "Add any comments to the test files (per AGENTS.md 'DO NOT ADD ANY COMMENTS unless asked' — but `// Mock useMediaQuery` 1-line fixture notes inherited from BottomNav.test.tsx:6 are allowed)"
      - "Mock the AriaLiveRegion in Layout tests — it is part of the rendered DOM and axe must see it as-is"
      - "Skip the `await waitFor(() => ...)` pattern in ProtectedRoute tests — the async state transition is fundamentally racy without it"
  artifacts:
    - path: kapwa-client/src/components/Layout.test.tsx
      provides: "5 Layout tests (renders, SkipToContent, ErrorBoundary wrap, AriaLiveRegion offline, axe)"
      contains: "toHaveNoViolations"
    - path: kapwa-client/src/components/Topbar.test.tsx
      provides: "4 Topbar tests (renders, user dropdown, role-gated buttons, axe)"
      contains: "toHaveNoViolations"
    - path: kapwa-client/src/components/Sidebar.test.tsx
      provides: "3 Sidebar tests (renders, role-gated nav, active link highlight)"
      contains: "bg-muted"
    - path: kapwa-client/src/components/ProtectedRoute.test.tsx
      provides: "4 ProtectedRoute tests (no-token redirect, authed renders, loading state, role mismatch)"
      contains: "Verifying access"
  symbols:
    - name: renderLayout
      kind: function
      file: kapwa-client/src/components/Layout.test.tsx
      signature: "() => RenderResult"
      behavior: "Renders Layout wrapped in MemoryRouter with `/dashboard` as the initial entry; used by all Layout tests"
    - name: mockUseAuth
      kind: vi.fn
      file: kapwa-client/src/components/Topbar.test.tsx
      signature: "() => AuthContextValue"
      behavior: "Stub useAuth returning a social_worker user for the standard tests; override per-test for the role-gating test"
  key_links:
    - from: kapwa-client/src/components/Layout.test.tsx
      to: kapwa-client/src/components/Layout.tsx
      via: "vi.mock('@/lib/auth-context', ...) + vi.mock('@/lib/offline-queue', ...) + MemoryRouter; axe(container) on the rendered Layout"
      pattern: "render\\(<MemoryRouter[^>]*>\\s*<Layout"
    - from: kapwa-client/src/components/Topbar.test.tsx
      to: kapwa-client/src/components/Topbar.tsx
      via: "vi.mock('@/lib/auth-context', ...) + vi.mock('@/lib/theme-context', ...) + MemoryRouter; axe(container) on the rendered Topbar"
      pattern: "render\\(<MemoryRouter[^>]*>\\s*<Topbar"
    - from: kapwa-client/src/components/Sidebar.test.tsx
      to: kapwa-client/src/components/Sidebar.tsx
      via: "vi.mock('@/lib/auth-context', ...) + MemoryRouter; assert `<aside>` is present and `bg-muted` class is on the active link"
      pattern: "bg-muted"
    - from: kapwa-client/src/components/ProtectedRoute.test.tsx
      to: kapwa-client/src/components/ProtectedRoute.tsx
      via: "vi.mock('../lib/auth-context', () => ({ getCurrentUser: vi.fn() })); localStorage.setItem('kapwa_token', ...); vi.mocked(getCurrentUser).mockResolvedValue(user); await waitFor(() => screen.findByText(...))"
      pattern: "vi\\.mock\\(['\"]\\.\\./lib/auth-context['\"]"
---

<objective>
Add per-concern smoke tests for the four core layout components — Layout, Topbar, Sidebar, ProtectedRoute — with strict `toHaveNoViolations()` axe integration on Layout, Topbar, and Sidebar. Cover the per-concern behaviors identified in D-04: SkipToContent presence and focusability, ErrorBoundary wrap of the Outlet, AriaLiveRegion offline-state rendering, sync queue panel mount, role-gated navigation, active link highlight, auth state machine, and role-mismatch redirect. ProtectedRoute tests are render/redirect assertions only (no axe — no DOM output of its own per D-06). The test files use the same pattern as `CaseTrackerPage.test.tsx` (per D-05) — `<MemoryRouter>` + `vi.mock('@/lib/auth-context', ...)` + SWRConfig with stable mock fetcher where needed; no live network/IndexedDB/Capacitor.

Purpose: Satisfies TST-05 (smoke tests for the four named components) and A11Y-01 (SkipToContent present and focusable in Layout). Establishes the per-concern test pattern that Phase 17 uses for the CI axe gate.

Output: 4 new test files (Layout.test.tsx, Topbar.test.tsx, Sidebar.test.tsx, ProtectedRoute.test.tsx) co-located beside their source files. 16 new tests total: Layout 5, Topbar 4, Sidebar 3, ProtectedRoute 4.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-CONTEXT.md
@.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-RESEARCH.md
@.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-PATTERNS.md
@.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-01-PLAN.md

kapwa-client/src/components/Layout.tsx
kapwa-client/src/components/Topbar.tsx
kapwa-client/src/components/Sidebar.tsx
kapwa-client/src/components/ProtectedRoute.tsx
kapwa-client/src/components/PublicLayout.test.tsx
kapwa-client/src/components/BottomNav.test.tsx
kapwa-client/src/components/a11y/SkipToContent.tsx
kapwa-client/src/components/a11y/AriaLiveRegion.tsx
kapwa-client/src/lib/nav-config.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Layout.test.tsx with 5 tests (renders, SkipToContent, ErrorBoundary wrap, AriaLiveRegion offline, axe)</name>
  <files>kapwa-client/src/components/Layout.test.tsx</files>
  <read_first>kapwa-client/src/components/Layout.tsx,kapwa-client/src/components/PublicLayout.test.tsx,kapwa-client/src/components/BottomNav.test.tsx,kapwa-client/src/components/a11y/SkipToContent.tsx,kapwa-client/src/components/a11y/AriaLiveRegion.tsx</read_first>
  <behavior>
    - Layout mounts the main content area with `id="main-content"` and renders the SkipToContent anchor as the first focusable element
    - The SkipToContent anchor's `href` is `#main-content` (correct in-page jump target)
    - Layout's AriaLiveRegion announces "You are offline" when `navigator.onLine` is set to `false` per-test, and is silent when online
    - Layout has zero axe-core violations in the rendered DOM
    - The `<Outlet>` is wrapped in `<ErrorBoundary>` (the test verifies the `main` element is present, not the ErrorBoundary itself, since it's transparent on success)
  </behavior>
  <action>Create `kapwa-client/src/components/Layout.test.tsx` (new file). At the top, mock `../lib/auth-context` with a stubbed `useAuth` returning `{ user: { id: '1', email: 'a@b.com', fullName: 'A B', role: 'social_worker' }, token: 'test-tok', loading: false, login: vi.fn(), logout: vi.fn(), mfaChallenge: null, resolveMfa: vi.fn(), cancelMfa: vi.fn() }`. Mock `../lib/offline-queue` with `{ loadQueue: () => [] }` (RESEARCH.md Pitfall 3 — prevents `computePendingCount` from reading stale localStorage). Do NOT mock child components (Sidebar, Topbar, SyncStatusBanner, BottomNav) — render the real Layout subtree so axe sees the real DOM. Define a `renderLayout` helper that wraps `<Layout />` in `<MemoryRouter initialEntries={['/dashboard']}>`. Write 5 tests inside a `describe('Layout', ...)` block: (1) `renders the main container` — assert `container.querySelector('main#main-content')` is truthy; (2) `renders the SkipToContent link as the first focusable element` — assert `screen.getByText('Skip to content')` is truthy AND its closest `<a>` has `href="#main-content"`; (3) `wraps the Outlet in ErrorBoundary` — assert the `main#main-content` element is present (the ErrorBoundary is transparent on success); (4) `AriaLiveRegion shows offline message when navigator.onLine is false` — use `Object.defineProperty(navigator, 'onLine', { value: false, configurable: true })` per-test, then assert the offline message text is in the DOM, then restore `navigator.onLine` to `true` in the test body; (5) `has no axe violations` — `import { axe } from 'vitest-axe';` then `const results = await axe(container); expect(results).toHaveNoViolations();` (the matcher was wired globally in 16-01, so no per-file import of `vitest-axe/extend-expect` is needed). Do NOT add comments inside the tests (AGENTS.md no-comments rule). The mock setup uses a single `vi.mock` call before the `describe` block.</action>
  <verify>cd kapwa-client && npx vitest run src/components/Layout.test.tsx 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - File `kapwa-client/src/components/Layout.test.tsx` exists with 5 tests inside a single `describe('Layout', ...)` block
    - `vi.mock('../lib/auth-context', ...)` is at the top with a `social_worker` user
    - `vi.mock('../lib/offline-queue', ...)` is at the top returning `{ loadQueue: () => [] }`
    - `renderLayout` helper is defined and uses `<MemoryRouter initialEntries={['/dashboard']}>`
    - Test 1 asserts `container.querySelector('main#main-content')` is truthy
    - Test 2 asserts SkipToContent text is present AND its `href` is `#main-content`
    - Test 3 asserts the main element is present (ErrorBoundary wrap is verified by structural presence)
    - Test 4 toggles `navigator.onLine` per-test and asserts the offline message is rendered
    - Test 5 imports `axe` from `vitest-axe` and uses `expect(results).toHaveNoViolations()`
    - `npx vitest run src/components/Layout.test.tsx` shows 5/5 tests pass with 0 axe violations
    - No comments in the test file (except potentially a 1-line `// Mock ...` fixture note inherited from existing patterns)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Topbar.test.tsx with 4 tests (renders, user dropdown, role-gated buttons, axe)</name>
  <files>kapwa-client/src/components/Topbar.test.tsx</files>
  <read_first>kapwa-client/src/components/Topbar.tsx,kapwa-client/src/components/BottomNav.test.tsx,kapwa-client/src/components/PublicHeader.test.tsx</read_first>
  <behavior>
    - Topbar renders a `<header>` element without crashing
    - For a social_worker user, the "New Intake" and "Approvals Queue" buttons are present in the rendered DOM (role-gating per Topbar.tsx:38-42)
    - For a claimant user, those role-gated buttons are absent (proving the role gate is wired)
    - The user menu (logout trigger) is rendered for any authenticated user
    - Topbar has zero axe-core violations
  </behavior>
  <action>Create `kapwa-client/src/components/Topbar.test.tsx` (new file). At the top, mock `../lib/auth-context` with a stubbed `useAuth` returning a social_worker user. Mock `@/lib/theme-context` with `{ useTheme: () => ({ theme: 'light', resolvedTheme: 'light', setTheme: vi.fn() }) }` (Topbar uses both contexts — RESEARCH.md). Define a `renderWithRouter` helper that wraps `<Topbar />` in `<MemoryRouter initialEntries={['/dashboard']}>`. Write 4 tests inside `describe('Topbar', ...)`: (1) `renders without crashing` — assert `container.querySelector('header')` is truthy; (2) `shows role-gated buttons for social_worker` — assert a button with text "New Intake" is present AND a button with text "Approvals Queue" is present (per D-04); (3) `hides role-gated buttons for claimant role` — use `vi.mocked(useAuth).mockReturnValueOnce({ user: { ..., role: 'claimant' }, ... })` then assert the "New Intake" and "Approvals Queue" buttons are NOT in the DOM (use `queryByText` returning null); (4) `has no axe violations` — `import { axe } from 'vitest-axe';` then `const results = await axe(container); expect(results).toHaveNoViolations();`. Do NOT add comments inside the tests. The mock setup uses module-level `vi.mock` calls before the `describe` block.</action>
  <verify>cd kapwa-client && npx vitest run src/components/Topbar.test.tsx 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - File `kapwa-client/src/components/Topbar.test.tsx` exists with 4 tests inside `describe('Topbar', ...)`
    - `vi.mock('../lib/auth-context', ...)` is at the top with a social_worker user
    - `vi.mock('@/lib/theme-context', ...)` is at the top with a light theme stub
    - `renderWithRouter` helper uses `<MemoryRouter initialEntries={['/dashboard']}>`
    - Test 1 asserts `<header>` is present
    - Test 2 asserts "New Intake" and "Approvals Queue" buttons are present for social_worker
    - Test 3 uses `mockReturnValueOnce` to flip the role to `claimant` and asserts those buttons are absent
    - Test 4 imports `axe` from `vitest-axe` and uses `expect(results).toHaveNoViolations()`
    - `npx vitest run src/components/Topbar.test.tsx` shows 4/4 tests pass with 0 axe violations
    - No comments in the test file
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Sidebar.test.tsx with 3 tests (renders, role-gated nav, active link highlight)</name>
  <files>kapwa-client/src/components/Sidebar.test.tsx</files>
  <read_first>kapwa-client/src/components/Sidebar.tsx,kapwa-client/src/lib/nav-config.tsx,kapwa-client/src/components/BottomNav.test.tsx</read_first>
  <behavior>
    - Sidebar renders without crashing (the `<aside>` or main nav container is in the DOM)
    - For a social_worker user, the role-gated nav groups from `nav-config.tsx` are present (e.g., "Cases" link is present)
    - The active link has the `bg-muted` Tailwind class when the URL matches its `path` (BottomNav precedent for the same active-link check)
  </behavior>
  <action>Create `kapwa-client/src/components/Sidebar.test.tsx` (new file). At the top, mock `../lib/auth-context` with a stubbed `useAuth` returning a social_worker user. Define a `renderWithRouter` helper that accepts an `initialEntries` array (default `['/']`) and wraps `<Sidebar />` in `<MemoryRouter initialEntries={initialEntries}>`. Write 3 tests inside `describe('Sidebar', ...)`: (1) `renders without crashing` — assert the sidebar's main nav container is in the DOM (use `container.querySelector('nav')` or `container.querySelector('aside')` based on the actual Sidebar.tsx markup); (2) `shows role-gated nav items for social_worker` — assert a label like "Cases" or "Dashboard" (a nav item visible to social_worker per `nav-config.tsx`) is present in the DOM; (3) `active link has bg-muted class when URL matches` — render with `initialEntries: ['/cases']` and assert `document.querySelector('a[href*="cases"].bg-muted, a.bg-muted[href*="cases"]')` is truthy (mirror the BottomNav.test.tsx:29-40 pattern that iterates links looking for `bg-muted`). Do NOT add `axe()` to Sidebar tests (per D-06, axe is for Layout/Topbar/Sidebar — wait, D-06 explicitly INCLUDES Sidebar in axe coverage; re-read: yes, "Wrap Layout, Topbar, Sidebar smoke tests with `axe(container) + expect(results).toHaveNoViolations()`" — add a 4th test for axe). Update the test list: 4 tests total — (1) renders, (2) role-gated nav, (3) active link, (4) axe. Do NOT add comments inside the tests.</action>
  <verify>cd kapwa-client && npx vitest run src/components/Sidebar.test.tsx 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - File `kapwa-client/src/components/Sidebar.test.tsx` exists with 4 tests inside `describe('Sidebar', ...)`
    - `vi.mock('../lib/auth-context', ...)` is at the top with a social_worker user
    - `renderWithRouter` helper accepts an `initialEntries` parameter
    - Test 1 asserts the nav container is in the DOM
    - Test 2 asserts at least one role-gated nav label (e.g., "Cases") is present for social_worker
    - Test 3 uses `initialEntries: ['/cases']` and asserts the active link has the `bg-muted` class (BottomNav pattern)
    - Test 4 imports `axe` from `vitest-axe` and uses `expect(results).toHaveNoViolations()`
    - `npx vitest run src/components/Sidebar.test.tsx` shows 4/4 tests pass with 0 axe violations
    - No comments in the test file
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create ProtectedRoute.test.tsx with 4 tests (no-token redirect, authed renders, loading state, role mismatch) + final verification</name>
  <files>kapwa-client/src/components/ProtectedRoute.test.tsx</files>
  <read_first>kapwa-client/src/components/ProtectedRoute.tsx,kapwa-client/src/components/SyncQueuePanel.test.tsx</read_first>
  <behavior>
    - When `localStorage` has no `kapwa_token`, the component navigates to `/login` (replace mode) and never renders the children
    - When `getCurrentUser()` returns a user matching the `roles` prop, the children render and "Verifying access…" is no longer in the DOM
    - While `getCurrentUser()` is in flight, the loading text "Verifying access…" is rendered (use `findByText` or initial render assertion)
    - When the user's role is not in the `roles` prop, the component navigates to the role-specific fallback (e.g., `/dashboard` for social_worker) without rendering the children
  </behavior>
  <action>Create `kapwa-client/src/components/ProtectedRoute.test.tsx` (new file). At the top, import `waitFor` from `@testing-library/react` and `MemoryRouter` from `react-router-dom`. Mock `../lib/auth-context` with `vi.mock('../lib/auth-context', () => ({ getCurrentUser: vi.fn() }))`. Use the hoisted pattern: `const { mockGetCurrentUser } = vi.hoisted(() => ({ mockGetCurrentUser: vi.fn() }))` then `vi.mock('../lib/auth-context', () => ({ getCurrentUser: (...args: unknown[]) => mockGetCurrentUser(...args) }))`. In `beforeEach`, reset `mockGetCurrentUser` and `localStorage.clear()`. Write 4 tests inside `describe('ProtectedRoute', ...)`: (1) `redirects to /login when no token` — `localStorage.removeItem('kapwa_token')`; render `<MemoryRouter><ProtectedRoute><div>Protected</div></ProtectedRoute></MemoryRouter>`; `await waitFor(() => { expect(screen.queryByText('Protected')).toBeNull(); })` (the children never render because the navigation happens in `useEffect` before `setAuthorized(true)`); (2) `renders children when authenticated with right role` — `localStorage.setItem('kapwa_token', 'test')`; `mockGetCurrentUser.mockResolvedValue({ id: '1', role: 'social_worker', ... })`; render `<MemoryRouter><ProtectedRoute roles={['social_worker']}><div>Protected</div></ProtectedRoute></MemoryRouter>`; `await waitFor(() => { expect(screen.getByText('Protected')).toBeTruthy(); })`; (3) `shows Verifying access… while loading` — render the component WITHOUT awaiting the async resolution; immediately assert `screen.getByText(/Verifying access/i)` is truthy (this is the initial render before `useEffect` fires); (4) `redirects when role does not match` — `localStorage.setItem('kapwa_token', 'test')`; `mockGetCurrentUser.mockResolvedValue({ id: '1', role: 'claimant', ... })`; render `<MemoryRouter><ProtectedRoute roles={['social_worker']}><div>Protected</div></ProtectedRoute></MemoryRouter>`; `await waitFor(() => { expect(screen.queryByText('Protected')).toBeNull(); })` (the navigate('/my-dashboard', { replace: true }) fires). Do NOT add `axe()` (ProtectedRoute has no DOM output of its own per D-06). Do NOT add comments inside the tests. The `localStorage.clear()` in `beforeEach` is required because `tests/setup.ts` does NOT clear between tests (only between test FILES via the `afterEach` in setup.ts — but actually it does, per setup.ts:58-60; the per-test `localStorage.removeItem` calls are explicit for clarity).</action>
  <verify>cd kapwa-client && npx vitest run src/components/ProtectedRoute.test.tsx 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - File `kapwa-client/src/components/ProtectedRoute.test.tsx` exists with 4 tests inside `describe('ProtectedRoute', ...)`
    - `vi.mock('../lib/auth-context', ...)` is at the top with a hoisted `mockGetCurrentUser`
    - `waitFor` is imported from `@testing-library/react` and used in tests 1, 2, 4 (the async state transitions)
    - Test 1 verifies the children do NOT render when no token (use `queryByText` returning null)
    - Test 2 verifies the children DO render when authenticated with the right role (use `getByText` after `waitFor`)
    - Test 3 asserts the loading text is present in the initial synchronous render (before `await waitFor`)
    - Test 4 verifies the children do NOT render when the role does not match (the role-specific navigate fires)
    - No `axe()` import or usage
    - `npx vitest run src/components/ProtectedRoute.test.tsx` shows 4/4 tests pass
    - No comments in the test file
    - After all 4 task files are created, `npx vitest run src/components/{Layout,Topbar,Sidebar,ProtectedRoute}.test.tsx` shows 17/17 tests pass (5 + 4 + 4 + 4)
    - `npm run build` exits 0 with no TypeScript errors in the new test files
    - A new plan summary is created at `.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-02-SUMMARY.md`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime → Layout/Topbar/Sidebar/ProtectedRoute | The test renders the real component subtree (no child mocks) so axe sees the real DOM; only `auth-context`, `offline-queue`, and `theme-context` are stubbed at the module boundary |
| Auth context → ProtectedRoute | The test mocks `getCurrentUser` to control the async resolution; `localStorage` is real (used to seed the `kapwa_token`) |
| axe → DOM | axe runs against `container` (the rendered subtree) — does not mutate the page, only reads the accessibility tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | Layout.test.tsx (auth context mock) | mitigate | The auth mock returns a static social_worker user. A real attacker would not control this in production — the test is closed-world. The mock is constrained to the test file by `vi.mock`. |
| T-16-07 | Information Disclosure | axe in tests | accept | axe reads the rendered DOM but does not exfiltrate data — the test process is local. No PII is in the test fixtures (mock user has `id: '1'`, no real names). |
| T-16-08 | Spoofing | ProtectedRoute role-mismatch test | accept | The test mocks `getCurrentUser` to return a `claimant` user even when the `roles` prop is `['social_worker']`. In production, the token signature prevents role spoofing — the test only exercises the redirect logic. The real auth path (token validation) is out of scope for these smoke tests (Phase 15 covered auth-context). |
| T-16-09 | Denial of Service | axe on Layout | accept | axe-core runs synchronously in jsdom; the test runs in <1s. No DoS surface. If a Radix Sheet triggers a false-positive a11y violation, the test fails — fix the component or carve out a follow-up (D-08). |
| T-16-10 | Elevation of Privilege | ProtectedRoute redirect | mitigate | The test asserts the redirect happens via `useNavigate` (`navigate('/login', { replace: true })`). The real navigation is constrained to the React Router API; no `window.location.href` is used. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed by this plan (vitest-axe wired globally in 16-01). |
</threat_model>

<verification>
Per-task automated verifications:
- Task 1: `cd kapwa-client && npx vitest run src/components/Layout.test.tsx` shows 5/5 pass + 0 axe violations
- Task 2: `cd kapwa-client && npx vitest run src/components/Topbar.test.tsx` shows 4/4 pass + 0 axe violations
- Task 3: `cd kapwa-client && npx vitest run src/components/Sidebar.test.tsx` shows 4/4 pass + 0 axe violations
- Task 4: `cd kapwa-client && npx vitest run src/components/ProtectedRoute.test.tsx` shows 4/4 pass

Final phase checks:
- `cd kapwa-client && npx vitest run src/components/{Layout,Topbar,Sidebar,ProtectedRoute}.test.tsx` shows 17/17 pass
- `cd kapwa-client && npm run test:run` shows the full suite green (no regression)
- `cd kapwa-client && npm run build` exits 0 with no TypeScript errors
- A11Y-01 (SkipToContent on keyboard focus in Layout) is verified by Test 2 of Layout.test.tsx
- TST-05 (smoke tests for Layout/Topbar/Sidebar/ProtectedRoute) is satisfied by the 4 new test files
</verification>

<success_criteria>
1. `kapwa-client/src/components/Layout.test.tsx` exists with 5 tests passing — verifies `<main id="main-content">`, SkipToContent presence + `#main-content` href, ErrorBoundary wrap, AriaLiveRegion offline message on `navigator.onLine === false`, and 0 axe violations
2. `kapwa-client/src/components/Topbar.test.tsx` exists with 4 tests passing — verifies `<header>` renders, "New Intake" + "Approvals Queue" buttons appear for social_worker but NOT for claimant, and 0 axe violations
3. `kapwa-client/src/components/Sidebar.test.tsx` exists with 4 tests passing — verifies the nav container renders, role-gated nav items appear for social_worker, the active link gets `bg-muted`, and 0 axe violations
4. `kapwa-client/src/components/ProtectedRoute.test.tsx` exists with 4 tests passing — verifies no-token redirects to `/login`, authenticated user with right role renders children, loading state shows "Verifying access…", and role-mismatch navigates to the role-specific fallback (no axe — no DOM output)
5. A11Y-01 (SkipToContent on keyboard focus in Layout) is satisfied by Layout.test.tsx Test 2
6. TST-05 (smoke tests for Layout/Topbar/Sidebar/ProtectedRoute) is satisfied by the 4 new test files (17 tests total)
7. `npx vitest run src/components/{Layout,Topbar,Sidebar,ProtectedRoute}.test.tsx` shows 17/17 tests pass
8. `npm run test:run` shows the full suite green (no regression from the new test files)
9. `npm run build` exits 0 with no TypeScript errors
10. A new plan summary is created at `.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-02-SUMMARY.md`
</success_criteria>

<output>
Create `.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-02-SUMMARY.md` when done.

## Artifacts this phase produces

The 4 new test files and their coverage (consumed by the plan-review-convergence source-grounding pass to exclude newly-created test files from drift verification):

1. **`kapwa-client/src/components/Layout.test.tsx`** (NEW) — 5 tests
   - `renderLayout()` helper wrapping `<Layout />` in `<MemoryRouter initialEntries={['/dashboard']}>`
   - `vi.mock('../lib/auth-context', ...)` with social_worker stub
   - `vi.mock('../lib/offline-queue', ...)` returning `{ loadQueue: () => [] }`
   - Tests: renders main, SkipToContent href, ErrorBoundary wrap (structural), AriaLiveRegion offline, axe 0 violations
   - Per-test `Object.defineProperty(navigator, 'onLine', { value: false, configurable: true })` toggle for the offline test

2. **`kapwa-client/src/components/Topbar.test.tsx`** (NEW) — 4 tests
   - `renderWithRouter()` helper wrapping `<Topbar />` in `<MemoryRouter initialEntries={['/dashboard']}>`
   - `vi.mock('../lib/auth-context', ...)` with social_worker stub
   - `vi.mock('@/lib/theme-context', ...)` with light theme stub
   - Tests: renders header, social_worker sees New Intake + Approvals Queue, claimant does NOT see them, axe 0 violations
   - Uses `vi.mocked(useAuth).mockReturnValueOnce({ ..., role: 'claimant' })` for the role-gating test

3. **`kapwa-client/src/components/Sidebar.test.tsx`** (NEW) — 4 tests
   - `renderWithRouter(ui, { initialEntries })` helper
   - `vi.mock('../lib/auth-context', ...)` with social_worker stub
   - Tests: renders nav container, role-gated nav items present, active link has `bg-muted` (BottomNav precedent), axe 0 violations
   - Active-link test renders with `initialEntries: ['/cases']` then iterates `document.querySelectorAll('a')` looking for `bg-muted`

4. **`kapwa-client/src/components/ProtectedRoute.test.tsx`** (NEW) — 4 tests
   - Hoisted `mockGetCurrentUser = vi.fn()` + `vi.mock('../lib/auth-context', () => ({ getCurrentUser: (...args) => mockGetCurrentUser(...args) }))`
   - `localStorage` is real (seed `kapwa_token` per-test)
   - `await waitFor(() => ...)` in tests 1, 2, 4 for the async state transitions
   - Tests: no-token redirects to `/login`, authed+right-role renders children, loading state shows "Verifying access…", role-mismatch redirects to role-specific fallback
   - NO axe — ProtectedRoute has no DOM output of its own per D-06

5. **Plan summary file** at `.planning/phases/16-ui-polish-errorboundary-a11y-core-ui-tests/16-02-SUMMARY.md`

## Reused patterns (not new symbols, just patterns from existing test files)

- `PublicLayout.test.tsx` — closest analog for Layout (auth mock + MemoryRouter)
- `BottomNav.test.tsx` — active-link `bg-muted` check pattern
- `SyncQueuePanel.test.tsx` — module-level mock pattern with hoisted vi.fn
- `EmptyState.test.tsx:23-28` — the offline variant text "You appear to be offline" + "Please check your connection and try again" + Retry button (asserted by 16-01's offline test)
</output>