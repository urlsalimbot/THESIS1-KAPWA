# 26-01 — SPEC-GAP Implementation

**Goal:** Close gaps between Master Specification v1.1 and the codebase: hybrid physical-digital filing, remote data wipe, LGU ID format, duplicate detection, COA export, config change audit, daily backups.

**Status:** PARTIAL — modules for physical-files, export, and wipe exist; `PhysicalFilesModule` is NOT yet registered in `app.module.ts`

**Implemented:**
- `physical-files/` module (entity, controller, service) — built but unregistered
- `export/` module (COA export controller/service/spec) — registered
- `admin/` wipe module (admin-wipe.controller/service) — registered
- `lcr/` bulk import module (controller/service/spec) — registered

**Pending:**
- Register `PhysicalFilesModule` in `app.module.ts`
- Physical filing search + browse page, hash chain, backup cron, 4Ps compliance (per full plan)

**Plan reference:** `docs/superpowers/plans/2026-07-30-spec-gap-implementation.md`
