---
phase: 15-core-module-tests
plan: 04
type: execute
wave: 4
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified:
  - kapwa-client/src/lib/secure-storage.browser.test.ts
  - kapwa-client/src/lib/secure-storage.native.test.ts
autonomous: true
requirements:
  - TST-04

must_haves:
  truths:
    - "`secure-storage.browser.test.ts` covers the browser (encrypted-db) path: init, getItem, setItem, removeItem, plus the corruption path"
    - "`secure-storage.native.test.ts` covers the native (SQLCipher) path: init, getItem, setItem, removeItem with mocked `@capacitor-community/sqlite` + spied `Capacitor.isNativePlatform()`"
    - "The browser corruption test asserts `SecureStorage.getItem` REJECTS (propagates the error) when `encryptedDb.getItem` throws — matches the actual source contract at secure-storage.ts:71 (no try/catch)"
    - "The browser init test asserts `encryptedDb.init` is called (and not the native bridge)"
    - "The native init test asserts the call order: createConnection → open → execute → close"
    - "The native setItem test asserts an INSERT OR REPLACE statement with JSON-stringified values is executed"
    - "The native removeItem test asserts a DELETE statement with the key as the value is executed"
    - "The existing 3 tests in `secure-storage.test.ts` continue to pass with no regression"
    - "`secure-storage.ts` is instrumented by the v8 coverage provider and reports ≥70% lines in the per-file threshold"
    - "The full `npm run coverage` report (all 4 modules) shows all 4 source files at ≥70% lines — the TST-01..TST-04 + ROADMAP success criteria #5 are all satisfied"
  prohibitions:
    - "Tests must NOT add new cases to the existing `secure-storage.test.ts` (per D-13: the existing file stays at 3 tests; new scenarios go in the 2 new files)"
    - "Browser-path tests must NOT spy on `Capacitor.isNativePlatform` (the default `false` in jsdom is correct for the browser path)"
    - "Native-path tests must NOT mock `encryptedDb` (the native path does not use it; mocking it would falsely include it in the coverage report)"
    - "Tests must NOT assert on real encryption keys or decrypted PII (per the security domain in RESEARCH §"Security Domain": never assert on actual key material)"
    - "Tests must NOT add a global mock for `@capacitor-community/sqlite` in `tests/setup.ts` (per RESEARCH §"Open Questions" #1: per-test-file mocks are explicit and visible)"
    - "Tests must NOT add code comments beyond the `// Mock ...` 1-line fixture note pattern"
  artifacts:
    - path: kapwa-client/src/lib/secure-storage.browser.test.ts
      provides: 4 tests for browser path (init + 3 corruption + getItem/setItem/removeItem proxy)
      contains: "describe('SecureStorage — browser init"
    - path: kapwa-client/src/lib/secure-storage.native.test.ts
      provides: 4 tests for native path (init + getItem + setItem + removeItem)
      contains: "describe('SecureStorage — native path"
    - path: kapwa-client/src/lib/secure-storage.test.ts
      provides: existing 3 tests (unchanged — verify no regression)
      contains: "describe('SecureStorage (browser fallback)'"
  symbols:
    added:
      - "secure-storage.browser.test.ts with 4 tests (D-08 corruption + D-09 browser init + getItem/setItem/removeItem proxy)"
      - "secure-storage.native.test.ts with 4 tests (D-07 native init + getItem + setItem + removeItem)"
    tests_added:
      - "Browser init: `SecureStorage.init()` calls `encryptedDb.init()` (browser path)"
      - "Browser init: reads `kapwa_db_key` from localStorage as the encryption-key fallback (no direct assertion on key; verified by init succeeding)"
      - "Browser corruption: `SecureStorage.getItem()` propagates the error (rejects) when `encryptedDb.getItem` rejects (matches the source: no try/catch at secure-storage.ts:71)"
      - "Browser getItem/setItem/removeItem proxy to `encryptedDb` (no native bridge call)"
      - "Native init: `SecureStorage.init()` calls `CapacitorSQLite.createConnection` + `open` + `execute` + `close` in order"
      - "Native getItem: opens + queries + closes the DB; returns JSON-parsed value from query result"
      - "Native getItem: returns null when query result has no rows"
      - "Native setItem: executes INSERT OR REPLACE INTO sync_cache with JSON-stringified value"
      - "Native removeItem: executes DELETE FROM sync_cache with the key as the value"
  key_links:
    - from: kapwa-client/src/lib/secure-storage.browser.test.ts
      to: kapwa-client/src/lib/encrypted-db.ts
      via: "vi.mock('./encrypted-db', ...) replaces the browser AES-256-GCM fallback; tests import encryptedDb and assert on its mocked methods"
      pattern: "vi.mock\\('\\./encrypted-db'"
    - from: kapwa-client/src/lib/secure-storage.native.test.ts
      to: @capacitor-community/sqlite
      via: "vi.mock('@capacitor-community/sqlite', ...) replaces the native SQLCipher bridge; tests dynamically import CapacitorSQLite and assert on its mocked methods"
      pattern: "vi.mock\\('@capacitor-community/sqlite'"
    - from: kapwa-client/src/lib/secure-storage.native.test.ts
      to: @capacitor/core
      via: "vi.spyOn(Capacitor, 'isNativePlatform').mockReturnValue(true) toggles the platform detection in beforeEach; vi.restoreAllMocks() in afterEach restores it"
      pattern: "vi\\.spyOn\\(Capacitor, 'isNativePlatform'\\)"
    - from: kapwa-client/src/lib/secure-storage.native.test.ts
      to: kapwa-client/src/lib/secure-storage.ts
      via: "SecureStorage.init/getItem/setItem/removeItem branch on Capacitor.isNativePlatform() — the spy in beforeEach routes calls to the native (SQLCipher) path"
      pattern: "function getPlatform"
---

<objective>
Add 8 new unit tests for the public surface of `secure-storage.ts` (SecureStorage.init/getItem/setItem/removeItem) covering both browser (encrypted-db) and native (SQLCipher via @capacitor-community/sqlite) platforms, plus the corruption path, so the module crosses the 70% per-file line threshold required by TST-04. Produce the final full-coverage report for all 4 modules.

Purpose: TST-04 demands ≥70% coverage on `secure-storage.ts` (encrypt, decrypt, key rotation, corrupted data handling). Per D-04, 6-8 new tests are needed (3 existing + 6-8 new). Per D-07, the strategy is to test both browser and native paths in jsdom by mocking `@capacitor-community/sqlite` and spying on `Capacitor.isNativePlatform()`. Per D-08, the corruption path is tested by mocking `encryptedDb.getItem` to throw. Per D-13, the new tests split into 2 files: `secure-storage.browser.test.ts` and `secure-storage.native.test.ts`. Per D-15, Plan 15-04 is the final plan and runs after 15-01/15-02/15-03 — it produces the final full-suite coverage report verifying all 4 modules hit ≥70% lines.

Output: 2 new test files (8 tests total), all passing, with `secure-storage.ts` reporting ≥70% lines and the full `npm run coverage` report showing all 4 source files at ≥70% lines.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-core-module-tests/15-CONTEXT.md
@.planning/phases/15-core-module-tests/15-RESEARCH.md
@.planning/phases/15-core-module-tests/15-PATTERNS.md
@.planning/phases/15-core-module-tests/15-VALIDATION.md

# Source under test
@kapwa-client/src/lib/secure-storage.ts
@kapwa-client/src/lib/secure-storage.test.ts
@kapwa-client/src/lib/encrypted-db.ts (the browser AES-256-GCM fallback, mocked in the new tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create secure-storage.browser.test.ts with 4 tests for browser init + corruption path (per D-08 + D-09)</name>
  <files>kapwa-client/src/lib/secure-storage.browser.test.ts</files>
  <read_first>
    - kapwa-client/src/lib/secure-storage.ts (the browser branch at line 49 calls `encryptedDb.init()`; the corruption path at line 71 calls `encryptedDb.getItem<T>(key)` — the catch is at line 71's `encryptedDb.getItem` returning null on parse error; actually re-read: the corruption path is the getItem function's browser branch at line 71 which calls encryptedDb.getItem; if encryptedDb.getItem throws OR returns malformed JSON, the call propagates — verify the actual source)
    - kapwa-client/src/lib/secure-storage.test.ts (the existing `vi.mock('./encrypted-db', ...)` pattern at lines 4-11; the dynamic `import('./secure-storage')` pattern at lines 30-31)
    - .planning/phases/15-core-module-tests/15-PATTERNS.md (the secure-storage.browser.test.ts pattern at lines 578-642 — Pattern C: vi.mock hoisted factory; Pattern B: localStorage.clear() in beforeEach)
  </read_first>
  <acceptance_criteria>
    - File `secure-storage.browser.test.ts` exists at `kapwa-client/src/lib/`
    - File uses `vi.mock('./encrypted-db', ...)` at the top (hoisted) with a factory providing `encryptedDb.init`, `encryptedDb.getItem`, `encryptedDb.setItem`, `encryptedDb.removeItem` as `vi.fn()`s
    - File has 2 top-level `describe` blocks: `SecureStorage — browser init (encrypted-db path)` and `SecureStorage — corruption path` — 4 tests total
    - Test 1 (browser init calls encryptedDb.init): calls `await SecureStorage.init()`; asserts `encryptedDb.init` was called exactly once; asserts no native bridge was touched (the test runs in jsdom where `Capacitor.isNativePlatform()` returns false by default, but the test does not need to assert this — the absence of an import error from `@capacitor-community/sqlite` is sufficient)
    - Test 2 (browser init with kapwa_db_key): pre-seeds `localStorage.setItem('kapwa_db_key', 'user-pass-123')`; calls `await SecureStorage.init()`; asserts `encryptedDb.init` was called (the key is consumed internally, not directly asserted on — verified by init succeeding without throwing)
    - Test 3 (corruption — encryptedDb.getItem rejects, error propagates): mocks `encryptedDb.getItem` to reject with `new SyntaxError('malformed')`; calls `await SecureStorage.getItem('any-key')`; asserts the promise REJECTS with the same SyntaxError (secure-storage.ts:71 has NO try/catch around the `encryptedDb.getItem` call — the rejection propagates to the caller; this matches the actual source contract)
    - Test 4 (browser getItem/setItem/removeItem proxy): asserts `encryptedDb.getItem` is called with the key when the browser path is taken; asserts `encryptedDb.setItem` is called with key + value when the browser path is taken; asserts `encryptedDb.removeItem` is called with the key when the browser path is taken (proxy verification)
    - All 4 tests pass
    - `localStorage.clear()` is called in `beforeEach` of each describe block
    - `vi.clearAllMocks()` is called in `beforeEach` to reset the encryptedDb mock invocations between tests
  </acceptance_criteria>
  <action>
    Create a new test file at `kapwa-client/src/lib/secure-storage.browser.test.ts`. Per Pattern C from .planning/phases/15-core-module-tests/15-PATTERNS.md, use `vi.mock('./encrypted-db', ...)` at the top of the file (hoisted) with a factory that provides `encryptedDb: { init: vi.fn().mockResolvedValue(undefined), getItem: vi.fn().mockImplementation(() => Promise.resolve(null)), setItem: vi.fn().mockResolvedValue(undefined), removeItem: vi.fn().mockResolvedValue(undefined) }`. Import `encryptedDb` from `./encrypted-db` AFTER the mock declaration (TypeScript hoisting works correctly here — the import is resolved through the mock factory). Use `localStorage.clear()` + `vi.clearAllMocks()` in `beforeEach` per Pattern B. Write 2 describe blocks: (A) `SecureStorage — browser init (encrypted-db path)` with 2 tests — (A.1) `init()` calls `encryptedDb.init` exactly once (no native bridge call); (A.2) `init()` with `localStorage.setItem('kapwa_db_key', 'user-pass-123')` succeeds and `encryptedDb.init` is called (the key is consumed by `getStorageKey()` internally — no direct assertion on the key); (B) `SecureStorage — corruption path` with 2 tests — (B.1) when `encryptedDb.getItem` rejects with a `SyntaxError` (simulating a corrupted encrypted entry), `SecureStorage.getItem` REJECTS with the same `SyntaxError` (the rejection PROPAGATES — source at secure-storage.ts:71 has NO try/catch around the `encryptedDb.getItem` call); (B.2) `SecureStorage.getItem/setItem/removeItem` proxy to the corresponding `encryptedDb` methods when the browser path is taken (asserts the correct method is called with the correct arguments). Use the dynamic `import('./secure-storage')` pattern from the existing `secure-storage.test.ts:30-31` to ensure the module is loaded AFTER the mock is registered. Per the security domain in RESEARCH §"Security Domain": do NOT assert on actual key material — assert only on the call to `encryptedDb.init` succeeding. Do NOT add code comments beyond `// Mock ...`. No emoji.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/secure-storage.browser.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - File `secure-storage.browser.test.ts` exists with 4 passing tests
    - Tests cover the browser init path (encryptedDb.init) and the corruption path (encryptedDb.getItem throws / malformed JSON)
    - Source confirmed at secure-storage.ts:71 has NO try/catch around `encryptedDb.getItem` — the corruption test asserts the rejection PROPAGATES (matches the actual source)
    - No regression in the existing 3 tests in `secure-storage.test.ts`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create secure-storage.native.test.ts with 4 tests for native init + getItem + setItem + removeItem (per D-07 + D-09)</name>
  <files>kapwa-client/src/lib/secure-storage.native.test.ts</files>
  <read_first>
    - kapwa-client/src/lib/secure-storage.ts (the native branches at lines 22-46 for init, 55-69 for getItem, 76-86 for setItem, 93-104 for removeItem; the `getPlatform()` helper at lines 5-10)
    - kapwa-client/src/lib/secure-storage.test.ts (the existing `vi.mock('./encrypted-db', ...)` pattern — do NOT use this for the native file; use `@capacitor-community/sqlite` mock instead)
    - .planning/phases/15-core-module-tests/15-PATTERNS.md (the secure-storage.native.test.ts pattern at lines 644-748 — Pattern E: vi.spyOn(Capacitor, 'isNativePlatform'))
  </read_first>
  <acceptance_criteria>
    - File `secure-storage.native.test.ts` exists at `kapwa-client/src/lib/`
    - File uses `vi.mock('@capacitor-community/sqlite', ...)` at the top (hoisted) with a factory providing `CapacitorSQLite.createConnection`, `CapacitorSQLite.open`, `CapacitorSQLite.close`, `CapacitorSQLite.query`, `CapacitorSQLite.execute` as `vi.fn()`s with the canonical mock return shapes from CONTEXT D-07
    - File has 1 top-level `describe('SecureStorage — native path (SQLCipher)', ...)` block with 4 tests
    - `beforeEach` calls `localStorage.clear()` + `vi.clearAllMocks()` + `vi.spyOn(Capacitor, 'isNativePlatform').mockReturnValue(true)`
    - `afterEach` calls `vi.restoreAllMocks()` (restores the spy per RESEARCH Pitfall 2)
    - Test 1 (native init): calls `await SecureStorage.init('user-pass')`; asserts `CapacitorSQLite.createConnection` was called with an object containing `{ database: 'kapwa', encrypted: true, mode: 'secret' }`; asserts `CapacitorSQLite.open`, `CapacitorSQLite.execute`, `CapacitorSQLite.close` were each called; asserts the call order is createConnection → open → execute → close (using `mock.invocationCallOrder`)
    - Test 2 (native getItem): mocks `CapacitorSQLite.query` to resolve with `{ values: [{ value: JSON.stringify({ foo: 'bar' }) }] }`; calls `await SecureStorage.getItem('test-key')`; asserts the result equals `{ foo: 'bar' }`; asserts `open` + `query` + `close` were called
    - Test 3 (native getItem no rows): mocks `CapacitorSQLite.query` to resolve with `{ values: [] }`; calls `await SecureStorage.getItem('missing')`; asserts the result is `null`
    - Test 4 (native setItem): calls `await SecureStorage.setItem('k', { a: 1 })`; asserts `CapacitorSQLite.execute` was called with an object containing `statement: expect.stringContaining('INSERT OR REPLACE INTO sync_cache')` and `values: ['k', JSON.stringify({ a: 1 })]`
    - Test 5 (native removeItem): calls `await SecureStorage.removeItem('k')`; asserts `CapacitorSQLite.execute` was called with an object containing `statement: expect.stringContaining('DELETE FROM sync_cache')` and `values: ['k']`
    - All 5 tests pass (the spec says 4 tests, but the actual scope covers init + getItem + setItem + removeItem which is 4 export functions; the getItem has 2 cases so 5 total test cases)
  </acceptance_criteria>
  <action>
    Create a new test file at `kapwa-client/src/lib/secure-storage.native.test.ts`. Per Pattern E from .planning/phases/15-core-module-tests/15-PATTERNS.md, use `vi.mock('@capacitor-community/sqlite', ...)` at the top of the file (hoisted) with a factory that provides `CapacitorSQLite: { createConnection: vi.fn().mockResolvedValue(undefined), open: vi.fn().mockResolvedValue(undefined), close: vi.fn().mockResolvedValue(undefined), query: vi.fn().mockResolvedValue({ values: [] }), execute: vi.fn().mockResolvedValue({ changes: 1 }) }` per the exact shape from CONTEXT D-07. Import `Capacitor` from `@capacitor/core` and the dynamically-imported `CapacitorSQLite` from `@capacitor-community/sqlite` (the dynamic import is what `secure-storage.ts` does at lines 24, 57, 78, 96 — vitest's mocker rewrites dynamic imports too, so the mock applies). Use a `describe('SecureStorage — native path (SQLCipher)', ...)` block with 4-5 tests. In `beforeEach`, call `localStorage.clear()` + `vi.clearAllMocks()` + `vi.spyOn(Capacitor, 'isNativePlatform').mockReturnValue(true)` — the spy is the key toggle that routes SecureStorage's `getPlatform()` helper to the native branch. In `afterEach`, call `vi.restoreAllMocks()` to restore the spy (per RESEARCH Pitfall 2 — the spy does not auto-restore). Write the tests in this order: (1) native init — call `await SecureStorage.init('user-pass')`, assert `CapacitorSQLite.createConnection` was called with an object containing `{ database: 'kapwa', encrypted: true, mode: 'secret' }` (use `expect.objectContaining`), assert open + execute + close were each called, assert the call order via `mock.invocationCallOrder` (the order should be createConnection → open → execute → close); (2) native getItem returns parsed value — mock `CapacitorSQLite.query` to resolve with `{ values: [{ value: JSON.stringify({ foo: 'bar' }) }] }`, call `await SecureStorage.getItem('test-key')`, assert result equals `{ foo: 'bar' }`, assert open + query + close were called; (3) native getItem no rows — mock query to resolve with `{ values: [] }`, call `await SecureStorage.getItem('missing')`, assert result is null; (4) native setItem — call `await SecureStorage.setItem('k', { a: 1 })`, assert `CapacitorSQLite.execute` was called with an object containing `statement: expect.stringContaining('INSERT OR REPLACE INTO sync_cache')` and `values: ['k', JSON.stringify({ a: 1 })]`; (5) native removeItem — call `await SecureStorage.removeItem('k')`, assert `CapacitorSQLite.execute` was called with an object containing `statement: expect.stringContaining('DELETE FROM sync_cache')` and `values: ['k']`. Use the dynamic `import('@capacitor-community/sqlite')` pattern in each test to access the mocked `CapacitorSQLite` object for assertions. Per the security domain in RESEARCH §"Security Domain": assert on `createConnection` options containing `encrypted: true` and `mode: 'secret'`, but do NOT assert on actual key material. Do NOT add code comments beyond `// Mock ...`. No emoji.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/secure-storage.native.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - File `secure-storage.native.test.ts` exists with 4-5 passing tests
    - All native-path operations (init, getItem, setItem, removeItem) are tested against the mocked CapacitorSQLite bridge
    - The `vi.spyOn(Capacitor, 'isNativePlatform').mockReturnValue(true)` correctly routes to the native branch
    - `vi.restoreAllMocks()` in `afterEach` prevents spy leakage to other tests
    - No regression in the existing 3 tests in `secure-storage.test.ts`
  </done>
</task>

<task type="auto">
  <name>Task 3: Run final `npm run coverage` + verify all 4 modules ≥70% lines + full-suite + plan summary</name>
  <files>.planning/phases/15-core-module-tests/15-04-SUMMARY.md</files>
  <read_first>
    - kapwa-client/vite.config.ts (the coverage block from Plan 15-01 — per-file thresholds, the include glob)
    - kapwa-client/package.json (the coverage + coverage:check scripts from Plan 15-01)
    - .planning/phases/15-core-module-tests/15-VALIDATION.md (the Phase 15 task verification map — all 4 modules must hit 70% lines)
  </read_first>
  <acceptance_criteria>
    - `npm run coverage:check` exits 0 (the per-file threshold gate is satisfied for all 4 source files)
    - The full coverage report shows all 4 source files at ≥70% lines: api.ts (TST-01), auth-context.tsx (TST-02), offline-queue.ts (TST-03), secure-storage.ts (TST-04)
    - `npm run test:run` shows the full suite passing (no regression in any of the 200+ pre-existing tests)
    - The HTML coverage report at `kapwa-client/coverage/index.html` is generated (verify the file exists)
    - Plan summary file `15-04-SUMMARY.md` is created with: 2 task commit hashes, the list of 8 new tests across 2 files, the 4-module coverage table, and the self-check
    - All 4 ROADMAP Phase 15 success criteria are met: api.ts CRUD/auth/timeout/4xx, auth-context login/logout/role, offline-queue queue/conflict/versions, secure-storage encrypt/decrypt/key-rotation/corruption, ≥70% lines combined
  </acceptance_criteria>
  <action>
    Per D-06, run the full coverage gate: `cd kapwa-client && npm run coverage:check` and verify it exits 0 — the per-file threshold gate is the canonical success criterion. The expected outcome: vitest prints a coverage table showing all 4 source files at ≥70% lines (api.ts, auth-context.tsx, offline-queue.ts, secure-storage.ts) and exits 0. If any of the 4 modules is still below 70%, the gate fails — diagnose by running `cd kapwa-client && npx vitest run --coverage --reporter=text-summary` and inspecting the per-file percentages, then add additional tests to the offending module (this would require either re-scoping the plan or adding a Wave 0.5 patch — but given the 50-65 test budget per D-04, all 4 modules should clear 70% with the planned tests). Then run `cd kapwa-client && npm run test:run` to confirm no regression in any of the 200+ existing tests. Then run `cd kapwa-client && npm run coverage` (no :check) to produce the full HTML report — verify `kapwa-client/coverage/index.html` exists (this is the deliverable artifact for the Phase 15 success criterion #5). Then create the plan summary file at `.planning/phases/15-core-module-tests/15-04-SUMMARY.md` with: 2 task commit hashes (Task 1 + Task 2 — Task 3 is the verification+summary), the list of 8 new tests across 2 files, the 4-module coverage table (api.ts, auth-context.tsx, offline-queue.ts, secure-storage.ts — with lines/functions/branches/statements percentages for each), the existing-tests status (no regression), and the self-check section confirming all 4 ROADMAP success criteria are met. The summary should also include a final "Phase 15 Complete" section verifying the 5 success criteria from ROADMAP Phase 15 boundary: (1) api.ts CRUD/auth/timeout/4xx — covered by 19+ tests; (2) auth-context login/logout/role — covered by 13 tests across 3 files; (3) offline-queue queue/conflict/versions — covered by 27 tests across 4 files; (4) secure-storage encrypt/decrypt/key-rotation/corruption — covered by 11 tests across 3 files; (5) combined ≥70% lines — verified by `npm run coverage:check` exiting 0. Do NOT commit the summary file — the orchestrator commits after the plan-checker passes.
  </action>
  <verify>
    <automated>cd kapwa-client && npm run coverage:check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run coverage:check` exits 0
    - All 4 source files (api.ts, auth-context.tsx, offline-queue.ts, secure-storage.ts) report ≥70% lines
    - `npm run test:run` shows the full suite passing
    - `kapwa-client/coverage/index.html` exists (the full coverage report)
    - Plan summary `15-04-SUMMARY.md` is created with 2 commit hashes + list of 8 new tests + 4-module coverage table + self-check + Phase 15 completion confirmation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → encrypted-db | The browser-path tests mock `./encrypted-db` via `vi.mock`; the production AES-256-GCM code is not exercised |
| Test → @capacitor-community/sqlite | The native-path tests mock `@capacitor-community/sqlite` via `vi.mock`; no real SQLCipher bridge is invoked |
| Test → @capacitor/core | The native-path tests spy on `Capacitor.isNativePlatform` to route to the native branch; the production platform-detection code path is exercised |
| Test → localStorage | The in-memory `localStorage` mock from `tests/setup.ts` is shared; `localStorage.clear()` in `beforeEach` is required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | Browser corruption test | mitigate | The test asserts `SecureStorage.getItem` REJECTS (propagates) when `encryptedDb.getItem` rejects with SyntaxError — matches the actual source contract at secure-storage.ts:71 (no try/catch). No deviation needed. |
| T-15-04-02 | Information Disclosure | localStorage key leak across tests | mitigate | `localStorage.clear()` in `beforeEach` of every describe block |
| T-15-04-03 | Elevation of Privilege | Native-path spy could leak to other tests | mitigate | `vi.restoreAllMocks()` in `afterEach` (per RESEARCH Pitfall 2) — the spy is restored at the end of each test |
| T-15-04-04 | Spoofing | Native-path test could pass with the browser path | mitigate | The `vi.spyOn(Capacitor, 'isNativePlatform').mockReturnValue(true)` is the only routing mechanism; if the test sees `encryptedDb.init` called instead of `CapacitorSQLite.createConnection`, the spy was not applied — add an explicit `expect(Capacitor.isNativePlatform()).toBe(true)` assertion at the top of the test |
| T-15-04-05 | Repudiation | Browser-path test could pass with the native path | mitigate | The default `Capacitor.isNativePlatform()` is `false` in jsdom; the browser-path tests do NOT spy on it, so any accidental native call would fail because `@capacitor-community/sqlite` is not mocked in the browser-path test file — the dynamic import would throw |
| T-15-04-06 | Denial of Service | Test could hang on an unresolvable promise | mitigate | All `vi.fn()` mocks return `undefined` or `{ values: [] }` for the default case; `mockResolvedValueOnce` overrides for specific tests; no real network or DB is involved |
| T-15-04-SC | Tampering | npm package install | accept | No new package installed in this plan; only test files added |
</threat_model>

<verification>
1. `cd kapwa-client && npx vitest run src/lib/secure-storage.browser.test.ts` shows 4/4 tests passing
2. `cd kapwa-client && npx vitest run src/lib/secure-storage.native.test.ts` shows 4-5/4-5 tests passing
3. `cd kapwa-client && npx vitest run src/lib/secure-storage.test.ts` shows 3/3 tests passing (no regression)
4. `cd kapwa-client && npm run coverage:check` exits 0 (the per-file threshold gate)
5. `cd kapwa-client && npm run test:run` shows the full suite passing (no regression in any of the 200+ pre-existing tests)
6. `cd kapwa-client && npm run coverage` produces the HTML report at `kapwa-client/coverage/index.html` (the full deliverable artifact)
7. Plan summary `15-04-SUMMARY.md` exists with 2 commit hashes + list of 8 new tests + 4-module coverage table + self-check
</verification>

<success_criteria>
1. File `secure-storage.browser.test.ts` exists with 4 passing tests (browser init + corruption)
2. File `secure-storage.native.test.ts` exists with 4-5 passing tests (native init + getItem + setItem + removeItem)
3. The existing 3 tests in `secure-storage.test.ts` still pass with no regression
4. `secure-storage.ts` reports ≥70% lines in the per-file coverage table (TST-04 satisfied)
5. Browser init test asserts `encryptedDb.init` is called (no native bridge)
6. Native init test asserts the call order: createConnection → open → execute → close
7. Native getItem test asserts the JSON-parsed value is returned and open/query/close are all called
8. Native setItem test asserts INSERT OR REPLACE INTO sync_cache with JSON-stringified values
9. Native removeItem test asserts DELETE FROM sync_cache with the key as the value
10. The full `npm run coverage:check` exits 0 (all 4 source files at ≥70% lines)
11. The full `npm run test:run` passes (no regression in any existing test)
12. The HTML coverage report at `kapwa-client/coverage/index.html` is generated
13. No code comments added beyond the `// Mock ...` 1-line fixture note pattern
14. No emoji, no decorative formatting
15. Plan summary `15-04-SUMMARY.md` created with 2 commit hashes + 8 new tests + 4-module coverage table + self-check + Phase 15 completion confirmation
</success_criteria>

<output>
Create `.planning/phases/15-core-module-tests/15-04-SUMMARY.md` when done.

## Artifacts this phase produces

### Tests added
- `kapwa-client/src/lib/secure-storage.browser.test.ts` — NEW file, 4 tests:
  - Browser init: `SecureStorage.init()` calls `encryptedDb.init()` (browser path, no native bridge)
  - Browser init: with `kapwa_db_key` in localStorage, init succeeds (key consumed internally)
  - Browser corruption: `SecureStorage.getItem()` rejects (propagates the error) when `encryptedDb.getItem` rejects with SyntaxError (matches the source — no try/catch at secure-storage.ts:71)
  - Browser proxy: `SecureStorage.getItem/setItem/removeItem` delegate to the corresponding `encryptedDb` methods when the browser path is taken
- `kapwa-client/src/lib/secure-storage.native.test.ts` — NEW file, 4-5 tests:
  - Native init: `SecureStorage.init()` calls `CapacitorSQLite.createConnection` + `open` + `execute` + `close` in order with `encrypted: true` and `mode: 'secret'`
  - Native getItem: returns JSON-parsed value from query result + open/query/close called
  - Native getItem: returns null when query result has no rows
  - Native setItem: executes INSERT OR REPLACE INTO sync_cache with JSON-stringified value
  - Native removeItem: executes DELETE FROM sync_cache with the key as the value

### Coverage gate progression
- `secure-storage.ts` reaches ≥70% lines in the per-file coverage table (TST-04 satisfied)

### Final deliverable (Phase 15 completion)
- `kapwa-client/coverage/index.html` — full coverage HTML report covering all 4 source files (api.ts, auth-context.tsx, offline-queue.ts, secure-storage.ts) + api-error.ts
- `npm run coverage:check` exits 0 — the canonical per-file threshold gate passes for all 4 modules

### Documentation
- `.planning/phases/15-core-module-tests/15-04-SUMMARY.md` — plan summary with 2 commit hashes + 8 new tests + 4-module coverage table + Phase 15 completion confirmation

### Phase 15 ROADMAP success criteria (all 5)
1. `api.ts` has tests covering: CRUD operations, auth header injection, error responses, timeout handling — ✅ covered by 19+ tests
2. `auth-context.tsx` has tests covering: login flow, logout flow, role-based guards, token persistence — ✅ covered by 13 tests across 3 files
3. `offline-queue.ts` has tests covering: queue push, dequeue, conflict detection, version vector updates — ✅ covered by 27 tests across 4 files
4. `secure-storage.ts` has tests covering: encrypt, decrypt, key rotation, corrupted data handling — ✅ covered by 11 tests across 3 files
5. Combined coverage report shows ≥70% lines covered across all 4 modules — ✅ `npm run coverage:check` exits 0
</output>
