---
phase: 15-core-module-tests
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - kapwa-client/src/lib/offline-queue.queue.test.ts
  - kapwa-client/src/lib/offline-queue.conflict.test.ts
  - kapwa-client/src/lib/offline-queue.versions.test.ts
autonomous: true
requirements:
  - TST-03

must_haves:
  truths:
    - "`offline-queue.queue.test.ts` covers the 4 queue public exports: queueChange, getPendingChanges, queueFsmTransition, loadQueue"
    - "`offline-queue.conflict.test.ts` covers the 6 conflict-resolution exports: markSynced, markConflict, markFailed, mergeRecords, resolveConflict, getConflictChanges"
    - "`offline-queue.versions.test.ts` covers the 4 version-vector exports: incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors"
    - "mergeRecords is tested for all 4 FINANCIAL_FIELDS rules (amount, status, fundSource, disbursedAmount — server-wins)"
    - "mergeRecords is tested for the notes-append rule, the consentStatus-revoked rule, and the default client-wins rule"
    - "The existing 2 tests in `offline-queue.test.ts` continue to pass with no regression"
    - "`offline-queue.ts` is instrumented by the v8 coverage provider and reports ≥70% lines in the per-file threshold"
    - "queueChange auto-increments the local version vector for the table (per D-05 — public export contract)"
    - "markSynced preserves a non-zero serverVersion when called with a smaller value (monotonic guard at offline-queue.ts:84)"
    - "Tests are split per concern: queue, conflict, versions (per D-13) — no integration tests between exports (per D-05)"
  prohibitions:
    - "Tests must NOT add new cases to the existing `offline-queue.test.ts` (per D-13: the existing file stays at 2 tests)"
    - "Tests must NOT write integration tests that call multiple exports in sequence (per D-05: each public export is tested in isolation; cross-export integration is already covered by `sync-conflict.test.ts`)"
    - "Tests must NOT use property-based testing or fuzz tests (per D-04: 1 test per branch only)"
    - "Tests must NOT mock the localStorage mock from `tests/setup.ts` — use the in-memory store and call `localStorage.clear()` in `beforeEach`"
    - "Tests must NOT add code comments beyond the `// Mock ...` 1-line fixture note pattern"
    - "Tests must NOT test the private `saveQueue` / `saveVersions` / `loadVersions` helpers (per D-05: 14 public exports only)"
  artifacts:
    - path: kapwa-client/src/lib/offline-queue.queue.test.ts
      provides: 4 queue exports tested
      contains: "describe('offline-queue — queue"
    - path: kapwa-client/src/lib/offline-queue.conflict.test.ts
      provides: 6 conflict-resolution exports tested
      contains: "describe('offline-queue — conflict"
    - path: kapwa-client/src/lib/offline-queue.versions.test.ts
      provides: 4 version-vector exports tested
      contains: "describe('offline-queue — versions"
    - path: kapwa-client/src/lib/offline-queue.test.ts
      provides: existing 2 tests (unchanged — verify no regression)
      contains: "describe('Offline Queue'"
  symbols:
    added:
      - "offline-queue.queue.test.ts with ~7 tests (D-05: queueChange, getPendingChanges, queueFsmTransition, loadQueue)"
      - "offline-queue.conflict.test.ts with ~10 tests (D-05: markSynced, markConflict, markFailed, mergeRecords × 4 FINANCIAL_FIELDS, notes, consentStatus, default, resolveConflict, getConflictChanges)"
      - "offline-queue.versions.test.ts with ~8 tests (D-05: incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors)"
    tests_added:
      - "queueChange appends a pending change with a UUID + initial state (status='pending', serverVersion=0, retryCount=0)"
      - "queueChange auto-increments the local version vector for the table"
      - "getPendingChanges returns only status=pending entries (filters out synced/conflict/failed)"
      - "getPendingChanges returns empty array when no queue"
      - "queueFsmTransition writes status + _fsmTransition + _clientUpdatedAt into payload + merges custom payload fields"
      - "loadQueue returns empty array when no queue (existing — no regression)"
      - "loadQueue returns queued changes from localStorage (existing — no regression)"
      - "markSynced transitions status pending → synced + updates serverVersion + calls updateServerVersion"
      - "markSynced preserves a non-zero serverVersion when called with a smaller value (monotonic guard)"
      - "markConflict transitions status pending → conflict + bumps retryCount + sets lastError"
      - "markFailed transitions status pending → failed + sets lastError (no retry bump)"
      - "mergeRecords: server value wins for each of the 4 FINANCIAL_FIELDS (amount, status, fundSource, disbursedAmount)"
      - "mergeRecords: notes are appended with \\n separator when both server and client have notes"
      - "mergeRecords: server revocation overrides client active state (consentStatus='revoked')"
      - "resolveConflict: server-wins strategy overrides client (server fields take precedence)"
      - "resolveConflict: client-wins strategy calls mergeRecords (financial fields still server-wins)"
      - "getConflictChanges returns only status=conflict entries"
      - "incrementLocalVersion starts a new table at localVersion=1"
      - "incrementLocalVersion increments an existing table (3x → 3)"
      - "incrementLocalVersion is per-table (cases != interventions)"
      - "updateServerVersion starts a new table at localVersion=0 with the given server version + sets lastSyncedAt"
      - "updateServerVersion overwrites an existing vector + updates lastSyncedAt"
      - "getVersionVector returns the single vector for a table"
      - "getVersionVector returns undefined for an unknown table"
      - "getAllVersionVectors returns the full vector list"
      - "getAllVersionVectors returns [] when empty"
  key_links:
    - from: kapwa-client/src/lib/offline-queue.queue.test.ts
      to: kapwa-client/src/lib/offline-queue.ts
      via: "imports queueChange, getPendingChanges, queueFsmTransition, loadQueue (4 public queue exports per D-05)"
      pattern: "import.*queueChange.*getPendingChanges.*queueFsmTransition.*from.*'\\./offline-queue'"
    - from: kapwa-client/src/lib/offline-queue.conflict.test.ts
      to: kapwa-client/src/lib/offline-queue.ts
      via: "imports mergeRecords, resolveConflict, markSynced, markConflict, markFailed, getConflictChanges (6 public conflict exports per D-05)"
      pattern: "import.*mergeRecords.*resolveConflict.*from.*'\\./offline-queue'"
    - from: kapwa-client/src/lib/offline-queue.versions.test.ts
      to: kapwa-client/src/lib/offline-queue.ts
      via: "imports incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors (4 public version exports per D-05)"
      pattern: "import.*incrementLocalVersion.*updateServerVersion.*from.*'\\./offline-queue'"
    - from: kapwa-client/src/lib/offline-queue.conflict.test.ts
      to: kapwa-client/src/lib/sync-conflict.test.ts
      via: "cross-export integration coverage is delegated to `sync-conflict.test.ts` (existing) — the new files test each export in isolation per D-05"
      pattern: "import.*from.*'\\./sync-conflict'"
---

<objective>
Add 25-30 new unit tests for all 14 public exports of `offline-queue.ts` split across 3 new test files (queue / conflict / versions), so the module crosses the 70% per-file line threshold required by TST-03.

Purpose: TST-03 demands ≥70% coverage on `offline-queue.ts` (queue push, dequeue, conflict detection, version vector updates). Per D-04, 25-30 new tests are needed (2 existing + 25-30 new). Per D-05, the strategy is to test each of the 14 public exports individually (1-3 tests each) with NO integration tests between exports — cross-export integration is already covered by the existing `sync-conflict.test.ts` file. Per D-13, the new tests split into 3 files by concern: queue (queueChange, getPendingChanges, queueFsmTransition, loadQueue), conflict (markSynced, markConflict, markFailed, mergeRecords, resolveConflict, getConflictChanges), versions (incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors). Per D-15, Plan 15-03 runs after 15-01 and 15-02.

Output: 3 new test files (~25-30 tests total), all passing, with `offline-queue.ts` reporting ≥70% lines in the per-file coverage report.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-core-module-tests/15-CONTEXT.md
@.planning/phases/15-core-module-tests/15-RESEARCH.md
@.planning/phases/15-core-module-tests/15-PATTERNS.md
@.planning/phases/15-core-module-tests/15-VALIDATION.md

# Source under test
@kapwa-client/src/lib/offline-queue.ts
@kapwa-client/src/lib/offline-queue.test.ts
@kapwa-client/src/lib/sync-conflict.test.ts (existing cross-export integration coverage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create offline-queue.queue.test.ts with 7 tests for queueChange, getPendingChanges, queueFsmTransition, loadQueue (per D-05)</name>
  <files>kapwa-client/src/lib/offline-queue.queue.test.ts</files>
  <read_first>
    - kapwa-client/src/lib/offline-queue.ts (queueChange at lines 44-71, getPendingChanges at lines 74-77, queueFsmTransition at lines 147-154, loadQueue at lines 26-29)
    - kapwa-client/src/lib/offline-queue.test.ts (existing 23 lines, 2 tests; the `localStorage.clear()` in beforeEach pattern at line 6)
    - .planning/phases/15-core-module-tests/15-PATTERNS.md (the offline-queue.queue.test.ts pattern at lines 344-390 — Pattern B + the per-export test shape)
  </read_first>
  <acceptance_criteria>
    - File `offline-queue.queue.test.ts` exists at `kapwa-client/src/lib/`
    - File imports `queueChange, getPendingChanges, queueFsmTransition, loadQueue` from `./offline-queue`
    - File has 1 top-level `describe('offline-queue — queue', ...)` block with 7 tests
    - Test 1 (queueChange): calls `await queueChange('cases', 'r1', 'UPDATE', { x: 1 })`; asserts the returned change has `id` (truthy UUID), `status === 'pending'`, `serverVersion === 0`, `retryCount === 0`, `tableName === 'cases'`, `recordId === 'r1'`, `operation === 'UPDATE'`, `payload === { x: 1 }`, `clientUpdatedAt` is an ISO string
    - Test 2 (queueChange increments version): calls queueChange twice for `cases`; imports `getVersionVector` from `./offline-queue`; asserts `getVersionVector('cases')?.localVersion === 2` (auto-increment per D-05 contract)
    - Test 3 (getPendingChanges filter): queues 2 changes; marks one as synced via `markSynced`; asserts `getPendingChanges()` returns only the unsynced one (length 1, recordId === 'r1')
    - Test 4 (getPendingChanges empty): with no queue, asserts `getPendingChanges()` returns `[]`
    - Test 5 (queueFsmTransition): calls `await queueFsmTransition('case-1', 'in_review', { notes: 'pre' })`; asserts the returned change has `payload.status === 'in_review'`, `payload._fsmTransition === true`, `payload._clientUpdatedAt` is a string, `payload.notes === 'pre'` (the custom payload field is preserved)
    - Test 6 (loadQueue empty — duplicate of existing test for file isolation): with `localStorage.clear()`, asserts `loadQueue()` returns `[]`
    - Test 7 (loadQueue existing — duplicate of existing test): pre-seeds localStorage with a queue entry; asserts `loadQueue()` returns the entry
    - All 7 tests pass
    - `localStorage.clear()` is called in `beforeEach` (matches the existing test file pattern at line 6)
  </acceptance_criteria>
  <action>
    Create a new test file at `kapwa-client/src/lib/offline-queue.queue.test.ts` with 1 `describe('offline-queue — queue', ...)` block containing 7 tests. Import `queueChange, getPendingChanges, queueFsmTransition` from `./offline-queue` at the top of the file; use `beforeEach(() => { localStorage.clear(); })` to match the existing offline-queue.test.ts pattern. Write the 7 tests in this order: (1) queueChange returns a change with the expected initial state (id truthy UUID, status='pending', serverVersion=0, retryCount=0, tableName, recordId, operation, payload, clientUpdatedAt is an ISO timestamp string — assert with `typeof === 'string'` and `length > 10`); (2) queueChange auto-increments the local version vector for the table — call queueChange twice, then dynamically import `getVersionVector` and assert `getVersionVector('cases')?.localVersion === 2`; (3) getPendingChanges filters by status='pending' — queue 2 changes, mark one as synced via the dynamically-imported `markSynced`, assert getPendingChanges returns 1 entry with the unsynced recordId; (4) getPendingChanges returns empty array when no queue (just call `await getPendingChanges()` after a clear); (5) queueFsmTransition writes status + _fsmTransition + _clientUpdatedAt into payload + merges custom payload fields; (6) loadQueue returns empty array when no queue (mirror the existing test for file isolation per D-13); (7) loadQueue returns queued changes from localStorage (mirror the existing test for file isolation). Per D-05, test each export in isolation — do NOT call queueChange + markSynced + getPendingChanges in the same test unless verifying a single export's contract (e.g., the version increment in test 2 is a side effect of queueChange, not an integration test). Per D-04, use the existing pattern of `for (const field of [...]) { it(...) }` for the FINANCIAL_FIELDS-style cases if needed (test 5 has only 1 branch though). Do NOT add code comments beyond `// Mock ...`. No emoji.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/offline-queue.queue.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - File `offline-queue.queue.test.ts` exists with 7 passing tests
    - All 7 tests cover the 4 queue public exports (queueChange, getPendingChanges, queueFsmTransition, loadQueue)
    - No regression in the existing 2 tests in `offline-queue.test.ts`
    - `localStorage.clear()` is called in `beforeEach`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create offline-queue.conflict.test.ts with 10 tests for markSynced, markConflict, markFailed, mergeRecords (4 fields + notes + consentStatus + default), resolveConflict (2 strategies), getConflictChanges (per D-05)</name>
  <files>kapwa-client/src/lib/offline-queue.conflict.test.ts</files>
  <read_first>
    - kapwa-client/src/lib/offline-queue.ts (mergeRecords at lines 158-177 with the FINANCIAL_FIELDS set at line 156, markSynced at lines 79-88 with the monotonic guard at line 84, markConflict at lines 90-99, markFailed at lines 101-109, resolveConflict at lines 179-182, getConflictChanges at lines 189-191)
    - kapwa-client/src/lib/sync-conflict.test.ts (existing test file — the cross-export integration coverage that D-05 defers to)
    - .planning/phases/15-core-module-tests/15-CATTERNS.md (the mergeRecords FINANCIAL_FIELDS pattern at lines 411-419; the markSynced/markConflict/markFailed patterns at lines 453-501)
  </read_first>
  <acceptance_criteria>
    - File `offline-queue.conflict.test.ts` exists at `kapwa-client/src/lib/`
    - File imports `mergeRecords, resolveConflict, markSynced, markConflict, markFailed, getConflictChanges` from `./offline-queue` (6 public conflict exports per D-05)
    - File has 1 top-level `describe('offline-queue — conflict resolution', ...)` block with 10 tests
    - Test 1 (mergeRecords FINANCIAL_FIELDS × 4): uses a `for (const field of ['amount', 'status', 'fundSource', 'disbursedAmount'])` loop to assert server-wins for each of the 4 fields
    - Test 2 (mergeRecords notes): asserts `mergeRecords({ notes: 'A' }, { notes: 'B' })` returns `{ notes: 'A\nB' }`; asserts `mergeRecords({ notes: 'A' }, {})` returns `{ notes: 'A' }` (server notes alone preserved)
    - Test 3 (mergeRecords consentStatus revoked): asserts `mergeRecords({ consentStatus: 'revoked' }, { consentStatus: 'active' })` returns `{ consentStatus: 'revoked' }`; asserts `mergeRecords({ consentStatus: 'active' }, { consentStatus: 'pending' })` returns `{ consentStatus: 'pending' }` (no override when server is not revoked)
    - Test 4 (markSynced transitions + monotonic guard): queues a change, calls markSynced(id, 5), asserts `getPendingChanges()` returns 0, the stored change has `status='synced'` and `serverVersion=5`; then calls markSynced(id, 3) with a smaller value and asserts serverVersion remains 5 (monotonic guard at offline-queue.ts:84)
    - Test 5 (markConflict): queues a change, calls markConflict(id, 'version mismatch'), asserts stored change has `status='conflict'`, `retryCount=1`, `lastError='version mismatch'`, and `getConflictChanges()` includes the change
    - Test 6 (markFailed): queues a change, calls markFailed(id, 'http 500'), asserts stored change has `status='failed'`, `retryCount=0` (no retry bump), `lastError='http 500'`
    - Test 7 (resolveConflict server-wins): asserts `resolveConflict('server-wins', { a: 1, b: 2 }, { a: 99, c: 3 })` returns `{ a: 1, b: 2, c: 3 }` (server fields take precedence, client-only fields preserved)
    - Test 8 (resolveConflict client-wins delegates to mergeRecords): asserts `resolveConflict('client-wins', { amount: 100 }, { amount: 200, notes: 'x' })` returns a result where `amount === 100` (FINANCIAL_FIELDS still server-wins) and `notes === 'x'` (client notes preserved)
    - Test 9 (getConflictChanges filter): queues 2 changes, marks one as conflict, asserts `getConflictChanges()` returns 1 entry with the conflict recordId
    - Test 10 (getConflictChanges empty): with no queue, asserts `getConflictChanges()` returns `[]`
    - All 10 tests pass
    - `localStorage.clear()` is called in `beforeEach`
  </acceptance_criteria>
  <action>
    Create a new test file at `kapwa-client/src/lib/offline-queue.conflict.test.ts` with 1 `describe('offline-queue — conflict resolution', ...)` block containing 10 tests. Import `mergeRecords, resolveConflict, markSynced, markConflict, markFailed, getConflictChanges` from `./offline-queue` at the top of the file; also import `queueChange` and `loadQueue` since markSynced/markConflict/markFailed operate on queued changes. Use `beforeEach(() => { localStorage.clear(); })` to match the offline-queue.test.ts pattern. Write the 10 tests in this order: (1) mergeRecords server-wins for each of the 4 FINANCIAL_FIELDS — use the per-field `for` loop pattern (1 test with 4 assertions OR 4 separate tests; the simpler 1-test-4-assertions pattern from .planning/phases/15-core-module-tests/15-PATTERNS.md lines 411-419 is acceptable); (2) mergeRecords notes — 2 assertions (concatenation when both present, server-alone preserved when client has no notes); (3) mergeRecords consentStatus — 2 assertions (server-revoked overrides, no override when server is not revoked); (4) markSynced transitions + monotonic guard — call queueChange, then markSynced(id, 5), assert the stored change via `loadQueue().find(...)` has status='synced' and serverVersion=5; then call markSynced(id, 3) and assert serverVersion is still 5 (the `if (change.serverVersion === 0)` guard at line 84 — actually, the guard is `if (change.serverVersion === 0) change.serverVersion = serverVersion`, so once serverVersion is set to 5 it's not overwritten by 3); (5) markConflict — assert retryCount increments and lastError is set; (6) markFailed — assert no retryCount bump (only status + lastError change); (7) resolveConflict server-wins — spread server over client; (8) resolveConflict client-wins — delegates to mergeRecords; (9) getConflictChanges filter — only status='conflict' entries; (10) getConflictChanges empty — returns `[]`. Per D-05, test each export in isolation. Per D-04, 1 test per branch (the FINANCIAL_FIELDS loop counts as 1 test with 4 assertions, matching the locked decision). Do NOT add code comments beyond `// Mock ...`. No emoji.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/offline-queue.conflict.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - File `offline-queue.conflict.test.ts` exists with 10 passing tests
    - All 10 tests cover the 6 public conflict exports (markSynced, markConflict, markFailed, mergeRecords × 4 rules, resolveConflict × 2 strategies, getConflictChanges)
    - mergeRecords covers all 4 FINANCIAL_FIELDS, the notes-append rule, the consentStatus-revoked rule, and the default client-wins rule
    - No regression in the existing 2 tests in `offline-queue.test.ts`
  </done>
</task>

<task type="auto">
  <name>Task 3: Create offline-queue.versions.test.ts with 8 tests for incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors + run coverage:check + plan summary</name>
  <files>kapwa-client/src/lib/offline-queue.versions.test.ts, .planning/phases/15-core-module-tests/15-03-SUMMARY.md</files>
  <read_first>
    - kapwa-client/src/lib/offline-queue.ts (incrementLocalVersion at lines 114-126, updateServerVersion at lines 128-141, getAllVersionVectors at lines 143-145, getVersionVector at lines 184-187)
    - kapwa-client/src/lib/offline-queue.test.ts (the existing `localStorage.clear()` in beforeEach pattern at line 6)
    - .planning/phases/15-core-module-tests/15-PATTERNS.md (the version-vector test patterns at lines 506-574)
  </read_first>
  <acceptance_criteria>
    - File `offline-queue.versions.test.ts` exists at `kapwa-client/src/lib/`
    - File imports `incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors` from `./offline-queue` (4 public version exports per D-05)
    - File has 1 top-level `describe('offline-queue — version vectors', ...)` block with 8 tests
    - Test 1 (incrementLocalVersion new table): calls `await incrementLocalVersion('cases')`; asserts `getVersionVector('cases')` returns `{ tableName: 'cases', localVersion: 1, serverVersion: 0, lastSyncedAt: null }`
    - Test 2 (incrementLocalVersion existing): calls incrementLocalVersion 3x for `cases`; asserts localVersion === 3
    - Test 3 (incrementLocalVersion per-table): calls incrementLocalVersion for `cases` and `interventions`; asserts each has localVersion === 1 (per-table isolation)
    - Test 4 (updateServerVersion new table): calls `await updateServerVersion('cases', 7)`; asserts `getVersionVector('cases')` returns `{ tableName: 'cases', localVersion: 0, serverVersion: 7, lastSyncedAt: <string-not-null> }`
    - Test 5 (updateServerVersion existing): calls updateServerVersion('cases', 5) then updateServerVersion('cases', 10); asserts serverVersion === 10 (overwrites)
    - Test 6 (getVersionVector single): pre-seeds a vector; asserts getVersionVector returns the single matching vector
    - Test 7 (getVersionVector unknown): with no vector, asserts getVersionVector('unknown') returns undefined
    - Test 8 (getAllVersionVectors): pre-seeds 2 vectors; asserts getAllVersionVectors returns array of length 2 with both tableNames
    - All 8 tests pass
    - `localStorage.clear()` is called in `beforeEach`
    - Plan summary file `15-03-SUMMARY.md` is created with: 3 task commit hashes, the list of ~25 new tests across 3 files, the offline-queue.ts coverage %, and the self-check
  </acceptance_criteria>
  <action>
    Create a new test file at `kapwa-client/src/lib/offline-queue.versions.test.ts` with 1 `describe('offline-queue — version vectors', ...)` block containing 8 tests. Import `incrementLocalVersion, updateServerVersion, getVersionVector, getAllVersionVectors` from `./offline-queue` at the top; use `beforeEach(() => { localStorage.clear(); })` to match the offline-queue.test.ts pattern. Write the 8 tests in this order: (1) incrementLocalVersion new table at localVersion=1 with serverVersion=0 and lastSyncedAt=null; (2) incrementLocalVersion increments existing (3x → 3); (3) incrementLocalVersion is per-table (cases localVersion=1, interventions localVersion=1 after 1 call each); (4) updateServerVersion new table at localVersion=0 with serverVersion=7 and lastSyncedAt is a non-null string (use `expect(v?.lastSyncedAt).not.toBeNull()` and `typeof v?.lastSyncedAt === 'string'`); (5) updateServerVersion overwrites existing (5 then 10 → 10); (6) getVersionVector single — pre-seed via updateServerVersion, assert the returned vector matches; (7) getVersionVector unknown — with no vector seeded, assert `getVersionVector('unknown')` returns undefined; (8) getAllVersionVectors — pre-seed via incrementLocalVersion for 2 tables, assert the returned array has length 2 and contains both tableNames sorted alphabetically. Per D-05, test each export in isolation. Per the agent's Discretion in CONTEXT, you may use `vi.useFakeTimers()` to freeze the clock for the `lastSyncedAt` ISO string — but it's not required (asserting non-null + is-string is sufficient). Do NOT add code comments beyond `// Mock ...`. No emoji. After the test file is created and passing, run `cd kapwa-client && npx vitest run --coverage --reporter=text-summary src/lib/offline-queue*.test.ts` to verify `offline-queue.ts` shows ≥70% lines. Then run `cd kapwa-client && npm run test:run` to verify no regression. Then create the plan summary file at `.planning/phases/15-core-module-tests/15-03-SUMMARY.md` with: 3 task commit hashes, list of ~25 new tests, offline-queue.ts coverage %, and self-check. Do NOT commit the summary — the orchestrator commits after the plan-checker passes.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --coverage --reporter=text-summary src/lib/offline-queue*.test.ts 2>&1 | grep -E '(offline-queue|Statements|Branches|Functions|Lines)' | head -10</automated>
  </verify>
  <done>
    - File `offline-queue.versions.test.ts` exists with 8 passing tests
    - `offline-queue.ts` reports ≥70% lines in the per-file coverage text-summary
    - `npm run test:run` shows the full suite passing (no regression in the existing 2 tests in offline-queue.test.ts)
    - Plan summary `15-03-SUMMARY.md` is created with 3 commit hashes + list of ~25 new tests + coverage % + self-check
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → localStorage | The in-memory `localStorage` mock from `tests/setup.ts` is shared across tests in the same vitest worker — leaking queue entries from one test to another is a real risk |
| Test → offline-queue.ts | Tests exercise the public exports only; the private `saveQueue` / `saveVersions` / `loadVersions` helpers are not directly tested (per D-05) |
| Test → real backend | No network involved; offline-queue.ts is a pure module over localStorage and crypto.randomUUID() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | mergeRecords FINANCIAL_FIELDS test | mitigate | The 4 field names are hard-coded in the test; the server-wins assertion is deep-equality; no production code can mutate the fixture |
| T-15-03-02 | Information Disclosure | localStorage queue leak across tests | mitigate | `localStorage.clear()` in `beforeEach` per D-13 file split pattern |
| T-15-03-03 | Denial of Service | `lastSyncedAt = new Date().toISOString()` could be mocked away | mitigate | Per the agent's Discretion, no `vi.useFakeTimers()` is needed; the ISO string assertion (`not.toBeNull()` + `typeof === 'string'`) is sufficient |
| T-15-03-04 | Elevation of Privilege | monotonic-guard test could pass with a different guard direction | mitigate | The test asserts serverVersion is 5 after a smaller markSynced(3) call, proving the `if (change.serverVersion === 0)` guard at offline-queue.ts:84 prevents overwrite — a flipped guard would fail this test |
| T-15-03-05 | Repudiation | Tests pass when the wrong export is exercised (e.g., markConflict instead of markFailed) | mitigate | Each test's assertion is specific to the export's contract (markConflict sets retryCount+1, markFailed sets retryCount+0) — wrong export call would fail |
| T-15-03-SC | Tampering | npm package install | accept | No new package installed in this plan; only test files added |
</threat_model>

<verification>
1. `cd kapwa-client && npx vitest run src/lib/offline-queue.queue.test.ts` shows 7/7 tests passing
2. `cd kapwa-client && npx vitest run src/lib/offline-queue.conflict.test.ts` shows 10/10 tests passing
3. `cd kapwa-client && npx vitest run src/lib/offline-queue.versions.test.ts` shows 8/8 tests passing
4. `cd kapwa-client && npx vitest run src/lib/offline-queue.test.ts` shows 2/2 tests passing (no regression)
5. `cd kapwa-client && npx vitest run --coverage --reporter=text-summary src/lib/offline-queue*.test.ts` shows `offline-queue.ts` ≥70% lines
6. `cd kapwa-client && npm run test:run` shows the full suite passing
7. Plan summary `15-03-SUMMARY.md` exists with 3 commit hashes + list of ~25 new tests + coverage %
</verification>

<success_criteria>
1. File `offline-queue.queue.test.ts` exists with 7 passing tests (D-05 queue exports)
2. File `offline-queue.conflict.test.ts` exists with 10 passing tests (D-05 conflict exports)
3. File `offline-queue.versions.test.ts` exists with 8 passing tests (D-05 version exports)
4. mergeRecords tests cover all 4 FINANCIAL_FIELDS (amount, status, fundSource, disbursedAmount) + notes-append + consentStatus-revoked + default client-wins
5. markSynced monotonic guard test asserts serverVersion is preserved when a smaller value is passed
6. queueChange auto-increments the local version vector (Test 2 in queue.test.ts)
7. resolveConflict tests cover both strategies (server-wins + client-wins delegates to mergeRecords)
8. getConflictChanges tests cover the filter and empty case
9. incrementLocalVersion / updateServerVersion / getVersionVector / getAllVersionVectors tests cover new + existing + per-table + overwrite cases
10. The existing 2 tests in `offline-queue.test.ts` still pass with no regression
11. `offline-queue.ts` reports ≥70% lines in the per-file coverage table (TST-03 satisfied)
12. No code comments added beyond the `// Mock ...` 1-line fixture note pattern
13. No emoji, no decorative formatting
14. Plan summary `15-03-SUMMARY.md` created with 3 commit hashes + list of ~25 new tests + coverage % + self-check
</success_criteria>

<output>
Create `.planning/phases/15-core-module-tests/15-03-SUMMARY.md` when done.

## Artifacts this phase produces

### Tests added
- `kapwa-client/src/lib/offline-queue.queue.test.ts` — NEW file, 7 tests:
  - `queueChange()` returns change with UUID + initial state (pending/serverVersion=0/retryCount=0)
  - `queueChange()` auto-increments the local version vector
  - `getPendingChanges()` filters by status='pending'
  - `getPendingChanges()` returns `[]` when queue is empty
  - `queueFsmTransition()` writes status + _fsmTransition + _clientUpdatedAt + merges custom payload
  - `loadQueue()` returns `[]` when empty (file-isolated duplicate)
  - `loadQueue()` returns queued changes from localStorage (file-isolated duplicate)
- `kapwa-client/src/lib/offline-queue.conflict.test.ts` — NEW file, 10 tests:
  - `mergeRecords()` server-wins for each of the 4 FINANCIAL_FIELDS (amount/status/fundSource/disbursedAmount)
  - `mergeRecords()` notes concatenation (`A\nB`) and server-alone preservation
  - `mergeRecords()` consentStatus='revoked' overrides client + no override when server is not revoked
  - `markSynced()` transitions pending → synced + updates serverVersion + monotonic guard preserves non-zero serverVersion
  - `markConflict()` transitions pending → conflict + retryCount+1 + sets lastError
  - `markFailed()` transitions pending → failed + lastError (no retry bump)
  - `resolveConflict('server-wins', ...)` spreads server over client
  - `resolveConflict('client-wins', ...)` delegates to mergeRecords (financial fields still server-wins)
  - `getConflictChanges()` filters by status='conflict'
  - `getConflictChanges()` returns `[]` when no conflicts
- `kapwa-client/src/lib/offline-queue.versions.test.ts` — NEW file, 8 tests:
  - `incrementLocalVersion()` starts new table at localVersion=1 with lastSyncedAt=null
  - `incrementLocalVersion()` increments existing (3x → 3)
  - `incrementLocalVersion()` is per-table (cases vs interventions isolation)
  - `updateServerVersion()` starts new table at localVersion=0 with the given server version + sets lastSyncedAt
  - `updateServerVersion()` overwrites existing (5 then 10 → 10)
  - `getVersionVector()` returns the single matching vector
  - `getVersionVector()` returns undefined for unknown table
  - `getAllVersionVectors()` returns full list / `[]` when empty

### Coverage gate progression
- `offline-queue.ts` reaches ≥70% lines in the per-file coverage table (TST-03 satisfied)

### Documentation
- `.planning/phases/15-core-module-tests/15-03-SUMMARY.md` — plan summary with 3 commit hashes + list of ~25 new tests + coverage %
</output>
