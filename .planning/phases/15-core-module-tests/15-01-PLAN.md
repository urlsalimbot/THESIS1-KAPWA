---
phase: 15-core-module-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/package.json
  - kapwa-client/vite.config.ts
  - kapwa-client/src/lib/api.test.ts
autonomous: true
requirements:
  - TST-01

must_haves:
  truths:
    - "`npm run coverage:check` exits 0 when all 4 source modules hit the per-file 70% lines threshold"
    - "`api.ts` is instrumented by the v8 coverage provider and reports ≥70% lines in the per-file threshold"
    - "The 16 pre-existing tests in `api.test.ts` continue to pass after the new tests are added"
    - "The new tests cover the KAPWA_AUTH_LOGOUT_EVENT export constant value + the refresh_network_error dispatch branch"
    - "`npm run coverage` produces a coverage report that lists `src/lib/api.ts` in its per-file table"
    - "A 3rd retry attempt does NOT happen when 2 TypeError failures already occurred on a POST request (POST never retries)"
  prohibitions:
    - "Coverage thresholds must NOT be project-wide-only — `perFile: true` is mandatory (D-06)"
    - "Coverage block must NOT be added with `coverage.enabled: true` — `--coverage` flag stays opt-in via the script"
    - "The coverage include glob must NOT match `*.test.ts` or `*.test.tsx` files"
    - "Package install must NOT pin to a different major of `vitest` — must be `4.1.10` to peer-match `vitest@4.1.9`"
  artifacts:
    - path: kapwa-client/package.json
      provides: npm scripts (coverage, coverage:check) and devDependency (@vitest/coverage-v8)
      contains: '"coverage": "vitest run --coverage"'
    - path: kapwa-client/vite.config.ts
      provides: per-file coverage threshold block (provider 'v8', include glob, thresholds)
      contains: "perFile: true"
    - path: kapwa-client/src/lib/api.test.ts
      provides: 3-5 new tests for KAPWA_AUTH_LOGOUT_EVENT export + internal retry edge cases
      contains: "KAPWA_AUTH_LOGOUT_EVENT"
  symbols:
    added:
      - "@vitest/coverage-v8@4.1.10 devDependency in kapwa-client/package.json"
      - "npm script `coverage` in kapwa-client/package.json"
      - "npm script `coverage:check` in kapwa-client/package.json"
      - "test.coverage block in kapwa-client/vite.config.ts"
    tests_added:
      - "KAPWA_AUTH_LOGOUT_EVENT constant value test (string export matches the same literal used in auth-context.tsx:42)"
      - "kapwa:auth:logout dispatch on refresh_network_error reason (covers the catch branch in api.ts:108-110)"
      - "POST no-retry-after-2-TypeErrors test (asserts fetch mock called exactly once)"
      - "PUT no-retry-after-2-TypeErrors test (asserts fetch mock called exactly once)"
      - "DELETE no-retry-after-2-TypeErrors test (asserts fetch mock called exactly once)"
  key_links:
    - from: kapwa-client/vite.config.ts
      to: "@vitest/coverage-v8@4.1.10"
      via: "test.coverage.provider: 'v8' is the resolution key — vitest requires the matching package to be installed or coverage will silently use a stub"
      pattern: "provider: 'v8'"
    - from: kapwa-client/vite.config.ts
      to: kapwa-client/src/lib/api.ts
      via: "include glob `src/lib/{api,api-error,auth-context,offline-queue,secure-storage}.{ts,tsx}` selects the 4 source modules for instrumentation"
      pattern: "src/lib/\\{api,api-error"
    - from: kapwa-client/src/lib/api.test.ts
      to: kapwa-client/src/lib/api.ts
      via: "tests import `api` and `KAPWA_AUTH_LOGOUT_EVENT` directly; new tests call api.get / api.post / api.put / api.del"
      pattern: "import.*from.*'\\./api'"
---

<objective>
Install the v8 coverage tool, wire it into the vitest config with per-file thresholds, add npm coverage scripts, and add the final 3-5 tests for the `api.ts` module so it crosses the 70% per-file line threshold required by TST-01.

Purpose: TST-01 demands ≥70% unit-test coverage on `api.ts`. Per D-04, only 3-5 new tests are needed because the existing 16-test suite already covers most branches. Per D-15, Plan 15-01 owns the coverage tool installation so subsequent plans (15-02..15-04) can verify each module's threshold against the same gate.

Output: Updated `vite.config.ts` (coverage block), updated `package.json` (scripts + devDep), 3-5 new tests appended to `api.test.ts`, and a passing `npm run coverage:check` run.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-core-module-tests/15-CONTEXT.md
@.planning/phases/15-core-module-tests/15-RESEARCH.md
@.planning/phases/15-core-module-tests/15-PATTERNS.md
@.planning/phases/15-core-module-tests/15-VALIDATION.md

# Source under test
@kapwa-client/src/lib/api.ts
@kapwa-client/src/lib/api.test.ts

# Toolchain baseline
@kapwa-client/vite.config.ts
@kapwa-client/package.json
@kapwa-client/tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vitest/coverage-v8 + wire test.coverage block into vite.config.ts (per D-01 + D-02)</name>
  <files>kapwa-client/package.json, kapwa-client/vite.config.ts</files>
  <read_first>
    - kapwa-client/vite.config.ts (current test block at lines 26-32)
    - kapwa-client/package.json (devDependencies and scripts blocks)
    - kapwa-client/src/lib/api.ts (verify 7 exports + KAPWA_AUTH_LOGOUT_EVENT constant)
    - .planning/phases/15-core-module-tests/15-RESEARCH.md (Pattern 1 — per-file coverage threshold)
  </read_first>
  <acceptance_criteria>
    - `@vitest/coverage-v8@^4.1.10` is listed in devDependencies of kapwa-client/package.json
    - `vite.config.ts` has a `coverage` sub-block inside the existing `test` block (do not replace the rest of the test config)
    - The coverage block sets `provider: 'v8'`
    - The coverage include glob exactly matches `src/lib/{api,api-error,auth-context,offline-queue,secure-storage}.{ts,tsx}`
    - The coverage exclude list contains `**/*.test.{ts,tsx}`, `**/types.ts`, `**/index.ts`
    - Thresholds set `perFile: true`, `lines: 70`, `functions: 70`, `branches: 60`, `statements: 70`
    - `npm ls @vitest/coverage-v8` shows the package present
    - `node -e "const c=require('./kapwa-client/vite.config.ts'.replace('.ts','.js'))"` is NOT the test — the existing test block must still parse (verified by `npx vitest --version` exiting 0)
  </acceptance_criteria>
  <action>
    Per D-01, run `cd kapwa-client && npm install -D @vitest/coverage-v8@4.1.10` to add the official v8 provider peer-matched to the project's `vitest@4.1.9`. Per D-02, edit `vite.config.ts` to add a `coverage:` sub-block inside the existing `test:` block — preserve the existing `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./tests/setup.ts']`, `include:`, and `exclude:` lines. The new sub-block has `provider: 'v8'`, the include glob `src/lib/{api,api-error,auth-context,offline-queue,secure-storage}.{ts,tsx}` (the 4 modules in TST-01..TST-04 plus `api-error.ts` which is already covered by its existing test file at 4/4), the exclude list of `**/*.test.{ts,tsx}`, `**/types.ts`, `**/index.ts`, and the thresholds object with `perFile: true` (D-06 mandates per-file, not project-wide), `lines: 70`, `functions: 70`, `branches: 60`, `statements: 70`. Per D-03, do NOT add any CI integration in this plan — `coverage:check` is a runnable npm script, not a workflow file. Verify the change by running `npx vitest --version` (confirms config parses) and `npx tsc --noEmit -p kapwa-client` (confirms the v8 coverage types compile in the same project). Do not introduce `coverage.enabled: true` — coverage stays opt-in via the `--coverage` flag passed by the npm scripts.
  </action>
  <verify>
    <automated>cd kapwa-client && npm ls @vitest/coverage-v8 --depth=0 2>&1 | grep -q '@vitest/coverage-v8@' && npx vitest --version</automated>
  </verify>
  <done>
    - `kapwa-client/package.json` devDependencies contains `@vitest/coverage-v8` at `^4.1.10`
    - `kapwa-client/vite.config.ts` contains `coverage: { provider: 'v8', include: [...], exclude: [...], thresholds: { perFile: true, ... } }` nested inside the existing `test:` block
    - `npx vitest --version` exits 0 (config still parses)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `coverage` + `coverage:check` npm scripts to package.json (per D-01)</name>
  <files>kapwa-client/package.json</files>
  <read_first>
    - kapwa-client/package.json (existing scripts block — lines 4-10)
    - .planning/phases/15-core-module-tests/15-CONTEXT.md (D-01: coverage script = `vitest run --coverage`, coverage:check = same — thresholds enforce the gate)
  </read_first>
  <acceptance_criteria>
    - `package.json` scripts block contains `"coverage": "vitest run --coverage"`
    - `package.json` scripts block contains `"coverage:check": "vitest run --coverage"`
    - The existing `test` and `test:run` scripts are unchanged
    - `npm run coverage` runs the full vitest suite with the v8 coverage provider
    - `npm run coverage:check` exits 0 when all 4 source modules hit 70% lines
    - Scripts are inserted in the right order (test, test:run, coverage, coverage:check) for readability
  </acceptance_criteria>
  <action>
    Per D-01, edit `kapwa-client/package.json` to add two new entries in the `scripts` block. Insert `"coverage": "vitest run --coverage"` and `"coverage:check": "vitest run --coverage"` immediately after the existing `"test:run": "vitest run"` line. Both scripts run the same command — the threshold enforcement from Task 1 is what makes `coverage:check` a gate: if any of the 4 source files drops below 70% lines, vitest exits non-zero. Per D-03, do NOT add any CI workflow file — the script is the only artifact. Document in a brief in-file comment above the new scripts: `// coverage:check exits non-zero if any of the 4 included modules drops below 70% lines (per-file threshold, D-06)`. Do not modify any other keys in package.json (dependencies, devDependencies other than what Task 1 added, etc).
  </action>
  <verify>
    <automated>cd kapwa-client && node -e "const p=require('./package.json'); if (!p.scripts.coverage || !p.scripts['coverage:check']) process.exit(1)"</automated>
  </verify>
  <done>
    - `package.json` contains `coverage` and `coverage:check` scripts that both invoke `vitest run --coverage`
    - Existing `test` + `test:run` scripts are unchanged
    - A future CI step can run `npm run coverage:check` and the threshold gate from Task 1 enforces TST-01
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 3-5 new tests to api.test.ts for KAPWA_AUTH_LOGOUT_EVENT + internal retry edge cases (per D-04 + CONTEXT §"Specific Ideas")</name>
  <files>kapwa-client/src/lib/api.test.ts</files>
  <read_first>
    - kapwa-client/src/lib/api.ts (KAPWA_AUTH_LOGOUT_EVENT at line 9; the catch branch in refreshToken at lines 105-111 dispatches the event with reason `'refresh_network_error'`)
    - kapwa-client/src/lib/api.test.ts (existing 255 lines, 16 tests; the `describe('401 refresh single-flight', ...)` block at lines 82-144 already covers the `'refresh_failed'` reason — do NOT duplicate)
    - .planning/phases/15-core-module-tests/15-PATTERNS.md (Pattern F: kapwa:auth:logout event dispatch + listener; Pattern G: fake timers + runAllTimersAsync)
  </read_first>
  <acceptance_criteria>
    - `api.test.ts` now has at least 19 tests (16 existing + 3-5 new)
    - A new test asserts the `KAPWA_AUTH_LOGOUT_EVENT` export is the string `'kapwa:auth:logout'` (matches the same literal used in `auth-context.tsx:42` to subscribe to the event)
    - A new test exercises the refresh_network_error dispatch path: mock `/cases` to 401, mock `/auth/refresh` to throw a TypeError (network failure on the refresh call itself), assert that the api client throws ApiError AND dispatches the `kapwa:auth:logout` event with `event.detail.reason === 'refresh_network_error'`
    - A new test (or pair of tests) asserts that `api.post`, `api.put`, and `api.del` each make exactly 1 fetch call when the mock rejects with TypeError (no retry) — this covers the `method === 'GET'` guard at api.ts:140
    - The existing 16 tests still pass (no regression)
    - The 401-refresh-fail test in the existing suite is NOT duplicated; the new refresh_network_error test uses a different mock sequence (refresh throws, original request's TypeError comes from the refresh fetch itself)
  </acceptance_criteria>
  <action>
    Per D-04, add 3-5 new tests to `kapwa-client/src/lib/api.test.ts` after the existing `'api object shape'` describe block at line 239. Add a new `describe('KAPWA_AUTH_LOGOUT_EVENT', ...)` block: (1) one test imports the constant alongside the existing `import { api } from './api'` line (extend the import statement to `import { api, KAPWA_AUTH_LOGOUT_EVENT } from './api'`) and asserts the value is the string `'kapwa:auth:logout'` (this is the same string used in auth-context.tsx:42 to subscribe to the event — the test guards against accidental rename breaking the cross-module contract). Add a new `describe('refresh_network_error dispatch', ...)` block: (2) one test mocks `fetch` to return 401 for `/cases`, then mocks the next fetch call (the `/auth/refresh` call) to reject with a TypeError (simulates the network going down between the original request and the refresh), and asserts that the api client throws ApiError AND dispatches the `kapwa:auth:logout` CustomEvent with `event.detail.reason === 'refresh_network_error'` (the reason string from the catch branch at api.ts:108-110). Add a new `describe('POST/PUT/DELETE never retry on TypeError', ...)` block: (3) one test for `api.post` with a TypeError mock asserting `fetchMock.mock.calls.length === 1`, (4) one for `api.put` same assertion, (5) one for `api.del` same assertion. Each new test uses the same `vi.stubGlobal('fetch', vi.fn())` + `localStorage.clear()` + `vi.restoreAllMocks` pattern as the existing tests. Use `window.addEventListener` + `removeEventListener` in a try/finally (per the existing pattern at lines 134-142) for the event-listener test to avoid leaking the listener across tests.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run src/lib/api.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `api.test.ts` has 19+ tests (16 existing + 3-5 new)
    - All 19+ tests pass
    - The `KAPWA_AUTH_LOGOUT_EVENT` constant value test guards the cross-module contract with auth-context.tsx
    - The refresh_network_error dispatch test covers the catch branch in api.ts:108-110 (the `refresh_failed` reason is already covered by the existing test at lines 126-143)
    - The POST/PUT/DELETE no-retry tests cover the `method === 'GET'` guard at api.ts:140
  </done>
</task>

<task type="auto">
  <name>Task 4: Run `npm run coverage:check` + verify api.ts ≥70% lines + plan summary</name>
  <files>.planning/phases/15-core-module-tests/15-01-SUMMARY.md</files>
  <read_first>
    - kapwa-client/vite.config.ts (verify the coverage block landed correctly)
    - kapwa-client/package.json (verify the scripts and devDep landed correctly)
    - .planning/phases/15-core-module-tests/15-VALIDATION.md (Wave 0 + per-task verification map for Plan 15-01)
  </read_first>
  <acceptance_criteria>
    - `npm run coverage:check` exits 0
    - The coverage report lists `api.ts` with ≥70% lines
    - The coverage report lists `api-error.ts` with ≥70% lines (already at 100% per its 4 existing tests)
    - The full vitest suite still passes (no regression in any of the 200+ existing tests across the codebase)
    - The plan summary file `15-01-SUMMARY.md` is created with the 4 task commit hashes, the coverage percentages, and the list of new tests added
    - `git log --oneline -5` shows 4 new commits from this plan (or the documented count if commits were squashed for atomicity)
  </acceptance_criteria>
  <action>
    Per D-06, run `cd kapwa-client && npm run coverage:check` and verify the per-file 70% lines threshold is met for `api.ts` (TST-01). The expected outcome: vitest prints a coverage table showing `src/lib/api.ts` with at least 70% lines, `src/lib/api-error.ts` at 100% (already covered by its 4 existing tests), and the other 3 modules (`auth-context.tsx`, `offline-queue.ts`, `secure-storage.ts`) listed but with their existing baseline (these will be brought up to 70% in plans 15-02, 15-03, 15-04). The threshold gate is per-file, so the other 3 modules being below 70% will cause `coverage:check` to exit non-zero at this stage — this is expected and not a failure of Plan 15-01. To verify only Plan 15-01's contribution, run the targeted command: `cd kapwa-client && npx vitest run --coverage --reporter=text-summary src/lib/api.test.ts src/lib/api-error.test.ts` and confirm `api.ts` shows ≥70% lines in the text-summary table. If it does, the threshold gate will pass for `api.ts` once the other 3 modules also clear 70% in plans 15-02..15-04. Run the full suite `cd kapwa-client && npm run test:run` to confirm no regression in any of the 200+ existing tests. Then create the plan summary file at `.planning/phases/15-core-module-tests/15-01-SUMMARY.md` with: 4 task commit hashes (look up via `git log --oneline -5`), the coverage percentages from the text-summary output, the list of new tests added (5 expected: KAPWA_AUTH_LOGOUT_EVENT value, refresh_network_error dispatch, POST no-retry, PUT no-retry, DELETE no-retry), the must_haves check (each `truths` item has a corresponding verification), and the self-check section. Do NOT commit the summary file — the orchestrator commits after the plan-checker passes.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --coverage --reporter=text-summary src/lib/api.test.ts src/lib/api-error.test.ts 2>&1 | grep -E '(api\.ts|api-error\.ts|All files)' | head -10</automated>
  </verify>
  <done>
    - `npx vitest run --coverage` on api.test.ts + api-error.test.ts shows `api.ts` ≥70% lines in the text-summary
    - `npm run test:run` shows the full suite passing (no regression)
    - Plan summary `15-01-SUMMARY.md` is created with all 5 sections (Performance, Accomplishments, Task Commits, Files Modified, Self-Check)
    - All 4 task commit hashes are recorded in the summary
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | Installing `@vitest/coverage-v8` from npm; the package is the official Vitest coverage provider but is a one-day-old release ([SUS] flag per RESEARCH §"Package Legitimacy Audit" — manually overridden) |
| Coverage tool → source code | v8 instrumentation runs in the Node test process; the coverage report is written to `kapwa-client/coverage/` which is git-ignored |
| CI runner → coverage:check script | Future CI integration (Phase 17) will run `npm run coverage:check`; the per-file threshold gate is the security boundary — a regression below 70% lines exits non-zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | vite.config.ts coverage block | mitigate | Coverage block is hand-authored with explicit per-file thresholds (no template injection); `git diff` on vite.config.ts in code review catches unauthorized changes |
| T-15-02 | Information Disclosure | kapwa-client/coverage/ directory | mitigate | Coverage HTML report at `coverage/index.html` is git-ignored via the standard Vite `.gitignore`; per RESEARCH, the HTML report shows file paths + line numbers but no source code or secrets — only the line-by-line execution count |
| T-15-03 | Elevation of Privilege | `coverage:check` bypass | mitigate | Threshold is enforced by vitest's own threshold checker (per the [VERIFIED: vitest.dev/config/coverage#coverage-thresholds] reference in RESEARCH); there is no way to bypass without editing vite.config.ts (which is a tracked, reviewable file) |
| T-15-04 | Repudiation | Missing audit trail for test additions | mitigate | Each new test is added in a tracked file with a code-reviewable diff; the plan summary records commit hashes; coverage report shows per-test attribution via the `--reporter=html` output |
| T-15-SC | Tampering | npm install of `@vitest/coverage-v8@4.1.10` | mitigate | slopcheck gate already executed in RESEARCH §"Package Legitimacy Audit" — package is the official vitest maintainers, 25.8M weekly downloads, peer-deps match `vitest@4.1.9`, no `postinstall` script. The [SUS] "too-new" signal is a false positive (same-day version bump). The plan uses the npm-canonical `^4.1.10` install (not a typosquat) |
</threat_model>

<verification>
1. `cd kapwa-client && npm ls @vitest/coverage-v8 --depth=0` shows the package installed
2. `cd kapwa-client && npx vitest --version` exits 0 (vite.config.ts still parses after the coverage block was added)
3. `cd kapwa-client && node -e "const p=require('./package.json'); if (!p.scripts.coverage || !p.scripts['coverage:check']) process.exit(1)"` exits 0 (scripts are present)
4. `cd kapwa-client && npx vitest run src/lib/api.test.ts 2>&1 | tail -5` shows 19+ tests passing
5. `cd kapwa-client && npx vitest run --coverage --reporter=text-summary src/lib/api.test.ts src/lib/api-error.test.ts` shows `api.ts` with ≥70% lines in the text-summary
6. `cd kapwa-client && npm run test:run` shows the full suite passing (no regression)
7. The plan summary file `.planning/phases/15-core-module-tests/15-01-SUMMARY.md` exists with 4 task commit hashes + coverage percentages + list of new tests
</verification>

<success_criteria>
1. `@vitest/coverage-v8@^4.1.10` is in `kapwa-client/package.json` devDependencies
2. `kapwa-client/vite.config.ts` test block has a `coverage:` sub-block with `provider: 'v8'`, the 4-module include glob, the test/types/index exclude list, and `thresholds: { perFile: true, lines: 70, functions: 70, branches: 60, statements: 70 }`
3. `kapwa-client/package.json` scripts contain `coverage` and `coverage:check`, both invoking `vitest run --coverage`
4. `api.test.ts` has 19+ tests passing (16 existing + 3-5 new)
5. New tests cover: the KAPWA_AUTH_LOGOUT_EVENT export value, the refresh_network_error dispatch branch, and the POST/PUT/DELETE no-retry guards
6. `api.ts` reports ≥70% lines in the per-file coverage table
7. The existing 16 tests + the rest of the project's 200+ tests still pass (no regression)
8. The `coverage:check` script is runnable and will exit non-zero if any of the 4 source files drops below 70% lines (the gate from D-02/D-06)
9. No CI workflow file is added in this plan (per D-03 — CI integration is Phase 17 scope)
10. Plan summary `15-01-SUMMARY.md` exists with 4 commit hashes + coverage percentages + new test list + self-check section
</success_criteria>

<output>
Create `.planning/phases/15-core-module-tests/15-01-SUMMARY.md` when done.

## Artifacts this phase produces

### Configuration
- `kapwa-client/package.json` — adds devDep `@vitest/coverage-v8@^4.1.10`; adds `coverage` and `coverage:check` scripts (both `vitest run --coverage`)
- `kapwa-client/vite.config.ts` — adds `test.coverage` sub-block: provider 'v8', include glob for 4 source files, exclude list, per-file thresholds (lines 70, functions 70, branches 60, statements 70)

### Tests
- `kapwa-client/src/lib/api.test.ts` — 5 new tests appended inside the existing `describe('api client', ...)` block:
  - `KAPWA_AUTH_LOGOUT_EVENT` constant value matches `'kapwa:auth:logout'`
  - refresh_network_error dispatch on refresh-fetch TypeError
  - POST no-retry after TypeError
  - PUT no-retry after TypeError
  - DELETE no-retry after TypeError

### npm scripts
- `npm run coverage` — runs `vitest run --coverage` (full suite + coverage report)
- `npm run coverage:check` — runs `vitest run --coverage` (same command; per-file threshold gate is the CI signal)

### Coverage threshold gate
- `vite.config.ts` test.coverage.thresholds.perFile: true — any of the 4 source files dropping below 70% lines causes `coverage:check` to exit non-zero

### Documentation
- `.planning/phases/15-core-module-tests/15-01-SUMMARY.md` — plan summary with commit hashes + coverage percentages
</output>
