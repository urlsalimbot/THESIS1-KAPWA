---
phase: 10-shared-components-responsive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/package.json
  - kapwa-client/src/components/ui/pagination.tsx
  - kapwa-client/src/hooks/use-media-query.ts
  - kapwa-client/src/components/EmptyState.tsx
  - kapwa-client/src/components/EmptyState.test.tsx
  - kapwa-client/src/components/PageShell.tsx
  - kapwa-client/src/components/PageShell.test.tsx
  - kapwa-client/src/components/skeletons/TableSkeleton.tsx
  - kapwa-client/src/components/skeletons/TableSkeleton.test.tsx
  - kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
  - kapwa-client/src/components/skeletons/CardGridSkeleton.test.tsx
  - kapwa-client/src/components/skeletons/FormSkeleton.tsx
  - kapwa-client/src/components/skeletons/FormSkeleton.test.tsx
  - kapwa-client/src/components/skeletons/index.ts
  - kapwa-client/src/components/ErrorBoundary.tsx
  - kapwa-client/src/components/ErrorBoundary.test.tsx
  - kapwa-client/src/components/Layout.tsx
  - kapwa-client/src/routes.tsx
autonomous: true
requirements:
  - STT-01
  - STT-02
  - STT-03
  - STT-04
  - STT-05
user_setup: []
must_haves:
  truths:
    - "Every page renders with a consistent PageShell wrapper providing title + description header + proper spacing"
    - "Data-fetching pages show skeleton components matching real content dimensions during loading"
    - "Empty list/search views display Lucide icon + message + CTA button instead of blank areas"
    - "Route crashes show error boundary fallback with error icon, message, retry button, and home link — not a white screen"
    - "CRUD operations show Sonner toast notifications (success, error, promise/loading states)"
    - "All toasts require manual dismiss — no auto-dismiss"
  artifacts:
    - path: "kapwa-client/src/components/PageShell.tsx"
      provides: "Sticky header wrapper with title+description+actions+children"
      min_lines: 30
      exports: ["PageShell"]
    - path: "kapwa-client/src/components/EmptyState.tsx"
      provides: "Empty state with 4 variants (no-data, no-results, offline, no-access)"
      min_lines: 50
      exports: ["EmptyState"]
    - path: "kapwa-client/src/components/ErrorBoundary.tsx"
      provides: "Network-aware error boundary class component with retry + home link"
      min_lines: 80
      exports: ["ErrorBoundary"]
    - path: "kapwa-client/src/components/skeletons/TableSkeleton.tsx"
      provides: "Table loading skeleton with variable-width rows"
      exports: ["TableSkeleton"]
    - path: "kapwa-client/src/components/skeletons/CardGridSkeleton.tsx"
      provides: "Card grid skeleton matching 2-col desktop / 1-col mobile grid"
      exports: ["CardGridSkeleton"]
    - path: "kapwa-client/src/components/skeletons/FormSkeleton.tsx"
      provides: "Full form layout skeleton with labels + inputs + button"
      exports: ["FormSkeleton"]
    - path: "kapwa-client/src/hooks/use-media-query.ts"
      provides: "React hook for responsive media query matching"
      exports: ["useMediaQuery"]
    - path: "kapwa-client/src/components/Layout.tsx"
      provides: "Modified to wrap <main> with ErrorBoundary; adds pb-16 for bottom nav clearance"
    - path: "kapwa-client/src/routes.tsx"
      provides: "Modified to render Sonner <Toaster> inside ThemeProvider"
  key_links:
    - from: "src/components/Layout.tsx"
      to: "src/components/ErrorBoundary.tsx"
      via: "wraps <main> content with <ErrorBoundary> — catches render errors in any page"
      pattern: "ErrorBoundary"
    - from: "src/routes.tsx"
      to: "src/components/ui/sonner.tsx"
      via: "renders <Toaster position=\"top-center\" duration={Infinity} /> inside ThemeProvider"
      pattern: "Toaster"
    - from: "src/components/PageShell.tsx"
      to: "src/lib/utils.ts"
      via: "uses cn() for Tailwind class merging"
      pattern: "cn\\("
---

<objective>
Create all shared page-state components that every data-fetching page in Kapwa uses: PageShell wrapper, 3 skeleton variants (table, card grid, form), 4-variant EmptyState component, and a network-aware ErrorBoundary. Wire ErrorBoundary around `<main>` in Layout.tsx and mount the Sonner `<Toaster>` in routes.tsx. Install `@tanstack/react-table` and shadcn Pagination for use by Plan 10-02.

**Purpose:** Eliminate ad-hoc loading/empty/error state patterns across 18+ pages. Every page gets a consistent PageShell wrapper, loading skeletons matching content shape, an EmptyState component with 4 predefined variants, a global ErrorBoundary that differentiates network vs render errors, and Sonner toast notifications for all CRUD operations.

**Output:** 8 new components (PageShell, EmptyState, TableSkeleton, CardGridSkeleton, FormSkeleton, ErrorBoundary rewrite, use-media-query hook), 7 test files, modified Layout.tsx and routes.tsx, installed @tanstack/react-table and shadcn Pagination.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-shared-components-responsive/10-CONTEXT.md
@.planning/phases/10-shared-components-responsive/10-RESEARCH.md
@.planning/phases/10-shared-components-responsive/10-PATTERNS.md

# Source files to read
@kapwa-client/src/components/Layout.tsx
@kapwa-client/src/routes.tsx
@kapwa-client/src/components/ErrorBoundary.tsx
@kapwa-client/src/lib/utils.ts
@kapwa-client/src/components/ui/skeleton.tsx
@kapwa-client/src/components/ui/sonner.tsx
@kapwa-client/src/components/ui/table.tsx
@kapwa-client/src/components/cards/AccessCard.test.tsx
@kapwa-client/vitest.config.ts (actually in vite.config.ts — test config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create use-media-query hook, and build EmptyState component with tests</name>
  <files>
    kapwa-client/package.json
    kapwa-client/src/components/ui/pagination.tsx
    kapwa-client/src/hooks/use-media-query.ts
    kapwa-client/src/components/EmptyState.tsx
    kapwa-client/src/components/EmptyState.test.tsx
  </files>
  <read_first>
    kapwa-client/package.json
    kapwa-client/components/cards/AccessCard.test.tsx
  </read_first>
  <action>
    **Substep A — Install packages:**
    From kapwa-client/, run `npm install @tanstack/react-table`. Then run `npx shadcn@latest add pagination --yes` to install shadcn Pagination component (generates src/components/ui/pagination.tsx with standard forwardRef/cn pattern). Confirm pagination.tsx exists with `ls src/components/ui/pagination.tsx`.

    **Substep B — Create `src/hooks/use-media-query.ts`:**
    Named-export function `useMediaQuery(query: string): boolean`. Uses `useState` initialized with `window.matchMedia(query).matches`. Registers a `change` event listener on `MediaQueryList` object in a `useEffect`, calls `setMatches(e.matches)` on change. Cleans up via `removeEventListener`. This is the first custom hook in the codebase — establish the pattern. No default export.

    **Substep C — Create `src/components/EmptyState.tsx`:**
    Named-export function `EmptyState` with props: `{ variant: 'no-data' | 'no-results' | 'offline' | 'no-access'; onAction?: () => void }`.
    Define a `CONFIG` Record keyed by variant with shape `{ icon: LucideIcon; message: string; cta: string }`.
    - `no-data`: Inbox icon (Inbox from lucide-react), message "No data found", CTA "Add first record" — CTA navigates to `/intake` via useNavigate
    - `no-results`: SearchX icon, message "No results match your search", CTA "Clear filters" — CTA calls the `onAction` prop (parent resets filters)
    - `offline`: WifiOff icon, message "You appear to be offline", CTA "Retry" — CTA calls `onAction` prop (parent retries fetch), also displays a secondary hint "Please check your connection and try again"
    - `no-access`: ShieldOff icon, message "You don't have access to this section", CTA "Go to Dashboard" — CTA navigates to `/dashboard` via useNavigate

    Render layout: `flex flex-col items-center justify-center py-16 gap-4 text-center` wrapping the Icon (h-12 w-12 text-muted-foreground), `<p>` for message (text-base font-medium text-foreground), and `<Button variant="outline"` with onClick handler. The CTA behavior: for `no-results` and `offline` calls `onAction?.()` if provided, for `no-data` and `no-access` navigates via `useNavigate` (per D-11 named CTAs).

    **Substep D — Create `src/components/EmptyState.test.tsx`:**
    Per TDD heuristic. Four `it()` blocks, one per variant. For each: render `<EmptyState variant={variant} />`, assert the correct message text appears via `screen.getByText()`. Also assert the CTA button text matches CONFIG. Use the AccessCard.test.tsx pattern (describe/it/expect, render from @testing-library/react). Also test that the icon renders (check for a specific aria-hidden or role="img" element).

    Per D-09: single component with variant prop. Per D-10: Lucide icons. Per D-11: named CTAs. Per D-12: professional tone (no jokes/confetti).
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(tests|passed|failed)"; ls src/components/ui/pagination.tsx</automated>
  </verify>
  <done>
    @tanstack/react-table in package.json, pagination.tsx exists, use-media-query.ts exports useMediaQuery, EmptyState renders 4 variant UIs, all 4+ EmptyState tests pass
  </done>
  <acceptance_criteria>
    - `npm test -- --run` passes with EmptyState tests
    - `ls src/components/ui/pagination.tsx` succeeds
    - `grep '@tanstack/react-table' package.json` shows it
    - EmptyState renders icon + message + CTA for each of 4 variants
    - All D-09 through D-12 behaviors present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create PageShell component and three skeleton variants with tests</name>
  <files>
    kapwa-client/src/components/PageShell.tsx
    kapwa-client/src/components/PageShell.test.tsx
    kapwa-client/src/components/skeletons/TableSkeleton.tsx
    kapwa-client/src/components/skeletons/TableSkeleton.test.tsx
    kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
    kapwa-client/src/components/skeletons/CardGridSkeleton.test.tsx
    kapwa-client/src/components/skeletons/FormSkeleton.tsx
    kapwa-client/src/components/skeletons/FormSkeleton.test.tsx
    kapwa-client/src/components/skeletons/index.ts
  </files>
  <read_first>
    kapwa-client/src/components/ui/skeleton.tsx
    kapwa-client/src/components/Layout.tsx
  </read_first>
  <action>
    **Substep A — Create `src/components/PageShell.tsx`:**
    Named-export function `PageShell` with props: `{ title: string; description?: string; actions?: React.ReactNode; children: React.ReactNode }`.
    Renders a `<div className="flex flex-col gap-4 lg:gap-8">` wrapper (per D-02 responsive spacing).
    Inside, a sticky header section: `<div className="flex items-start justify-between gap-4">` containing:
    - Left column: `<h1 className="text-2xl font-bold font-heading text-foreground">{title}</h1>` (per D-01 title)
    - If `description`: `<p className="text-sm text-muted-foreground mt-1">{description}</p>` (per D-04 single muted subtitle)
    - Right side: `{actions && <div className="flex items-center gap-2 shrink-0">{actions}</div>}` (per D-01 action slot)
    - Uses `font-heading` CSS variable (Lexend) for title — matches existing pattern in index.css

    Then renders `{children}` after the header.

    Uses `cn` from `@/lib/utils`. Does NOT import or render breadcrumbs (per D-03 — breadcrumbs stay in Layout.tsx).

    **Substep B — Create `src/components/PageShell.test.tsx`:**
    Test 1: renders title text. Test 2: renders description when provided. Test 3: renders actions slot. Test 4: renders children. Test 5: does NOT render description when omitted. Use AccessCard.test.tsx pattern.

    **Substep C — Create `src/components/skeletons/TableSkeleton.tsx`:**
    Named export `function TableSkeleton({ rows = 5 }: { rows?: number })`.
    Renders a `<div className="space-y-3">` containing `rows` copies of `<Skeleton className="h-4 w-[var(--width)]" />` where each row gets a different random-looking width (use inline style or className array: `['w-[80%]', 'w-[60%]', 'w-[90%]', 'w-[70%]', 'w-[50%]']`, cycling through with modulo). This creates variable-width columns matching table content shape (per D-05). Also renders a header row `<Skeleton className="h-6 w-full mb-2" />` before the body rows, and a final pagination bar row with `<Skeleton className="h-4 w-[40%]" />`. Uses shadcn Skeleton (default pulse animation per D-07 — no shimmer variants).

    **Substep D — Create `src/components/skeletons/TableSkeleton.test.tsx`:**
    Test 1: renders 5 rows by default. Test 2: renders `rows` prop number of skeleton lines. Test 3: renders header skeleton.

    **Substep E — Create `src/components/skeletons/CardGridSkeleton.tsx`:**
    Named export `function CardGridSkeleton({ count = 4 }: { count?: number })`.
    Renders `<div className="grid grid-cols-1 md:grid-cols-2 gap-4">` containing `count` card-shaped skeleton blocks. Each card: `<div className="border border-border rounded-lg p-4 space-y-3">` with `<Skeleton className="h-4 w-[60%]" />` (title), `<Skeleton className="h-3 w-[90%]" />` (body line 1), `<Skeleton className="h-3 w-[80%]" />` (body line 2), `<Skeleton className="h-8 w-full mt-2" />` (action bar). Matches actual card dimensions (per D-06 card grid = 2 cols desktop, 1 mobile).

    **Substep F — Create `src/components/skeletons/CardGridSkeleton.test.tsx`:**
    Test 1: renders 4 cards by default. Test 2: renders responsive grid columns. Test 3: renders card border/skeleton elements.

    **Substep G — Create `src/components/skeletons/FormSkeleton.tsx`:**
    Named export `function FormSkeleton({ fields = 4 }: { fields?: number })`.
    Renders `<div className="space-y-6">` containing `fields` form-field blocks. Each field: `<div className="space-y-2">` with `<Skeleton className="h-4 w-[120px]" />` (label), `<Skeleton className="h-10 w-full" />` (input). After the fields, adds `<Skeleton className="h-10 w-[140px] mt-4" />` for the submit button. Matches full form layout (per D-08 — labels + input blocks + button).

    **Substep H — Create `src/components/skeletons/FormSkeleton.test.tsx`:**
    Test 1: renders 4 fields by default. Test 2: renders with custom field count. Test 3: renders submit button skeleton.

    **Substep I — Create `src/components/skeletons/index.ts`:**
    Barrel export: re-export TableSkeleton, CardGridSkeleton, FormSkeleton.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(tests|passed|failed)"</automated>
  </verify>
  <done>
    PageShell.tsx renders title+description+children with correct classes; 3 skeleton components render with shadcn Skeleton pulse animation; all test files pass; all D-01 through D-08 behaviors present
  </done>
  <acceptance_criteria>
    - PageShell renders title h1 with font-heading, description in muted text, actions slot, children
    - PageShell uses gap-4 lg:gap-8 responsive spacing (per D-02)
    - PageShell does NOT render breadcrumbs (per D-03)
    - TableSkeleton renders variable-width row skeletons, header skeleton, pagination skeleton (per D-05)
    - CardGridSkeleton renders grid-cols-1 md:grid-cols-2 with card-shaped skeleton blocks (per D-06)
    - No shimmer or static skeleton variants — only default pulse animation (per D-07)
    - FormSkeleton renders label + input + button skeleton blocks (per D-08)
    - All 4 skeleton files pass tests via `npm test -- --run`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite ErrorBoundary with network awareness, integrate into Layout.tsx, and wire Sonner Toaster into routes.tsx</name>
  <files>
    kapwa-client/src/components/ErrorBoundary.tsx
    kapwa-client/src/components/ErrorBoundary.test.tsx
    kapwa-client/src/components/Layout.tsx
    kapwa-client/src/routes.tsx
  </files>
  <read_first>
    kapwa-client/src/components/ErrorBoundary.tsx
    kapwa-client/src/components/Layout.tsx
    kapwa-client/src/routes.tsx
    kapwa-client/src/components/ui/sonner.tsx
  </read_first>
  <action>
    **Substep A — Rewrite `src/components/ErrorBoundary.tsx`:**
    Rewrite the existing class component. Keep the same signature (`extends React.Component<Props, State>`) with same Props interface (children + optional fallback). Per D-13 this is the single global ErrorBoundary wrapping `<main>`.

    New fallback UI (when `hasError` is true):
    - Detect network error per D-16: `const isNetworkError = !navigator.onLine || (error?.message ?? '').toLowerCase().includes('fetch') || error?.name === 'TypeError'`
    - Network error view: WifiOff icon (h-12 w-12 text-muted-foreground), heading "You are offline", message "Please check your internet connection and try again." (matches D-16 wording), Button "Retry" calls `this.setState({ hasError: false, error: null })`
    - Render error view: TriangleAlert icon (h-12 w-12 text-destructive), heading "Something went wrong", message "An unexpected error occurred. Please try again later." (D-14/16 generic), two buttons side-by-side: "Try Again" (resets state per D-15) and "Go to Dashboard" — use Button with `asChild` wrapping `<Link to="/dashboard">` from react-router-dom (D-15 says no `window.location.href`, use Link)
    - Do NOT display `this.state.error.message` directly in the UI (D-16 network error differentiation uses navigator.onLine, not error message leaking)
    - Remove the `fallback` prop rendering path — per D-13 single global use, keep fallback prop for flexibility but the default fallback is always the rich UI above
    - `componentDidCatch` still logs `console.error('ErrorBoundary caught:', error, info)` — stack traces go to console only (security best practice per RESEARCH.md V7)
    - Use `import { TriangleAlert, WifiOff } from 'lucide-react'`, `import { Button } from '@/components/ui/button'`, `import { Link } from 'react-router-dom'`

    **Substep B — Create `src/components/ErrorBoundary.test.tsx`:**
    Test 1: renders children when no error.
    Test 2: catches thrown error and shows fallback (use `jest.spyOn(console, 'error').mockImplementation(() => {})` to suppress expected console.error from componentDidCatch; render component that throws; assert fallback text appears).
    Test 3: "Try Again" button resets state (render throwing child, click "Try Again", verify children re-render).
    Test 4: "Go to Dashboard" link has correct `to="/dashboard"` attribute (use MemoryRouter wrapper).

    **Substep C — Modify `src/components/Layout.tsx`:**
    Import ErrorBoundary at top: `import { ErrorBoundary } from '@/components/ErrorBoundary'`.
    In the render, wrap the `<main>` content area inside the existing div structure, replacing direct children with ErrorBoundary wrapper. Pattern:
    ```
    <main id="main-content" className="flex-1 p-6 bg-background min-h-[calc(100vh-4rem)] overflow-auto pb-16 lg:pb-6">
      <ErrorBoundary>
        <BreadcrumbNav pathname={location.pathname} />
        {children || <Outlet />}
      </ErrorBoundary>
    </main>
    ```
    Note: Add `pb-16 lg:pb-6` to the main element — this provides bottom padding clearance for the BottomNav component that Plan 10-02 will add. The `pb-16` ensures page content doesn't scroll behind the mobile bottom nav (Pitfall 2 from RESEARCH.md). `lg:pb-6` restores normal padding on desktop where BottomNav is hidden.
    Keep all other Layout.tsx code unchanged (offline banner, Topbar, Sidebar, Sheet, layout div).

    **Substep D — Modify `src/routes.tsx`:**
    Import Toaster: `import { Toaster } from '@/components/ui/sonner'`.
    Add `<Toaster position="top-center" duration={Infinity} />` inside the `<ThemeProvider>` and before `<AuthProvider>` in `MainRoutes()` (per D-17 top-center, D-18/19 manual dismiss via duration:Infinity, D-20 default styling — no custom rich toast component).
    Remove the `<ErrorBoundary>` wrapper around `<RouterProvider>` — ErrorBoundary is now in Layout.tsx per D-13. Keep the import if still referenced elsewhere, otherwise remove it. The `<RouterProvider>` now sits directly inside `<AuthProvider>`.

    Final routes.tsx MainRoutes structure:
    ```tsx
    export function MainRoutes() {
      return (
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
          <Toaster position="top-center" duration={Infinity} />
          <AuthProvider>
            <RouterProvider router={router} />
          </AuthProvider>
        </ThemeProvider>
      );
    }
    ```
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(tests|passed|failed)"</automated>
  </verify>
  <done>
    ErrorBoundary shows network-aware fallback with retry+home, Layout.tsx wraps <main> with ErrorBoundary with pb-16 clearance, routes.tsx renders Toaster top-center with duration Infinity, test suite passes
  </done>
  <acceptance_criteria>
    - ErrorBoundary catches render errors and shows TriangleAlert + "Something went wrong" heading + "Try Again" button + "Go to Dashboard" link (per D-14)
    - ErrorBoundary detects network errors (offline/fetch failure) and shows WifiOff + "You are offline" heading (per D-16)
    - "Try Again" resets this.state.hasError to false — does NOT call window.location.reload() (per D-15)
    - Layout.tsx wraps <main> content with ErrorBoundary (per D-13)
    - Layout.tsx main has pb-16 lg:pb-6 classes for bottom nav clearance
    - routes.tsx renders Toaster with position="top-center" and duration={Infinity} (per D-17, D-18, D-19)
    - routes.tsx no longer has ErrorBoundary wrapping RouterProvider
    - All ErrorBoundary tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ErrorBoundary → console | Caught error details logged to console — not displayed in UI |
| DataTable → API | Sort/filter params sent as URL query parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | ErrorBoundary | mitigate | componentDidCatch logs error details to console; fallback UI shows only generic messages. Never renders `this.state.error.message` or stack traces in the DOM. |
| T-10-02 | Tampering | DataTable sort params | mitigate | Column definitions in DataTable act as allowlist — only defined columns can be sorted. API layer should also validate sort column against permitted list. |
| T-10-SC | Tampering | npm install @tanstack/react-table | accept | [ASSUMED] per RESEARCH.md package legitimacy audit — well-known package, 4M weekly downloads, official source. |
</threat_model>

<verification>
1. `npm test -- --run` passes — all component tests green
2. `npm run build` succeeds — no TypeScript or import errors
3. Verify ErrorBoundary wraps `<main>` in Layout.tsx (grep pattern)
4. Verify `<Toaster position="top-center" duration={Infinity} />` exists in routes.tsx (grep pattern)
5. Verify PageShell uses `gap-4 lg:gap-8` (grep pattern)
6. Verify EmptyState exports 4 variants with correct icon names (grep lucide-react imports)
</verification>

<success_criteria>
- [ ] All 8 component files created + 7 test files created
- [ ] @tanstack/react-table installed and in package.json
- [ ] shadcn Pagination component installed
- [ ] npm test -- --run passes with all new tests
- [ ] npm run build succeeds (no TS errors)
- [ ] Layout.tsx wraps `<main>` with ErrorBoundary
- [ ] Layout.tsx includes `pb-16 lg:pb-6` on `<main>`
- [ ] routes.tsx includes `<Toaster position="top-center" duration={Infinity} />`
- [ ] routes.tsx ErrorBoundary wrapper removed
- [ ] PageShell.component renders with title/description/actions/children interface
- [ ] EmptyState renders with variant prop (4 states)
- [ ] ErrorBoundary differentiates network vs render errors
- [ ] 3 skeleton components use shadcn Skeleton with pulse animation only (no shimmer)
</success_criteria>

## Artifacts this phase produces

### New Components
- `PageShell` — `src/components/PageShell.tsx` — Page wrapper with sticky title/description/actions header
- `EmptyState` — `src/components/EmptyState.tsx` — 4-variant empty state with Lucide icons + CTA
- `ErrorBoundary` — `src/components/ErrorBoundary.tsx` — Rewritten network-aware error boundary
- `TableSkeleton` — `src/components/skeletons/TableSkeleton.tsx` — Row-based loading skeleton
- `CardGridSkeleton` — `src/components/skeletons/CardGridSkeleton.tsx` — Card grid loading skeleton
- `FormSkeleton` — `src/components/skeletons/FormSkeleton.tsx` — Form layout loading skeleton
- `useMediaQuery` — `src/hooks/use-media-query.ts` — Responsive media query React hook

### Modified Files
- `Layout.tsx` — ErrorBoundary wraps `<main>`, pb-16 spacing added
- `routes.tsx` — Toaster wired in, ErrorBoundary wrapper removed

### Installed Packages
- `@tanstack/react-table` (npm)
- shadcn Pagination component (CLI)

### Tests
- `EmptyState.test.tsx` — 4 variant renders
- `PageShell.test.tsx` — title/desc/actions/children renders
- `TableSkeleton.test.tsx` — row count and structure
- `CardGridSkeleton.test.tsx` — card count and grid
- `FormSkeleton.test.tsx` — field count and layout
- `ErrorBoundary.test.tsx` — error catch, retry, network vs render
