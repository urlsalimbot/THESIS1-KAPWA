---
phase: 12-toolchain-cleanup-vitest-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - kapwa-client/vite.config.ts
  - kapwa-client/src/__tests__/ (new directory, 10 files)
  - kapwa-client/src/pages/*.test.tsx (15 files moved from __tests__/ subdir)
  - kapwa-client/src/pages/*.test.tsx (3 files moved from tests/pages/)
  - kapwa-client/src/lib/*.test.ts (3 files moved from tests/)
  - kapwa-client/src/components/PublicHeader.test.tsx (moved from tests/components/)
  - kapwa-client/src/components/PublicLayout.test.tsx (moved from tests/layouts/)
  - kapwa-client/src/pages/IntakePage.test.tsx (moved from tests/)
  - kapwa-client/tests/ (10 files removed, dir retained for setup.ts)
autonomous: true
requirements:
  - DEP-01
  - DEP-02
user_setup: []

must_haves:
  truths:
    - "All 196 tests pass under vitest v4 after co-location"
    - "`find kapwa-client/tests -name '*.test.*' -type f` returns 0 test files (only setup.ts remains)"
    - "`find kapwa-client/src/pages/__tests__ -type f` returns 0 files (subdir flattened)"
    - "`find kapwa-client/src/tests -name '*.test.*' -type f` returns 0 files (orphans moved to src/__tests__/)"
    - "New `kapwa-client/src/__tests__/` directory exists with 10 orphan test files"
    - "`find kapwa-client/src -name '*.test.*' -type f | wc -l` returns 46 (same count as before migration)"
    - "`npx vitest run --prefix kapwa-client` discovers tests via new `src/__tests__/**/*.test.{ts,tsx}` include pattern"
  artifacts:
    - path: "kapwa-client/src/__tests__/"
      provides: "New orphan test directory (Vitest/Next.js convention with double-underscore)"
      contains: "10 test files for cross-cutting concerns (e2e, a11y, pii, sla, search, bulk-actions, etc.)"
    - path: "kapwa-client/src/pages/*.test.tsx"
      provides: "18 co-located page tests (15 from __tests__/ subdir + 3 from tests/pages/)"
      min_lines: 10
    - path: "kapwa-client/src/lib/*.test.ts"
      provides: "3 co-located lib tests (offline-queue, secure-storage, sync-conflict)"
      min_lines: 10
    - path: "kapwa-client/vite.config.ts"
      provides: "Updated test.include pattern covering src/__tests__/"
      contains: "src/__tests__/\\*\\*/\\*\\.test"
  key_links:
    - from: "kapwa-client/vite.config.ts"
      to: "kapwa-client/src/__tests__/e2e.test.ts"
      via: "test.include pattern src/__tests__/**/*.test.{ts,tsx} discovers the moved orphan tests"
      pattern: "src/__tests__/\\*\\*/\\*\\.test"
    - from: "kapwa-client/src/__tests__/e2e.test.ts"
      to: "kapwa-client/src/lib/offline-queue"
      via: "vi.mock('../lib/offline-queue', ...) — relative depth reduced from ../src/lib to ../lib after move"
      pattern: "vi.mock\\('\\.\\./lib/"
    - from: "kapwa-client/src/pages/LoginPage.test.tsx"
      to: "kapwa-client/src/pages/LoginPage.tsx"
      via: "import { LoginPage } from './LoginPage' — same-directory relative import after co-location"
      pattern: "from '\\./[A-Z][a-zA-Z]+'"
---

<objective>
Co-locate all 31 existing test files alongside their source files per D-05, D-06 (or move orphans to `src/__tests__/` per D-07), and update vitest's include pattern per D-08 — completing the vitest v4 toolchain upgrade with the canonical v4 file layout.

Purpose: Plan 12-01 made vitest v4 work under the OLD layout. This plan migrates to vitest v4's recommended convention (co-located `*.test.tsx` next to source) so that future test additions don't require a separate test directory, the test count for any component is discoverable from the source file, and the codebase is ready for future phases (15: core module tests, 16/17: page smoke tests) to add tests at the same location as the code they test.

Output: A new `kapwa-client/src/__tests__/` directory containing 10 orphan tests, 23 test files moved to co-located positions in `src/pages/`, `src/lib/`, and `src/components/`, updated import paths in moved files (especially `tests/e2e.test.ts` whose relative `../src/lib/...` imports become `../lib/...`), and an updated `vite.config.ts` `include` pattern. All 196 tests still pass.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-CONTEXT.md
@.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@kapwa-client/vite.config.ts
@kapwa-client/tests/e2e.test.ts (for relative path update)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/__tests__/ and move 10 orphan tests (D-07)</name>
  <files>
    kapwa-client/src/__tests__/e2e.test.ts (moved from kapwa-client/tests/e2e.test.ts)
    kapwa-client/src/__tests__/beneficiaries-search.test.ts (moved from kapwa-client/tests/)
    kapwa-client/src/__tests__/family-graph.test.ts (moved from kapwa-client/tests/)
    kapwa-client/src/__tests__/consent-manager.test.ts (moved from kapwa-client/tests/)
    kapwa-client/src/__tests__/a11y/components.test.tsx (moved from kapwa-client/src/tests/a11y/)
    kapwa-client/src/__tests__/a11y/pages.test.ts (moved from kapwa-client/src/tests/a11y/)
    kapwa-client/src/__tests__/pii/masking.test.ts (moved from kapwa-client/src/tests/pii/)
    kapwa-client/src/__tests__/sla/timer.test.ts (moved from kapwa-client/src/tests/sla/)
    kapwa-client/src/__tests__/search/global.test.ts (moved from kapwa-client/src/tests/search/)
    kapwa-client/src/__tests__/bulk-actions/selection.test.tsx (moved from kapwa-client/src/tests/bulk-actions/)
  </files>
  <read_first>
    - kapwa-client/tests/e2e.test.ts (lines 1-30, the relative `../src/lib/` imports that need rewriting)
    - kapwa-client/src/tests/a11y/components.test.tsx (lines 1-20, the @/... alias import pattern)
    - .planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md (D-07 section, lines 196-249)
  </read_first>
  <action>
Create the new orphan directory and move 10 test files in 4 logical batches.

Batch A — Create the directory:
  mkdir -p kapwa-client/src/__tests__/a11y
  mkdir -p kapwa-client/src/__tests__/pii
  mkdir -p kapwa-client/src/__tests__/sla
  mkdir -p kapwa-client/src/__tests__/search
  mkdir -p kapwa-client/src/__tests__/bulk-actions
  (Note: double-underscore `__tests__` is the Vitest/Next.js convention, distinct from the existing `src/tests/` dir which uses single underscore. Per D-07.)

Batch B — Move 4 orphan tests from kapwa-client/tests/ (these have no 1:1 source file):
  git mv kapwa-client/tests/e2e.test.ts kapwa-client/src/__tests__/e2e.test.ts
  git mv kapwa-client/tests/beneficiaries-search.test.ts kapwa-client/src/__tests__/beneficiaries-search.test.ts
  git mv kapwa-client/tests/family-graph.test.ts kapwa-client/src/__tests__/family-graph.test.ts
  git mv kapwa-client/tests/consent-manager.test.ts kapwa-client/src/__tests__/consent-manager.test.ts

Batch C — Move 6 cross-cutting tests from kapwa-client/src/tests/ (the existing single-underscore orphan dir):
  git mv kapwa-client/src/tests/a11y/components.test.tsx kapwa-client/src/__tests__/a11y/components.test.tsx
  git mv kapwa-client/src/tests/a11y/pages.test.ts kapwa-client/src/__tests__/a11y/pages.test.ts
  git mv kapwa-client/src/tests/pii/masking.test.ts kapwa-client/src/__tests__/pii/masking.test.ts
  git mv kapwa-client/src/tests/sla/timer.test.ts kapwa-client/src/__tests__/sla/timer.test.ts
  git mv kapwa-client/src/tests/search/global.test.ts kapwa-client/src/__tests__/search/global.test.ts
  git mv kapwa-client/src/tests/bulk-actions/selection.test.tsx kapwa-client/src/__tests__/bulk-actions/selection.test.tsx

Batch D — Update the relative imports ONLY in the moved e2e.test.ts (the other moved files use @/... alias which still resolves to src/):

The file moved from kapwa-client/tests/e2e.test.ts → kapwa-client/src/__tests__/e2e.test.ts (depth changed by one level).
Every `../src/lib/X` import must become `../lib/X`:
  - vi.mock('../src/lib/offline-queue', ...) → vi.mock('../lib/offline-queue', ...)
  - vi.mock('../src/lib/sync', ...) → vi.mock('../lib/sync', ...)
  - vi.mock('../src/lib/auth', ...) → vi.mock('../lib/auth', ...)
  - const { initDatabase } = await import('../src/lib/database') → const { initDatabase } = await import('../lib/database')
  - All other `../src/lib/...` patterns similarly.

Use sed -i 's|\.\./src/lib/|../lib/|g' kapwa-client/src/__tests__/e2e.test.ts to apply globally. Then grep -nE "'\.\./src/" kapwa-client/src/__tests__/e2e.test.ts to confirm zero matches remain.

For the other 9 moved files, NO import rewrites are needed because they use the @/ alias or relative imports from within their own dir.

Do NOT delete kapwa-client/src/tests/ yet — that dir also contains the bulk-actions/ and other subdirs. Use rmdir to clean up empty subdirs at the end (after Batch C above). Remove kapwa-client/src/tests/ subdirs: a11y/, pii/, sla/, search/, bulk-actions/. Then if kapwa-client/src/tests/ is empty, rmdir it.

Also clean up now-empty parent subdirs of kapwa-client/tests/: the orphan tests from Batch B were the last `.test.ts` files in those subdirs — but components/ and layouts/ and pages/ subdirs still have files moved in Task 2, so leave them for now.
  </action>
  <verify>
    <automated>ls -la kapwa-client/src/__tests__/ && echo "---ORPHAN COUNT---" && find kapwa-client/src/__tests__ -name "*.test.*" -type f | wc -l && echo "---e2e.test.ts IMPORTS---" && grep -nE "'\.\./src/" kapwa-client/src/__tests__/e2e.test.ts || echo "NO_OLD_PATHS_REMAIN" && echo "---src/tests/ CLEANUP---" && ls kapwa-client/src/tests/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - kapwa-client/src/__tests__/ exists with 4 subdirs: a11y/, pii/, sla/, search/, bulk-actions/
    - kapwa-client/src/__tests__/ contains exactly 10 .test files: e2e, beneficiaries-search, family-graph, consent-manager (4 at top level) + components.test.tsx, pages.test.ts under a11y/ + masking.test.ts under pii/ + timer.test.ts under sla/ + global.test.ts under search/ + selection.test.tsx under bulk-actions/
    - kapwa-client/src/__tests__/e2e.test.ts has zero `../src/...` imports remaining (all rewritten to `../lib/...`)
    - kapwa-client/src/tests/ directory is removed OR empty
    - kapwa-client/src/tests/a11y/, pii/, sla/, search/, bulk-actions/ are all removed
    - Source files at the 4 new top-level orphans (e2e, beneficiaries-search, family-graph, consent-manager) are NOT present in src/ — these are intentionally orphan per D-07
  </acceptance_criteria>
  <done>
    10 orphan test files moved to src/__tests__/, e2e.test.ts imports rewritten for new relative depth, empty src/tests/ subdirs cleaned up
  </done>
</task>

<task type="auto">
  <name>Task 2: Co-locate 24 test files (15 page flatten + 9 from tests/) with import path rewrites (D-05, D-06)</name>
  <files>
15 files moved from kapwa-client/src/pages/__tests__/ to kapwa-client/src/pages/:
    AccessCardPage.test.tsx, AdminPage.test.tsx, ApprovalPipelinePage.test.tsx,
    BeneficiariesPage.test.tsx, BeneficiaryViewPage.test.tsx, CasesPage.test.tsx,
    CaseTrackerPage.test.tsx, ClaimantDashboardPage.test.tsx, CsrPage.test.tsx,
    DashboardPage.test.tsx, FilingPage.test.tsx, InterventionsPage.test.tsx,
    IrfPage.test.tsx, MessagesPage.test.tsx, MfaSetupPage.test.tsx
kapwa-client/src/components/PublicHeader.test.tsx (moved from kapwa-client/tests/components/PublicHeader.test.tsx)
    kapwa-client/src/components/PublicLayout.test.tsx (moved from kapwa-client/tests/layouts/PublicLayout.test.tsx — note dir rename)
    kapwa-client/src/pages/AboutPage.test.tsx (moved from kapwa-client/tests/pages/)
    kapwa-client/src/pages/LandingPage.test.tsx (moved from kapwa-client/tests/pages/)
    kapwa-client/src/pages/LoginPage.test.tsx (moved from kapwa-client/tests/pages/)
    kapwa-client/src/pages/RegisterPage.test.tsx (moved from kapwa-client/tests/pages/)
    kapwa-client/src/pages/IntakePage.test.tsx (moved from kapwa-client/tests/intake-page.test.ts)
    kapwa-client/src/lib/secure-storage.test.ts (moved from kapwa-client/tests/secure-storage.test.ts)
    kapwa-client/src/lib/sync-conflict.test.ts (moved from kapwa-client/tests/sync-conflict.test.ts)
  </files>
  <read_first>
    - kapwa-client/src/pages/__tests__/DashboardPage.test.tsx (lines 1-15: import path pattern)
    - kapwa-client/tests/intake-page.test.ts (lines 1-20: vi.mock('../src/lib/...', ...) pattern)
    - kapwa-client/tests/components/PublicHeader.test.tsx (lines 1-15)
    - .planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md (lines 269-289: co-location move table)
  </read_first>
  <action>
Move 24 test files to co-located positions in src/ via 4 batches. All file moves use `git mv` to preserve history.

Batch A — Flatten 15 src/pages/__tests__/*.test.tsx → src/pages/*.test.tsx (D-05):
  for f in AccessCardPage AdminPage ApprovalPipelinePage BeneficiariesPage BeneficiaryViewPage CasesPage CaseTrackerPage ClaimantDashboardPage CsrPage DashboardPage FilingPage InterventionsPage IrfPage MessagesPage MfaSetupPage; do
    git mv kapwa-client/src/pages/__tests__/${f}.test.tsx kapwa-client/src/pages/${f}.test.tsx
  done

After the move, the depth changes (from src/pages/__tests__/ to src/pages/). Update imports:
  cd kapwa-client/src/pages && for f in *.test.tsx; do
    # Same-page imports: '../Foo' → './Foo'  (one level up → same dir)
    sed -i "s|from '\.\./\([A-Z][a-zA-Z]*\)'|from './\1'|g" "$f"
    # Two-level mocks: '../../lib/X' → '../lib/X'; '../../hooks/X' → '../hooks/X'; '../../components/X' → '../components/X'
    sed -i "s|'\.\./\.\./\([^']*\)'|'\.\./\1'|g" "$f"
  done
  cd ../../..

Batch B — Move 4 page tests from kapwa-client/tests/pages/ → src/pages/:
  git mv kapwa-client/tests/pages/AboutPage.test.tsx kapwa-client/src/pages/AboutPage.test.tsx
  git mv kapwa-client/tests/pages/LandingPage.test.tsx kapwa-client/src/pages/LandingPage.test.tsx
  git mv kapwa-client/tests/pages/LoginPage.test.tsx kapwa-client/src/pages/LoginPage.test.tsx
  git mv kapwa-client/tests/pages/RegisterPage.test.tsx kapwa-client/src/pages/RegisterPage.test.tsx

Batch C — Move 3 lib tests from kapwa-client/tests/ → src/lib/:
  git mv kapwa-client/tests/secure-storage.test.ts kapwa-client/src/lib/secure-storage.test.ts
  git mv kapwa-client/tests/sync-conflict.test.ts kapwa-client/src/lib/sync-conflict.test.ts
  git mv kapwa-client/tests/offline-queue.test.ts kapwa-client/src/lib/offline-queue.test.ts
  (If offline-queue.test.ts collides with sync-conflict.test.ts content, disambiguate by renaming.)

Batch D — Move 1 intake test + 2 component tests:
  git mv kapwa-client/tests/intake-page.test.ts kapwa-client/src/pages/IntakePage.test.tsx
  git mv kapwa-client/tests/components/PublicHeader.test.tsx kapwa-client/src/components/PublicHeader.test.tsx
  git mv kapwa-client/tests/layouts/PublicLayout.test.tsx kapwa-client/src/components/PublicLayout.test.tsx
  (Note: tests/layouts/ dir is renamed to src/components/ since PublicLayout.tsx lives in components/.)

After Batches B/C/D, the files moved from kapwa-client/tests/ to kapwa-client/src/ need import rewrites:
  - Files using `vi.mock('../src/lib/X', ...)` must become `vi.mock('../lib/X', ...)` (one fewer `../src/` level)
  - Files using `import { X } from '../src/components/X'` must become `import { X } from '../components/X'`

Apply the global rewrite (works for all 9 files in Batches B/C/D):
  cd kapwa-client/src && grep -rl "['"]\.\./src/" --include="*.test.ts" --include="*.test.tsx" . | xargs sed -i "s|['"]\.\./src/\([^'"]*\)['"]|'.\/\1'|g; s|vi.mock(['"]\.\./src/\([^'"]*\)['"]|vi.mock('./\1'|g"
  # Verify: grep -rE "['"]\.\./src/" kapwa-client/src --include="*.test.*" should return 0 matches.

Clean up empty parent subdirs of kapwa-client/tests/:
  rmdir kapwa-client/tests/pages 2>/dev/null
  rmdir kapwa-client/tests/components 2>/dev/null
  rmdir kapwa-client/tests/layouts 2>/dev/null
  (kapwa-client/tests/ still contains setup.ts — do NOT rmdir it.)

DO NOT touch the 10 files already moved in Task 1 (they went to src/__tests__/).
  </action>
  <verify>
    <automated>find kapwa-client/tests -name "*.test.*" -type f | wc -l && echo "---SETUP_TS---" && ls kapwa-client/tests/setup.ts && echo "---CO-LOCATED---" && find kapwa-client/src -name "*.test.*" -type f | wc -l && echo "---NO STALE PATHS---" && grep -rE "['"]\.\./src/" kapwa-client/src --include="*.test.ts" --include="*.test.tsx" 2>/dev/null | head -3 || echo "NO_OLD_PATHS"
  </verify>
  <acceptance_criteria>
    - 24 test files moved to co-located positions: 15 pages (flattened from __tests__/), 4 pages (from tests/pages/), 3 lib (from tests/), 2 components (from tests/components/, tests/layouts/)
    - kapwa-client/tests/ now contains ONLY setup.ts (no .test files)
    - kapwa-client/src/pages/__tests__/ directory is removed (flattened)
    - All same-page imports use './X' (not '../X')
    - All mock paths use '../lib/...' or '../hooks/...' (not '../../lib/...' or '../src/lib/...')
    - Zero `'../src/` patterns remain in any *.test file under src/
    - PublicLayout.test.tsx is at src/components/ (renamed dir from src/layouts/)
  </acceptance_criteria>
  <done>
    24 test files co-located with their source files; import paths shortened by one level; empty parent subdirs in tests/ removed
  </done>
</task>

<task type="auto">
  <name>Task 3: Update vite.config.ts include pattern + verify all 196 tests pass + final cleanup (D-08)</name>
  <files>
    kapwa-client/vite.config.ts
    kapwa-client/package.json (script test:run still works, may need to add tests/**/*.test.ts if e2e.test.ts is no longer in tests/)
  </files>
  <read_first>
    - kapwa-client/vite.config.ts (lines 23-29, current test config block)
    - .planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md (D-08 section, lines 85-95)
  </read_first>
  <action>
Update vite.config.ts to recognize the new src/__tests__/ orphan directory and verify the entire suite still works.

The current include pattern (line 27):
  include: ['tests/**/*.test.{ts,tsx}', 'src/**/*.test.{ts,tsx}'],

After Task 1+2, the test files are distributed as:
  - src/**/*.test.{ts,tsx}  → covers co-located tests in src/components/, src/pages/, src/lib/ (matches 36 files)
  - src/__tests__/**/*.test.{ts,tsx}  → is a SUBSET of src/**, so already covered
  - tests/setup.ts  → not a test file (not matched by *.test.*), referenced via setupFiles instead

So the include pattern `['src/**/*.test.{ts,tsx}', 'tests/**/*.test.{ts,tsx}']` ALREADY covers everything after the move. The `tests/**/*.test.{ts,tsx}` glob now matches 0 files (no .test files in tests/), but the glob is harmless (matches nothing).

However, per D-08 ("Update vitest include patterns to `src/**/*.test.{ts,tsx}` for co-located + `tests/**/*.test.ts` for e2e"), we should explicitly clean this up. Since e2e.test.ts was moved to src/__tests__/ in Task 1, the `tests/**/*.test.ts` pattern is no longer needed. The simplified include should be:

  include: ['src/**/*.test.{ts,tsx}'],

Also update the exclude array (line 28). Current:
  exclude: ['tests/e2e.test.ts', 'src/tests/a11y/pages.test.ts'],

After Task 1, BOTH files are no longer at those paths (e2e.test.ts moved to src/__tests__/, a11y/pages.test.ts moved to src/__tests__/a11y/). The exclude patterns now match nothing. The migration is complete, so we can simplify:

  exclude: [],

(Note: in vitest v4, default exclude is just `**/node_modules/**` and `**/.git/**` — see migration guide "Simplified exclude". Since our test files are inside src/ and never in node_modules or .git, the empty exclude is safe and matches v4 defaults.)

Apply the edit to vite.config.ts:
  // Before (lines 23-29):
  //   test: {
  //     environment: 'jsdom',
  //     globals: true,
  //     setupFiles: ['./tests/setup.ts'],
  //     include: ['tests/**/*.test.{ts,tsx}', 'src/**/*.test.{ts,tsx}'],
  //     exclude: ['tests/e2e.test.ts', 'src/tests/a11y/pages.test.ts'],
  //   },
  // After:
  //   test: {
  //     environment: 'jsdom',
  //     globals: true,
  //     setupFiles: ['./tests/setup.ts'],
  //     include: ['src/**/*.test.{ts,tsx}'],
  //     exclude: [],
  //   },

Run the test suite to verify:
  cd kapwa-client && npx vitest run 2>&1 | tail -20

Expected output: "Test Files  46 passed (46)" and "Tests  196 passed (196)" — same test count as Plan 12-01 baseline, but now test files are in their new locations.

If any test fails, debug by:
  1. Checking the failing test's import paths (might have missed a `../src/` rewrite)
  2. Running just the failing test: npx vitest run path/to/test
  3. Verifying the test file's new location is actually where vitest is looking

Final cleanup:
  - Verify kapwa-client/tests/ contains ONLY setup.ts (and any .gitkeep etc.)
  - Verify kapwa-client/src/pages/__tests__/ does not exist
  - Verify kapwa-client/src/tests/ does not exist
  - Verify kapwa-client/src/__tests__/ contains 10 test files
  - Verify total .test file count = 46 (matches pre-migration)
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --reporter=default 2>&1 | tail -8 && echo "---DIR STRUCTURE---" && find kapwa-client/tests -name "*.test.*" -type f | wc -l && find kapwa-client/src/__tests__ -name "*.test.*" -type f | wc -l && find kapwa-client/src -name "*.test.*" -type f | wc -l && ls kapwa-client/src/pages/__tests__ 2>&1 | head -2 && ls kapwa-client/src/tests 2>&1 | head -2  </verify>
  <acceptance_criteria>
    - kapwa-client/vite.config.ts test block has `include: ['src/**/*.test.{ts,tsx}']` (single entry; `tests/` glob removed since e2e.test.ts is now in src/__tests__/)
    - kapwa-client/vite.config.ts test block has `exclude: []` (no stale exclusions)
    - kapwa-client/vite.config.ts `setupFiles: ['./tests/setup.ts']` is UNCHANGED (D-08 says keep setup.ts at tests/)
    - `npx vitest run` shows "Test Files 46 passed" and "Tests 196 passed" (same count as before migration)
    - find kapwa-client/tests -name "*.test.*" -type f returns 0
    - find kapwa-client/src/__tests__ -name "*.test.*" -type f returns 10
    - find kapwa-client/src -name "*.test.*" -type f returns 46
    - kapwa-client/src/pages/__tests__ does not exist
    - kapwa-client/src/tests does not exist (or is empty)
  </acceptance_criteria>
  <done>
    vite.config.ts updated per D-08, all 196 tests pass under vitest v4 with the new co-located layout, empty directories cleaned up
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file location → vitest discovery | vitest's `include` pattern is the only trust anchor; a missing or wrong glob = tests don't run |
| Relative imports in moved files → source code | If a test imports the wrong path, the test may silently pass with no assertions (false positive) |
| `tests/setup.ts` (preserved at original path) | Must remain referenced by vite.config.ts; if the path breaks, all 196 tests fail to start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | moved test files | mitigate | Each `git mv` preserves git history; the rewrite of `../src/lib/` → `../lib/` is mechanically verified by `grep -rE "'\.\./src/"` returning 0 matches. |
| T-12-06 | Information Disclosure | src/__tests__/e2e.test.ts | accept | This test makes HTTP calls to `http://localhost:3000/api` — local dev only, not production. Mocked fetch (via `vi.mock('../lib/...')`) is not actually executed; the test would fail without a backend, so it's already gated by environment. |
| T-12-07 | Denial of Service | vitest discovery | mitigate | `find` commands count test files; `npx vitest run` runs the full suite. If include pattern is wrong, vitest reports 0 tests, not a silent pass. |
| T-12-08 | Elevation of Privilege | (none applicable) | accept | This is a file-relocation refactor, not a security boundary change. No new trust boundaries are crossed. |
| T-12-SC | Tampering | (no new packages installed) | accept | No package.json changes in this plan. All test files move with git history; no fresh code is introduced. |

## Out of Scope for This Plan

- Adding new test cases (handled in Phase 15, 16, 17)
- Coverage threshold configuration (explicitly deferred per CONTEXT.md)
- Test file refactoring beyond import path rewrites (PATTERNS.md says: "preservation, not invention")
- Modifying vitest v4 APIs (already handled in Plan 12-01)
</threat_model>

<verification>
After this plan completes, the following must all be true:

1. `cd kapwa-client && npx vitest run` shows "Test Files 46 passed" and "Tests 196 passed"
2. `cd kapwa-client && npm run build` still exits 0 (verify no broken imports)
3. `find kapwa-client/tests -name "*.test.*" -type f` returns 0 files
4. `find kapwa-client/src/__tests__ -name "*.test.*" -type f` returns 10 files
5. `find kapwa-client/src -name "*.test.*" -type f` returns 46 files (same as pre-migration)
6. `ls kapwa-client/src/pages/__tests__` reports "No such file or directory"
7. `ls kapwa-client/src/tests` reports "No such file or directory" (or empty)
8. `kapwa-client/vite.config.ts` has `include: ['src/**/*.test.{ts,tsx}']` and `exclude: []`
9. `kapwa-client/tests/setup.ts` still exists and is referenced by `setupFiles`
10. `grep -rE "'\.\./src/" kapwa-client/src --include="*.test.*"` returns 0 matches

If items 1-2 fail, the plan has not met its success criteria. If items 3-10 fail, the file structure is incomplete but tests may still pass — fix the structure for cleanliness.
</verification>

<success_criteria>
This plan completes the file-layout portion of Phase 12 (D-05, D-06, D-07, D-08). Combined with Plan 12-01, all four ROADMAP.md success criteria are satisfied:

1. `npm ls --prod` clean — completed in Plan 12-01
2. `npx vitest run` passes 196 tests under vitest v4 — completed in Plan 12-01, re-verified here
3. `npm run build` no regressions — verified in Plan 12-01, re-verified here
4. Dev server starts without deprecation warnings — completed in Plan 12-01

Additional (plan-specific) success criteria:
- All 31+ test files are at their co-located or src/__tests__/ positions per D-05/D-06/D-07
- vitest `include` pattern is updated per D-08
- No empty/orphan directories remain in src/ or tests/
</success_criteria>

<output>
Create `.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New directory
- `kapwa-client/src/__tests__/` — new orphan test directory (Vitest/Next.js double-underscore convention, distinct from the existing single-underscore `src/tests/` which is removed)

### Subdirectories under `kapwa-client/src/__tests__/`
- `a11y/` (2 files), `pii/` (1 file), `sla/` (1 file), `search/` (1 file), `bulk-actions/` (1 file)

### New file paths (10 orphan test files at src/__tests__/)
- `kapwa-client/src/__tests__/e2e.test.ts` (moved from `kapwa-client/tests/e2e.test.ts`)
- `kapwa-client/src/__tests__/beneficiaries-search.test.ts`
- `kapwa-client/src/__tests__/family-graph.test.ts`
- `kapwa-client/src/__tests__/consent-manager.test.ts`
- `kapwa-client/src/__tests__/a11y/components.test.tsx`
- `kapwa-client/src/__tests__/a11y/pages.test.ts`
- `kapwa-client/src/__tests__/pii/masking.test.ts`
- `kapwa-client/src/__tests__/sla/timer.test.ts`
- `kapwa-client/src/__tests__/search/global.test.ts`
- `kapwa-client/src/__tests__/bulk-actions/selection.test.tsx`

### Co-located file paths (23 moved files — new test locations)
**Pages (4 new co-locations, plus 15 flattened from __tests__/ subdir):**
- `kapwa-client/src/pages/AboutPage.test.tsx` (moved from `tests/pages/`)
- `kapwa-client/src/pages/LandingPage.test.tsx` (moved from `tests/pages/`)
- `kapwa-client/src/pages/LoginPage.test.tsx` (moved from `tests/pages/`)
- `kapwa-client/src/pages/RegisterPage.test.tsx` (moved from `tests/pages/`)
- `kapwa-client/src/pages/IntakePage.test.tsx` (moved from `tests/intake-page.test.ts`)
- 15 files moved from `src/pages/__tests__/*.test.tsx` to `src/pages/*.test.tsx`: AccessCardPage, AdminPage, ApprovalPipelinePage, BeneficiariesPage, BeneficiaryViewPage, CasesPage, CaseTrackerPage, ClaimantDashboardPage, CsrPage, DashboardPage, FilingPage, InterventionsPage, IrfPage, MessagesPage, MfaSetupPage

**Components (2 new co-locations):**
- `kapwa-client/src/components/PublicHeader.test.tsx` (moved from `tests/components/`)
- `kapwa-client/src/components/PublicLayout.test.tsx` (moved from `tests/layouts/` — note: dir renamed)

**Lib (3 new co-locations):**
- `kapwa-client/src/lib/secure-storage.test.ts` (moved from `tests/secure-storage.test.ts`)
- `kapwa-client/src/lib/sync-conflict.test.ts` (moved from `tests/sync-conflict.test.ts`)
- `kapwa-client/src/lib/offline-queue.test.ts` (moved from `tests/offline-queue.test.ts` — verify no name collision with `sync-conflict.test.ts`)

### Modified files
- `kapwa-client/vite.config.ts` — `test.include` simplified to `['src/**/*.test.{ts,tsx}']`; `test.exclude` set to `[]`
- `kapwa-client/src/__tests__/e2e.test.ts` — all `../src/lib/...` relative imports rewritten to `../lib/...`
- All 23 moved test files — import paths adjusted for new relative depth (most use `@/...` alias which is unchanged)

### Removed directories
- `kapwa-client/src/tests/` (and all subdirs: a11y, pii, sla, search, bulk-actions)
- `kapwa-client/src/pages/__tests__/` (flattened into parent)
- `kapwa-client/tests/pages/`, `tests/components/`, `tests/layouts/` (now empty after moves)
- `kapwa-client/tests/` retained — still contains `setup.ts` (referenced by `vite.config.ts`)

### Git artifacts
- 1+ new commits on top of Plan 12-01 (one commit per logical batch, or one combined commit if the executor prefers)
- Commit message must reference D-05/D-06/D-07/D-08 for traceability
