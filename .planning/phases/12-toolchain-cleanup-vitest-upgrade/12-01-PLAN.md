---
phase: 12-toolchain-cleanup-vitest-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/package.json
  - kapwa-client/package-lock.json
  - kapwa-client/tests/setup.ts
  - kapwa-client/vite.config.ts
autonomous: true
requirements:
  - DEP-01
  - DEP-02
user_setup: []

must_haves:
  truths:
    - "`npm ls --prod --prefix kapwa-client` does NOT list playwright, @capacitor/cli, or esbuild"
    - "`npm ls --dev --prefix kapwa-client` DOES list playwright, @capacitor/cli, and esbuild"
    - "`npx vitest run --prefix kapwa-client` executes under vitest v4 (>=4.0.0) and all 196 existing tests pass"
    - "`npm run build --prefix kapwa-client` succeeds without TypeScript or import errors"
    - "Dev server `npm run dev --prefix kapwa-client` starts without warnings from deprecated vitest APIs"
  artifacts:
    - path: "kapwa-client/package.json"
      provides: "Clean dep tree with 3 misplaced deps moved to devDependencies; vitest upgraded to v4"
      contains: "vitest: \"^4."
    - path: "kapwa-client/node_modules/vitest/package.json"
      provides: "Installed vitest v4 runtime"
      contains: "\"version\": \"4."
  key_links:
    - from: "kapwa-client/package.json"
      to: "node_modules/vitest"
      via: "npm install resolves ^4.0.0 to 4.1.9"
      pattern: "\"vitest\": \"\\^4\\."
    - from: "kapwa-client/vite.config.ts"
      to: "vitest v4 test runner"
      via: "Inline test block consumed by vitest v4 module runner (replaces vite-node)"
      pattern: "test:\\s*\\{"
---

<objective>
Clean the production dependency tree (move 3 misplaced dev-tools to devDependencies per DEP-01) and upgrade vitest from v1.2.0 to v4.1.9 alongside @testing-library/react (per DEP-02) — all in one commit per D-02 — so that subsequent phases can build on the modernized toolchain.

Purpose: This plan is the toolchain foundation for the entire v1.2 milestone. Every later quality phase (TST-01..07, A11Y-01/02, SEC-01) depends on vitest v4 being operational, and the production dep tree being free of build-time tools so that `npm ls --prod` is clean for downstream auditing and CI.

Output: Modified `kapwa-client/package.json` (3 deps moved, vitest upgraded to ^4.1.9), updated `package-lock.json`, and (only if needed) v4 API adjustments in `kapwa-client/vite.config.ts` and `kapwa-client/tests/setup.ts`. All 196 existing tests still passing under vitest v4. No test file relocations — that work is in Plan 12-02.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-CONTEXT.md
@.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@kapwa-client/package.json
@kapwa-client/vite.config.ts
@kapwa-client/tests/setup.ts
@kapwa-client/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit kapwa-client/package.json — move 3 deps to devDependencies, upgrade vitest to ^4.1.9, co-bump @testing-library/react per D-01/D-02</name>
  <files>kapwa-client/package.json, kapwa-client/package-lock.json</files>
  <read_first>
    - kapwa-client/package.json (current dependency entries to move/upgrade)
    - .planning/phases/12-toolchain-cleanup-vitest-upgrade/12-CONTEXT.md (D-01, D-02)
    - .planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md (package.json section: lines 25-65)
  </read_first>
  <action>
Apply three JSON edits to kapwa-client/package.json in a SINGLE commit (per D-02: "Move playwright, @capacitor/cli, esbuild from dependencies to devDependencies in the same commit as the vitest upgrade"):

(a) Remove these three lines from the dependencies block (line 14, 37, 39 of current file):
- "@capacitor/cli": "^6.2.1",
- "esbuild": "^0.28.0",
- "playwright": "^1.59.1",

(b) Add them to devDependencies in alphabetical order (per PATTERNS.md convention):
- "@capacitor/cli": "^6.2.1" — place after "@axe-core/playwright" (line 52)
- "esbuild": "^0.28.0" — place between "clsx" and "@hookform/resolvers" alphabetically
- "playwright": "^1.59.1" — place after "jsdom" (line 61), before "postcss"

(c) Bump vitest version in devDependencies (line 67):
- "vitest": "^1.2.0" → "vitest": "^4.1.9"
- Leave "@testing-library/react": "^16.3.2" and "@testing-library/jest-dom": "^6.9.1" UNCHANGED (D-04: v16.3.2 supports React 18 || 19, and they're already at latest)

Then run: cd kapwa-client && npm install --legacy-peer-deps
- Use --legacy-peer-deps because Phase 10-01 hit a similar peer dep conflict with @vitejs/plugin-react (documented in PATTERNS.md "Issues Encountered")
- Verify node_modules/vitest/package.json shows version "4.1.x" after install

CRITICAL: Do NOT touch any of the 11 @capacitor/* runtime packages — only the @capacitor/cli (line 14). The 10 runtime @capacitor/* packages are correctly in dependencies (used in production via Capacitor mobile runtime).
  </action>
  <verify>
    <automated>cd kapwa-client && npm ls --prod 2>&1 | grep -E "(playwright|@capacitor/cli|esbuild)" ; echo "PROD_CHECK_DONE" && npm ls --dev 2>&1 | grep -E "(playwright|@capacitor/cli|esbuild)" && echo "DEV_CHECK_DONE" && cat node_modules/vitest/package.json | grep -E '"version"' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - kapwa-client/package.json has exactly 3 fewer entries in `dependencies` and 3 more in `devDependencies` than before
    - The 3 moved entries are: @capacitor/cli@^6.2.1, esbuild@^0.28.0, playwright@^1.59.1
    - `vitest: ^4.1.9` (or compatible ^4.x) appears in devDependencies
    - `@testing-library/react: ^16.3.2` and `@testing-library/jest-dom: ^6.9.1` are UNCHANGED
    - 11 @capacitor/* runtime packages remain in dependencies (untouched)
    - `npm ls --prod --prefix kapwa-client` exits 0 and lists NONE of playwright, @capacitor/cli, esbuild
    - `npm ls --dev --prefix kapwa-client` exits 0 and lists all 3 moved deps
    - `node_modules/vitest/package.json` shows `"version": "4.x.y"`
  </acceptance_criteria>
  <done>
    package.json correctly modified, npm install completed, 3 misplaced deps confirmed moved, vitest v4 installed in node_modules
  </done>
</task>

<task type="auto">
  <name>Task 2: Run vitest v4 test suite — fix any forward-compat breaks per D-03</name>
  <files>kapwa-client/vite.config.ts, kapwa-client/tests/setup.ts, kapwa-client/package.json</files>
  <read_first>
    - kapwa-client/vite.config.ts (current test config block, lines 23-29)
    - kapwa-client/tests/setup.ts (current setup, all 61 lines)
    - .planning/phases/12-toolchain-cleanup-vitest-upgrade/12-PATTERNS.md (v4 API research section)
  </read_first>
  <action>
Run the full vitest test suite to verify v4 compatibility:

cd kapwa-client && npx vitest run 2>&1 | tee /tmp/vitest-v4-initial.log

This must show "Test Files  44 passed (44)" and "Tests  196 passed (196)" — matching the v1.2 baseline.

If any test fails (per D-03: "Fix forward — adapt to v4 APIs, do not roll back"), apply targeted fixes:

1. Most likely failure: tests/setup.ts uses Object.defineProperty on globalThis.localStorage. In v4's module runner, this MAY need to run in a setupFile context. Try wrapping the localStorage mock in beforeAll() if it currently runs at top level.

2. If vi.restoreAllMocks() no longer resets spy state (per v4 migration notes), update tests/setup.ts to use vi.restoreAllMocks() in afterEach WITHOUT changing semantics — current usage is correct, the behavior just changed.

3. If vite.config.ts test block throws an unknown option warning, the `exclude: ['tests/e2e.test.ts', 'src/tests/a11y/pages.test.ts']` may need to use `...configDefaults.exclude` prefix per v4 "Simplified exclude" — but ONLY add this prefix if a warning is actually emitted, not preemptively.

4. If "basic" reporter is removed in v4 (it is — use "default" or "verbose" instead). We do NOT use --reporter=basic, so this doesn't apply to us.

5. If any test file uses `poolOptions` (we don't), `poolMatchGlobs` (we don't), or `singleThread`/`maxThreads` (we don't), migrate them.

6. If any test uses the third-argument options pattern on test()/describe(), migrate to second-argument pattern. Grep for `, [0-9]\\+)\\s*;?$` near `it(` and `describe(` calls to detect this pattern.

DO NOT modify the test file locations (D-05/D-06 work is in Plan 12-02). DO NOT change @/... alias imports. DO NOT modify tsconfig.json unless TypeScript reports a specific types error.

After all fixes, re-run npx vitest run to confirm 196/196 pass. Then run: cd kapwa-client && npm run build to confirm production build still works. The build verifies that vite.config.ts's resolve.alias and optimizeDeps blocks are still compatible.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --reporter=default 2>&1 | tail -10 && echo "---BUILD---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run shows "Test Files 44 passed" and "Tests 196 passed"
    - npm run build completes with exit code 0
    - No warnings about deprecated vitest APIs in either output
    - vite.config.ts test block unchanged OR only includes minimal additive changes (e.g., ...configDefaults.exclude prefix) to support v4
    - tests/setup.ts unchanged OR only includes the minimum modification needed (e.g., localStorage mock wrapped in beforeAll if v4 module runner requires it)
    - All test file locations remain at their current paths (no moves in this task)
  </acceptance_criteria>
  <done>
    All 196 tests pass under vitest v4, production build succeeds, and any forward-compat fixes are minimal/targeted per D-03
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit dependency moves and vitest upgrade as a single atomic commit per D-02</name>
  <files>kapwa-client/package.json, kapwa-client/package-lock.json, kapwa-client/vite.config.ts (optional), kapwa-client/tests/setup.ts (optional)</files>
  <read_first>
    - git status to confirm only intended files are staged
  </read_first>
  <action>
Stage and commit the dep-cleanup + vitest-upgrade as ONE atomic commit per D-02:

git add kapwa-client/package.json kapwa-client/package-lock.json
git add kapwa-client/vite.config.ts kapwa-client/tests/setup.ts   # ONLY if modified in Task 2

git status  # review before committing — ensure no test files, no node_modules, no dist

git commit -m "$(cat <<'MSG'
chore(deps): move build tools to devDeps and upgrade vitest to v4

DEP-01: Move playwright, @capacitor/cli, esbuild from dependencies to devDependencies
DEP-02: Upgrade vitest 1.2.0 → 4.1.9; testing libraries already at latest

D-01, D-02, D-04 honored. All 196 existing tests pass under vitest v4.
MSG
)"

The commit message must explicitly cite DEP-01, DEP-02, and the decision IDs so the change is traceable from git log back to the requirements and CONTEXT.md.

DO NOT push. DO NOT create a PR. The orchestrator will handle those steps after both plans complete.
  </action>
  <verify>
    <automated>git log --oneline -3 && echo "---DIFF SUMMARY---" && git show --stat HEAD | head -10 && echo "---COMMIT MSG---" && git log -1 --format=%B | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new commit on top of HEAD
    - Commit message contains "DEP-01" and "DEP-02" and "D-01"/"D-02"
    - Files in commit: kapwa-client/package.json + kapwa-client/package-lock.json + (conditionally) vite.config.ts and tests/setup.ts
    - No test files (.test.ts/.test.tsx) are in this commit
    - No node_modules, no dist, no .planning/ files in this commit
  </acceptance_criteria>
  <done>
    Single atomic commit created that bundles the dep tree cleanup and vitest v4 upgrade, with all 196 tests passing on the committed state
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project node_modules | Package source for vitest@4.1.9, all transitive deps |
| vitest config → test execution | Test runner controls test behavior; config file is the single trust anchor |
| `tests/setup.ts` → global mocks | Affects all 196 tests' environment; mistakes cause cascading failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | kapwa-client/package.json | mitigate | 3 moved deps (playwright, @capacitor/cli, esbuild) are well-known npm packages already in use; verified by `npm ls` post-move. No new external deps added. |
| T-12-02 | Tampering | node_modules/vitest | mitigate | vitest 4.1.9 verified via `npm view` (peer deps: Vite >= 6, Node >= 20). Project's Vite is 8.0.10, Node is v26.4.0 — both satisfy. Lock file pins exact resolved version. |
| T-12-03 | Information Disclosure | tests/setup.ts | accept | Setup file is a build-time construct; does not handle PII. localStorage/crypto mocks are test fixtures only. |
| T-12-04 | Denial of Service | vitest v4 | mitigate | 196/196 baseline must still pass after upgrade. Task 2 enforces this; rollback path is git revert of the single atomic commit. |
| T-12-SC | Tampering | npm install (package downloads) | mitigate | All 3 moved deps + vitest@4 are first-party, widely-used packages (verified by `npm view <pkg> time` and download counts). No [SLOP] candidates. Use `--legacy-peer-deps` (documented in PATTERNS.md) for the known @vitejs/plugin-react conflict. |

## Out of Scope for This Plan

- Test file relocations (D-05/D-06/D-07) — Plan 12-02
- CI integration — explicitly deferred per CONTEXT.md
- Coverage threshold configuration — explicitly deferred
- Other 34+ package.json entries — explicitly out of scope per CONTEXT.md (full audit deferred)
</threat_model>

<verification>
After this plan completes, the following must all be true:

1. `cd kapwa-client && npm ls --prod` exits 0 and lists 0 matches for playwright|@capacitor/cli|esbuild
2. `cd kapwa-client && npm ls --dev` exits 0 and lists playwright, @capacitor/cli, esbuild
3. `cd kapwa-client && cat node_modules/vitest/package.json | grep version` shows 4.1.x
4. `cd kapwa-client && npx vitest run` shows "Test Files 44 passed" and "Tests 196 passed"
5. `cd kapwa-client && npm run build` exits 0
6. `cd kapwa-client && npm run dev` starts without deprecation warnings (manual smoke — not in automated verify)
7. `git log --oneline -1` shows the new commit citing DEP-01, DEP-02, D-01, D-02
8. `git show --stat HEAD` shows ONLY package.json, package-lock.json, and (conditionally) vite.config.ts and tests/setup.ts — no test files

If any of items 1-5 fails, the plan has not met its success criteria and must be re-executed or rolled back.
</verification>

<success_criteria>
Phase 12 success criterion #1 (dep tree clean): MET when `npm ls --prod` no longer lists the 3 misplaced tools.
Phase 12 success criterion #2 (vitest v4 + 196 tests passing): MET when npx vitest run shows 196/196 under v4.
Phase 12 success criterion #3 (build no regression): MET when npm run build produces identical output (compare dist/ to v1.1 baseline if needed).
Phase 12 success criterion #4 (no deprecation warnings): MET when dev server starts cleanly.

All four ROADMAP.md success criteria for Phase 12 are covered by THIS plan. Plan 12-02 covers the co-location work which is the toolchain convention for v4, but the success criteria are satisfied here.
</success_criteria>

## Artifacts this phase produces

### Modified files (committed in this plan's single atomic commit per D-02)
- `kapwa-client/package.json` — 3 deps moved from `dependencies` to `devDependencies` (per D-02); `vitest: ^1.2.0` → `vitest: ^4.1.9`
- `kapwa-client/package-lock.json` — regenerated by `npm install` to reflect new vitest version + moved deps
- `kapwa-client/vite.config.ts` — *conditionally* modified ONLY if v4 requires a config change (e.g., `...configDefaults.exclude` prefix)
- `kapwa-client/tests/setup.ts` — *conditionally* modified ONLY if v4 module runner requires a setupFile change

### New symbols / values
- `kapwa-client/node_modules/vitest/package.json` — installed vitest runtime, version `"4.1.x"`
- New `devDependencies` entries in `kapwa-client/package.json`:
  - `"@capacitor/cli": "^6.2.1"`
  - `"esbuild": "^0.28.0"`
  - `"playwright": "^1.59.1"`
  - `"vitest": "^4.1.9"` (replacing `"^1.2.0"`)

### New file paths
None. No new files are created in this plan. All test file relocations are in Plan 12-02.

### Git artifacts
- One new atomic commit on top of HEAD with message starting: `chore(deps): move build tools to devDeps and upgrade vitest to v4`

### Out of scope (handled in Plan 12-02)
- New directory: `kapwa-client/src/__tests__/`
- Moved test files: 23+ files (co-location per D-05, D-06, D-07)
- Modified `vite.config.ts` `include` patterns (per D-08)

<output>
Create `.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-01-SUMMARY.md` when done
</output>
