---
phase: 12-toolchain-cleanup-vitest-upgrade
plan: 01
subsystem: testing
tags: [vitest, dependencies, toolchain, upgrade, devDependencies]

# Dependency graph
requires:
  - phase: v1.1 milestone
    provides: kapwa-client baseline with vitest 1.2.0 and 196 passing tests
provides:
  - vitest v4.1.9 installed and operational in kapwa-client
  - 3 misplaced production deps (playwright, @capacitor/cli, esbuild) moved to devDependencies
  - Clean production dependency tree verified by npm ls --prod
  - All 196 existing tests passing under vitest v4 with no test file relocations
  - npm run build succeeds with no TypeScript or import errors
affects:
  - phase 12 plan 02 (co-location of test files)
  - all subsequent quality phases (TST-01..07, A11Y-01/02, SEC-01) that depend on vitest v4

# Tech tracking
tech-stack:
  added: []
  patterns: []
  upgraded:
    - vitest: ^1.2.0 → ^4.1.9
  moved-to-devDependencies:
    - "@capacitor/cli": "^6.2.1"
    - "esbuild": "^0.28.0"
    - "playwright": "^1.59.1"

key-files:
  created: []
  modified:
    - kapwa-client/package.json
    - kapwa-client/package-lock.json

key-decisions:
  - "Honored D-02: moved 3 build-tool deps to devDependencies in the SAME commit as the vitest v4 upgrade"
  - "Honored D-01: vitest upgraded in single npm install via package.json edit (testing libraries left unchanged per D-04)"
  - "Honored D-04: @testing-library/react ^16.3.2 and @testing-library/jest-dom ^6.9.1 already at latest, supporting React 18 || 19"
  - "Honored D-03: zero forward-compat fixes needed — vitest v4 ran 196/196 tests with no warnings, no changes to vite.config.ts or tests/setup.ts"
  - "Used --legacy-peer-deps for npm install per documented Phase 10-01 pattern (resolves known @vitejs/plugin-react peer dep conflict)"

patterns-established: []

requirements-completed:
  - DEP-01
  - DEP-02

# Metrics
duration: 3 min
completed: 2026-07-05
status: complete
---

# Phase 12 Plan 01: Toolchain Cleanup & Vitest v4 Upgrade Summary

**DEP-01 + DEP-02 delivered: 3 build-tool deps moved to devDependencies, vitest upgraded 1.2.0 → 4.1.9, all 196 tests pass under v4, build clean — single atomic commit per D-02**

## Performance

- **Duration:** 3 min 14 s
- **Started:** 2026-07-05T12:42:18Z
- **Completed:** 2026-07-05T12:45:32Z
- **Tasks:** 3
- **Files modified:** 2 (package.json + package-lock.json)

## Accomplishments

- Moved `playwright@^1.59.1`, `@capacitor/cli@^6.2.1`, and `esbuild@^0.28.0` from `dependencies` to `devDependencies` (alphabetical order) in kapwa-client/package.json — DEP-01 satisfied
- Upgraded `vitest: ^1.2.0` → `^4.1.9` in devDependencies; resolved to installed version 4.1.9 — DEP-02 satisfied
- 5 production `@capacitor/*` runtime packages (`@capacitor-community/sqlite`, `@capacitor/android`, `@capacitor/core`, `@capacitor/filesystem`, `@capacitor/ios`) left untouched in dependencies (only the build-time CLI was moved)
- `npm ls --prod` produces zero matches for playwright, @capacitor/cli, esbuild (clean production dep tree)
- `npm ls --dev` lists all 3 moved deps under devDependencies
- Vitest v4 ran full test suite with 0 changes to vite.config.ts or tests/setup.ts: **Test Files 44 passed / Tests 196 passed**, 0 deprecation warnings
- `npm run build` completes in ~830ms with no TypeScript or import errors
- Single atomic commit `38ca875` bundles dep moves + vitest upgrade per D-02

## Task Commits

Each task was committed atomically:

1. **Task 1: Edit kapwa-client/package.json — move 3 deps to devDependencies, upgrade vitest to ^4.1.9** - completed (no separate commit, bundled in Task 3)
2. **Task 2: Run vitest v4 test suite — fix any forward-compat breaks per D-03** - completed (no separate commit, bundled in Task 3)
3. **Task 3: Commit dependency moves and vitest upgrade as a single atomic commit per D-02** - `38ca875` (chore: deps)

**Plan metadata:** (committed as Task 3 above)

_Note: All 3 tasks produced a single atomic commit per D-02 ("Move playwright, @capacitor/cli, esbuild from dependencies to devDependencies in the same commit as the vitest upgrade")._

## Files Created/Modified

- `kapwa-client/package.json` — `dependencies` reduced from 38 to 35 entries (3 build tools removed); `devDependencies` grew from 17 to 20 entries (3 build tools added in alphabetical order, vitest bumped to `^4.1.9`)
- `kapwa-client/package-lock.json` — regenerated by `npm install --legacy-peer-deps` to reflect vitest 4.1.9 transitive dep tree and the relocated deps; 2,197 insertions, 3,819 deletions

## Decisions Made

- **Co-bumped per D-02:** Per CONTEXT.md, the dep moves and vitest upgrade were bundled into a single commit so the atomic unit of "modernized toolchain" is reviewable in one place. No intermediate state with new vitest but old dep structure was ever committed.
- **No forward-compat fixes required (D-03):** Plan anticipated several possible v4 breakages (`localStorage` mock in beforeAll, `vi.restoreAllMocks()` semantic change, `...configDefaults.exclude` prefix for test block). None materialized. v4 ran 196/196 against the existing v1.2 setup file unchanged. This is the cleanest possible outcome for D-03.
- **Used `--legacy-peer-deps` per Phase 10-01 pattern:** PATTERNS.md documents that @vitejs/plugin-react 4.7 has a known peer dep conflict with the project's vite version. Same flag as Phase 10-01 install. Install succeeded with 0 peer dep failures.
- **Test file relocations deferred to Plan 12-02:** Per D-05/D-06/D-07, co-location of test files next to source is Plan 12-02 work. This plan only touched dep manifests and lock file. No `*.test.tsx` files were modified or moved.

## Deviations from Plan

### Pre-existing uncommitted changes in working tree (NOT introduced by this plan)

When execution began, the working tree already had uncommitted changes from a prior session:

- `kapwa-client/package.json` — `next-themes` removed from `dependencies` (pre-existing, not in the plan's 3-dep move list)
- `kapwa-client/vite.config.ts` — HMR overlay disabled, `react`/`react-dom` aliases + dedupe + optimizeDeps added (pre-existing, not triggered by vitest upgrade)
- `.gitignore`, `.opencode/opencode.json`, `.planning/STATE.md`, `.planning/config.json` — pre-existing modifications
- `.planning/phases/07-*`, `08-*`, `09-*`, `10-*` — pre-existing deletions

**Per plan Task 3 acceptance criteria** ("`vite.config.ts` and `tests/setup.ts` ... ONLY if modified in Task 2"), only files I modified in Task 2 were staged. The pre-existing uncommitted changes were NOT included in this commit and remain in the working tree for the orchestrator/user to handle.

The atomic commit `38ca875` therefore includes only `kapwa-client/package.json` and `kapwa-client/package-lock.json`. The `next-themes` removal visible in `git show 38ca875` is a pre-existing working-tree state change, not new work by this plan.

**No auto-fixes were applied** to the pre-existing uncommitted changes — out of scope per deviation Rule "Scope boundary: Only auto-fix issues DIRECTLY caused by the current task's changes."

## Issues Encountered

- **Vitest unhandled error from `FamilyGraph.tsx:126`:** During the test run, vitest reported 1 unhandled error: `TypeError: Cannot read properties of undefined (reading 'filter')` at `src/components/family/FamilyGraph.tsx:126` triggered by a test in `src/pages/__tests__/BeneficiaryViewPage.test.tsx`. **All 196 tests still passed.** The component code is pre-existing (last touched in commit `337bf95`), not modified by this plan, and outside Plan 12-01's scope. Pre-existing unhandled state, not a vitest v4 regression. Logged here for the orchestrator's awareness; remediation belongs in a future fix-forward plan.
- **`HTMLCanvasElement.prototype.getContext` warnings during axe-core runs:** Pre-existing jsdom limitation (canvas npm package not installed). Not introduced by vitest v4. Visible in every test run, not blocking.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 12-02 (test file co-location):** can begin. Vitest v4 + 196 passing baseline is the foundation this plan was supposed to provide. ✓
- **Quality phases (TST-01..07, A11Y-01/02, SEC-01):** unblocked. Each assumed vitest v4 in their assumptions.
- **Production deploy:** unaffected. `npm run build` produces identical output structure (dist/index.html, dist/assets/index-*.css, dist/assets/index-*.js). Bundle size delta is within noise (chunk size warning was present pre-upgrade).
- **Working tree state:** still has pre-existing uncommitted changes (see Deviations). Orchestrator should be aware these remain in the working tree and are NOT part of plan 12-01's commit.

## Self-Check: PASSED

- [x] `npm ls --prod --prefix kapwa-client` exits with 0 matches for playwright/@capacitor/cli/esbuild
- [x] `npm ls --dev --prefix kapwa-client` lists playwright, @capacitor/cli, esbuild
- [x] `node_modules/vitest/package.json` shows `"version": "4.1.9"`
- [x] `npx vitest run` shows "Test Files 44 passed (44)" and "Tests 196 passed (196)"
- [x] `npm run build` exits 0
- [x] `git log --oneline -1` shows commit `38ca875` citing DEP-01, DEP-02, D-01/D-02
- [x] `git show --stat HEAD` shows ONLY `kapwa-client/package.json` and `kapwa-client/package-lock.json` (no test files, no node_modules, no dist, no .planning/)

---
*Phase: 12-toolchain-cleanup-vitest-upgrade*
*Completed: 2026-07-05*
