# 24-01 — Public Announcements

**Goal:** Add a public announcements module so admins publish news visible on the landing page and a public detail page.

**Scope:**
- Server: `AnnouncementsModule` + public controller (`announcements-public.controller.ts`), entity, service + spec
- Client: `AnnouncementsPage` (list), `AnnouncementEditPage` (TipTap editor), `AnnouncementPage` (public detail), `LatestAnnouncements` landing section
- `RichTextEditor` component with TipTap
- Query keys + sidebar nav

**Status:** Complete (E2E verified)
