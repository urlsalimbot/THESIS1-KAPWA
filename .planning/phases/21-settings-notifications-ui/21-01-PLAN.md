# 21-01 — Settings, Notifications & UI Consistency

**Goal:** Centralize user preferences in a unified Settings page and harden notification consent.

**Scope:**
- Unified `SettingsPage` with Profile, Security, Notifications tabs
- `NotificationPreference` entity + `consentSkipped` field + migration
- Bulk notification preferences endpoint (`549f8f6`)
- Notification preferences query key (`c3d636a`)
- Route, nav, and topbar link wiring (`793ff93`)
- SettingsPage tests (`149f18b`)

**Status:** Complete
