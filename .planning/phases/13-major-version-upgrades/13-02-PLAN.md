---
phase: 13-major-version-upgrades
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - kapwa-client/src/components/ErrorBoundary.tsx
  - kapwa-client/src/components/ErrorBoundary.test.tsx
  - kapwa-client/src/pages/DashboardPage.test.tsx
  - kapwa-client/src/pages/CasesPage.test.tsx
  - kapwa-client/src/pages/BeneficiariesPage.test.tsx
  - kapwa-client/src/pages/__snapshots__/DashboardPage.test.tsx.snap (auto-generated)
  - kapwa-client/src/pages/__snapshots__/CasesPage.test.tsx.snap (auto-generated)
  - kapwa-client/src/pages/__snapshots__/BeneficiariesPage.test.tsx.snap (auto-generated)
autonomous: true
requirements:
  - UPG-01
  - UPG-02
  - UPG-03
user_setup: []

must_haves:
  truths:
    - "`grep -c 'class ErrorBoundary' kapwa-client/src/components/ErrorBoundary.tsx` returns 0 (the only class component in the client is migrated to a function component per D-06)"
    - "`grep -c 'WifiOff\\|navigator.onLine' kapwa-client/src/components/ErrorBoundary.tsx` returns 0 (offline-detection branch dropped per D-07; SWR-style fetch hooks own offline handling in Phase 15)"
    - "`npx vitest run --prefix kapwa-client` passes all tests after the ErrorBoundary rewrite — 196 - 2 (offline tests deleted) + 4 (new API tests) + 3 (page snapshots) = 201 unit tests"
    - "`expect(container).toMatchSnapshot()` runs on DashboardPage, CasesPage, and BeneficiariesPage and produces 3 new `.snap` files in `src/pages/__snapshots__/` (per D-12/D-13/D-14)"
    - "`git log -1 --format=%s` shows a single commit message containing all the UPG-01/02/03 requirement IDs and the D-XX decision IDs — one atomic commit per D-01"
    - "`npm run build --prefix kapwa-client` exits 0 (final regression check after all phase changes)"
  artifacts:
    - path: "kapwa-client/src/components/ErrorBoundary.tsx"
      provides: "Function-component wrapper around react-error-boundary — replaces the class component (D-06)"
      contains: "from 'react-error-boundary'"
    - path: "kapwa-client/src/components/ErrorBoundary.test.tsx"
      provides: "Rewritten test suite for the new ErrorBoundary API — 2 offline tests deleted (D-07), 4 new tests for the react-error-boundary API"
      contains: "react-error-boundary"
    - path: "kapwa-client/src/pages/DashboardPage.test.tsx"
      provides: "Snapshot test added per D-12"
      contains: "toMatchSnapshot"
    - path: "kapwa-client/src/pages/CasesPage.test.tsx"
      provides: "Snapshot test added per D-13"
      contains: "toMatchSnapshot"
    - path: "kapwa-client/src/pages/BeneficiariesPage.test.tsx"
      provides: "Snapshot test added per D-14"
      contains: "toMatchSnapshot"
    - path: "kapwa-client/src/pages/__snapshots__/DashboardPage.test.tsx.snap"
      provides: "Auto-generated Vitest snapshot of DashboardPage rendered DOM"
    - path: "kapwa-client/src/pages/__snapshots__/CasesPage.test.tsx.snap"
      provides: "Auto-generated Vitest snapshot of CasesPage rendered DOM"
    - path: "kapwa-client/src/pages/__snapshots__/BeneficiariesPage.test.tsx.snap"
      provides: "Auto-generated Vitest snapshot of BeneficiariesPage rendered DOM"
  key_links:
    - from: "kapwa-client/src/components/ErrorBoundary.tsx"
      to: "react-error-boundary library"
      via: "import { ErrorBoundary as ReactErrorBoundary, type FallbackProps } from 'react-error-boundary' — installed in Plan 13-01 Task 1"
      pattern: "from ['\"]react-error-boundary['\"]"
    - from: "kapwa-client/src/components/ErrorBoundary.tsx ErrorFallback component"
      to: "Try Again button"
      via: "onClick={resetErrorBoundary} — react-error-boundary provides the reset callback (D-08)"
      pattern: "onClick=\\{resetErrorBoundary\\}"
    - from: "kapwa-client/src/components/ErrorBoundary.tsx"
      to: "src/components/a11y/AriaLiveRegion"
      via: "import + JSX wrap of h2 heading for screen reader announcement (preserved from original class component)"
      pattern: "from ['\"]@/components/a11y/AriaLiveRegion['\"]"
    - from: "kapwa-client/src/pages/DashboardPage.test.tsx"
      to: "src/pages/__snapshots__/DashboardPage.test.tsx.snap"
      via: "expect(container).toMatchSnapshot() — vitest auto-saves the .snap file on first run"
      pattern: "toMatchSnapshot"
    - from: "git log (single atomic commit per D-01)"
      to: "all phase changes (package.json, vite.config.ts, capacitor.config.ts, src/index.css, postcss.config.mjs deleted, tailwind.config.js deleted, ErrorBoundary.tsx, ErrorBoundary.test.tsx, 3 page test files, 3 snap files)"
      via: "single git commit at end of Task 3"
      pattern: "UPG-0[123]"
---

<objective>
Complete the atomic Phase 13 commit by (1) migrating the only class component (`ErrorBoundary.tsx`) to a thin function-component wrapper around `react-error-boundary` per D-06, dropping the custom offline-detection branch per D-07, (2) rewriting the ErrorBoundary test suite to match the new API per A3, and (3) adding 3 page-level snapshot tests to DashboardPage, CasesPage, and BeneficiariesPage per D-12/D-13/D-14 — then land all of Plan 13-01's working tree changes + this plan's changes as ONE atomic commit per D-01.

Purpose: Plan 13-01 left the working tree in a state where React 19 + Capacitor 8 + Tailwind v4 + TypeScript 5.7 are installed and the build/tests pass (with the class ErrorBoundary still in place). This plan completes the component-level migration and lands the single atomic commit. After this plan, the codebase is on React 19 / Tailwind v4 / Capacitor 8 / TypeScript 5.7 with no class components and a tested snapshot baseline for the 3 most-trafficked pages.

Output: 3 modified TypeScript files (ErrorBoundary.tsx function component, ErrorBoundary.test.tsx with rewritten tests, 3 page test files with new snapshot `it()` blocks), 3 auto-generated `.snap` files in `__snapshots__/`, and one atomic git commit containing all phase changes (Plan 13-01's working tree + this plan's changes).
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-major-version-upgrades/13-CONTEXT.md
@.planning/phases/13-major-version-upgrades/13-RESEARCH.md
@.planning/phases/13-major-version-upgrades/13-PATTERNS.md
@.planning/phases/13-major-version-upgrades/13-VALIDATION.md
@.planning/phases/12-toolchain-cleanup-vitest-upgrade/12-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@kapwa-client/src/components/ErrorBoundary.tsx
@kapwa-client/src/components/ErrorBoundary.test.tsx
@kapwa-client/src/components/a11y/AriaLiveRegion.tsx (target import for AriaLiveRegion)
@kapwa-client/src/pages/DashboardPage.test.tsx
@kapwa-client/src/pages/CasesPage.test.tsx
@kapwa-client/src/pages/BeneficiariesPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ErrorBoundary.tsx as a function-component wrapper around react-error-boundary</name>
  <files>kapwa-client/src/components/ErrorBoundary.tsx</files>
  <read_first>
    - kapwa-client/src/components/ErrorBoundary.tsx (current 83-line class component to be replaced)
    - .planning/phases/13-major-version-upgrades/13-CONTEXT.md (D-06, D-07, D-08)
    - .planning/phases/13-major-version-upgrades/13-RESEARCH.md (Pattern 1 lines 228-299, Code Example 2 lines 549-571)
    - .planning/phases/13-major-version-upgrades/13-PATTERNS.md (ErrorBoundary.tsx section lines 26-104)
  </read_first>
  <action>
Replace the class component in kapwa-client/src/components/ErrorBoundary.tsx with a thin function-component wrapper around the `<ErrorBoundary>` component from `react-error-boundary` per D-06, D-07, D-08:

(a) The new file structure:
  1. Imports (in this order — matches CONVENTIONS.md external → project-specific):
     - `import type { ReactNode } from 'react';` (type-only import — replaces the `import React from 'react'` default import)
     - `import { ErrorBoundary as ReactErrorBoundary, type FallbackProps } from 'react-error-boundary';` (installed in Plan 13-01 Task 1 as ^6.1.2)
     - `import { TriangleAlert } from 'lucide-react';` (DROP `WifiOff` import per D-07 — offline detection removed)
     - `import { Button } from '@/components/ui/button';`
     - `import { Link } from 'react-router-dom';`
     - `import { AriaLiveRegion } from '@/components/a11y/AriaLiveRegion';`
  2. A new `ErrorFallback` function component (defined in the same file) that takes `FallbackProps` from react-error-boundary and renders the rich "Something went wrong" UI. The `TriangleAlert` icon, the `<AriaLiveRegion role="alert" aria-live="assertive" message="Something went wrong">` wrapper, the h2 heading, the muted description paragraph, and the two action buttons (Try Again + Go to Dashboard) are preserved verbatim from the class component's render method (lines 59-79 of the original).
  3. A new `ErrorBoundary` function component (exported, same name as the class it replaces — preserves the public API for `<Layout>` and other consumers). It takes `{ children: ReactNode }`, wraps them in `<ReactErrorBoundary FallbackComponent={ErrorFallback} onError={(error, info) => console.error('ErrorBoundary caught:', error, info)}>{children}</ReactErrorBoundary>`, and returns the result.
  4. The `fallback` prop support (lines 8-9 of the original) is preserved as an optional `fallback?: ReactNode` prop on the new ErrorBoundary, passed to ReactErrorBoundary via the `fallback` prop (NOT `fallbackRender` — react-error-boundary's `fallback` prop accepts a ReactNode, which is what the original API contract expected). HOWEVER, the original's `fallback` prop was a UI-only override (no error/resetErrorBoundary access), and the simpler API is to drop it for now — the codebase has no current consumer of this prop (verify with `grep -rn '<ErrorBoundary' kapwa-client/src --include='*.tsx' | grep fallback`). If no consumer uses the prop, REMOVE it to keep the API minimal. If a consumer exists, KEEP it and pass through to ReactErrorBoundary's `fallback` prop.

(b) Drop these elements from the original (per D-07):
  - The `WifiOff` import (line 2 of original)
  - The `isNetworkError` detection logic (lines 35-38 of original) — `navigator.onLine` check, `error.message.includes('fetch')`, `error.name === 'TypeError'`
  - The entire "You are offline" branch of the render (lines 40-57 of original)
  - The `Retry` button (offline variant of Try Again) — replaced by the unified Try Again button

(c) Per D-08, the "Try Again" button is wired to `resetErrorBoundary` (the callback that react-error-boundary provides via `FallbackProps`). The `resetErrorBoundary` callback resets the boundary's internal state without needing a `resetKeys` array. This satisfies D-08's "timestamp key bump" semantically — the boundary is reset on click.

(d) The `onError` callback (replaces the class component's `componentDidCatch`) takes `(error, info) => console.error('ErrorBoundary caught:', error, info)`. This matches the original logging behavior — react-error-boundary does not call `console.error` automatically; without the explicit `onError` handler, caught errors would only be visible via the `FallbackComponent`'s `error` prop.

(e) The file should end up around 35-45 lines (down from 83). Verify the line count after the rewrite.

After the rewrite, run a quick syntax/type check (this catches typos without running the full test suite):
  cd kapwa-client && npx tsc --noEmit -p . 2>&1 | tee /tmp/tsc-13-errorboundary.log
  echo "EXIT: $?"

This should exit 0 — react-error-boundary's TypeScript types must be compatible with React 19's types. If TSC reports errors, the most likely cause is the FallbackProps type (verify it imports from 'react-error-boundary', not a sub-path). Fix any reported type errors before moving to Task 2.
  </action>
  <verify>
    <automated>cd kapwa-client && echo "---class ErrorBoundary removed:---" && (grep -c 'class ErrorBoundary' src/components/ErrorBoundary.tsx || true) && echo "---WifiOff removed:---" && (grep -c 'WifiOff' src/components/ErrorBoundary.tsx || true) && echo "---navigator.onLine removed:---" && (grep -c 'navigator.onLine' src/components/ErrorBoundary.tsx || true) && echo "---react-error-boundary imported:---" && grep -c "from 'react-error-boundary'" src/components/ErrorBoundary.tsx && echo "---ErrorFallback function:---" && grep -c 'function ErrorFallback' src/components/ErrorBoundary.tsx && echo "---export function ErrorBoundary:---" && grep -c 'export function ErrorBoundary' src/components/ErrorBoundary.tsx && echo "---resetErrorBoundary wired:---" && grep -c 'resetErrorBoundary' src/components/ErrorBoundary.tsx && echo "---TriangleAlert preserved:---" && grep -c 'TriangleAlert' src/components/ErrorBoundary.tsx && echo "---AriaLiveRegion preserved:---" && grep -c 'AriaLiveRegion' src/components/ErrorBoundary.tsx && echo "---WC:---" && wc -l src/components/ErrorBoundary.tsx
  </verify>
  <acceptance_criteria>
    - `kapwa-client/src/components/ErrorBoundary.tsx` does NOT contain `class ErrorBoundary` (the class is gone)
    - `kapwa-client/src/components/ErrorBoundary.tsx` does NOT contain `WifiOff` import or usage (D-07)
    - `kapwa-client/src/components/ErrorBoundary.tsx` does NOT contain `navigator.onLine` reference (D-07)
    - `kapwa-client/src/components/ErrorBoundary.tsx` imports `{ ErrorBoundary as ReactErrorBoundary, type FallbackProps }` from `react-error-boundary`
    - `kapwa-client/src/components/ErrorBoundary.tsx` defines a `function ErrorFallback({ resetErrorBoundary }: FallbackProps)` component
    - `kapwa-client/src/components/ErrorBoundary.tsx` exports `function ErrorBoundary({ children }: { children: ReactNode })` (same name, function not class — public API preserved)
    - The Try Again button uses `onClick={resetErrorBoundary}` (D-08)
    - The TriangleAlert icon, AriaLiveRegion wrapper, h2 heading, and "Go to Dashboard" Link are preserved from the original (only the offline branch is removed)
    - The `onError` prop on `<ReactErrorBoundary>` calls `console.error('ErrorBoundary caught:', error, info)` (preserves the class component's `componentDidCatch` logging)
    - File is 35-50 lines (down from 83)
    - `npx tsc --noEmit -p .` exits 0 (no type errors from the migration)
  </acceptance_criteria>
  <done>
    ErrorBoundary.tsx is a thin function-component wrapper around `<ErrorBoundary>` from `react-error-boundary`. The class is gone, the offline-detection branch is removed, the `Try Again` button uses `resetErrorBoundary`, and the rich fallback UI (TriangleAlert + AriaLiveRegion + Try Again + Go to Dashboard) is preserved. TypeScript compiles without errors.
  </done>
</task>

<!-- gsd:write-continue -->

<task type="auto">
  <name>Task 2: Rewrite ErrorBoundary.test.tsx for the new react-error-boundary API (drop 2 offline tests per D-07, add 4 new tests for the new API)</name>
  <files>kapwa-client/src/components/ErrorBoundary.test.tsx</files>
  <read_first>
    - kapwa-client/src/components/ErrorBoundary.test.tsx (current 118 lines — 6 existing tests, 2 of which must be deleted)
    - kapwa-client/src/components/ErrorBoundary.tsx (the new function component from Task 1)
    - .planning/phases/13-major-version-upgrades/13-CONTEXT.md (D-06, D-07, D-08, A3)
    - .planning/phases/13-major-version-upgrades/13-PATTERNS.md (ErrorBoundary.test.tsx section lines 108-168, A3 deviation note line 168)
    - .planning/phases/13-major-version-upgrades/13-RESEARCH.md (Don't Hand-Roll table entry "Class-component error boundary" line 430)
  </read_first>
  <action>
Rewrite kapwa-client/src/components/ErrorBoundary.test.tsx to match the new react-error-boundary API. The file currently has 6 tests; the new file will have 4 tests (per A3, 2 offline-detection tests are deleted; 4 new tests cover the new API).

(a) Tests to DELETE (per A3, D-07):
  - `it('shows offline UI when navigator.onLine is false', ...)` — lines 56-69 of the original (tests a UI branch that no longer exists)
  - `it('shows offline UI for fetch-related errors', ...)` — lines 71-87 of the original (tests a UI branch that no longer exists)

(b) Tests to KEEP (with minor adjustments):
  - `it('renders children when no error', ...)` — assertion: `screen.getByText('All good')` — UNCHANGED (the new ErrorBoundary still renders children when no error is thrown)
  - `it('catches thrown error and shows fallback with generic message', ...)` — assertion: `'Something went wrong'`, `'Try Again'`, `'Go to Dashboard'` all truthy — UNCHANGED (the new ErrorFallback renders the same UI)
  - `it('Go to Dashboard link has correct to="/dashboard" attribute', ...)` — assertion: `link.closest('a')?.getAttribute('href') === '/dashboard'` — UNCHANGED (the Link to /dashboard is preserved)

(c) Tests to REWRITE:
  - `it('Try Again button resets error state', ...)` — original asserts the fallback re-shows after clicking Try Again (which is correct for the class component's `setState({ hasError: false })` behavior). The new behavior (per D-08 + react-error-boundary semantics) is: clicking Try Again calls `resetErrorBoundary`, which re-renders the children. If the children still throw (the `Bomb` component still throws on mount), the fallback shows again. The test should verify the `resetErrorBoundary` callback is wired correctly:
    - Render a `<Bomb shouldThrow={true}>` inside ErrorBoundary
    - Click "Try Again" button
    - The fallback UI is still visible (because Bomb re-throws on the new render)
    - This is the same semantic outcome as the original test, but the implementation difference is invisible to the test (which only checks the resulting UI). The test name can stay as "Try Again button resets error state" — the behavior under test is preserved.

(d) New tests to ADD (per the 4-test target):
  1. `it('renders children when no error', ...)` — already exists, KEEP
  2. `it('catches thrown error and shows fallback with generic message', ...)` — already exists, KEEP
  3. `it('Go to Dashboard link has correct to="/dashboard" attribute', ...)` — already exists, KEEP
  4. `it('Try Again button resets error state', ...)` — REWRITTEN (same as (c) above)
  
  The above 4 are the target test count. The PATTERNS.md (lines 108-168) suggests 3-4 tests; the new ErrorBoundary's API is smaller than the old one (no offline branch), so 4 tests covering: (1) happy path, (2) error caught + fallback, (3) Try Again reset, (4) Go to Dashboard link, is sufficient. DO NOT add a "custom fallback prop" test — Task 1 step (a) decided to drop the `fallback` prop if no consumer uses it.

(e) The `setupOffline()` and `setupOnline()` helpers (lines 14-20 of the original) are no longer needed (offline tests are deleted) — REMOVE them.

(f) The `beforeEach` / `afterEach` lifecycle hooks (lines 23-30 of the original) are PRESERVED:
  - `beforeEach(() => { vi.spyOn(console, 'error').mockImplementation(() => {}); })` — needed because react-error-boundary logs caught errors via `console.error` in the `onError` callback (otherwise vitest reports noise)
  - `afterEach(() => { vi.restoreAllMocks(); })` — needed to restore the console.error spy between tests

(g) The `Bomb` test helper (lines 7-12 of the original) is PRESERVED (still needed for "catches thrown error" test).

(h) The `FetchBomb` test helper (lines 72-76 of the original, inside the "shows offline UI for fetch-related errors" test) is REMOVED along with its test.

(i) The imports (lines 1-5 of the original) are PRESERVED (the component name `ErrorBoundary` is the same, the test utilities are unchanged):
  ```tsx
  import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
  import { render, screen } from '@testing-library/react';
  import { MemoryRouter } from 'react-router-dom';
  import { ErrorBoundary } from './ErrorBoundary';
  ```
  The `import React from "react"` on line 2 of the original is no longer needed (the new ErrorBoundary uses `import type { ReactNode }` not default-imported React). REMOVE it.

(j) After the rewrite, run only the ErrorBoundary test file to verify the new tests pass:
    cd kapwa-client && npx vitest run src/components/ErrorBoundary.test.tsx 2>&1 | tee /tmp/eb-test.log
    grep -E "Test Files|Tests" /tmp/eb-test.log
  
  Must show "Test Files 1 passed (1)" and "Tests 4 passed (4)" (4 tests in this file). If a test fails, the most likely cause is:
  - `vi.spyOn(console, 'error')` not suppressing react-error-boundary's internal logging — verify the spy is set up in beforeEach
  - The Bomb component's `throw new Error('Test error')` not being caught — verify the new ErrorBoundary wraps children in ReactErrorBoundary correctly
  - The Try Again click not calling `resetErrorBoundary` — verify the `onClick={resetErrorBoundary}` wiring

(k) Then run the FULL test suite to verify the new ErrorBoundary didn't break any other test that imports it (the Layout component uses ErrorBoundary):
    cd kapwa-client && npx vitest run 2>&1 | tee /tmp/vitest-13-eb.log
    grep -E "^Test Files|^Tests" /tmp/vitest-13-eb.log
  
  Must show "Test Files 44 passed (44)" and "Tests 198 passed (198)" — 196 baseline - 2 offline tests + 4 new ErrorBoundary tests = 198. (3 page snapshot tests are added in Task 3; the suite stays at 198 until then.)
  </action>
  <verify>
    <automated>cd kapwa-client && echo "---offline tests removed:---" && (grep -c 'shows offline UI' src/components/ErrorBoundary.test.tsx || true) && echo "---setupOffline helper removed:---" && (grep -c 'setupOffline' src/components/ErrorBoundary.test.tsx || true) && echo "---WifiOff import removed:---" && (grep -c 'WifiOff' src/components/ErrorBoundary.test.tsx || true) && echo "---React default import removed:---" && (grep -c 'import React from' src/components/ErrorBoundary.test.tsx || true) && echo "---vi.spyOn console.error preserved:---" && grep -c "vi.spyOn(console, 'error')" src/components/ErrorBoundary.test.tsx && echo "---Bomb helper preserved:---" && grep -c 'function Bomb' src/components/ErrorBoundary.test.tsx && echo "---test count:---" && grep -cE "^  it\(" src/components/ErrorBoundary.test.tsx && echo "---only ErrorBoundary test file:---" && npx vitest run src/components/ErrorBoundary.test.tsx 2>&1 | grep -E "Test Files|Tests"
  </verify>
  <acceptance_criteria>
    - `kapwa-client/src/components/ErrorBoundary.test.tsx` does NOT contain "shows offline UI" (both offline tests deleted per D-07)
    - `kapwa-client/src/components/ErrorBoundary.test.tsx` does NOT contain `setupOffline` helper (no longer needed)
    - `kapwa-client/src/components/ErrorBoundary.test.tsx` does NOT contain `WifiOff` import (deleted in Task 1)
    - `kapwa-client/src/components/ErrorBoundary.test.tsx` does NOT contain `import React from` (no default React import needed)
    - `kapwa-client/src/components/ErrorBoundary.test.tsx` contains `vi.spyOn(console, 'error')` in beforeEach (preserved)
    - `kapwa-client/src/components/ErrorBoundary.test.tsx` contains the `function Bomb` test helper (preserved)
    - The test file has exactly 4 `it(` blocks (1 happy path + 1 catches error + 1 Try Again reset + 1 Go to Dashboard link)
    - `npx vitest run src/components/ErrorBoundary.test.tsx` shows "Test Files 1 passed (1)" and "Tests 4 passed (4)"
    - `npx vitest run` shows "Test Files 44 passed (44)" and "Tests 198 passed (198)" — 196 baseline - 2 offline + 4 new = 198
  </acceptance_criteria>
  <done>
    ErrorBoundary.test.tsx is rewritten for the new react-error-boundary API: 2 offline-detection tests deleted (D-07/A3), 4 tests covering the new API (renders children, catches error, Try Again reset, Go to Dashboard link). Full test suite: 198/198 tests passing. No regressions from the ErrorBoundary migration.
  </done>
</task>

<!-- gsd:write-continue -->

<task type="auto">
  <name>Task 3: Add 3 page snapshot tests (DashboardPage, CasesPage, BeneficiariesPage) per D-12/D-13/D-14, then land ONE atomic commit per D-01 covering all of Plan 13-01 + Plan 13-02</name>
  <files>kapwa-client/src/pages/DashboardPage.test.tsx, kapwa-client/src/pages/CasesPage.test.tsx, kapwa-client/src/pages/BeneficiariesPage.test.tsx, kapwa-client/src/pages/__snapshots__/DashboardPage.test.tsx.snap (generated), kapwa-client/src/pages/__snapshots__/CasesPage.test.tsx.snap (generated), kapwa-client/src/pages/__snapshots__/BeneficiariesPage.test.tsx.snap (generated)</files>
  <read_first>
    - kapwa-client/src/pages/DashboardPage.test.tsx (37 lines — append snapshot `it()` block)
    - kapwa-client/src/pages/CasesPage.test.tsx (63 lines — append snapshot `it()` block)
    - kapwa-client/src/pages/BeneficiariesPage.test.tsx (45 lines — append snapshot `it()` block)
    - .planning/phases/13-major-version-upgrades/13-CONTEXT.md (D-01, D-04, D-12, D-13, D-14)
    - .planning/phases/13-major-version-upgrades/13-PATTERNS.md (3 page test sections lines 170-288, Snapshot test pattern lines 688-705)
    - .planning/phases/13-major-version-upgrades/13-RESEARCH.md (Assumption A3 line 628, Open Question 3 lines 647-651)
    - .planning/phases/13-major-version-upgrades/13-VALIDATION.md (D-12/13/14 test map rows)
  </read_first>
  <action>
Add 3 page-level snapshot tests per D-12/D-13/D-14, then land the SINGLE atomic commit for the entire phase per D-01.

(a) Add a new `it('snapshot: renders ... page')` block to kapwa-client/src/pages/DashboardPage.test.tsx:
  - Insert AFTER the existing `it('renders PageShell heading, stat cards, and case table with mock data', ...)` test (line 36 of the original)
  - The new test should:
    1. `const { container } = render(<MemoryRouter><DashboardPage /></MemoryRouter>);`
    2. `expect(await screen.findByRole('heading', { name: 'Dashboard' })).toBeTruthy();` (waits for the page to render)
    3. `expect(container).toMatchSnapshot();` (captures the rendered DOM)
  - Test name: `it('snapshot: DashboardPage rendered DOM with stat cards + recent cases table', ...)` — explicit about what the snapshot captures per D-12

(b) Add a new `it('snapshot: renders ... page')` block to kapwa-client/src/pages/CasesPage.test.tsx:
  - Insert AFTER the existing `it('renders export CSV button', ...)` test (line 62 of the original, before the closing `});`)
  - The new test should:
    1. `const { container } = render(<MemoryRouter><CasesPage /></MemoryRouter>);`
    2. `expect(await screen.findByRole('heading', { name: 'Case Tracker' })).toBeTruthy();` (waits for the page to render)
    3. `expect(container).toMatchSnapshot();`
  - Test name: `it('snapshot: CasesPage rendered DOM with table layout + status badges + filter controls', ...)` per D-13

(c) Add a new `it('snapshot: renders ... page')` block to kapwa-client/src/pages/BeneficiariesPage.test.tsx:
  - Insert AFTER the existing `it('renders search input for beneficiaries', ...)` test (line 44 of the original, before the closing `});`)
  - The new test should:
    1. `const { container } = render(<MemoryRouter><BeneficiariesPage /></MemoryRouter>);`
    2. `expect(await screen.findByRole('heading', { name: 'Beneficiaries' })).toBeTruthy();`
    3. `expect(container).toMatchSnapshot();`
  - Test name: `it('snapshot: BeneficiariesPage rendered DOM with searchable list + action buttons + masked PII', ...)` per D-14

(d) Run the test suite to auto-generate the 3 .snap files (vitest creates them in `__snapshots__/` on first run):
    cd kapwa-client && npx vitest run 2>&1 | tee /tmp/vitest-13-snap.log
    grep -E "^Test Files|^Tests" /tmp/vitest-13-snap.log
  
  Must show "Test Files 44 passed (44)" and "Tests 201 passed (201)" — 198 after Task 2 + 3 new snapshot tests = 201.
  
  Verify the 3 .snap files were auto-generated:
    ls -la kapwa-client/src/pages/__snapshots__/
  Must show 3 files: DashboardPage.test.tsx.snap, CasesPage.test.tsx.snap, BeneficiariesPage.test.tsx.snap

(e) Build verification (final regression check before commit):
    cd kapwa-client && rm -rf dist && npm run build 2>&1 | tee /tmp/build-13-final.log
    grep -q 'bg-background' dist/assets/*.css && echo "BUILD_OK" || echo "BUILD_FAIL"
  Must show "BUILD_OK" — Tailwind v4 + shadcn utility classes still generate correctly after the full migration.

(f) LAND THE SINGLE ATOMIC COMMIT per D-01. The working tree contains:
  - From Plan 13-01: package.json, package-lock.json, vite.config.ts, capacitor.config.ts, src/index.css
  - From Plan 13-01 deletions: postcss.config.mjs, tailwind.config.js
  - From this plan: src/components/ErrorBoundary.tsx, src/components/ErrorBoundary.test.tsx, 3 page test files, 3 .snap files
  
  Run `cd kapwa-client && git status` and verify the list matches the above (no unintended files). Then:
  
  git add kapwa-client/package.json kapwa-client/package-lock.json
  git add kapwa-client/vite.config.ts kapwa-client/capacitor.config.ts
  git add kapwa-client/src/index.css
  git rm kapwa-client/postcss.config.mjs kapwa-client/tailwind.config.js
  git add kapwa-client/src/components/ErrorBoundary.tsx kapwa-client/src/components/ErrorBoundary.test.tsx
  git add kapwa-client/src/pages/DashboardPage.test.tsx kapwa-client/src/pages/CasesPage.test.tsx kapwa-client/src/pages/BeneficiariesPage.test.tsx
  git add kapwa-client/src/pages/__snapshots__/  # the 3 auto-generated .snap files
  
  git status  # review before committing
  
  git commit -m "$(cat <<'MSG'
feat(deps): upgrade React 19, Capacitor 8, Tailwind v4, TypeScript 5.7

UPG-01: React 18.2.0 → 19.2.7 (class ErrorBoundary migrated to react-error-boundary)
UPG-02: Capacitor 6.2.1 → 8.4.1 (bundledWebRuntime removed from capacitor.config.ts)
UPG-03: Tailwind CSS 3.4.1 → 4.3.2 (Vite plugin replaces PostCSS, @theme inline block)

D-01: ONE atomic commit per CONTEXT decision — single npm install + all code migrations
D-05: TypeScript 5.3.3 → 5.7.0 (locked decision, not 6.0.3 per research A1)
D-06: Migrate ErrorBoundary (only class component) to react-error-boundary v6.1.2
D-07: Drop custom offline-detection branch (TypeError 'Failed to fetch' → offline UI);
        SWR-style fetch hooks in Phase 15 will own offline handling
D-08: Try Again button wired to resetErrorBoundary callback (no manual resetKeys)
D-09: @tailwindcss/upgrade codemod run + manual hex-vs-HSL correction in @theme inline
D-10: All shadcn design tokens (--background, --primary, etc.) preserved verbatim
D-11: @tailwindcss/vite replaces PostCSS pipeline; postcss.config.mjs + tailwind.config.js deleted
D-12: DashboardPage snapshot test (stat cards + recent cases table)
D-13: CasesPage snapshot test (table layout + status badges + filter controls)
D-14: BeneficiariesPage snapshot test (searchable list + action buttons + masked PII)

Pitfall 1: @vitejs/plugin-react 4.2.1 → 5.2.0 (4.x has broken peer dep on Vite 8)
Pitfall 2: @theme inline uses var(--X) direct references (NOT hsl() wrapper) for raw hex tokens
Pitfall 3: @layer base, components, legacy, utilities comma-list line deleted (v4 doesn't need it)
Pitfall 5: bundledWebRuntime removed from capacitor.config.ts (removed in Capacitor 7)

All 201 tests passing (196 baseline - 2 offline + 4 new ErrorBoundary + 3 page snapshots).
npm run build exits 0 with bg-background utility class generated.
Web build verification only (D-03) — mobile Android/iOS builds deferred to follow-up phase.
MSG
)"
  
  DO NOT push. DO NOT create a PR. The orchestrator handles those steps after the phase completes.

(g) Verify the commit landed with the right shape:
    cd kapwa-client && git log -1 --format="%H %s"
    cd kapwa-client && git show --stat HEAD | head -30
  Must show ONE commit, the title matches the message above, and the stat lists 12 changed files (5 modified configs + 1 CSS + 2 deleted + 2 component + 3 test files = 13 file paths; the 3 .snap files are listed under the `__snapshots__/` directory addition).
  </action>
  <verify>
    <automated>cd kapwa-client && echo "---toMatchSnapshot count:---" && grep -c 'toMatchSnapshot' src/pages/DashboardPage.test.tsx src/pages/CasesPage.test.tsx src/pages/BeneficiariesPage.test.tsx && echo "---vitest run:---" && npx vitest run 2>&1 | grep -E "^Test Files|^Tests" && echo "---snap files generated:---" && ls -la src/pages/__snapshots__/ && echo "---build:---" && (rm -rf dist && npm run build > /tmp/build.log 2>&1 && grep -q 'bg-background' dist/assets/*.css && echo "BUILD_OK" || echo "BUILD_FAIL") && echo "---git log:---" && git log -1 --format="%H %s" && echo "---git show stat:---" && git show --stat HEAD | head -25
  </verify>
  <acceptance_criteria>
    - `kapwa-client/src/pages/DashboardPage.test.tsx` contains `toMatchSnapshot()` (D-12)
    - `kapwa-client/src/pages/CasesPage.test.tsx` contains `toMatchSnapshot()` (D-13)
    - `kapwa-client/src/pages/BeneficiariesPage.test.tsx` contains `toMatchSnapshot()` (D-14)
    - `npx vitest run` shows "Test Files 44 passed (44)" and "Tests 201 passed (201)" — 198 + 3 = 201
    - 3 .snap files exist in `kapwa-client/src/pages/__snapshots__/`: DashboardPage.test.tsx.snap, CasesPage.test.tsx.snap, BeneficiariesPage.test.tsx.snap
    - `npm run build` exits 0 AND `grep -q 'bg-background' dist/assets/*.css` succeeds (Tailwind v4 still generates the right utility classes)
    - `git log -1` shows ONE commit (not 2) with the title containing "UPG-01", "UPG-02", "UPG-03" and "D-01" (per D-01 atomic commit requirement)
    - `git show --stat HEAD` lists 12+ changed files: package.json, package-lock.json, vite.config.ts, capacitor.config.ts, src/index.css, 2 deletions (postcss.config.mjs, tailwind.config.js), 2 component files, 3 test files, 3 snapshot files
    - The commit message cites the D-XX decision IDs and the UPG-XX requirement IDs (traceability)
  </acceptance_criteria>
  <done>
    3 page snapshot tests added (DashboardPage, CasesPage, BeneficiariesPage) per D-12/D-13/D-14. 201/201 tests passing. Build succeeds. ONE atomic commit lands all of Plan 13-01's dep + config + CSS changes PLUS Plan 13-02's component migration + test rewrite + page snapshots. Commit message cites UPG-01/02/03 and all relevant D-XX decisions. Phase 13 complete.
  </done>
</task>

<!-- gsd:write-continue -->

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry (react-error-boundary@^6.1.2) → src/components/ErrorBoundary.tsx | Library is installed (Plan 13-01) and consumed; the library's internal state machine (caught error → fallback render → resetErrorBoundary) is the trust boundary |
| Vitest test runner → snapshot files (`__snapshots__/`) | Snapshot files are written to disk on first run; subsequent runs compare against them. A malicious or accidental change to the production component would NOT silently change the snapshot — vitest FAILS the test on mismatch, requiring a deliberate `--update` flag |
| git working tree → git commit (single atomic commit per D-01) | All phase changes land in one commit; rollback is one `git revert` away |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-EB-01 | Information Disclosure | kapwa-client/src/components/ErrorBoundary.tsx (class → function) | mitigate | The class component's `componentDidCatch` logged full error + component stack to console via `console.error('ErrorBoundary caught:', error, info)`. The new function component preserves this behavior via `onError={(error, info) => console.error('ErrorBoundary caught:', error, info)}` on the `<ReactErrorBoundary>`. Same logging semantics, no information disclosure regression. The `FallbackComponent` receives the `error` prop and could display it — but the new `ErrorFallback` does NOT display `error.message` directly (it shows the generic "Something went wrong" string), matching the original class component's behavior. |
| T-13-EB-02 | Tampering | kapwa-client/src/components/ErrorBoundary.tsx (offline branch removed per D-07) | mitigate | The original class component's offline detection (`navigator.onLine` + `error.message.includes('fetch')` + `error.name === 'TypeError'`) is removed in Task 1 step (b). This is a deliberate behavior change per D-07 — offline handling is deferred to Phase 15 (SWR fetch hooks). Any user-facing impact: a network error thrown during render will show the generic "Something went wrong" UI instead of the offline-specific UI. This is acceptable because: (1) the offline UI was a UX nice-to-have, not a security control, (2) the SWR hooks in Phase 15 will provide better offline detection at the data layer, (3) the "Try Again" button still works (it calls `resetErrorBoundary`). |
| T-13-SS-01 | Information Disclosure | kapwa-client/src/pages/__snapshots__/*.snap | mitigate | The snapshot files contain the rendered DOM (text content, class names, data attributes) of the 3 most-trafficked pages. The mock data in the test files (DashboardPage's `getDashboard` mock, CasesPage's `mockCases`, BeneficiariesPage's `mockBeneficiaries) contains realistic-looking but synthetic data (names, barangays, categories). The snap files will contain this synthetic data. NO PII, NO real beneficiary info, NO secrets. The mocks in DashboardPage.test.tsx (line 7-19), CasesPage.test.tsx (line 7-25), and BeneficiariesPage.test.tsx (line 7-11) must NOT be modified to include real PII. The existing mocks are preserved verbatim. |
| T-13-ST-01 | Tampering | kapwa-client/src/pages/__snapshots__/*.snap (regenerated on each test run if changed) | mitigate | The snapshot files are committed alongside the test files. If the production page components change in a way that affects the rendered DOM, the test FAILS (not silently updates). Developers must run `npx vitest -u` (the update flag) explicitly to regenerate snapshots, which surfaces in code review. The 3 .snap files added in Task 3 are committed as-is (no further changes expected in this phase). |
| T-13-AC-01 | Repudiation | git commit message (D-01 atomic commit) | mitigate | The commit message explicitly cites UPG-01/02/03 requirement IDs and all relevant D-XX decision IDs. This makes the commit traceable from git log back to ROADMAP.md and CONTEXT.md. The commit is a single atomic unit — there is no ambiguity about which changes belong to which decision. |
| T-13-SC | Tampering | react-error-boundary@^6.1.2 (installed in Plan 13-01) | mitigate | Per Package Legitimacy Audit (RESEARCH.md line 156), react-error-boundary is approved: 12.8M weekly downloads, source repo `bvaughn/react-error-boundary` (well-known React community library), 7+ years in registry, no `postinstall` script. The library's peer dep is `react: ^18.0.0 \|\| ^19.0.0` — confirmed compatible with React 19.2.7 (Task 1 install verified peer dep resolution with `--legacy-peer-deps`). |
</threat_model>

<verification>
Before declaring Plan 13-02 complete:
- [ ] `cd kapwa-client && grep -c 'class ErrorBoundary' src/components/ErrorBoundary.tsx` returns 0
- [ ] `cd kapwa-client && grep -c 'WifiOff\\|navigator.onLine' src/components/ErrorBoundary.tsx` returns 0
- [ ] `cd kapwa-client && grep -c "from 'react-error-boundary'" src/components/ErrorBoundary.tsx` returns 1
- [ ] `cd kapwa-client && grep -c 'function ErrorFallback\\|export function ErrorBoundary' src/components/ErrorBoundary.tsx` returns 2
- [ ] `cd kapwa-client && grep -c 'resetErrorBoundary' src/components/ErrorBoundary.tsx` returns ≥1
- [ ] `cd kapwa-client && npx tsc --noEmit -p .` exits 0
- [ ] `cd kapwa-client && grep -c 'shows offline UI' src/components/ErrorBoundary.test.tsx` returns 0
- [ ] `cd kapwa-client && grep -c '^  it(' src/components/ErrorBoundary.test.tsx` returns 4
- [ ] `cd kapwa-client && npx vitest run src/components/ErrorBoundary.test.tsx` shows "Tests 4 passed (4)"
- [ ] `cd kapwa-client && npx vitest run` shows "Test Files 44 passed (44)" and "Tests 201 passed (201)"
- [ ] `cd kapwa-client && grep -c 'toMatchSnapshot' src/pages/DashboardPage.test.tsx` returns 1
- [ ] `cd kapwa-client && grep -c 'toMatchSnapshot' src/pages/CasesPage.test.tsx` returns 1
- [ ] `cd kapwa-client && grep -c 'toMatchSnapshot' src/pages/BeneficiariesPage.test.tsx` returns 1
- [ ] `cd kapwa-client && ls src/pages/__snapshots__/*.snap | wc -l` returns 3
- [ ] `cd kapwa-client && rm -rf dist && npm run build && grep -q 'bg-background' dist/assets/*.css` exits 0
- [ ] `cd kapwa-client && git log --oneline -1` shows ONE commit (not 2) with "UPG-01" and "D-01" in the title
- [ ] `cd kapwa-client && git show --stat HEAD` lists 12+ changed files including package.json, vite.config.ts, capacitor.config.ts, src/index.css, ErrorBoundary.tsx, ErrorBoundary.test.tsx, 3 page test files, 3 snapshot files, and 2 deletions (postcss.config.mjs, tailwind.config.js)
</verification>

<success_criteria>
- All 3 tasks completed with their acceptance_criteria satisfied
- The only class component in the client (ErrorBoundary) is migrated to a function component using react-error-boundary@^6.1.2
- The custom offline-detection branch is removed (D-07); SWR-style fetch hooks in Phase 15 will own offline handling
- The Try Again button is wired to the `resetErrorBoundary` callback from react-error-boundary (D-08)
- 2 offline-detection tests are deleted from ErrorBoundary.test.tsx; 4 new tests cover the new API (renders children, catches error, Try Again reset, Go to Dashboard link)
- 3 page snapshot tests are added to DashboardPage, CasesPage, and BeneficiariesPage (D-12/D-13/D-14), producing 3 auto-generated .snap files
- The full test suite shows 201/201 tests passing (196 baseline - 2 offline + 4 new ErrorBoundary + 3 page snapshots)
- `npm run build` exits 0 and produces a CSS bundle with `bg-background` utility class
- ONE atomic git commit lands all of Plan 13-01's dep + config + CSS changes PLUS Plan 13-02's component migration + test rewrite + page snapshots
- The commit message cites UPG-01/02/03 requirement IDs and D-01/D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-13/D-14 decision IDs for full traceability
- Phase 13 complete: codebase is on React 19, Capacitor 8, Tailwind v4, TypeScript 5.7, with no class components and a tested snapshot baseline for the 3 most-trafficked pages
</success_criteria>

<output>
Create `.planning/phases/13-major-version-upgrades/13-02-SUMMARY.md` and run:
```
cd kapwa-client && git log -1 --format="%H %s"  # confirm the atomic commit
cd kapwa-client && git show --stat HEAD | head -30  # confirm 12+ files
cd kapwa-client && npx vitest run 2>&1 | tail -5  # confirm 201/201
```
</output>

## Artifacts this phase produces

### New Runtime Symbols (Wave 2)
- `ErrorFallback({ resetErrorBoundary }: FallbackProps)` — function component, defined and exported from `kapwa-client/src/components/ErrorBoundary.tsx`, renders the rich "Something went wrong" UI (TriangleAlert icon + AriaLiveRegion announcement + description + Try Again + Go to Dashboard buttons)
- `ErrorBoundary({ children }: { children: ReactNode })` — function component (replaces the class), exported from `kapwa-client/src/components/ErrorBoundary.tsx`, wraps children in `<ErrorBoundary>` from `react-error-boundary` with `FallbackComponent={ErrorFallback}` and `onError={(error, info) => console.error('ErrorBoundary caught:', error, info)}` — the only class component in the client is now gone (D-06)

### New Test Symbols (Wave 2)
- 3 new `it('snapshot: ...', ...)` blocks:
  - `kapwa-client/src/pages/DashboardPage.test.tsx` — captures stat cards, recent cases table, role-based widgets (D-12)
  - `kapwa-client/src/pages/CasesPage.test.tsx` — captures table layout, status badges, filter controls (D-13)
  - `kapwa-client/src/pages/BeneficiariesPage.test.tsx` — captures searchable list, action buttons, masked PII display (D-14)
- 3 auto-generated Vitest snapshot files in `kapwa-client/src/pages/__snapshots__/`:
  - `DashboardPage.test.tsx.snap`
  - `CasesPage.test.tsx.snap`
  - `BeneficiariesPage.test.tsx.snap`

### Files Modified (Wave 2)
- `kapwa-client/src/components/ErrorBoundary.tsx` — class → function wrapper around `react-error-boundary` (D-06), `WifiOff` import + offline branch removed (D-07), `Try Again` wired to `resetErrorBoundary` (D-08)
- `kapwa-client/src/components/ErrorBoundary.test.tsx` — 2 offline tests deleted (A3), 4 tests covering the new API
- `kapwa-client/src/pages/DashboardPage.test.tsx` — `toMatchSnapshot()` assertion added (D-12)
- `kapwa-client/src/pages/CasesPage.test.tsx` — `toMatchSnapshot()` assertion added (D-13)
- `kapwa-client/src/pages/BeneficiariesPage.test.tsx` — `toMatchSnapshot()` assertion added (D-14)

### Atomic Git Commit (Phase 13 deliverable)
- ONE commit containing all 12+ file changes from Plan 13-01 (package.json, package-lock.json, vite.config.ts, capacitor.config.ts, src/index.css, 2 deletions) and Plan 13-02 (ErrorBoundary.tsx, ErrorBoundary.test.tsx, 3 page test files, 3 snapshot files)
- Commit message cites UPG-01/02/03 requirement IDs and D-01/D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-13/D-14 decision IDs for full traceability per D-01
