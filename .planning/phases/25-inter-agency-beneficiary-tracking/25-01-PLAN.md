# 25-01 — Inter-Agency Beneficiary Tracking

**Goal:** Track beneficiaries across external agencies — agencies registry, cross-agency referrals, agency-normalized access cards, and PSN dedup.

**Status:** IN PROGRESS (on branch `feat/inter-agency-tracking`)

**Scope (6 tasks):**
1. Agencies infrastructure — migration + entity + module + `user.agencyId` (seed data)
2. Inter-agency referrals module (entity/controller/service/module/dto + status guard)
3. Access card agency normalization + card summary endpoint
4. PSN exact-match dedup + IRF export agency lookup
5. Client — inter-agency referrals page (inbox + create form)
6. Client — access card three sections (in progress)

**Commits:** `3b19181` (plan), `0c2c314` (agencies), `124387a` (inter-agency module), `0be39a1` (access card normalization), `db8d372` (agencyId/agency DTO), `a99d91f` (PSN dedup + IRF export), `7626baa` (client page)

**Local uncommitted:** `AccessCardViewPage.tsx` (3-section layout) + `AccessCardViewPage.test.tsx`
