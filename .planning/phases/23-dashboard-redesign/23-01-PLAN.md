# 23-01 — Worker/Admin Dashboard Redesign

**Goal:** Replace the monolithic dashboard with role-aware widget sets and align the cases table with the Cases page.

**Scope:**
- `DashboardEngine` + `StaticDashboard` with draggable grid (react-grid-layout)
- Role-based widget sets: `ClaimantWidgets`, `MayorWidgets`, `AuditorWidgets`, `CoordinatorWidgets`
- Shared widgets: StatsRow, SlaWidget, CaseStatusChart, TrendsChart, NeedsAttention, BarangayBreakdown, ActivityCalendar, QuickActionPanel
- Cases table on dashboard matches Cases page columns (`7db4993`)
- DnD dashboard switched out (`ac89555`) — role widgets kept

**Status:** Complete
