---
phase: 09-landing-page-auth-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kapwa-client/package.json
  - kapwa-client/src/components/PublicLayout.tsx
  - kapwa-client/src/components/PublicHeader.tsx
  - kapwa-client/src/components/PublicFooter.tsx
  - kapwa-client/src/components/ServicesGrid.tsx
  - kapwa-client/src/components/ApplicationSteps.tsx
  - kapwa-client/src/components/TeamSection.tsx
  - kapwa-client/src/components/ContactInfo.tsx
  - kapwa-client/src/components/ui/form.tsx
  - kapwa-client/src/routes.tsx
  - kapwa-client/src/components/ProtectedRoute.tsx
  - tests/components/PublicHeader.test.tsx
  - tests/layouts/PublicLayout.test.tsx
autonomous: false
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
user_setup: []

must_haves:
  truths:
    - "All public pages share a PublicLayout shell with header (logo, nav, Login/Dashboard toggle) and footer (LGU branding, quick links, contact info, copyright)"
    - "Login button in header changes to 'Go to Dashboard' when user is authenticated"
    - "Authenticated users visiting / are auto-redirected to /dashboard"
    - "Unauthorized role access redirects to user's own dashboard, not /unauthorized"
    - "Services card grid, application steps timeline, team section, and contact info components exist and render consistently"
  artifacts:
    - path: kapwa-client/src/components/PublicLayout.tsx
      provides: "Public layout shell with header + footer + Outlet"
      exports: ["PublicLayout"]
      min_lines: 30
    - path: kapwa-client/src/components/PublicHeader.tsx
      provides: "Auth-aware header with nav links and Login/Dashboard toggle"
      exports: ["PublicHeader"]
      min_lines: 50
    - path: kapwa-client/src/components/PublicFooter.tsx
      provides: "Footer with LGU branding, quick links, contact info"
      exports: ["PublicFooter"]
      min_lines: 40
    - path: kapwa-client/src/routes.tsx
      provides: "Restructured routes — public layout wrapper, / → LandingPageRedirect, /dashboard for protected"
      exports: ["MainRoutes"]
    - path: kapwa-client/src/components/ProtectedRoute.tsx
      provides: "Updated role mismatch redirect to role-based dashboard"
    - path: kapwa-client/src/components/ServicesGrid.tsx
      provides: "Card grid of MSWDO services"
      exports: ["ServicesGrid"]
    - path: kapwa-client/src/components/ApplicationSteps.tsx
      provides: "Numbered step indicator timeline"
      exports: ["ApplicationSteps"]
    - path: kapwa-client/src/components/TeamSection.tsx
      provides: "Officer placeholder profile card grid"
      exports: ["TeamSection"]
    - path: kapwa-client/src/components/ContactInfo.tsx
      provides: "Contact info display block (address, phone, email)"
      exports: ["ContactInfo"]
  key_links:
    - from: "PublicHeader"
      to: "lib/auth-context"
      via: "useAuth() for auth-aware Login/Dashboard toggle"
      pattern: "useAuth"
    - from: "routes.tsx LandingPageRedirect"
      to: "lib/auth-context"
      via: "useAuth() for auth-aware redirect"
      pattern: "useAuth.*Navigate"
    - from: "ProtectedRoute.tsx"
      to: "lib/auth-context"
      via: "getCurrentUser() then roleRedirectMap for unauthorized redirect"
      pattern: "roleRedirectMap"
---

<objective>
Public shell infrastructure: package installs, PublicLayout with header/footer, route restructuring (move dashboard to /dashboard, add public route wrapper), and reusable display components (ServicesGrid, ApplicationSteps, TeamSection, ContactInfo).

Purpose: Create the foundation all public pages depend on — layout shell, route structure, and reusable sub-components.
Output: PublicLayout, PublicHeader, PublicFooter, restructured routes, ProtectedRoute with role-based redirect, reusable components, installed packages (RHF/Zod/shadcn form), test scaffolds.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-landing-page-auth-flow/09-CONTEXT.md
@.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md
@.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md
@.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md
@kapwa-client/src/routes.tsx
@kapwa-client/src/components/Layout.tsx
@kapwa-client/src/components/ProtectedRoute.tsx
@kapwa-client/src/lib/auth-context.tsx
@kapwa-client/src/components/ui/button.tsx
@kapwa-client/src/components/ui/card.tsx
@kapwa-client/src/components/ui/avatar.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify react-hook-form package legitimacy before install</name>
  <files>kapwa-client/package.json, kapwa-client/src/components/ui/form.tsx</files>
  <read_first>
    @.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md (Section: Package Legitimacy Audit)
  </read_first>
  <action>Open https://www.npmjs.com/package/react-hook-form in a browser. Verify package name is "react-hook-form", publisher is "bluebill1049", weekly downloads ~55M, and the GitHub repo link points to github.com/react-hook-form/react-hook-form. Read the README to confirm it matches the well-known RHF library. After human confirms safety, run: cd kapwa-client && npm install react-hook-form zod @hookform/resolvers && npx shadcn@latest add form (accept defaults).</action>
  <what-built>
    RESEARCH.md flagged react-hook-form as [SUS] because the package was published 8 days ago. However, it has 55M weekly downloads and the legitimate GitHub repo at github.com/react-hook-form/react-hook-form. The SUS flag is a false positive from the recency heuristic.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-hook-form in a browser
    2. Verify: package name is "react-hook-form", publisher is "bluebill1049" (maintainer)
    3. Verify: weekly downloads ~55M, the repo link points to github.com/react-hook-form/react-hook-form
    4. Verify: The README shows legitimate documentation matching the well-known RHF library
    5. Confirm safety verbally to proceed
  </how-to-verify>
  <resume-signal>Type "react-hook-form is safe, proceed with install" to continue</resume-signal>

  <verify>npm view react-hook-form --json 2>/dev/null | grep -q "bluebill1049" && echo "Publisher verified" && cd kapwa-client && npm install react-hook-form zod @hookform/resolvers 2>/dev/null | tail -3 && npx shadcn@latest add form --yes 2>/dev/null | tail -5</verify>
  <done>Human confirmed react-hook-form is legitimate. npm packages (react-hook-form, zod, @hookform/resolvers) installed. shadcn form component created at src/components/ui/form.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Install packages, create PublicLayout/PublicHeader/PublicFooter, restructure routes, update ProtectedRoute</name>
  <files>
    kapwa-client/package.json,
    kapwa-client/src/components/ui/form.tsx,
    kapwa-client/src/components/PublicLayout.tsx,
    kapwa-client/src/components/PublicHeader.tsx,
    kapwa-client/src/components/PublicFooter.tsx,
    kapwa-client/src/routes.tsx,
    kapwa-client/src/components/ProtectedRoute.tsx
  </files>
  <read_first>
    @kapwa-client/src/routes.tsx (current structure - all route definitions)
    @kapwa-client/src/components/ProtectedRoute.tsx (current redirect logic)
    @kapwa-client/src/components/Layout.tsx (existing layout pattern for reference)
    @kapwa-client/src/lib/auth-context.tsx (User interface, useAuth() shape)
    @.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md (Route restructuring pattern, PublicLayout pattern, auth-aware header)
    @.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md (Layout structure diagram, copywriting contract, interaction states)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (Layout analog, Topbar analog, skip-to-content pattern, cn() utility)
  </read_first>
  <action>
    Run: npm install react-hook-form zod @hookform/resolvers in kapwa-client/.
    Run: npx shadcn@latest add form (accept defaults) in kapwa-client/.
    This installs packages and creates src/components/ui/form.tsx.

    Create src/components/PublicLayout.tsx: Named export PublicLayout. Renders flex-col min-h-screen container. First focusable element is skip-to-content link (<a href="#main-content">Skip to content</a> with sr-only/focus-visible classes per Layout.tsx pattern at line 74-79). Renders PublicHeader. Then <main id="main-content" className="flex-1"> with <Outlet />. Then PublicFooter at bottom (mt-auto). Use cn() for className merging. Import useAuth() for passing user/loading to PublicHeader. Import PublicHeader from ./PublicHeader, PublicFooter from ./PublicFooter, Outlet from react-router-dom. (See UI-SPEC layout diagram for structure.)

    Create src/components/PublicHeader.tsx: Named export PublicHeader. Calls useAuth() to get user and loading. If loading, render empty header bar (sticky top-0 z-40 bg-background/95 backdrop-blur-sm border-b border-border h-16) — this prevents flash of wrong auth state per RESEARCH Pitfall 2. When loaded, render sticky header with:
    - Logo/brand area: "KAPWA" text in font-heading text-xl font-bold. Placeholder per D-09.
    - Nav links (Home, About, Contact): Each as Button variant="ghost" asChild with <Link>. Nav is <nav aria-label="Main navigation">. Active route uses aria-current="page" and text-accent font-semibold.
    - Auth-sensitive CTA: If user exists, render Button variant="outline" asChild with <Link to={roleRedirectMap[user.role] || '/dashboard'}>Go to Dashboard</Link>. If no user, render Button with className "bg-accent text-accent-foreground hover:bg-accent/90" asChild with <Link to="/login">Login</Link>.
    Layout: max-w-7xl mx-auto px-4 lg:px-8 flex items-center h-16 justify-between.
    Import Link and useLocation from react-router-dom, useAuth from @/lib/auth-context, Button from @/components/ui/button, cn from @/lib/utils.
    Use useLocation().pathname to check current route for aria-current. (See UI-SPEC interaction states table for visual states.)

    Create src/components/PublicFooter.tsx: Named export PublicFooter. footer element with className="bg-card border-t border-border mt-auto". Inside: max-w-7xl mx-auto px-4 lg:px-8 py-8. Three-column grid (grid-cols-1 md:grid-cols-3 gap-8) with:
    - Column 1: LGU branding. Heading "KAPWA" font-heading, subtitle "MSWDO Norzagaray" text-sm text-muted-foreground.
    - Column 2: "Quick Links" heading (font-heading text-sm font-semibold mb-3), list of links: Home (/), About (/about), Services (#services), Contact (/contact). Each as Button variant="link" className="h-auto p-0" asChild with <Link>.
    - Column 3: "Contact Us" heading (font-heading text-sm font-semibold mb-3), contact details with lucide icons. Address: "Municipal Social Welfare and Development Office, Norzagaray, Bulacan". Phone: "(044) XXX-XXXX". Email: "mswdo@norzagaray.gov.ph".
    Below grid: Separator, then copyright line "© 2026 MSWDO Norzagaray. All rights reserved." centered text-sm text-muted-foreground pt-6.
    Import Separator from @/components/ui/separator, Link from react-router-dom, Button from @/components/ui/button, cn from @/lib/utils. Use MapPin, Phone, Mail from lucide-react for contact icons.

    Modify src/routes.tsx: Restructure the router as follows. Keep ALL existing imports plus add new ones:
    - import { PublicLayout } from './components/PublicLayout';
    - import { LandingPage } from './pages/LandingPage';
    - import { AboutPage } from './pages/AboutPage';
    - import { ContactPage } from './pages/ContactPage';
    - import { RegisterPage } from './pages/RegisterPage';
    Remove the standalone { path: '/login', element: <LoginPage /> } line from the flat route list (login moves to public routes).

    New route structure per RESEARCH.md architecture diagram:
    ```
    const router = createBrowserRouter([
      // === PUBLIC ROUTES ===
      {
        element: <PublicLayout />,
        children: [
          { index: true, element: <LandingPageRedirect /> },
          { path: 'about', element: <AboutPage /> },
          { path: 'contact', element: <ContactPage /> },
          { path: 'login', element: <LoginPage /> },
          { path: 'register', element: <RegisterPage /> },
        ],
      },
      // === PROTECTED ROUTES ===
      // Change / to /dashboard for the main dashboard route
      { path: 'dashboard', element: <Private><DashboardPage /></Private> },
      // ... ALL other existing protected routes remain at their current paths unchanged
      { path: '*', element: <Navigate to="/" /> },
    ]);
    ```

    Add the LandingPageRedirect function before the router definition (after the Private helper):
    ```
    function LandingPageRedirect() {
      const { user, loading } = useAuth();
      if (loading) return null;
      return user ? <Navigate to="/dashboard" replace /> : <LandingPage />;
    }
    ```
    Import useAuth from ./lib/auth-context at the top of the file.

    Modify src/components/ProtectedRoute.tsx: Update the role-check redirect at approximately line 28 (where currently `navigate('/')` is called on role mismatch). Replace with role-based redirect using the map from D-10:
    ```
    const roleRedirectMap: Record<string, string> = {
      social_worker: '/dashboard',
      admin: '/admin',
      coordinator: '/coordinator',
      claimant: '/my-dashboard',
      mayor: '/reports',
      auditor: '/audit-logs',
    };
    navigate(roleRedirectMap[user.role] || '/dashboard');
    ```
    This fulfills D-12: unauthorized role access redirects to user's own dashboard, not /unauthorized.
  </action>
  <verify>
    <automated>
      cd kapwa-client && npm run test:run 2>&1 | head -30
      # Full suite must compile and run without import/resolution errors.
      # Run specific import verification:
      node -e "require('./src/components/PublicLayout')" 2>&1 | head -5 || true
    </automated>
  </verify>
  <done>
    - npm packages (react-hook-form, zod, @hookform/resolvers) installed in package.json
    - src/components/ui/form.tsx exists after npx shadcn add form
    - PublicLayout.tsx renders skip-to-content, PublicHeader, main with Outlet, PublicFooter
    - PublicHeader.tsx shows Login when unauthenticated, Go to Dashboard when authenticated
    - PublicFooter.tsx renders 3-column grid with branding, links, contact info
    - routes.tsx has public routes wrapped in PublicLayout, dashboard at /dashboard, LandingPageRedirect at /
    - Old /login standalone route removed from flat list, login now in public routes
    - ProtectedRoute.tsx redirects unauthorized role access to role-specific dashboard instead of /
    - npm run build does not error on unresolved imports for LandingPage (will resolve at plan 2)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create reusable display components (ServicesGrid, ApplicationSteps, TeamSection, ContactInfo) and test scaffolds</name>
  <files>
    kapwa-client/src/components/ServicesGrid.tsx,
    kapwa-client/src/components/ApplicationSteps.tsx,
    kapwa-client/src/components/TeamSection.tsx,
    kapwa-client/src/components/ContactInfo.tsx,
    tests/components/PublicHeader.test.tsx,
    tests/layouts/PublicLayout.test.tsx
  </files>
  <read_first>
    @.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md (Services card grid layout, step indicator, team section, contact info sections)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (Card structure from PATTERNS line 506-519, Avatar pattern line 189-196, test patterns line 573-584)
    @kapwa-client/src/components/ui/card.tsx
    @kapwa-client/src/components/ui/avatar.tsx
    @kapwa-client/src/components/cards/AccessCard.test.tsx (test pattern)
  </read_first>
  <action>
    Create src/components/ServicesGrid.tsx: Named export ServicesGrid. Renders a section with id="services" and className="py-16 md:py-24". Section heading "Our Services" as h2 (font-heading text-2xl font-semibold leading-tight text-center mb-8). Below heading: div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto px-4". Each card is shadcn Card with CardHeader (lucide icon in text-accent), CardTitle (service name), CardContent (description p text-muted-foreground text-base).

    Service data (static array, placeholders per D-17):
    - Social Welfare Assistance / HandHeart icon / "Financial, medical, and burial assistance for qualified residents of Norzagaray."
    - Crisis Intervention / ShieldAlert icon / "Immediate response and support for individuals and families in crisis situations."
    - Children and Family Services / Users icon / "Protective services, counseling, and support for children and families."
    - Senior Citizen Programs / Heart icon / "Social pensions, wellness programs, and community activities for senior citizens."
    - Women's Welfare / Flower2 icon / "Empowerment programs, livelihood assistance, and support for women."
    - Youth Development / GraduationCap icon / "Educational assistance, youth formation, and leadership development."

    Each Card: hover:border-accent/30 hover:shadow-md transition-all duration-200. Card uses Card with className="bg-card border-border transition-all duration-200 hover:border-accent/30 hover:shadow-md". CardHeader has the icon (size 8, text-accent), CardTitle uses font-heading font-semibold text-lg md:text-xl. CardContent has description text. Import Card, CardHeader, CardTitle, CardContent from @/components/ui/card.

    Create src/components/ApplicationSteps.tsx: Named export ApplicationSteps. Renders section with heading "How to Apply" (h2 font-heading text-2xl font-semibold leading-tight text-center mb-12). Steps arranged in a responsive layout: vertical stack on mobile, horizontal row on md+ (grid grid-cols-1 md:grid-cols-4 gap-8 max-w-5xl mx-auto px-4).

    Four steps:
    1. "Visit MSWDO Office" — "Come to the MSWDO office at the Municipal Hall during office hours."
    2. "Submit Requirements" — "Present valid IDs, barangay certificate, and supporting documents for your application."
    3. "Social Worker Interview" — "A licensed social worker will assess your needs and determine eligibility for services."
    4. "Receive Services" — "Once approved, you will receive the appropriate assistance and support."

    Each step: flex flex-col items-center text-center gap-3. Step number: w-12 h-12 rounded-full bg-accent/10 text-accent flex items-center justify-center font-heading font-bold text-lg. Step title: font-heading font-semibold text-lg. Step description: text-muted-foreground text-base.

    Between steps on desktop (md+): optional connecting line/arrow using a hidden md:block CSS element (a short horizontal line after each step except the last, using after:content-['→'] or a border element between grid cells).

    Create src/components/TeamSection.tsx: Named export TeamSection. Renders section with heading "Our Team" (h2 font-heading text-2xl font-semibold leading-tight text-center mb-8). Grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 max-w-4xl mx-auto px-4.

    Officer data (placeholder per D-04, static array):
    - "MSWDO Officer" / "OIC - MSWDO" — initials "MO"
    - "Senior Social Worker" / "Team Leader" — initials "SS"
    - "Administrative Staff" / "Admin Support" — initials "AS"
    - "Social Worker" / "Field Worker" — initials "SW"
    - "Community Organizer" / "Extension Worker" — initials "CO"
    - "Records Officer" / "Documentation" — initials "RO"

    Each card: Card with CardContent className="flex flex-col items-center text-center p-6 gap-3". Avatar with AvatarFallback for initials (text-xs font-medium bg-muted text-foreground). Officer name (font-heading font-semibold). Position (text-sm text-muted-foreground).

    Create src/components/ContactInfo.tsx: Named export ContactInfo. Renders a display block with contact details. div className="space-y-4". Each item flex gap-3 items-start:
    - MapPin icon + "Municipal Social Welfare and Development Office\nNorzagaray, Bulacan"
    - Phone icon + "(044) XXX-XXXX"
    - Mail icon + "mswdo@norzagaray.gov.ph"
    Icons in text-accent mt-0.5. Text in text-muted-foreground. Import MapPin, Phone, Mail from lucide-react.

    Create tests/components/PublicHeader.test.tsx: Using vitest + testing-library pattern from AccessCard.test.tsx.
    ```
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { BrowserRouter } from 'react-router-dom';
    import { PublicHeader } from '../../src/components/PublicHeader';

    vi.mock('../../src/lib/auth-context', () => ({
      useAuth: vi.fn(),
    }));

    import { useAuth } from '../../src/lib/auth-context';
    const mockUseAuth = useAuth as ReturnType<typeof vi.fn>;

    describe('PublicHeader', () => {
      it('shows Login button when not authenticated', () => {
        mockUseAuth.mockReturnValue({ user: null, loading: false });
        render(<BrowserRouter><PublicHeader /></BrowserRouter>);
        expect(screen.getByText('Login')).toBeTruthy();
      });

      it('shows Go to Dashboard when authenticated', () => {
        mockUseAuth.mockReturnValue({ user: { role: 'social_worker' }, loading: false });
        render(<BrowserRouter><PublicHeader /></BrowserRouter>);
        expect(screen.getByText('Go to Dashboard')).toBeTruthy();
      });

      it('renders empty header bar when loading', () => {
        mockUseAuth.mockReturnValue({ user: null, loading: true });
        const { container } = render(<BrowserRouter><PublicHeader /></BrowserRouter>);
        // Should not render Login or Go to Dashboard text
        expect(container.querySelector('header')).toBeTruthy();
      });

      it('renders nav links', () => {
        mockUseAuth.mockReturnValue({ user: null, loading: false });
        render(<BrowserRouter><PublicHeader /></BrowserRouter>);
        expect(screen.getByText('Home')).toBeTruthy();
        expect(screen.getByText('About')).toBeTruthy();
        expect(screen.getByText('Contact')).toBeTruthy();
      });
    });
    ```

    Create tests/layouts/PublicLayout.test.tsx:
    ```
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { PublicLayout } from '../../src/components/PublicLayout';

    vi.mock('../../src/lib/auth-context', () => ({
      useAuth: vi.fn(() => ({ user: null, loading: false })),
    }));

    // Mock child components since they have their own auth dependencies
    vi.mock('../../src/components/PublicHeader', () => ({
      PublicHeader: () => <div data-testid="mock-header">Header</div>,
    }));
    vi.mock('../../src/components/PublicFooter', () => ({
      PublicFooter: () => <div data-testid="mock-footer">Footer</div>,
    }));

    describe('PublicLayout', () => {
      it('renders header and footer', () => {
        render(
          <MemoryRouter>
            <PublicLayout />
          </MemoryRouter>
        );
        expect(screen.getByTestId('mock-header')).toBeTruthy();
        expect(screen.getByTestId('mock-footer')).toBeTruthy();
      });

      it('contains skip-to-content link', () => {
        render(
          <MemoryRouter>
            <PublicLayout />
          </MemoryRouter>
        );
        expect(screen.getByText('Skip to content')).toBeTruthy();
      });

      it('renders main content area with id main-content', () => {
        render(
          <MemoryRouter>
            <PublicLayout />
          </MemoryRouter>
        );
        expect(screen.getByRole('main')).toBeTruthy();
      });
    });
    ```
    Ensure test files create the parent directories (tests/components/ and tests/layouts/) if they don't exist (mkdir -p).
  </action>
  <verify>
    <automated>
      cd kapwa-client && npx vitest run --reporter=verbose tests/components/PublicHeader.test.tsx tests/layouts/PublicLayout.test.tsx 2>&1
    </automated>
  </verify>
  <done>
    - ServicesGrid renders 6 service cards in a responsive grid with lucide icons
    - ApplicationSteps renders 4 steps in numbered circles with title and description
    - TeamSection renders 6 officer placeholder cards with AvatarFallback initials
    - ContactInfo renders address, phone, email with lucide icons
    - PublicHeader.test.tsx passes 4 tests (unauthenticated, authenticated, loading, nav links)
    - PublicLayout.test.tsx passes 3 tests (header/footer, skip-to-content, main)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local | Unverified package installation crosses trust boundary |
| Client-side auth state → PublicHeader | Auth state derived from localStorage token, no server validation on public pages (by design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | npm install | mitigate | checkpoint:human-verify for [SUS] react-hook-form before npm install |
| T-09-02 | Spoofing | PublicHeader auth toggle | mitigate | Render null until loading=false to prevent flash of incorrect auth state |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint at Task 1 for [SUS] react-hook-form |
</threat_model>

<verification>
- `npm run test:run` passes without import errors
- Tests/components/PublicHeader.test.tsx all green
- Tests/layouts/PublicLayout.test.tsx all green
- node -e verifies that all new component files export named symbols
</verification>

<success_criteria>
- Packages installed and verified
- PublicLayout, PublicHeader, PublicFooter created and rendering correctly
- Routes restructured: public routes in PublicLayout, dashboard at /dashboard, LandingPageRedirect at /
- ProtectedRoute updated with role-based redirect map
- ServicesGrid, ApplicationSteps, TeamSection, ContactInfo components created
- Test files created and passing
</success_criteria>

<output>
Create .planning/phases/09-landing-page-auth-flow/09-01-SUMMARY.md when done
</output>
