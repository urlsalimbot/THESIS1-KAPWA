---
phase: 09-landing-page-auth-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - kapwa-client/src/pages/LandingPage.tsx
  - kapwa-client/src/pages/AboutPage.tsx
  - kapwa-client/src/pages/ContactPage.tsx
  - tests/pages/LandingPage.test.tsx
  - tests/pages/AboutPage.test.tsx
autonomous: true
requirements:
  - PUB-01
  - PUB-02
user_setup: []

must_haves:
  truths:
    - "Visitor at / sees hero section with MSWDO branding, CTA button, services card grid, application steps timeline, about summary, and contact info"
    - "Visitor at /about sees mission statement, team placeholder profiles, and program listings"
    - "Visitor at /contact sees a form with name, email, message fields with validation errors displayed inline"
    - "Contact form submission shows success toast and resets fields"
    - "All sections use design tokens (--color-*) and shadcn components"
  artifacts:
    - path: kapwa-client/src/pages/LandingPage.tsx
      provides: "Full landing page with 5 sections (hero, services, steps, about, contact)"
      exports: ["LandingPage"]
      min_lines: 120
    - path: kapwa-client/src/pages/AboutPage.tsx
      provides: "About page with mission, team, programs"
      exports: ["AboutPage"]
      min_lines: 60
    - path: kapwa-client/src/pages/ContactPage.tsx
      provides: "Contact form page (name, email, message) with validation + toast feedback"
      exports: ["ContactPage"]
      min_lines: 80
    - path: tests/pages/LandingPage.test.tsx
      provides: "Rendering smoke tests for landing page sections"
    - path: tests/pages/AboutPage.test.tsx
      provides: "Rendering smoke tests for about page"
  key_links:
    - from: "LandingPage"
      to: "components/ServicesGrid"
      via: "Imports <ServicesGrid /> in services section"
    - from: "LandingPage"
      to: "components/ApplicationSteps"
      via: "Imports <ApplicationSteps /> in steps section"
    - from: "LandingPage"
      to: "components/ContactInfo"
      via: "Imports <ContactInfo /> in contact section"
    - from: "AboutPage"
      to: "components/TeamSection"
      via: "Imports <TeamSection /> in team section"
    - from: "ContactPage"
      to: "sonner"
      via: "toast.success / toast.error for form feedback"
---

<objective>
Public content pages: LandingPage (hero, services grid, steps timeline, about summary, contact info), AboutPage (mission, team profiles, programs), and ContactPage (simple form with client-side toast feedback).

Purpose: Deliver PUB-01 and PUB-02 — the two primary public-facing pages visitors see when they first encounter the Kapwa platform.
Output: LandingPage, AboutPage, ContactPage, and their test files.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-landing-page-auth-flow/09-CONTEXT.md
@.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md
@.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md
@.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md
@kapwa-client/src/components/ServicesGrid.tsx
@kapwa-client/src/components/ApplicationSteps.tsx
@kapwa-client/src/components/TeamSection.tsx
@kapwa-client/src/components/ContactInfo.tsx
@kapwa-client/src/components/ui/button.tsx
@kapwa-client/src/components/ui/card.tsx
@kapwa-client/src/components/ui/textarea.tsx
@kapwa-client/src/components/ui/label.tsx
@kapwa-client/src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LandingPage with all 5 sections (hero, services, steps, about summary, contact info)</name>
  <files>kapwa-client/src/pages/LandingPage.tsx</files>
  <read_first>
    @.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md (Full landing page layout diagram, copywriting contract, hero section, services card grid, application steps sections)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (DashboardPage section-based pattern, Card structure)
    @kapwa-client/src/components/ServicesGrid.tsx
    @kapwa-client/src/components/ApplicationSteps.tsx
    @kapwa-client/src/components/ContactInfo.tsx
  </read_first>
  <action>
    Named export LandingPage. A single scroll page with 5 sections, each as a <section> element with appropriate id for anchor scrolling.

    Structure (top to bottom):

    1. Hero Section (id="hero", py-16 md:py-24 text-center):
       - Section centered with max-w-4xl mx-auto px-4
       - Optional small decorative icon/seal (use Shield icon from lucide, className="w-12 h-12 text-accent mx-auto mb-6")
       - h1: "MSWDO Norzagaray" — font-heading text-3xl md:text-4xl font-bold leading-tight text-foreground mb-4
       - p: "Empowering communities through compassionate social welfare services — accessible to every resident of Norzagaray, Bulacan." — text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto mb-8
       - Two CTA buttons side by side (flex gap-4 justify-center flex-wrap):
         a) Primary: Button with className "bg-accent text-accent-foreground hover:bg-accent/90 h-11 px-8 text-base font-semibold rounded-md" — scrolls to #services (onClick -> document.getElementById('services')?.scrollIntoView({ behavior: 'smooth' }))
         b) Secondary: Button variant="outline" asChild with <Link to="/about">Learn More</Link>
       - Import Link from react-router-dom, Button from @/components/ui/button
       - Import Shield from lucide-react for the icon

    2. Services Section (id="services", py-16 md:py-24 bg-muted/50):
       - Renders <ServicesGrid /> component (already created in Plan 1)

    3. Steps Section (id="steps", py-16 md:py-24):
       - Renders <ApplicationSteps /> component

    4. About Summary Section (id="about", py-16 md:py-24 bg-muted/50):
       - h2: "About MSWDO Norzagaray" — font-heading text-2xl font-semibold text-center mb-4
       - p: Brief professional placeholder about MSWDO's role (2-3 sentences, text-muted-foreground max-w-3xl mx-auto text-center mb-6)
       - "Learn More" link: Button variant="outline" asChild with <Link to="/about">Learn More →</Link>, centered
       - Import Link from react-router-dom

    5. Contact Section (id="contact", py-16 md:py-24):
       - h2: "Get in Touch" — font-heading text-2xl font-semibold text-center mb-8
       - Two-column layout on desktop: grid grid-cols-1 md:grid-cols-2 gap-12 max-w-4xl mx-auto px-4
       - Left column: <ContactInfo /> component
       - Right column: Summary text "We are here to help. Reach out to us during office hours." plus Button variant="outline" asChild with <Link to="/contact">Contact Us →</Link>

    All text content uses professional placeholder copy per D-17. All styling uses design tokens (--color-*, --font-*) via Tailwind classes. No legacy CSS classes used.

    Import location: @/components/ServicesGrid, @/components/ApplicationSteps, @/components/ContactInfo, @/components/ui/button.

    Ensure all section spacing creates visual separation: use py-16 md:py-24 between major sections, alternating bg-background and bg-muted/50 for visual rhythm.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --reporter=verbose src/pages/LandingPage.test.tsx 2>&1 || echo "Test file created in Task 3"</automated>
  </verify>
  <done>
    - LandingPage has all 5 sections: hero, services (ServicesGrid), steps (ApplicationSteps), about summary, contact info
    - Hero section contains MSWDO heading, subheading text, Access Services CTA, Learn More button
    - Services grid renders 6 service cards
    - Application steps renders 4 step indicators
    - About section has summary text + link to /about
    - Contact section has ContactInfo component + link to /contact
    - No legacy CSS classes used — only Tailwind + shadcn
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AboutPage and ContactPage</name>
  <files>kapwa-client/src/pages/AboutPage.tsx, kapwa-client/src/pages/ContactPage.tsx</files>
  <read_first>
    @.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md (About page layout, Contact form form validation rules, copywriting contract)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (shadcn form pattern from RESEARCH, ContactForm pattern)
    @kapwa-client/src/components/TeamSection.tsx
    @kapwa-client/src/components/ui/input.tsx
    @kapwa-client/src/components/ui/textarea.tsx
    @kapwa-client/src/components/ui/label.tsx
    @kapwa-client/src/components/ui/button.tsx
    @kapwa-client/src/components/ui/card.tsx
    @kapwa-client/src/components/ui/separator.tsx
  </read_first>
  <action>
    --- AboutPage.tsx ---
    Named export AboutPage. Sections:

    1. Hero Header (py-16 md:py-24 bg-muted/50 text-center):
       - h1: "About MSWDO Norzagaray" — font-heading text-3xl md:text-4xl font-bold leading-tight
       - p: Brief subtitle "Serving the community of Norzagaray with dedication and compassion."

    2. Mission Section (py-16 md:py-24, max-w-3xl mx-auto px-4):
       - h2: "Our Mission" — font-heading text-2xl font-semibold mb-6 text-center
       - p: Professional placeholder mission statement (3-4 sentences about MSWDO commitment to social welfare, community service, and vulnerable populations). text-base text-muted-foreground leading-relaxed
       - Import Separator and use at bottom for visual break

    3. Team Section (py-16 md:py-24 bg-muted/50):
       - Renders <TeamSection /> component

    4. Programs Section (py-16 md:py-24, max-w-4xl mx-auto px-4):
       - h2: "Our Programs" — font-heading text-2xl font-semibold mb-8 text-center
       - Program cards in grid grid-cols-1 md:grid-cols-2 gap-6
       - Static program data (placeholder per D-17):
         a) "Social Pension" — "Monthly stipend for senior citizens aged 60 and above."
         b) "Medical Assistance" — "Financial support for medical treatment and hospitalization."
         c) "Burial Assistance" — "Financial aid for funeral expenses of qualified residents."
         d) "Educational Assistance" — "Scholarship and educational support for deserving students."
         e) "Livelihood Programs" — "Skills training and seed capital for small business startups."
         f) "Crisis Intervention" — "Emergency assistance for individuals and families in crisis."
       - Each as Card with CardHeader + CardTitle (font-heading font-semibold) + CardContent (text-muted-foreground)
       - Icons: Heart, Stethoscope, Cross, GraduationCap, Briefcase, ShieldAlert from lucide-react
       - Add hover:border-accent/30 hover:shadow-md transition-all duration-200 on cards

    --- ContactPage.tsx ---
    Named export ContactPage. Centered layout with two-column grid on desktop (grid grid-cols-1 md:grid-cols-2 gap-12 max-w-4xl mx-auto px-4 py-16 md:py-24).

    Left column: Contact information
    - h1: "Get in Touch" — font-heading text-3xl font-bold mb-6
    - <ContactInfo /> component
    - Office hours: "Office Hours: Monday to Friday, 8:00 AM — 5:00 PM"

    Right column: Contact form
    - Simple form using useState (no RHF - per RESEARCH recommendation for simple client-only form):
      - Import useState from react
      - State: name, email, message, errors object, submitting boolean
      - Validation: name required min 2 chars, email valid format, message required min 10 chars
      - Error display: p className="text-sm text-destructive mt-1" below each field
      - Fields: Input type="text" for Name, Input type="email" for Email, Textarea for Message
        Each field has Label above it (Label from @/components/ui/label)
      - Submit button: Button type="submit" with className "bg-accent text-accent-foreground hover:bg-accent/90 w-full"
      - On submit: validate, then simulate 1s delay, then toast.success("Message sent! We'll respond within 1-2 business days."), reset fields
      - On error: toast.error("Message failed to send. Please try again or call us directly.")
      - Import toast from sonner
    - Form uses space-y-4 for field spacing
    - Ensure each form field matches the UI-SPEC copywriting contract exactly (labels, placeholders, error messages)

    Both files: named exports, no default exports. No legacy CSS classes.
  </action>
  <verify>
    <automated>
      # Verify files compile without import errors
      cd kapwa-client && npx tsc --noEmit --pretty 2>&1 | head -20
    </automated>
  </verify>
  <done>
    - AboutPage renders hero header, mission section, TeamSection, program cards grid with lucide icons
    - AboutPage team section uses TeamSection component
    - ContactPage renders two-column layout with contact info and form
    - Contact form validates name, email, message with inline errors
    - Contact form shows toast.success on submit, toast.error on failure
    - All copy matches UI-SPEC copywriting contract
    - No legacy CSS classes used
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests for LandingPage and AboutPage</name>
  <files>tests/pages/LandingPage.test.tsx, tests/pages/AboutPage.test.tsx</files>
  <read_first>
    @kapwa-client/src/components/cards/AccessCard.test.tsx (test pattern reference)
  </read_first>
  <action>
    Create tests/pages/LandingPage.test.tsx:
    ```
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { BrowserRouter } from 'react-router-dom';
    import { LandingPage } from '../../src/pages/LandingPage';

    describe('LandingPage', () => {
      it('renders hero heading', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText('MSWDO Norzagaray')).toBeTruthy();
      });

      it('renders hero subheading', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText(/Empowering communities through compassionate/i)).toBeTruthy();
      });

      it('renders Access Services CTA button', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText('Access Services')).toBeTruthy();
      });

      it('renders Learn More button', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText('Learn More')).toBeTruthy();
      });

      it('renders services section heading', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText('Our Services')).toBeTruthy();
      });

      it('renders application steps heading', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText('How to Apply')).toBeTruthy();
      });

      it('renders about section heading', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText(/About MSWDO Norzagaray/i)).toBeTruthy();
      });

      it('renders contact section heading', () => {
        render(<BrowserRouter><LandingPage /></BrowserRouter>);
        expect(screen.getByText('Get in Touch')).toBeTruthy();
      });
    });
    ```

    Create tests/pages/AboutPage.test.tsx:
    ```
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { BrowserRouter } from 'react-router-dom';
    import { AboutPage } from '../../src/pages/AboutPage';

    describe('AboutPage', () => {
      it('renders about heading', () => {
        render(<BrowserRouter><AboutPage /></BrowserRouter>);
        expect(screen.getByText(/About MSWDO Norzagaray/i)).toBeTruthy();
      });

      it('renders mission heading', () => {
        render(<BrowserRouter><AboutPage /></BrowserRouter>);
        expect(screen.getByText('Our Mission')).toBeTruthy();
      });

      it('renders programs heading', () => {
        render(<BrowserRouter><AboutPage /></BrowserRouter>);
        expect(screen.getByText('Our Programs')).toBeTruthy();
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd kapwa-client && npx vitest run --reporter=verbose tests/pages/LandingPage.test.tsx tests/pages/AboutPage.test.tsx 2>&1
    </automated>
  </verify>
  <done>
    - LandingPage.test.tsx passes 8 tests covering all page sections
    - AboutPage.test.tsx passes 3 tests covering heading, mission, and programs
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side form → sonner toast | Contact form data processed entirely client-side, no API call |
| User input → form validation | Untrusted input parsed through validation rules before acceptance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Contact form input | accept | Client-side only processing (no API). Simple useState validation prevents empty/malformed submissions from reaching toast feedback logic. |
| T-09-02 | Spoofing | Contact form fields | accept | React's default JSX escaping handles safe rendering. Form data never rendered raw back to user. |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose tests/pages/LandingPage.test.tsx tests/pages/AboutPage.test.tsx` passes
- `npx tsc --noEmit --pretty` has no import errors (as long as routes.tsx was updated in Plan 1)
</verification>

<success_criteria>
- LandingPage renders all 5 sections with correct placeholder content
- AboutPage renders mission, team cards, and programs grid
- ContactPage renders contact info and validated form with toast feedback
- All tests pass
- No legacy CSS classes used — only Tailwind + shadcn + design tokens
</success_criteria>

<output>
Create .planning/phases/09-landing-page-auth-flow/09-02-SUMMARY.md when done
</output>
