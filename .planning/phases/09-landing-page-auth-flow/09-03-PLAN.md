---
phase: 09-landing-page-auth-flow
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - kapwa-client/src/pages/LoginPage.tsx
  - kapwa-client/src/pages/RegisterPage.tsx
  - tests/pages/LoginPage.test.tsx
  - tests/pages/RegisterPage.test.tsx
autonomous: true
requirements:
  - PUB-03
  - PUB-04
user_setup: []

must_haves:
  truths:
    - "User can log in with email/password via a polished shadcn form with inline validation errors"
    - "After login, user is redirected to role-specific dashboard (social_worker → /dashboard, admin → /admin, etc.)"
    - "MFA challenge screen appears when mfaRequired is true, showing 6-digit code input"
    - "Invalid credentials show generic error: 'Invalid email or password. Please try again.'"
    - "Claimant can self-register with all required fields and is auto-logged-in on success"
    - "'Register as claimant' link appears below login form"
    - "Login form uses the PublicLayout shell (consistent with other public pages)"
  artifacts:
    - path: kapwa-client/src/pages/LoginPage.tsx
      provides: "shadcn-ified login form with RHF + Zod validation, MFA flow, role-based redirect"
      exports: ["LoginPage"]
      min_lines: 120
    - path: kapwa-client/src/pages/RegisterPage.tsx
      provides: "Claimant self-registration with RHF + Zod, auto-login, redirect to /my-dashboard"
      exports: ["RegisterPage"]
      min_lines: 140
    - path: tests/pages/LoginPage.test.tsx
      provides: "Form validation and behavior tests for login"
    - path: tests/pages/RegisterPage.test.tsx
      provides: "Form validation tests for registration"
  key_links:
    - from: "LoginPage"
      to: "lib/auth-context"
      via: "useAuth().login(), useAuth().resolveMfa(), useAuth().mfaChallenge"
      pattern: "useAuth\\(\\)"
    - from: "LoginPage"
      to: "react-router-dom"
      via: "useNavigate for role-based redirect after login"
      pattern: "useNavigate"
    - from: "RegisterPage"
      to: "lib/auth-context"
      via: "useAuth().login() for auto-login after registration"
      pattern: "useAuth\\(\\)"
---

<objective>
Auth pages: shadcn-ified LoginPage with RHF + Zod validation, role-aware redirect, MFA flow, and Claimant Registration page with auto-login.

Purpose: Deliver PUB-03 (polished login with role-aware redirect) and PUB-04 (claimant self-registration entry point).
Output: Rewritten LoginPage.tsx, new RegisterPage.tsx, and their test files.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-landing-page-auth-flow/09-CONTEXT.md
@.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md
@.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md
@.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md
@kapwa-client/src/pages/LoginPage.tsx (current — to be replaced)
@kapwa-client/src/lib/auth-context.tsx
@kapwa-client/src/components/ui/form.tsx
@kapwa-client/src/components/ui/input.tsx
@kapwa-client/src/components/ui/label.tsx
@kapwa-client/src/components/ui/button.tsx
@kapwa-client/src/components/ui/card.tsx
@kapwa-client/src/components/ui/select.tsx
@kapwa-client/src/components/ui/avatar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite LoginPage with shadcn components, RHF + Zod validation, role-aware redirect, and MFA flow</name>
  <files>kapwa-client/src/pages/LoginPage.tsx</files>
  <read_first>
    @kapwa-client/src/pages/LoginPage.tsx (current — preserve auth flow, MFA logic, import structure)
    @kapwa-client/src/lib/auth-context.tsx (AuthContextType: login, resolveMfa, mfaChallenge, cancelMfa — see PATTERNS.md lines 470-490)
    @.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md (Login page layout diagram, interaction states, form validation rules, copywriting)
    @.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md (RHF + Zod pattern, roleRedirectMap, MFA screen, anti-patterns)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (LoginForm pattern with RHF, error display with shadcn Alert, submit button loading)
    @kapwa-client/src/components/ui/form.tsx (Form, FormField, FormItem, FormLabel, FormControl, FormMessage)
  </read_first>
  <behavior>
    - Login form validates email (required, valid format) and password (required) via Zod schema
    - Login calls auth-context login() and on success checks mfaRequired
    - When mfaRequired: show MFA screen with 6-digit code input (numeric only)
    - MFA screen: resolveMfa on submit, cancelMfa on cancel
    - On successful login (no MFA or MFA verified): redirect to role-specific route per D-10
    - On error: show generic "Invalid email or password. Please try again." in alert-like error div
    - Error message does not reveal which field is wrong (anti-enumeration per UI-SPEC)
    - Submit button shows spinner while loading: Loader2 animate-spin
    - Form auto-focuses email field on mount
  </behavior>
  <action>
    Full rewrite of src/pages/LoginPage.tsx. Named export LoginPage.

    **Architecture notes:** The component renders inside PublicLayout (set up in Plan 1). No layout wrapper needed — just the centered card. Uses react-hook-form + zod + shadcn Form. Preserves existing auth flow from current LoginPage.tsx.

    **Schema:**
    ```
    const loginSchema = z.object({
      email: z.string().email('Please enter a valid email address.'),
      password: z.string().min(1, 'Please enter your password.'),
    });
    type LoginValues = z.infer<typeof loginSchema>;
    ```

    **State:**
    - RHF form (useForm with zodResolver)
    - error string (for API error display)
    - Use useAuth() for login, resolveMfa, cancelMfa, mfaChallenge
    - Use useNavigate() for redirect

    **Render — two modes:**
    
    Mode A — MFA Challenge (when mfaChallenge is truthy):
    - Centered card layout: div className="flex items-center justify-center min-h-[calc(100vh-8rem)] px-4"
    - Card (w-full max-w-md mx-auto)
    - CardHeader: Avatar with AvatarFallback "K" (w-16 h-16 mx-auto), CardTitle "Two-Factor Authentication", CardDescription "Enter the verification code from your authenticator app."
    - CardContent: form
      - MFA input: Input with text-center tracking-[0.25em] text-2xl h-12, placeholder "000000", maxLength 6, onChange strips non-digits
      - Verify button: disabled until value.length === 6, shows Loader2 spinner when submitting
      - Error div: bg-destructive/10 text-destructive text-sm p-3 rounded-md role="alert"
    - CardFooter: Cancel button (Button variant="ghost") calls cancelMfa()

    Mode B — Login Form:
    - Centered card layout: div className="flex items-center justify-center min-h-[calc(100vh-8rem)] px-4"
    - Card (w-full max-w-md mx-auto)
    - CardHeader: Avatar with AvatarFallback "K" (w-16 h-16 mx-auto), CardTitle "KAPWA" text-center, CardDescription "MSWDO Norzagaray Social Welfare System" text-center
    - CardContent:
      - Error div (when error string non-empty): bg-destructive/10 text-destructive text-sm p-3 rounded-md role="alert" (do NOT use shadcn Alert — use plain div to avoid import complexity)
      - Form with FormProvider from shadcn:
        - FormField for email: FormItem, FormLabel "Email", FormControl with Input type="email" placeholder "Email", FormMessage
        - FormField for password: FormItem, FormLabel "Password", FormControl with Input type="password" placeholder "Password", FormMessage
      - Submit Button: className="w-full bg-accent text-accent-foreground hover:bg-accent/90", disabled={formState.isSubmitting}
        - Shows Loader2 icon (animate-spin) when submitting + "Sign In" text
    - CardFooter: Button variant="link" asChild with <Link to="/register">Register as claimant</Link> (centered, per D-13)

    **Submit handler:**
    ```
    async function onSubmit(values: LoginValues) {
      setError('');
      try {
        const result = await login(values.email, values.password);
        if (!result?.mfaRequired) {
          const redirectPath = roleRedirectMap[user.role] || '/dashboard';
          navigate(redirectPath, { replace: true });
        }
        // If mfaRequired, auth-context sets mfaChallenge state — component re-renders in MFA mode
      } catch (err: any) {
        setError('Invalid email or password. Please try again.');
      }
    }
    ```

    **Role redirect map** (shared constant at top of file or inside component):
    ```
    const roleRedirectMap: Record<string, string> = {
      social_worker: '/dashboard',
      admin: '/admin',
      coordinator: '/coordinator',
      claimant: '/my-dashboard',
      mayor: '/reports',
      auditor: '/audit-logs',
    };
    ```
    (Maps match D-10 exactly.)

    **Imports:**
    ```
    import { useState } from 'react';
    import { useNavigate, Link } from 'react-router-dom';
    import { useAuth } from '@/lib/auth-context';
    import { useForm } from 'react-hook-form';
    import { z } from 'zod';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form';
    import { Input } from '@/components/ui/input';
    import { Button } from '@/components/ui/button';
    import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card';
    import { Avatar, AvatarFallback } from '@/components/ui/avatar';
    import { Loader2 } from 'lucide-react';
    ```

    **Preserve from existing LoginPage.tsx:** The login/resolveMfa API calls, mfaChallenge detection, MFA cancel flow, localStorage token pattern. The only behavioral changes are: (1) use RHF instead of useState for form fields, (2) use role-based redirect instead of navigate('/'), (3) use shadcn components instead of legacy CSS classes, (4) add "Register as claimant" link in CardFooter.
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --reporter=verbose tests/pages/LoginPage.test.tsx 2>&1</automated>
  </verify>
  <done>
    - LoginPage renders centered Card with Avatar, CardTitle "KAPWA", CardDescription
    - Email field validates with Zod (required, email format) — shows FormMessage on invalid
    - Password field validates with Zod (required) — shows FormMessage on empty
    - Submit button shows Loader2 spinner during submission
    - Error display shows generic "Invalid email or password" in destuctive-styled div
    - "Register as claimant" link in CardFooter links to /register
    - When mfaChallenge exists, switches to MFA screen with 6-digit code input
    - MFA verify button disabled until 6 digits entered
    - MFA cancel button returns to login form
    - On successful login (no MFA): navigates to role-specific route per D-10
    - On MFA success: navigates to role-specific route per D-10
    - No legacy CSS classes (form-input, form-label, btn, btn-primary, login-card, error-msg)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RegisterPage with claimant self-registration, auto-login, and redirect</name>
  <files>kapwa-client/src/pages/RegisterPage.tsx</files>
  <read_first>
    @.planning/phases/09-landing-page-auth-flow/09-UI-SPEC.md (Registration page title, form validation rules, interaction states, copywriting)
    @.planning/phases/09-landing-page-auth-flow/09-RESEARCH.md (RegisterPage schema, RHF form pattern, auto-login flow, registration API contract)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (RegisterForm pattern with RHF, auto-login redirect)
    @kapwa-client/src/lib/auth-context.tsx (login() signature for auto-login)
    @kapwa-client/src/components/ui/form.tsx (Form, FormField, FormItem, FormLabel, FormControl, FormMessage)
    @kapwa-client/src/components/ui/select.tsx (Select, SelectTrigger, SelectValue, SelectContent, SelectItem)
  </read_first>
  <behavior>
    - Registration form validates all 7 fields via Zod schema per UI-SPEC validation rules
    - Password confirmation must match password
    - Barangay rendered as shadcn Select with all Norzagaray barangays
    - Date of Birth rendered as Input type="date" with age ≥ 18 validation
    - On submit: POST to /api/auth/register, on success auto-login via auth-context login()
    - Auto-login success: navigate to /my-dashboard (per D-15)
    - On error: toast.error with generic registration failure message
    - Submit button shows Loader2 spinner while submitting
  </behavior>
  <action>
    Named export RegisterPage at src/pages/RegisterPage.tsx.

    **Schemas:**
    ```
    const barangays = [
      'Bangkal', 'Binitagan', 'Bitungol', 'Matictic', 'Maturanoc',
      'Minantok', 'Palayan', 'Pandawan', 'Pinagtulayan', 'Poblacion',
      'San Mateo', 'Tigbe',
    ];

    const registerSchema = z.object({
      fullName: z.string().min(2, 'Please enter your full name.')
        .regex(/^[A-Za-z\s]+$/, 'Letters and spaces only.'),
      email: z.string().email('Please enter a valid email address.'),
      phone: z.string().regex(/^09\d{2}\s?\d{3}\s?\d{4}$/, 'Please enter a valid phone number.'),
      password: z.string().min(8, 'Password must be at least 8 characters.'),
      confirmPassword: z.string(),
      barangay: z.string().min(1, 'Please select your barangay.'),
      dateOfBirth: z.string().refine((val) => {
        const age = Math.floor((Date.now() - new Date(val).getTime()) / 31557600000);
        return age >= 18;
      }, 'You must be at least 18 years old.'),
    }).refine((data) => data.password === data.confirmPassword, {
      message: 'Passwords do not match.',
      path: ['confirmPassword'],
    });

    type RegisterValues = z.infer<typeof registerSchema>;
    ```

    **Setup:**
    - useForm with zodResolver(registerSchema), defaultValues for all fields as empty strings
    - useAuth() for login()
    - useState for serverError string
    - toast from sonner for success/error feedback

    **Layout:** Centered card layout: flex items-center justify-center min-h-[calc(100vh-8rem)] px-4 py-8
    - Card w-full max-w-lg mx-auto (slightly wider than login to fit fields)

    **CardHeader:**
    - CardTitle "Claimant Registration" text-center font-heading
    - CardDescription "Create an account to track your services and applications." text-center

    **CardContent:** Form with FormProvider

    Fields (each as FormField with FormItem, FormLabel, FormControl, FormMessage). Fields ordered:
    1. fullName — Input placeholder "Full Name"
    2. email — Input type="email" placeholder "Email"
    3. phone — Input type="tel" placeholder "09XX XXX XXXX"
    4. password — Input type="password" placeholder "Password"
    5. confirmPassword — Input type="password" placeholder "Confirm Password"
    6. barangay — shadcn Select with SelectTrigger, SelectValue placeholder "Select barangay", SelectContent with SelectItem for each barangay
    7. dateOfBirth — Input type="date"

    Submit Button: className="w-full bg-accent text-accent-foreground hover:bg-accent/90", disabled while submitting
    Shows Loader2 animate-spin when submitting + "Create Account"

    **Submit handler:**
    ```
    async function onSubmit(values: RegisterValues) {
      try {
        // POST to registration API
        const res = await fetch('/api/auth/register', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            fullName: values.fullName,
            email: values.email,
            phone: values.phone,
            password: values.password,
            barangay: values.barangay,
            dateOfBirth: values.dateOfBirth,
          }),
        });

        if (!res.ok) throw new Error('Registration failed');

        // Auto-login after successful registration
        await login(values.email, values.password);
        navigate('/my-dashboard', { replace: true });
      } catch (err) {
        toast.error('Registration failed. Please check your information and try again.');
      }
    }
    ```
    Note: The registration API may not exist yet (RESEARCH.md Pitfall 4). If the fetch fails, the catch handler shows a toast error. The form design assumes POST /api/auth/register with JSON body and standard REST response.

    **CardFooter:** Button variant="link" asChild with <Link to="/login">Already have an account? Sign in</Link> (centered)

    **Imports:**
    ```
    import { useState } from 'react';
    import { useNavigate, Link } from 'react-router-dom';
    import { useAuth } from '@/lib/auth-context';
    import { useForm } from 'react-hook-form';
    import { z } from 'zod';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form';
    import { Input } from '@/components/ui/input';
    import { Button } from '@/components/ui/button';
    import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card';
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
    import { toast } from 'sonner';
    import { Loader2 } from 'lucide-react';
    ```

    **Key details:**
    - Touch targets: all form controls use h-11 on mobile (responsive: h-11 md:h-10) per UI-SPEC spacing exceptions
    - Form spacing: fields in grid grid-cols-1 md:grid-cols-2 gap-4 for fullName + email row, phone + password row, confirmPassword + barangay row, dateOfBirth full width
    - All Labels use font-body text-sm font-medium leading-tight
    - FormMessage renders in text-sm text-destructive
    - No legacy CSS classes anywhere in this file
  </action>
  <verify>
    <automated>cd kapwa-client && npx vitest run --reporter=verbose tests/pages/RegisterPage.test.tsx 2>&1</automated>
  </verify>
  <done>
    - RegisterPage renders centered Card with "Claimant Registration" title
    - 7 form fields rendered: fullName, email, phone, password, confirmPassword, barangay (Select), dateOfBirth (Input type="date")
    - All fields validate with Zod per UI-SPEC validation rules
    - Password confirmation mismatch shows "Passwords do not match" error on confirmPassword field
    - Barangay Select shows all 12 Norzagaray barangays
    - Date of Birth validates age ≥ 18
    - Submit shows Loader2 spinner during submission
    - On success: auto-login via auth-context login() + navigate to /my-dashboard
    - On error: toast.error with generic message
    - "Already have an account? Sign in" link in CardFooter links to /login
    - No legacy CSS classes used
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests for LoginPage and RegisterPage</name>
  <files>tests/pages/LoginPage.test.tsx, tests/pages/RegisterPage.test.tsx</files>
  <read_first>
    @kapwa-client/src/components/cards/AccessCard.test.tsx (test pattern)
    @.planning/phases/09-landing-page-auth-flow/09-PATTERNS.md (LoginPage test mock pattern lines 439-461)
  </read_first>
  <action>
    Create tests/pages/LoginPage.test.tsx:
    ```
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { BrowserRouter } from 'react-router-dom';
    import { LoginPage } from '../../src/pages/LoginPage';

    // Mock auth context
    const mockLogin = vi.fn();
    const mockResolveMfa = vi.fn();
    const mockCancelMfa = vi.fn();

    vi.mock('../../src/lib/auth-context', () => ({
      useAuth: vi.fn(() => ({
        user: null,
        token: null,
        login: mockLogin,
        logout: vi.fn(),
        loading: false,
        mfaChallenge: null,
        resolveMfa: mockResolveMfa,
        cancelMfa: mockCancelMfa,
      })),
    }));

    // Mock react-hook-form for test simplicity
    vi.mock('react-hook-form', () => ({
      useForm: vi.fn(() => ({
        register: vi.fn(),
        handleSubmit: vi.fn((cb) => (e: any) => { e?.preventDefault?.(); cb({ email: 'test@test.com', password: 'password' }); }),
        formState: { errors: {}, isSubmitting: false },
        control: {},
        reset: vi.fn(),
      })),
      Controller: vi.fn(),
      useController: vi.fn(),
    }));

    // Mock zodResolver
    vi.mock('@hookform/resolvers/zod', () => ({ zodResolver: vi.fn(() => (data: any) => ({ values: data, errors: {} })) }));

    // Mock shadcn Form components
    vi.mock('../../src/components/ui/form', () => ({
      Form: ({ children }: any) => <div data-testid="form">{children}</div>,
      FormField: ({ render }: any) => render({ field: { value: '', onChange: vi.fn() } }),
      FormItem: ({ children }: any) => <div>{children}</div>,
      FormLabel: ({ children }: any) => <label>{children}</label>,
      FormControl: ({ children }: any) => <div>{children}</div>,
      FormMessage: () => <div data-testid="form-message" />,
    }));

    describe('LoginPage', () => {
      beforeEach(() => {
        vi.clearAllMocks();
      });

      it('renders brand title KAPWA', () => {
        render(<BrowserRouter><LoginPage /></BrowserRouter>);
        expect(screen.getByText('KAPWA')).toBeTruthy();
      });

      it('renders login form with email and password fields', () => {
        render(<BrowserRouter><LoginPage /></BrowserRouter>);
        expect(screen.getByText('Email')).toBeTruthy();
        expect(screen.getByText('Password')).toBeTruthy();
      });

      it('renders submit button with Sign In text', () => {
        render(<BrowserRouter><LoginPage /></BrowserRouter>);
        expect(screen.getByText('Sign In')).toBeTruthy();
      });

      it('renders Register as claimant link', () => {
        render(<BrowserRouter><LoginPage /></BrowserRouter>);
        expect(screen.getByText('Register as claimant')).toBeTruthy();
      });

      it('shows MFA screen when mfaChallenge is set', () => {
        // Override mock for mfaChallenge
        const useAuthModule = require('../../src/lib/auth-context');
        useAuthModule.useAuth.mockReturnValue({
          user: null, token: null, login: mockLogin, logout: vi.fn(),
          loading: false, mfaChallenge: { tempToken: 'abc123' },
          resolveMfa: mockResolveMfa, cancelMfa: mockCancelMfa,
        });
        render(<BrowserRouter><LoginPage /></BrowserRouter>);
        expect(screen.getByText('Two-Factor Authentication')).toBeTruthy();
        expect(screen.getByText('Verify')).toBeTruthy();
      });
    });
    ```

    Create tests/pages/RegisterPage.test.tsx:
    ```
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { BrowserRouter } from 'react-router-dom';
    import { RegisterPage } from '../../src/pages/RegisterPage';

    vi.mock('../../src/lib/auth-context', () => ({
      useAuth: vi.fn(() => ({
        user: null, token: null, login: vi.fn(), logout: vi.fn(),
        loading: false, mfaChallenge: null, resolveMfa: vi.fn(), cancelMfa: vi.fn(),
      })),
    }));

    vi.mock('react-hook-form', () => ({
      useForm: vi.fn(() => ({
        register: vi.fn(),
        handleSubmit: vi.fn((cb) => (e: any) => { e?.preventDefault?.(); cb({}); }),
        formState: { errors: {}, isSubmitting: false },
        control: {},
        reset: vi.fn(),
      })),
      Controller: vi.fn(),
      useController: vi.fn(),
    }));

    vi.mock('@hookform/resolvers/zod', () => ({ zodResolver: vi.fn(() => (data: any) => ({ values: data, errors: {} })) }));

    vi.mock('../../src/components/ui/form', () => ({
      Form: ({ children }: any) => <div data-testid="form">{children}</div>,
      FormField: ({ render }: any) => render({ field: { value: '', onChange: vi.fn() } }),
      FormItem: ({ children }: any) => <div>{children}</div>,
      FormLabel: ({ children }: any) => <label>{children}</label>,
      FormControl: ({ children }: any) => <div>{children}</div>,
      FormMessage: () => <div data-testid="form-message" />,
    }));

    describe('RegisterPage', () => {
      beforeEach(() => { vi.clearAllMocks(); });

      it('renders registration heading', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Claimant Registration')).toBeTruthy();
      });

      it('renders Full Name field', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Full Name')).toBeTruthy();
      });

      it('renders Email field', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Email')).toBeTruthy();
      });

      it('renders Password field', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Password')).toBeTruthy();
      });

      it('renders Barangay field', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Barangay')).toBeTruthy();
      });

      it('renders Date of Birth field', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Date of Birth')).toBeTruthy();
      });

      it('renders Create Account submit button', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText('Create Account')).toBeTruthy();
      });

      it('renders link back to login', () => {
        render(<BrowserRouter><RegisterPage /></BrowserRouter>);
        expect(screen.getByText(/Already have an account/i)).toBeTruthy();
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd kapwa-client && npx vitest run --reporter=verbose tests/pages/LoginPage.test.tsx tests/pages/RegisterPage.test.tsx 2>&1
    </automated>
  </verify>
  <done>
    - LoginPage.test.tsx passes 5 tests: brand title, form fields, submit button, register link, MFA screen
    - RegisterPage.test.tsx passes 8 tests: heading, all field labels, submit button, login link
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client login form → auth-context login() | User credentials cross from UI to auth layer (client-side only in this phase) |
| Client registration form → fetch POST /api/auth/register | Registration data sent to backend API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Login form error handling | mitigate | Generic error message "Invalid email or password. Please try again." — does not reveal whether email exists (mitigates enumeration per UI-SPEC + RESEARCH) |
| T-09-02 | Tampering | Registration form input | mitigate | Zod schema validation before fetch: strips unknown fields, validates types, enforces regex patterns (name: letters+spaces, phone: PH format) |
| T-09-03 | Spoofing | Form field display | accept | React JSX escaping handles XSS. Form errors show static text, not user input reflected raw. |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose tests/pages/LoginPage.test.tsx tests/pages/RegisterPage.test.tsx` passes
- LoginPage renders all form elements correctly with shadcn components
- RegisterPage renders all 7 form fields with proper labels
- No legacy CSS classes (form-input, form-label, btn, btn-primary, login-card, error-msg) present in either file
</verification>

<success_criteria>
- LoginPage fully rewritten with shadcn components (Card, Input, Button, Label, Form)
- LoginPage validates email/password with RHF + Zod
- LoginPage uses role-based redirect map (D-10) instead of hardcoded navigate('/')
- LoginPage shows MFA screen with 6-digit code input when mfaChallenge is active
- LoginPage shows "Register as claimant" link in CardFooter (D-13)
- RegisterPage renders all 7 fields per D-14 with Zod validation
- RegisterPage auto-logins and redirects to /my-dashboard on success (D-15)
- Registration API call handle 404/500 gracefully with toast error
- All tests passing
</success_criteria>

<output>
Create .planning/phases/09-landing-page-auth-flow/09-03-SUMMARY.md when done
</output>
