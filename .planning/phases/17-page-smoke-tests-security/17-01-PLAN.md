---
phase: 17-page-smoke-tests-security
plan: 01
type: infrastructure
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .planning/SECURITY.md
  - kapwa-client/src/lib/api.ts
autonomous: true
requirements: [TST-07, SEC-01]
must_haves:
  truths:
    - "CI workflow runs npm run test:run on every push to main and PR to main"
    - "CI workflow runs npm run coverage:check after tests pass (depends-on test)"
    - "CI workflow runs npm run build in parallel with test job"
    - "Coverage HTML report is downloadable as an artifact from the Actions run page"
    - "SECURITY.md documents the 401 single-flight refresh flow with ASCII sequence diagram"
    - "SECURITY.md documents manual test steps for SEC-01 token rotation verification"
    - "api.ts line 130 (the 401 branch) has a JSDoc comment referencing SECURITY.md"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "3-job CI workflow: test, coverage (with artifact upload), build"
      min_lines: 45
    - path: ".planning/SECURITY.md"
      provides: "Security documentation: auth, token refresh, threat model, manual test steps, reporting"
      min_lines: 80
    - path: "kapwa-client/src/lib/api.ts"
      provides: "JSDoc on the 401 retry branch at line 130"
      contains: "See SECURITY.md"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "kapwa-client/package.json"
      via: "npm run test:run, npm run coverage:check, npm run build scripts"
      pattern: "npm run"
    - from: ".planning/SECURITY.md"
      to: "kapwa-client/src/lib/api.ts"
      via: "Documents refreshToken() and executeWithRetry() at api.ts:80-134"
      pattern: "refreshToken|onUnauthorized"
    - from: "kapwa-client/src/lib/api.ts:130"
      to: ".planning/SECURITY.md"
      via: "JSDoc references SECURITY.md for the full refresh flow"
      pattern: "SECURITY.md"
---

<objective>
Create GitHub Actions CI pipeline with 3 jobs (test → coverage, build parallel) and write complete security documentation (SECURITY.md).

**Purpose:** Establish automated quality gates (CI) and make the existing SEC-01 token refresh interceptor discoverable and verifiable.
**Output:**
- `.github/workflows/ci.yml` — CI workflow file
- `.planning/SECURITY.md` — Security documentation
- `kapwa-client/src/lib/api.ts` — +3-line JSDoc on line 130
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-page-smoke-tests-security/17-CONTEXT.md
@.planning/phases/17-page-smoke-tests-security/17-RESEARCH.md
@.planning/REQUIREMENTS.md
@kapwa-client/src/lib/api.ts
</context>

<tasks>

<task id="17-01-01" type="config" wave="1">
<name>Create CI workflow (.github/workflows/ci.yml)</name>
<files>.github/workflows/ci.yml</files>
<read_first>@.planning/phases/17-page-smoke-tests-security/17-RESEARCH.md (Pattern 1 example, lines 154-209)</read_first>
<action>
Create `.github/workflows/ci.yml` with the following spec. Create the directory `.github/workflows/` if it does not exist (confirmed absent). The workflow YAML must contain:

1. **Name:** "CI", triggers: `push` to `main`, `pull_request` to `main` (per D-01).
2. **Default run:** `working-directory: kapwa-client` for all run steps (per D-02).
3. **Job `test`:** `runs-on: ubuntu-latest`, `timeout-minutes: 10`, steps: checkout@v4 → setup-node@v4 (node-version: 20) → `npm ci` → `npm run test:run`. No npm cache key (per D-03).
4. **Job `coverage`:** `needs: test`, same runner/env, steps: checkout@v4 → setup-node@v4 (node-version: 20) → `npm ci` → `npm run coverage:check`. After coverage check: upload artifact step using `actions/upload-artifact@v4` with `if: always()` (so report is saved even if threshold fails), name: `coverage-report`, path: `kapwa-client/coverage/` (path is relative to repo root, not working-directory — per Pitfall 3), retention-days: 30.
5. **Job `build`:** no `needs` (parallel with `test` and `coverage`), same runner/env, steps: checkout@v4 → setup-node@v4 → `npm ci` → `npm run build`.

All three jobs independently run `npm ci` (no shared node_modules across jobs). All use Node 20. No matrix, no npm cache, no manual dispatch, no schedule triggers (per D-01, D-02, D-03). Use `actions/checkout@v4` and `actions/setup-node@v4` with pinning to the major version tag (not commit SHA — per Research pattern). The exact YAML structure is in RESEARCH.md Pattern 1 (lines 154-209) — follow it verbatim.
</action>
<verify>
<automated>
# Validate YAML syntax
node -e "try { const fs=require('fs'); const y = fs.readFileSync('.github/workflows/ci.yml','utf8'); const lines = y.split('\n').filter(l => l.trim()).length; console.log('CI workflow created: ' + lines + ' non-empty lines'); if (y.includes('working-directory: kapwa-client')) console.log('OK: working-directory is kapwa-client'); if (y.includes('npm run coverage:check')) console.log('OK: coverage job runs coverage:check'); if (y.includes('actions/upload-artifact@v4')) console.log('OK: artifact upload configured'); if (y.includes('kapwa-client/coverage/')) console.log('OK: artifact path is kapwa-client/coverage/'); } catch(e) { console.error('FAIL: ' + e.message); process.exit(1); }"
</automated>
</verify>
<done>
CI workflow YAML is valid syntax, references correct action versions, runs 3 jobs from kapwa-client/ working directory, uploads coverage report as artifact.
</done>
</task>

<task id="17-01-02" type="doc" wave="1">
<name>Write SECURITY.md</name>
<files>.planning/SECURITY.md</files>
<read_first>
@kapwa-client/src/lib/api.ts (lines 80-134)
@kapwa-client/src/lib/auth-context.tsx (lines 36-43)
@kapwa-client/src/lib/auth.ts (lines 37-59)
@.planning/codebase/INTEGRATIONS.md
</read_first>
<action>
Write `.planning/SECURITY.md` covering these sections (per D-10):

1. **Supported Versions** — Table listing version 1.x as currently supported.
2. **Security Architecture** — Overview paragraph: JWT bearer authentication with 1h access token + 7d refresh token rotation, ABAC role-based enforcement on every protected endpoint. Mention roles: admin, social_worker, intake_officer, auditor, barangay_coordinator, claimant.
3. **Authentication Flow** — Describe the single-flight refresh interceptor already implemented in `api.ts:80-134`:
   - Original request returns 401
   - `refreshToken()` checks `refreshInFlight` (single-flight dedup)
   - Calls `POST /api/auth/refresh` (raw fetch — NOT through the api client, per the circular dependency carve-out)
   - On success: stores new access/refresh tokens, retries original request via `executeWithRetry` with `isRetry=true`
   - On refresh 401/network error: clears both tokens from localStorage, dispatches `kapwa:auth:logout` CustomEvent, `auth-context.tsx:36-43` subscriber redirects to /login (loop-breaking)
4. **ASCII Sequence Diagram** — Text-based diagram showing Client ↔ API Server interaction for the refresh flow (use literal ASCII art, not mermaid — per Discretion). Show: original request → 401 → POST /auth/refresh → new tokens → retry original → 200 OK. Also show the failure path: refresh 401 → clear tokens → logout event → redirect to /login.
5. **Threat Model** — Table with threats: replay attack (mitigation: 1h short-lived tokens, HTTPS), CSRF (N/A — Bearer header not cookies), XSS via token theft (CSP + React auto-escaping), MITM on refresh endpoint (HTTPS enforced).
6. **Manual Verification (SEC-01)** — Step-by-step: login → set kapwa_token to expired value via DevTools → navigate to a protected page → observe refresh fires → verify DevTools network tab shows POST /auth/refresh → verify page loads. Reference ROADMAP #6 success criterion.
7. **Integration Test Coverage** — Reference api.test.ts:82-143 with 4 tests covering the SEC-01 contract (refresh-once-and-retry, single-flight, break-loop-on-refresh-401, logout-event-dispatch).
8. **Reporting a Vulnerability** — GitHub Private Vulnerability Reporting channel. Template for what to include (version, description, reproduction steps, impact). Acknowledgments within 5 business days.

Per D-10, cross-reference the existing project context: websocket failure → /login redirect per AGENTS.md.
</action>
<verify>
<automated>
# Verify SECURITY.md has required sections
node -e "
const fs=require('fs');
const content = fs.readFileSync('.planning/SECURITY.md','utf8');
const sections = ['Supported Versions','Security Architecture','Authentication','Threat Model','Manual Verification','Integration Test Coverage','Reporting a Vulnerability'];
const missing = sections.filter(s => !content.includes(s));
if (missing.length) { console.error('Missing sections: ' + missing.join(', ')); process.exit(1); }
if (!content.includes('|')) { console.error('No tables found'); process.exit(1); }
if (!content.includes('---') || !content.includes('|')) { console.error('Missing ASCII diagram arrows'); process.exit(1); }
console.log('SECURITY.md has all ' + sections.length + ' required sections and diagram markers');
"
</automated>
</verify>
<done>
SECURITY.md written at `.planning/SECURITY.md` with all 8 required sections, ASCII token refresh diagram, threat model table, and manual verification steps.
</done>
</task>

<task id="17-01-03" type="doc" wave="1">
<name>Add JSDoc to api.ts 401 branch</name>
<files>kapwa-client/src/lib/api.ts</files>
<read_first>
@kapwa-client/src/lib/api.ts (lines 119-134)
</read_first>
<action>
In `kapwa-client/src/lib/api.ts`, add a brief 3-line JSDoc comment immediately before the `if (err instanceof ApiError && err.status === 401 && !isRetry)` condition on line 130. The JSDoc must explain the single-flight refresh pattern and reference SECURITY.md. Use the exact text (per D-11):

```
/**
 * On 401, attempt /auth/refresh exactly once via single-flight pattern.
 * Concurrent 401s share the same refresh promise.
 * See SECURITY.md for the full flow.
 */
```

This is an explicit carve-out from the AGENTS.md "no comments" rule — permitted per D-11. Do NOT add any other comments to the file.
</action>
<verify>
<automated>
node -e "
const fs=require('fs');
const content = fs.readFileSync('kapwa-client/src/lib/api.ts','utf8');
if (!content.includes('/**\n * On 401, attempt /auth/refresh exactly once via single-flight pattern.\n * Concurrent 401s share the same refresh promise.\n * See SECURITY.md for the full flow.\n */')) {
  // Try different line endings
  const cleaned = content.replace(/\r\n/g, '\n');
  if (!cleaned.includes('On 401, attempt /auth/refresh exactly once via single-flight pattern')) {
    console.error('JSDoc comment not found on the 401 branch');
    process.exit(1);
  }
}
console.log('JSDoc comment found on api.ts 401 branch');
"
</automated>
</verify>
<done>
api.ts line 130 has the 3-line JSDoc comment explaining single-flight refresh with SECURITY.md reference.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → npm registry | npm ci fetches packages from the public npm registry — supply chain risk |
| SECURITY.md → reader | Documentation file — no executable content, no threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `.github/workflows/ci.yml` | accept | CI config is tracked in git; changes are reviewed via PRs to main. No secrets or deploy credentials in the workflow. |
| T-17-01-SC | Supply Chain | npm install in CI | mitigate | `npm ci` uses lockfile (package-lock.json) which pins transitive dependency versions. No `--force` or `--legacy-peer-deps` in CI commands. GitHub Actions runner is ephemeral (every run is a fresh VM). |
</threat_model>

<verification>
1. Validate YAML syntax: `node -e "try { require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml','utf8')); console.log('YAML valid'); } catch(e) { console.error(e.message); process.exit(1); }"` (if js-yaml not available, use node's JSON parse on a simple serial form or parse manually)
2. Verify `npm run test:run` still passes (all existing tests green)
3. Verify `npm run build` passes (Vite build succeeds)
4. Verify SECURITY.md has all 8 required sections (script in task 2 verify)
</verification>

<success_criteria>
- `.github/workflows/ci.yml` — 3 jobs (test, coverage[needs:test], build[parallel]), ubuntu-latest, Node 20, no cache, coverage artifact uploaded
- `.planning/SECURITY.md` — 8 sections with ASCII token refresh diagram
- `kapwa-client/src/lib/api.ts:130` — JSDoc comment on the 401 branch
- Full test suite green: `npm run test:run` passes
- Build passes: `npm run build` passes
</success_criteria>

<artifacts_this_plan_creates>
- `.github/workflows/ci.yml` — GitHub Actions CI workflow
- `.planning/SECURITY.md` — Security documentation
- `kapwa-client/src/lib/api.ts` — +3-line JSDoc (modified)
</artifacts_this_plan_creates>
