---
phase: 11-page-migration-print-offline-ui
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - kapwa-client/src/pages/CasesPage.tsx
  - kapwa-client/src/pages/InterventionsPage.tsx
  - kapwa-client/src/pages/CaseTrackerPage.tsx
  - kapwa-client/src/pages/ApprovalPipelinePage.tsx
  - kapwa-client/src/pages/AccessCardPage.tsx
  - kapwa-client/src/pages/CsrPage.tsx
  - kapwa-client/src/pages/IrfPage.tsx
  - kapwa-client/src/pages/__tests__/CasesPage.test.tsx
  - kapwa-client/src/pages/__tests__/InterventionsPage.test.tsx
  - kapwa-client/src/pages/__tests__/CaseTrackerPage.test.tsx
  - kapwa-client/src/pages/__tests__/ApprovalPipelinePage.test.tsx
  - kapwa-client/src/pages/__tests__/AccessCardPage.test.tsx
  - kapwa-client/src/pages/__tests__/CsrPage.test.tsx
  - kapwa-client/src/pages/__tests__/IrfPage.test.tsx
autonomous: true
requirements: [PGM-02, PGM-03, PGM-04, PRN-02]
user_setup: []
must_haves:
  truths:
    - "Cases page shows DataTable with sort/search/paginate — legacy table/badge/btn classes eliminated"
    - "Interventions page shows DataTable with action buttons — legacy classes eliminated"
    - "CaseTracker page shows daily tracker with proper loading/empty/error states"
    - "ApprovalPipeline page shows approval workflow with proper states"
    - "Access Card page shows card data with print-ready A4 layout — .no-print on chrome, print-header/footer for government document"
    - "CSR page shows report with print-ready layout, signature block, running headers"
    - "IRF page shows incident form with print-ready layout"
    - "Each migrated page has a regression test with mock data"
    - "Print documents (CSR, Access Card, IRF) show MSWDO header, Page X of Y, RA 11032 reference, signature block on last page"
  artifacts:
    - path: kapwa-client/src/pages/CasesPage.tsx
      provides: "CasesPage migrated to PageShell, DataTable, TableSkeleton, EmptyState, shadcn Badge/Button/Input"
      min_lines: 280
      contains: "PageShell"
    - path: kapwa-client/src/pages/InterventionsPage.tsx
      provides: "InterventionsPage migrated to PageShell, DataTable, TableSkeleton, EmptyState"
      min_lines: 100
      contains: "PageShell"
    - path: kapwa-client/src/pages/CaseTrackerPage.tsx
      provides: "CaseTrackerPage migrated to PageShell, CardGridSkeleton, EmptyState"
      min_lines: 80
      contains: "PageShell"
    - path: kapwa-client/src/pages/ApprovalPipelinePage.tsx
      provides: "ApprovalPipelinePage migrated to PageShell, TableSkeleton, EmptyState"
      min_lines: 80
      contains: "PageShell"
    - path: kapwa-client/src/pages/AccessCardPage.tsx
      provides: "AccessCardPage migrated to PageShell + print view with .print-header, .print-footer, signature-block, .no-print on actions"
      min_lines: 150
      contains: "PageShell"
    - path: kapwa-client/src/pages/CsrPage.tsx
      provides: "CsrPage migrated to PageShell + print layout with MSWDO branding, signature block, running header/footer, page numbers"
      min_lines: 120
      contains: "PageShell"
    - path: kapwa-client/src/pages/IrfPage.tsx
      provides: "IrfPage migrated to PageShell + print layout with incident report formatting, signature block"
      min_lines: 120
      contains: "PageShell"
  key_links:
    - from: kapwa-client/src/pages/CasesPage.tsx
      to: kapwa-client/src/components/PageShell.tsx
      via: "Wraps cases content with PageShell"
      pattern: "PageShell"
    - from: kapwa-client/src/pages/AccessCardPage.tsx
      to: kapwa-client/src/index.css
      via: "Uses .no-print on action buttons, .print-header/.print-footer/.signature-block for A4 layout"
      pattern: "no-print|print-header|signature-block"
    - from: kapwa-client/src/pages/CsrPage.tsx
      to: kapwa-client/src/index.css
      via: "Uses global print classes for A4 government report layout"
      pattern: "print-header|signature-block|print-footer"
    - from: kapwa-client/src/pages/IrfPage.tsx
      to: kapwa-client/src/index.css
      via: "Uses global print classes for IRF print layout"
      pattern: "no-print|signature-block"
---

<objective>

**Purpose:** Migrate seven pages across three page groups — Cases (list + interventions + tracker + approval pipeline), Access Card/CSR (print-document-heavy), and IRF (form-heavy + print). This plan handles the largest number of pages and introduces print-ready document layouts for government reports.

**Output:**
1. CasesPage, InterventionsPage, CaseTrackerPage, ApprovalPipelinePage — migrated to PageShell + DataTable + proper states
2. AccessCardPage — migrated with PageShell + print layout (A4, MSWDO header, signature block, .no-print on controls)
3. CsrPage — migrated with PageShell + full print-ready CSR document (RA 11032 footer, Page X of Y, signature block)
4. IrfPage — migrated with PageShell + print-ready IRF layout
5. Regression tests for all 7 pages with mock data

</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-page-migration-print-offline-ui/11-CONTEXT.md
@.planning/phases/11-page-migration-print-offline-ui/11-RESEARCH.md
@.planning/phases/11-page-migration-print-offline-ui/11-UI-SPEC.md

@kapwa-client/src/pages/CasesPage.tsx
@kapwa-client/src/pages/InterventionsPage.tsx
@kapwa-client/src/pages/CaseTrackerPage.tsx
@kapwa-client/src/pages/ApprovalPipelinePage.tsx
@kapwa-client/src/pages/AccessCardPage.tsx
@kapwa-client/src/pages/CsrPage.tsx
@kapwa-client/src/pages/IrfPage.tsx
@kapwa-client/src/components/PageShell.tsx
@kapwa-client/src/components/data-table/DataTable.tsx
@kapwa-client/src/components/skeletons/TableSkeleton.tsx
@kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
@kapwa-client/src/components/EmptyState.tsx
@kapwa-client/src/components/ui/button.tsx
@kapwa-client/src/components/ui/badge.tsx
@kapwa-client/src/components/ui/card.tsx
@kapwa-client/src/components/ui/input.tsx
@kapwa-client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cases group migration — CasesPage, InterventionsPage, CaseTrackerPage, ApprovalPipelinePage with regression tests</name>
  <files>
    kapwa-client/src/pages/CasesPage.tsx,
    kapwa-client/src/pages/InterventionsPage.tsx,
    kapwa-client/src/pages/CaseTrackerPage.tsx,
    kapwa-client/src/pages/ApprovalPipelinePage.tsx,
    kapwa-client/src/pages/__tests__/CasesPage.test.tsx,
    kapwa-client/src/pages/__tests__/InterventionsPage.test.tsx,
    kapwa-client/src/pages/__tests__/CaseTrackerPage.test.tsx,
    kapwa-client/src/pages/__tests__/ApprovalPipelinePage.test.tsx
  </files>
  <action>
    **Migrate all four Cases-group pages per D-03 (refactor in place), D-04 (regression test), D-16 (cache staleness).**

    **Pattern to follow for ALL four pages (from Plan 11-02 established pattern):**

    1. **Imports:** Add PageShell, appropriate skeleton (TableSkeleton for list pages, CardGridSkeleton for tracker), EmptyState, shadcn Button/Badge/Input. Remove `import '../index.css'`.
    2. **Business logic:** Keep ALL existing state, effects, API calls, event handlers, utility functions. Do NOT rewrite logic.
    3. **Wrap with PageShell:** Replace the top-level `<div>` with `<PageShell title="..." description="..." cachedAt={...}>`.
    4. **Loading state:** Replace `<div className="spinner" />` with skeleton component (TableSkeleton for table-based pages, CardGridSkeleton for card-based).
    5. **Empty state:** Replace `<div className="empty-state">` with `<EmptyState variant="no-data" />`.
    6. **Error state:** If the page has error handling, show EmptyState variant="offline" for network errors.
    7. **Data tables:** Replace `<table className="table">` with `<DataTable>` component (Phase 10). Define ColumnDef array matching the page's data shape.
    8. **Badges:** Replace `<span className="badge badge-*">` with `<Badge variant="*">`.
    9. **Buttons:** Replace `<button className="btn btn-primary">` with `<Button variant="default">`.
    10. **Form inputs:** Replace `<input className="form-input">` with `<Input />`. Replace `<select className="form-select">` with `<Select>`.
    11. **cachedAt:** Add `lastSync` state, set on successful data load, pass to PageShell.
    12. **No legacy classes:** Sweep for any remaining `.page-header`, `.page-title`, `.page-desc`, `.spinner`, `.empty-state`, `.table`, `.badge`, `.badge-*`, `.btn`, `.form-input`, `.form-select`, `.form-label`, `.form-group`, `.text-text-secondary`, `.toolbar`, `.pagination`, `.filter-pills`, `.filter-pill` class usages and replace every one.

    **Specific notes per page:**

    **CasesPage.tsx** (largest page, PGM-02):
    - Contains `STATUS_BADGES` map with legacy class names — replace values with Badge variant names
    - Action buttons (requestReview, disburseCase, closeCase, overrideCaseStatus) — wrap in shadcn Button
    - Show online/offline awareness for offline queue actions — keep existing isOnline + queueFsmTransition logic
    - Case action modals/confirmation dialogs — keep existing logic, just replace button wrappers

    **InterventionsPage.tsx** (PGM-02):
    - Data-driven page with intervention list
    - Replace table with DataTable
    - Keep intervention CRUD logic intact

    **CaseTrackerPage.tsx** (PGM-02):
    - Daily tracker view — likely card/detail based
    - Replace legacy card classes with shadcn Card components
    - Keep tracker-specific data display logic

    **ApprovalPipelinePage.tsx** (PGM-02):
    - Approval workflow with status transitions
    - Replace legacy table/badge classes
    - Keep approval action logic intact

    **Regression tests (per D-04):**
    Create tests for each page:
    ```
    // __tests__/CasesPage.test.tsx
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { CasesPage } from '../CasesPage';

    vi.mock('../../lib/api', () => ({
      getCases: vi.fn().mockResolvedValue([
        { id: 'C-001', no: 1, surname: 'Dela Cruz', first: 'Juan', middle: 'M', gender: 'M', ageRange: '60-70', category: 'Senior', barangay: 'Barangay 1', remarks: 'Monthly', date: '2026-06-28', status: 'approved', controlNo: 'CN-001' },
      ]),
    }));

    describe('CasesPage', () => {
      it('renders cases heading and table', async () => {
        render(<MemoryRouter><CasesPage /></MemoryRouter>);
        expect(await screen.findByText(/Cases/i)).toBeInTheDocument();
        expect(screen.getByText('C-001')).toBeInTheDocument();
      });
    });
    ```

    Similar patterns for InterventionsPage, CaseTrackerPage, ApprovalPipelinePage — each with appropriate mock data and assertions for page title and key content presence.

    **Do NOT import index.css** in any migrated page file.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(PASS|FAIL|✗|✓)" | head -10; for page in CasesPage InterventionsPage CaseTrackerPage ApprovalPipelinePage; do echo "$page: $(grep -c 'PageShell' kapwa-client/src/pages/$page.tsx) PageShell refs"; done</automated>
  </verify>
  <done>
    - All four Cases-group pages use PageShell, DataTable, proper skeletons, EmptyState, shadcn components
    - Zero legacy CSS classes remain on any page
    - All business logic preserved intact
    - Each page has a passing regression test with mock data
    - cachedAt prop wired on all pages
  </done>
</task>

<task type="auto">
  <name>Task 2: Access Card + CSR migration — print-ready government document layouts with regression tests</name>
  <files>
    kapwa-client/src/pages/AccessCardPage.tsx,
    kapwa-client/src/pages/CsrPage.tsx,
    kapwa-client/src/pages/__tests__/AccessCardPage.test.tsx,
    kapwa-client/src/pages/__tests__/CsrPage.test.tsx
  </files>
  <action>
    **Migrate AccessCardPage.tsx and CsrPage.tsx (per PGM-03, PRN-02, D-05, D-06, D-08).**

    These pages have TWO rendering modes:
    1. **Screen mode** — normal interactive page with PageShell wrapper, DataTable, buttons for manage/print
    2. **Print mode** — A4 document layout triggered via `window.print()` with MSWDO branding header, government footer, signature block

    **A. AccessCardPage.tsx migration:**

    Screen mode:
    - Wrap content with `<PageShell title="Access Cards" description="Manage access card records">`
    - Replace legacy table with DataTable
    - Use shadcn Badge for status, Button for actions
    - Add "Print Access Card" button that triggers `window.print()`

    Print mode:
    - Wrap the printable content (the actual card/report) in a container with these classes:
      - Top section: `<div className="print-header">` — shows MSWDO logo placeholder (SVG or text), office name "MSWDO Norzagaray", address
      - Content section: the access card details in a clean A4 layout
      - Footer section: `<div className="print-footer">` — "DSWD Seal placeholder | RA 11032 reference"
      - Signature block: `<div className="signature-block">` — Social Worker name + signature + date, Client name + signature + date
    - Add `.no-print` class to ALL interactive elements (buttons, search bars, filters, navigation)
    - Wrap the print-only content in `<div className="hidden print:block">` and screen-only content in `<div className="block print:hidden">`
    - Add `@page`-compatible class names that match the global print stylesheet from Plan 11-01

    **B. CsrPage.tsx migration (Case Study Report, PGM-03, PRN-02):**

    Similar approach as AccessCardPage but for the CSR document format:
    - Screen mode: PageShell + DataTable listing CSR entries with "Print CSR" actions
    - Print mode:
      - `<div className="print-header">` with MSWDO header (logo + name + address)
      - Document body with case details, interventions, assessment notes
      - `<div className="print-footer">` with page numbers, print date, control number
      - DSWD seal and RA 11032 reference
      - `<div className="signature-block">` on last page with break-inside-avoid

    **Important CSS notes:**
    - The global print stylesheet (Plan 11-01) already provides `.no-print`, `.print-header`, `.print-footer`, `.signature-block`, `.no-break`, `.page-break` classes — use them declaratively
    - For Chromium page numbering: the `@page` rules handle `counter(page)` automatically
    - For non-Chromium graceful degradation: add a `beforeprint` event listener that injects static "Page 1 of N" text into a hidden span that becomes visible only in non-Chromium print contexts (practical approach: use a small JS snippet that counts pages after render and inserts page numbers as regular text)

    **Regression tests (per D-04):**
    ```
    // __tests__/AccessCardPage.test.tsx
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { AccessCardPage } from '../AccessCardPage';

    vi.mock('../../lib/api', () => ({
      getAccessCards: vi.fn().mockResolvedValue([{ id: 'AC-001', beneficiaryName: 'Juan Dela Cruz', cardNumber: 'KAPWA-001', status: 'active', issuedAt: '2026-06-01' }]),
    }));

    describe('AccessCardPage', () => {
      it('renders access cards heading', async () => {
        render(<MemoryRouter><AccessCardPage /></MemoryRouter>);
        expect(await screen.findByText(/Access Cards/i)).toBeInTheDocument();
      });
    });
    ```

    Similar for CsrPage.

    **Do NOT import index.css** in the page files. The print classes are global from Plan 11-01.
  </action>
  <verify>
    <automated>grep -c "PageShell" kapwa-client/src/pages/AccessCardPage.tsx && grep -c "print-header\|signature-block\|no-print" kapwa-client/src/pages/AccessCardPage.tsx && grep -c "PageShell" kapwa-client/src/pages/CsrPage.tsx && grep -c "print-header\|signature-block\|print-footer" kapwa-client/src/pages/CsrPage.tsx && npm test -- --run 2>&1 | grep -E "(PASS|FAIL)" | head -5</automated>
  </verify>
  <done>
    - AccessCardPage has screen mode (PageShell + DataTable) and print mode (A4 layout with header/footer/signature)
    - CsrPage has screen mode and print mode with full government document layout
    - .no-print hides all interactive elements during print
    - Both pages have regression tests passing
    - Zero legacy CSS classes remain
  </done>
</task>

<task type="auto">
  <name>Task 3: IRF page migration with print-ready layout and regression test</name>
  <files>
    kapwa-client/src/pages/IrfPage.tsx,
    kapwa-client/src/pages/__tests__/IrfPage.test.tsx
  </files>
  <action>
    **Migrate IrfPage.tsx (per PGM-04, PRN-02, D-05, D-06, D-08).**

    IRF (Incident Report Form) is a form-heavy page that also needs print output.

    **Screen mode:**
    - Wrap with `<PageShell title="Incident Report Form" description="File and manage incident reports">`
    - Replace legacy form classes with shadcn Input, Label, Select, Textarea, Button
    - Replace legacy table classes with DataTable for any IRF listings
    - Keep ALL existing form logic, validation, submission handlers intact per D-03
    - Add `.no-print` to buttons, form controls, navigation elements

    **Print mode:**
    - Printable content area with complete IRF document layout:
      - `<div className="print-header">` with MSWDO header
      - Incident details: date, time, location, type, narrative
      - Persons involved section with table
      - Actions taken section
      - `<div className="signature-block">` with reporting officer signature
      - `<div className="print-footer">` with page numbers, control number, RA 11032 reference
    - Use `.no-break` class on major sections to prevent awkward page breaks
    - Use `.page-break` before signature block if needed (signature must be on its own page per D-08)

    **CRITICAL per D-08:** The signature block must be on the last page ONLY. Use one of:
    - If content fits one page: signature block follows normally with `break-inside: avoid`
    - If content spans multiple pages: wrap preceding content in a div and let natural pagination flow, with signature block after a `.page-break` div

    **Regression test (per D-04):**
    ```
    // __tests__/IrfPage.test.tsx
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { IrfPage } from '../IrfPage';

    vi.mock('../../lib/api', () => ({
      getIrfCases: vi.fn().mockResolvedValue([]),
    }));

    describe('IrfPage', () => {
      it('renders IRF heading', async () => {
        render(<MemoryRouter><IrfPage /></MemoryRouter>);
        expect(await screen.findByText(/Incident Report/i)).toBeInTheDocument();
      });
    });
    ```

    **Do NOT import index.css.**
  </action>
  <verify>
    <automated>grep -c "PageShell" kapwa-client/src/pages/IrfPage.tsx && grep -c "no-print\|signature-block\|print-header" kapwa-client/src/pages/IrfPage.tsx && npm test -- --run 2>&1 | grep -E "(PASS|FAIL)" | head -5</automated>
  </verify>
  <done>
    - IrfPage has screen mode (PageShell + shadcn forms) and print mode (A4 IRF document with header/signature/footer)
    - Zero legacy CSS classes remain
    - Original form logic preserved intact
    - Regression test passes
    - Signature block on last page with break-inside-avoid
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → page component | Data rendered in DataTable cells — data enters from API responses |
| Print output | Document content printed — could contain sensitive PII |
| Conflict resolution | No server writes from UI in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | Print output of CSR/Access Card/IRF | accept | These are official government documents meant for printing; PII in documents is by design |
| T-11-03-02 | Denial of Service | Large DataTable renders | accept | Cases page may have 100+ rows; DataTable uses virtual pagination with pageSize=10 default |
| T-11-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required |
</threat_model>

<verification>
1. Run `npm test -- --run` in kapwa-client/ — verify all 7 page regression tests pass
2. Verify zero legacy CSS class usage on all migrated pages:
   - For each page: `grep 'className="[^"]*\(spinner\|page-header\|page-title\|page-desc\|empty-state\|\.table\|badge[^-]\|btn \|form-input\|form-select\|form-label\|form-group\|text-text-secondary\|toolbar\|pagination\|filter-pill\)'` returns 0
3. Manual: Open each page in browser, verify rendering matches pre-migration
4. Manual: Verify print preview for AccessCardPage, CsrPage, IrfPage shows A4 layout with header/footer/signature
</verification>

<success_criteria>
1. [ ] CasesPage, InterventionsPage, CaseTrackerPage, ApprovalPipelinePage all use shadcn + PageShell — zero legacy classes
2. [ ] AccessCardPage, CsrPage, IrfPage have print-ready layouts with MSWDO header, signature block, RA 11032 footer
3. [ ] All 7 pages have regression tests passing
4. [ ] Loading/empty/error states on each migrated page
5. [ ] cachedAt prop wired on all data-fetching pages
6. [ ] Print output hides .no-print elements, shows proper government document layout
</success_criteria>

<output>
Create `.planning/phases/11-page-migration-print-offline-ui/11-03-SUMMARY.md` when done
</output>
