---
phase: 11-page-migration-print-offline-ui
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - kapwa-client/src/pages/AdminPage.tsx
  - kapwa-client/src/pages/MfaSetupPage.tsx
  - kapwa-client/src/pages/FilingPage.tsx
  - kapwa-client/src/pages/MessagesPage.tsx
  - kapwa-client/src/pages/ClaimantDashboardPage.tsx
  - kapwa-client/src/pages/__tests__/AdminPage.test.tsx
  - kapwa-client/src/pages/__tests__/MfaSetupPage.test.tsx
  - kapwa-client/src/pages/__tests__/FilingPage.test.tsx
  - kapwa-client/src/pages/__tests__/MessagesPage.test.tsx
  - kapwa-client/src/pages/__tests__/ClaimantDashboardPage.test.tsx
autonomous: true
requirements: [PGM-05, PGM-06]
user_setup: []
must_haves:
  truths:
    - "Admin/settings pages use PageShell + shadcn components — zero legacy CSS classes"
    - "MFA setup page uses PageShell + shadcn form elements with proper loading states"
    - "Filing page uses PageShell + DataTable with proper states"
    - "Messages page uses PageShell + proper states — legacy classes eliminated"
    - "ClaimantDashboardPage uses PageShell + CardGridSkeleton + shadcn Card — legacy classes eliminated"
    - "All phase 11 migrated pages have regression tests passing"
    - "All 16 pages migrated — every page uses PageShell, proper loading/empty/error states, shadcn components"
    - "Zero legacy CSS classes remain in any migrated src/pages/ file"
    - "Cache staleness indicators appear on all data-fetching pages"
    - "Sync queue panel accessible from any page via sync status banner"
  artifacts:
    - path: kapwa-client/src/pages/AdminPage.tsx
      provides: "AdminPage migrated to PageShell, DataTable, TableSkeleton, EmptyState, shadcn Button/Badge"
      min_lines: 80
      contains: "PageShell"
    - path: kapwa-client/src/pages/MfaSetupPage.tsx
      provides: "MfaSetupPage migrated to PageShell, shadcn Input/Button/Label, FormSkeleton"
      min_lines: 60
      contains: "PageShell"
    - path: kapwa-client/src/pages/FilingPage.tsx
      provides: "FilingPage migrated to PageShell, DataTable, TableSkeleton, EmptyState"
      min_lines: 80
      contains: "PageShell"
    - path: kapwa-client/src/pages/MessagesPage.tsx
      provides: "MessagesPage migrated to PageShell, CardGridSkeleton, EmptyState"
      min_lines: 80
      contains: "PageShell"
    - path: kapwa-client/src/pages/ClaimantDashboardPage.tsx
      provides: "ClaimantDashboardPage migrated to PageShell, CardGridSkeleton, EmptyState, shadcn Card"
      min_lines: 80
      contains: "PageShell"
  key_links:
    - from: kapwa-client/src/pages/AdminPage.tsx
      to: kapwa-client/src/components/PageShell.tsx
      via: "Wraps admin content with PageShell"
      pattern: "PageShell"
    - from: kapwa-client/src/pages/MfaSetupPage.tsx
      to: kapwa-client/src/components/ui/input.tsx
      via: "Uses shadcn Input for MFA code entry"
      pattern: "from.*input"
    - from: kapwa-client/src/pages/FilingPage.tsx
      to: kapwa-client/src/components/data-table/DataTable.tsx
      via: "Uses DataTable for document list"
      pattern: "DataTable"
    - from: kapwa-client/src/pages/ClaimantDashboardPage.tsx
      to: kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
      via: "Uses CardGridSkeleton for loading state"
      pattern: "CardGridSkeleton"
---

<objective>

**Purpose:** Complete the final batch of page migrations (Admin, MFA Setup, Filing, Messages, Claimant Dashboard) and run the final verification pass ensuring all 16 pages meet the Phase 11 success criteria — zero legacy CSS classes, proper loading/empty/error states, cache staleness indicators, and sync queue access.

**Output:**
1. AdminPage, MfaSetupPage, FilingPage, MessagesPage — migrated to PageShell + shadcn components
2. ClaimantDashboardPage — migrated (per D-02, last in the order)
3. Regression tests for all 5 pages with mock data
4. Final sweep verification across all 16 migrated pages
5. Print preview verification for CSR, Access Card, IRF documents

</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-page-migration-print-offline-ui/11-CONTEXT.md
@.planning/phases/11-page-migration-print-offline-ui/11-RESEARCH.md
@.planning/phases/11-page-migration-print-offline-ui/11-UI-SPEC.md

@kapwa-client/src/pages/AdminPage.tsx
@kapwa-client/src/pages/MfaSetupPage.tsx
@kapwa-client/src/pages/FilingPage.tsx
@kapwa-client/src/pages/MessagesPage.tsx
@kapwa-client/src/pages/ClaimantDashboardPage.tsx
@kapwa-client/src/components/PageShell.tsx
@kapwa-client/src/components/data-table/DataTable.tsx
@kapwa-client/src/components/skeletons/TableSkeleton.tsx
@kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
@kapwa-client/src/components/skeletons/FormSkeleton.tsx
@kapwa-client/src/components/EmptyState.tsx
@kapwa-client/src/components/ui/button.tsx
@kapwa-client/src/components/ui/badge.tsx
@kapwa-client/src/components/ui/card.tsx
@kapwa-client/src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin & utility pages migration — AdminPage, MfaSetupPage, FilingPage, MessagesPage with regression tests</name>
  <files>
    kapwa-client/src/pages/AdminPage.tsx,
    kapwa-client/src/pages/MfaSetupPage.tsx,
    kapwa-client/src/pages/FilingPage.tsx,
    kapwa-client/src/pages/MessagesPage.tsx,
    kapwa-client/src/pages/__tests__/AdminPage.test.tsx,
    kapwa-client/src/pages/__tests__/MfaSetupPage.test.tsx,
    kapwa-client/src/pages/__tests__/FilingPage.test.tsx,
    kapwa-client/src/pages/__tests__/MessagesPage.test.tsx
  </files>
  <action>
    **Migrate all four admin/utility pages per PGM-06, following the established D-03 pattern.**

    **Common migration steps for each page (per D-03 — refactor in place):**

    1. **Remove** `import '../index.css'`
    2. **Add imports:**
       ```
       import { PageShell } from '@/components/PageShell';
       import { TableSkeleton } from '@/components/skeletons/TableSkeleton';
       import { CardGridSkeleton } from '@/components/skeletons/CardGridSkeleton';
       import { FormSkeleton } from '@/components/skeletons/FormSkeleton';
       import { EmptyState } from '@/components/EmptyState';
       import { Button } from '@/components/ui/button';
       import { Badge } from '@/components/ui/badge';
       import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
       import { Input } from '@/components/ui/input';
       import { Label } from '@/components/ui/label';
       ```
    3. **Wrap with PageShell:** Replace top-level `<div>` with `<PageShell title="..." description="...">`. Use appropriate title/description for each page.
    4. **Replace loading state:** `<div className="spinner" />` → skeleton component (TableSkeleton for list pages, FormSkeleton for form pages, CardGridSkeleton for card pages)
    5. **Replace empty state:** `<div className="empty-state">` → `<EmptyState variant="no-data" />`
    6. **Replace tables:** `<table className="table">` → `<DataTable>` with appropriate ColumnDef
    7. **Replace badges:** `<span className="badge badge-*">` → `<Badge variant="*">`
    8. **Replace buttons:** `<button className="btn btn-primary">` → `<Button>`
    9. **Replace form inputs:** `<input className="form-input">` → `<Input />`. `<select className="form-select">` → `<Select>`. `<label className="form-label">` → `<Label>`.
    10. **Replace card classes:** `.card`, `.stat-card` → `<Card>` component
    11. **Add cachedAt:** Add `lastSync` state where data is fetched, pass to PageShell per D-16
    12. **Sweep for all legacy classes:** `.page-header`, `.page-title`, `.page-desc`, `.spinner`, `.empty-state`, `.table`, `.badge`, `.badge-*`, `.btn`, `.btn-primary`, `.form-input`, `.form-select`, `.form-label`, `.form-group`, `.text-text-secondary`, `.toolbar`, `.toolbar-left`, `.toolbar-right`, `.pagination`, `.filter-pills`, `.filter-pill`, `.stat-card`, `.stat-label`, `.stat-value`, `.search-bar`, `.search-input`, `.icon-btn`, `.clear-btn`, `.error-msg`, `.success-msg` — eliminate every one
    13. **Keep ALL business logic:** states, effects, API calls, handlers, helper functions unchanged

    **Page-specific notes:**

    **AdminPage.tsx (PGM-06):**
    - User management, role configuration, system settings
    - Likely has DataTable of users + role assignment UI
    - Replace admin-specific tables with DataTable, keep form submission logic

    **MfaSetupPage.tsx (PGM-06):**
    - MFA/TOTP setup flow — QR code, verification code input
    - Replace form inputs with shadcn Input
    - Keep QR code rendering logic intact
    - Use FormSkeleton for loading state

    **FilingPage.tsx (PGM-06):**
    - Document filing/list — likely file listing with upload capability
    - Replace table with DataTable
    - Keep file upload/existing document listing logic

    **MessagesPage.tsx (PGM-06):**
    - Chat/messaging interface
    - May be more complex — replace legacy wrappers but keep message rendering logic
    - Use EmptyState for "no messages" when appropriate

    **Regression tests (per D-04):**
    Create a test file for each page with the same pattern:
    ```
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';

    vi.mock('../../lib/api', () => ({
      // Mock appropriate API function for each page
    }));

    describe('PageName', () => {
      it('renders page heading', async () => {
        render(<MemoryRouter><PageName /></MemoryRouter>);
        expect(await screen.findByText(/Heading/i)).toBeInTheDocument();
      });
    });
    ```

    Mock the specific API function each page uses. Use the correct relative import path for the API module (typically `../../lib/api` from the `pages/` directory).

    **Do NOT import index.css.**
  </action>
  <verify>
    <automated>for page in AdminPage MfaSetupPage FilingPage MessagesPage; do echo "$page: $(grep -c 'PageShell' kapwa-client/src/pages/$page.tsx) PageShell refs"; done && npm test -- --run 2>&1 | grep -E "(PASS|FAIL)" | head -10</automated>
  </verify>
  <done>
    - AdminPage, MfaSetupPage, FilingPage, MessagesPage all use PageShell + shadcn components
    - Zero legacy CSS classes remain on any page
    - Each page has a passing regression test with mock data
    - cachedAt prop wired where applicable
  </done>
</task>

<task type="auto">
  <name>Task 2: ClaimantDashboardPage migration with regression test</name>
  <files>
    kapwa-client/src/pages/ClaimantDashboardPage.tsx,
    kapwa-client/src/pages/__tests__/ClaimantDashboardPage.test.tsx
  </files>
  <action>
    **Migrate ClaimantDashboardPage.tsx (per PGM-05, D-02 — last in migration order).**

    This is the beneficiary-facing self-service dashboard. It is listed last in D-02.

    Follow the same pattern:
    1. **Remove** `import '../index.css'`
    2. **Add imports:** PageShell, CardGridSkeleton, EmptyState, Card/CardContent, Badge, Button
    3. **Wrap with PageShell:** title="My Dashboard", description="Your case and assistance overview"
    4. **Loading state:** CardGridSkeleton inside PageShell
    5. **Empty state:** EmptyState variant
    6. **Replace legacy classes:** Same sweep as Task 1 — eliminate every legacy class usage
    7. **Replace stat cards:** Use shadcn Card components
    8. **Add cachedAt:** Add lastSync state, pass to PageShell per D-16
    9. **Keep ALL business logic:** dashboard data fetching, stat computations, status rendering

    **Regression test:**
    ```
    // __tests__/ClaimantDashboardPage.test.tsx
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { ClaimantDashboardPage } from '../ClaimantDashboardPage';

    vi.mock('../../lib/api', () => ({
      getClaimantDashboard: vi.fn().mockResolvedValue({
        cases: [{ id: 'C-001', status: 'approved', type: 'AICS' }],
        assistanceCount: 3,
        pendingCount: 1,
      }),
    }));

    describe('ClaimantDashboardPage', () => {
      it('renders claimant dashboard heading', async () => {
        render(<MemoryRouter><ClaimantDashboardPage /></MemoryRouter>);
        expect(await screen.findByText(/My Dashboard|Dashboard/i)).toBeInTheDocument();
      });
    });
    ```

    **Do NOT import index.css.**
  </action>
  <verify>
    <automated>grep -c "PageShell" kapwa-client/src/pages/ClaimantDashboardPage.tsx && grep -c "CardGridSkeleton\|EmptyState" kapwa-client/src/pages/ClaimantDashboardPage.tsx && npm test -- --run 2>&1 | grep -E "(PASS|FAIL)" | head -5</automated>
  </verify>
  <done>
    - ClaimantDashboardPage uses PageShell + Card/CardGridSkeleton/EmptyState + shadcn components
    - Zero legacy CSS classes remain
    - Business logic preserved intact
    - Regression test passes
    - cachedAt prop wired
  </done>
</task>

<task type="auto">
  <name>Task 3: Final integration verification — all 16 pages checked for completeness</name>
  <files>
    kapwa-client/src/pages/DashboardPage.tsx,
    kapwa-client/src/pages/BeneficiariesPage.tsx,
    kapwa-client/src/pages/BeneficiaryViewPage.tsx,
    kapwa-client/src/pages/IntakePage.tsx,
    kapwa-client/src/pages/CasesPage.tsx,
    kapwa-client/src/pages/InterventionsPage.tsx,
    kapwa-client/src/pages/CaseTrackerPage.tsx,
    kapwa-client/src/pages/ApprovalPipelinePage.tsx,
    kapwa-client/src/pages/AccessCardPage.tsx,
    kapwa-client/src/pages/CsrPage.tsx,
    kapwa-client/src/pages/IrfPage.tsx,
    kapwa-client/src/pages/AdminPage.tsx,
    kapwa-client/src/pages/MfaSetupPage.tsx,
    kapwa-client/src/pages/FilingPage.tsx,
    kapwa-client/src/pages/MessagesPage.tsx,
    kapwa-client/src/pages/ClaimantDashboardPage.tsx
  </files>
  <action>
    **Run the final verification sweep across ALL 16 migrated pages.**

    Do NOT modify any page business logic. This is a verification-only task.

    **Automated checks:**
    1. Run all tests: `npm test -- --run` in kapwa-client/ — ALL tests must pass
    2. Legacy CSS audit: Use grep across all pages in `kapwa-client/src/pages/` to confirm zero usages of:
       - `.spinner`, `.page-header`, `.page-title`, `.page-desc`, `.empty-state`
       - `.table`, `.badge` (not `.badge-` variant patterns — those should be gone too)
       - `.btn`, `.btn-primary`, `.btn-secondary`
       - `.form-input`, `.form-select`, `.form-label`, `.form-group`
       - `.text-text-secondary`, `.toolbar`, `.toolbar-left`, `.toolbar-right`
       - `.pagination`, `.filter-pills`, `.filter-pill`
       - `.stat-card`, `.stat-label`, `.stat-value`
       - `.search-bar`, `.search-input`, `.icon-btn`, `.clear-btn`
       - `.error-msg`, `.success-msg`
    3. Confirm every page file has `PageShell` import — no page should export raw content without the shell
    4. Confirm no `import '../index.css'` remains in any page file
    5. Confirm test files exist for every migrated page that was part of this phase

    **Manual checks:**
    1. Check that the build compiles: `npm run build` in kapwa-client/ (or `npx vite build`)
    2. Verify app loads without runtime errors
    3. Verify print preview for CSR, Access Card, and IRF shows proper A4 layout
    4. Verify sync queue panel opens/closes from sync status banner
    5. Verify cache staleness badge appears when using cached data

    **Fixes:**
    - If any legacy class usage is found in a page file, replace it with the appropriate shadcn equivalent immediately
    - If any test fails, fix the test or the page component
    - If build fails, fix the compilation error

    **Report:**
    Generate a report of all findings. For each page, note:
    - PageShell present: YES/NO
    - Legacy classes eliminated: YES/NO (with count of any remaining)
    - Test passing: YES/NO
    - Loading state: present/missing
    - Empty state: present/missing
    - Error state: present/missing
    - cachedAt wired: YES/NO

    This is the final gate before Phase 11 completes. Every page must meet all criteria.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -20; echo "---"; for page in DashboardPage BeneficiariesPage BeneficiaryViewPage IntakePage CasesPage InterventionsPage CaseTrackerPage ApprovalPipelinePage AccessCardPage CsrPage IrfPage AdminPage MfaSetupPage FilingPage MessagesPage ClaimantDashboardPage; do shell=$(grep -c "PageShell" "kapwa-client/src/pages/$page.tsx"); legacy=$(grep -cE 'className="[^"]*(spinner|page-header|page-title|page-desc|empty-state)"' "kapwa-client/src/pages/$page.tsx" 2>/dev/null || echo 0); echo "$page: PageShell=$shell, legacy_classes=$legacy"; done</automated>
  </verify>
  <done>
    - All 16 tests pass
    - Zero legacy class usages across all page files
    - Every page has PageShell wrapper
    - Build compiles without errors
    - Print previews for CSR, Access Card, IRF show correct A4 layout
    - Sync queue panel and cache staleness indicators functional
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → AdminPage | User management and role data crosses here |
| localStorage → all pages | cachedAt/lastSync timestamps read from local state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Elevation of Privilege | AdminPage role assignment UI | mitigate | Role assignment UI only renders what the API provides; actual authorization enforced server-side via RolesGuard + AbacGuard |
| T-11-04-02 | Information Disclosure | MessagesPage WebSocket data | accept | Chat messages are displayed as received from the API; no additional disclosure beyond existing chat-socket.ts implementation |
| T-11-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required |
</threat_model>

<verification>
1. Run `npm test -- --run` in kapwa-client/ — verify all tests pass
2. Run `npx vite build` in kapwa-client/ (or `npm run build`) — verify compilation succeeds
3. Legacy class sweep across ALL pages — zero matches
4. Manual: verify each page renders correctly in browser
5. Manual: verify print preview for document pages
6. Manual: verify sync queue panel opens from any page
</verification>

<success_criteria>
1. [ ] All 16 pages migrated to PageShell + shadcn — zero legacy CSS classes in any page file
2. [ ] All regression tests pass (one per migrated page, 16 tests total)
3. [ ] Build compiles without errors
4. [ ] Loading skeletons present on every page (cold load)
5. [ ] Empty states present on every page (zero results)
6. [ ] Error handling present (API failures show appropriate fallback)
7. [ ] cachedAt/wired on all data-fetching pages
8. [ ] Print-ready layouts for CSR, Access Card, IRF with MSWDO branding, signature block, Page X of Y
9. [ ] Sync queue panel accessible from sync status banner
10. [ ] Sync status banner shows connection state + pending count
11. [ ] Cache staleness badge appears when data exceeds 5 min threshold
</success_criteria>

<output>
Create `.planning/phases/11-page-migration-print-offline-ui/11-04-SUMMARY.md` when done
</output>
