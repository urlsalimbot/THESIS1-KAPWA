---
phase: 11-page-migration-print-offline-ui
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - kapwa-client/src/pages/DashboardPage.tsx
  - kapwa-client/src/pages/BeneficiariesPage.tsx
  - kapwa-client/src/pages/BeneficiaryViewPage.tsx
  - kapwa-client/src/pages/IntakePage.tsx
  - kapwa-client/src/pages/__tests__/DashboardPage.test.tsx
  - kapwa-client/src/pages/__tests__/BeneficiariesPage.test.tsx
  - kapwa-client/src/pages/__tests__/BeneficiaryViewPage.test.tsx
  - kapwa-client/src/pages/__tests__/IntakePage.test.tsx
autonomous: true
requirements: [PGM-01, PGM-05]
user_setup: []
must_haves:
  truths:
    - "Dashboard page shows stat cards and recent cases table using shadcn components — legacy classes .page-header, .page-title, .page-desc, .spinner, .table, .badge are eliminated"
    - "Dashboard shows proper loading skeleton (cold load), empty state (zero results), error state with retry"
    - "Beneficiaries page shows DataTable with search/filter — legacy table/badge classes eliminated"
    - "BeneficiaryView page shows detail card layout with proper states"
    - "IntakePage shows form layout with proper loading/empty/error states"
    - "Each migrated page has a regression test with mock data"
    - "All migrated pages accept cachedAt prop on PageShell and show cache staleness badge"
  artifacts:
    - path: kapwa-client/src/pages/DashboardPage.tsx
      provides: "DashboardPage migrated to PageShell, shadcn components (Button, Badge), CardGridSkeleton, EmptyState, DataTable"
      min_lines: 120
      contains: "PageShell"
      min_lines: 60
      contains: "PageShell"
    - path: kapwa-client/src/pages/BeneficiariesPage.tsx
      provides: "BeneficiariesPage migrated to PageShell, DataTable, TableSkeleton, EmptyState, shadcn Button/Badge/Input"
      min_lines: 150
      contains: "PageShell"
    - path: kapwa-client/src/pages/BeneficiaryViewPage.tsx
      provides: "BeneficiaryViewPage migrated to PageShell, CardGridSkeleton, EmptyState, shadcn Badge"
      min_lines: 80
      contains: "PageShell"
    - path: kapwa-client/src/pages/IntakePage.tsx
      provides: "IntakePage migrated to PageShell, FormSkeleton, EmptyState, shadcn Form"
      min_lines: 100
      contains: "PageShell"
    - path: kapwa-client/src/pages/__tests__/DashboardPage.test.tsx
      provides: "Regression test verifying DashboardPage renders heading, stat cards, and recent cases table with mock data"
      min_lines: 40
    - path: kapwa-client/src/pages/__tests__/BeneficiariesPage.test.tsx
      provides: "Regression test for BeneficiariesPage"
      min_lines: 30
    - path: kapwa-client/src/pages/__tests__/BeneficiaryViewPage.test.tsx
      provides: "Regression test for BeneficiaryViewPage"
      min_lines: 30
    - path: kapwa-client/src/pages/__tests__/IntakePage.test.tsx
      provides: "Regression test for IntakePage"
      min_lines: 30
  key_links:
    - from: kapwa-client/src/pages/DashboardPage.tsx
      to: kapwa-client/src/components/PageShell.tsx
      via: "Wraps content with PageShell component, passes title/description/cachedAt/actions"
      pattern: "PageShell"
    - from: kapwa-client/src/pages/BeneficiariesPage.tsx
      to: kapwa-client/src/components/data-table/DataTable.tsx
      via: "Replaces legacy .table with DataTable component"
      pattern: "DataTable"
    - from: kapwa-client/src/pages/DashboardPage.tsx
      to: kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
      via: "Loading state uses CardGridSkeleton instead of .spinner"
      pattern: "CardGridSkeleton"
    - from: kapwa-client/src/pages/BeneficiariesPage.tsx
      to: kapwa-client/src/components/skeletons/TableSkeleton.tsx
      via: "Loading state uses TableSkeleton"
      pattern: "TableSkeleton"
    - from: kapwa-client/src/pages/BeneficiariesPage.tsx
      to: kapwa-client/src/components/EmptyState.tsx
      via: "Empty state uses EmptyState variant"
      pattern: "EmptyState"
---

<objective>

**Purpose:** Migrate Dashboard (pattern-setter) and Beneficiaries pages (list, detail view, intake form) from legacy CSS classes to shadcn components + PageShell wrapper + proper loading/empty/error states. This plan establishes the migration pattern that Plans 11-03 and 11-04 follow.

**Output:**
1. `DashboardPage.tsx` — first page migrated, establishes the pattern for all others. Replaces legacy `.page-header`, `.page-title`, `.page-desc`, `.spinner`, `.table`, `.badge`, `.badge-category`, `.pagination`, `.stat-card`, `.stat-label`, `.stat-value`, `.text-text-secondary` with shadcn equivalents. Adds PageShell wrapper with title/description/actions, CardGridSkeleton for loading, EmptyState for error, DataTable for cases table.
2. `BeneficiariesPage.tsx` — migrated with PageShell, DataTable, TableSkeleton, EmptyState, shadcn Input/Badge/Button
3. `BeneficiaryViewPage.tsx` — migrated with PageShell, CardGridSkeleton, shadcn Badge
4. `IntakePage.tsx` — migrated with PageShell, FormSkeleton, shadcn Form
5. Regression test files per D-04 — each page gets a test with mock data verifying key elements render

</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-page-migration-print-offline-ui/11-CONTEXT.md
@.planning/phases/11-page-migration-print-offline-ui/11-RESEARCH.md
@.planning/phases/11-page-migration-print-offline-ui/11-UI-SPEC.md

@kapwa-client/src/pages/DashboardPage.tsx
@kapwa-client/src/pages/BeneficiariesPage.tsx
@kapwa-client/src/pages/BeneficiaryViewPage.tsx
@kapwa-client/src/pages/IntakePage.tsx
@kapwa-client/src/components/PageShell.tsx
@kapwa-client/src/components/data-table/DataTable.tsx
@kapwa-client/src/components/skeletons/CardGridSkeleton.tsx
@kapwa-client/src/components/skeletons/TableSkeleton.tsx
@kapwa-client/src/components/skeletons/FormSkeleton.tsx
@kapwa-client/src/components/EmptyState.tsx
@kapwa-client/src/components/ErrorBoundary.tsx
@kapwa-client/src/components/ui/button.tsx
@kapwa-client/src/components/ui/badge.tsx
@kapwa-client/src/components/ui/card.tsx
@kapwa-client/src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardPage migration — pattern-setter for all pages, with regression test</name>
  <files>
    kapwa-client/src/pages/DashboardPage.tsx,
    kapwa-client/src/pages/__tests__/DashboardPage.test.tsx
  </files>
  <action>
    **Migrate DashboardPage.tsx per D-02 (first, pattern-setter), D-03 (refactor in place), D-04 (regression test), D-16 (cache staleness).**

    This is the most important migration — it sets the pattern every other page follows. Be precise and thorough.

    **Step 1 — Identify all legacy class usage to eliminate (reference current code):**
    - `.page-header` → remove, PageShell provides the header
    - `.page-title` → remove, PageShell `title` prop
    - `.page-desc` → remove, PageShell `description` prop
    - `.spinner` → replace with `CardGridSkeleton` for stat cards loading and `TableSkeleton` for table loading (or a combined approach: show both in a single loading state)
    - `.badge` → replace with `<Badge variant="outline">` from shadcn (mapped via className for colors)
    - `.badge-category` → replace with `<Badge variant="secondary" className="...">`
    - `.table` → replace with `<DataTable>` from Phase 10
    - `.text-text-secondary` → use `text-muted-foreground` Tailwind class
    - `.pagination` → legacy structure, replace with DataTable's built-in pagination or inline display

    **Step 2 — Refactor in place per D-03:**
    - Keep ALL existing business logic: `loadData()`, `getDashboard()` API call, `StatusBadge` component, `StatCard` component, stat data mapping, error handling
    - Keep ALL state variables: `stats`, `cases`, `loading`
    - Only swap DOM/CSS wrappers

    **Step 3 — Implement new rendering structure:**

    Import block (add these, keep existing imports):
    ```
    import { PageShell } from '@/components/PageShell';
    import { CardGridSkeleton } from '@/components/skeletons/CardGridSkeleton';
    import { TableSkeleton } from '@/components/skeletons/TableSkeleton';
    import { EmptyState } from '@/components/EmptyState';
    import { DataTable } from '@/components/data-table';
    import { Button } from '@/components/ui/button';
    import { Badge } from '@/components/ui/badge';
    import { Card, CardContent } from '@/components/ui/card';
    import type { ColumnDef } from '@tanstack/react-table';
    ```
    Remove: `import '../index.css';` (no longer needed — tailwind globals handle it)

    **Loading state:**
    ```
    if (loading) {
      return (
        <PageShell title="Dashboard" description="Overview of social welfare operations and metrics.">
          <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
            {Array.from({ length: 4 }).map((_, i) => (
              <Card key={i}>
                <CardContent className="p-4">
                  <CardGridSkeleton />
                </CardContent>
              </Card>
            ))}
          </div>
          <TableSkeleton rows={5} />
        </PageShell>
      );
    }
    ```

    **Error / empty state:**
    When `stats.length === 0`: show `<EmptyState variant="no-data" />` inside PageShell.

    **Normal state:**
    ```
    return (
      <PageShell
        title="Dashboard"
        description="Overview of social welfare operations and metrics."
        cachedAt={lastSync ?? undefined}
      >
        {/* Stat cards grid */}
        <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
          {stats.map(s => <StatCard key={s.label} stat={s} />)}
        </div>

        {/* Recent cases section header */}
        <div className="flex items-center justify-between mt-2">
          <h3 className="text-lg font-semibold">Recent Cases</h3>
          <Button variant="outline" size="sm" onClick={() => navigate('/cases')}>
            View All Cases
          </Button>
        </div>

        {/* Recent cases DataTable */}
        <DataTable
          columns={dashboardCaseColumns}
          data={cases}
          rowCount={cases.length}
          pagination={{ pageIndex: 0, pageSize: 10 }}
          sorting={[]}
        />
      </PageShell>
    );
    ```

    **Refactor StatusBadge:**
    Replace the legacy `.badge` + hardcoded color classes pattern with `Badge` component:
    ```
    const StatusBadge = React.memo(({ status }: { status: string }) => {
      const variantMap: Record<string, 'default' | 'secondary' | 'outline' | 'destructive'> = {
        approved: 'default',
        in_review: 'secondary',
        pending_assessment: 'outline',
        disbursed: 'secondary',
        closed: 'outline',
      };
      const labelMap: Record<string, string> = {
        pending_assessment: 'Pending',
        in_review: 'In Review',
        approved: 'Approved',
        disbursed: 'Disbursed',
        closed: 'Closed',
      };
      return <Badge variant={variantMap[status] || 'outline'}>{labelMap[status] || status}</Badge>;
    });
    ```

    **Refactor StatCard:**
    Replace the legacy `rounded-xl p-4` div wrapper with `Card` + `CardContent`:
    ```
    const StatCard = React.memo(({ stat }: { stat: Stat }) => {
      const Icon = stat.icon;
      return (
        <Card>
          <CardContent className="p-4">
            <div className="flex items-center gap-3 mb-2">
              <span className="text-xs font-semibold text-muted-foreground uppercase tracking-wide">{stat.label}</span>
              <div className={`ml-auto rounded-full w-8 h-8 flex items-center justify-center ${stat.iconClass}`}>
                <Icon size={16} />
              </div>
            </div>
            <div className="text-2xl font-bold text-foreground font-heading">{stat.value}</div>
            <p className="text-xs text-muted-foreground mt-1">{stat.change}</p>
          </CardContent>
        </Card>
      );
    });
    ```

    **Define DataTable columns:**
    ```
    const dashboardCaseColumns: ColumnDef<CaseRow>[] = [
      { accessorKey: 'id', header: 'Case ID', cell: ({ row }) => <span className="text-muted-foreground">{row.original.id}</span> },
      { accessorKey: 'name', header: 'Name', cell: ({ row }) => <span className="font-medium">{row.original.name}</span> },
      { accessorKey: 'category', header: 'Category', cell: ({ row }) => <Badge variant="secondary">{row.original.category}</Badge> },
      { accessorKey: 'barangay', header: 'Barangay' },
      { accessorKey: 'remarks', header: 'Remarks', cell: ({ row }) => <span className="text-xs">{row.original.remarks}</span> },
      { accessorKey: 'status', header: 'Status', cell: ({ row }) => <StatusBadge status={row.original.status} /> },
      { accessorKey: 'date', header: 'Date', cell: ({ row }) => <span className="text-xs text-muted-foreground">{row.original.date}</span> },
    ];
    ```

    **Add .no-print class:**
    Wrap the entire PageShell div container with `className="no-print"`? NO — PageShell content should print. Only add `.no-print` to non-essential decorative elements like action buttons that have no printable value.

    **Step 4 — Add caching/lastSync:**
    Add `lastSync` state: `const [lastSync, setLastSync] = useState<number | null>(null);`
    Set it when data loads successfully: `setLastSync(Date.now());`
    Pass `cachedAt={lastSync ?? undefined}` to PageShell per D-16.

    **Step 5 — Create regression test (per D-04):**
    Create `kapwa-client/src/pages/__tests__/DashboardPage.test.tsx`:
    ```
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { DashboardPage } from '../DashboardPage';

    vi.mock('../../lib/api', () => ({
      getDashboard: vi.fn().mockResolvedValue({
        servedToday: 12,
        servedChange: '+8%',
        lastSync: '2m ago',
        pendingReview: 3,
        urgentCount: 1,
        disbursedMonth: 45000,
        beneficiaryCount: 28,
        recentCases: [
          { id: 'C-001', name: 'Juan Dela Cruz', category: 'Senior', barangay: 'Barangay 1', remarks: 'Monthly assistance', date: '2026-06-28', status: 'approved' },
        ],
      }),
    }));

    describe('DashboardPage', () => {
      it('renders dashboard heading and stat cards', async () => {
        render(<MemoryRouter><DashboardPage /></MemoryRouter>);
        expect(await screen.findByText('Dashboard')).toBeInTheDocument();
        expect(screen.getByText('Served Today')).toBeInTheDocument();
        expect(screen.getByText('Recent Cases')).toBeInTheDocument();
      });

      it('renders case table with mock data', async () => {
        render(<MemoryRouter><DashboardPage /></MemoryRouter>);
        expect(await screen.findByText('C-001')).toBeInTheDocument();
        expect(screen.getByText('Juan Dela Cruz')).toBeInTheDocument();
      });
    });
    ```

    **Do NOT:**
    - Import `index.css` at the top of the page
    - Use any `.badge*`, `.table*`, `.page-header`, `.page-title`, `.page-desc`, `.spinner`, `.empty-state`, `.pagination`, `.text-text-secondary` classes
    - Rewrite business logic or API calls per D-03
    - Remove array `import React` from react at top
  </action>
  <verify>
    <automated>grep -c "PageShell" kapwa-client/src/pages/DashboardPage.tsx && grep -c "DataTable" kapwa-client/src/pages/DashboardPage.tsx && npm test -- --run 2>&1 | grep -E "(PASS|FAIL|✓|✗)" | head -5</automated>
  </verify>
  <done>
    - DashboardPage uses PageShell, DataTable, Card, Badge, CardGridSkeleton, EmptyState
    - Zero legacy CSS classes remain (.page-header, .page-title, .page-desc, .spinner, .table, .badge all eliminated)
    - Original business logic preserved intact
    - Regression test passes with mock data
    - cachedAt prop wired to PageShell
  </done>
</task>

<task type="auto">
  <name>Task 2: BeneficiariesPage + BeneficiaryViewPage migration with regression tests</name>
  <files>
    kapwa-client/src/pages/BeneficiariesPage.tsx,
    kapwa-client/src/pages/BeneficiaryViewPage.tsx,
    kapwa-client/src/pages/__tests__/BeneficiariesPage.test.tsx,
    kapwa-client/src/pages/__tests__/BeneficiaryViewPage.test.tsx
  </files>
  <action>
    **A. Migrate BeneficiariesPage.tsx (per PGM-01):**

    Follow the same pattern established by DashboardPage migration:

    **Imports to add:**
    ```
    import { PageShell } from '@/components/PageShell';
    import { TableSkeleton } from '@/components/skeletons/TableSkeleton';
    import { EmptyState } from '@/components/EmptyState';
    import { DataTable } from '@/components/data-table';
    import { Button } from '@/components/ui/button';
    import { Badge } from '@/components/ui/badge';
    import { Input } from '@/components/ui/input';
    import type { ColumnDef } from '@tanstack/react-table';
    ```
    Remove: `import '../index.css';`

    **Keep ALL existing business logic:**
    - State variables (beneficiaries, searchInput, debouncedSearch, categoryFilter, barangayFilter, loading, fetching)
    - 300ms debounce useEffect
    - Data load useEffect with AbortController pattern
    - getBeneficiaries API call, data mapping to Beneficiary[]
    - All filter states and handlers

    **Replace loading rendering:**
    Old: `<div className="flex items-center justify-center p-12"><div className="spinner" />...</div>`
    New: `<PageShell title="Beneficiaries" description="Manage beneficiary records"><TableSkeleton rows={8} /></PageShell>`

    **Replace empty state:**
    Old: `<div className="empty-state">...</div>`
    New: `<EmptyState variant={searchInput ? 'no-results' : 'no-data'} onAction={...} />`

    **Replace table with DataTable:**
    Define `beneficiaryColumns: ColumnDef<Beneficiary>[]` with columns for name, age, barangay, householdSize, programs (rendered as Badge list), status.

    The existing filter bar (search, category, barangay dropdowns) should stay as-is but use shadcn Input and shadcn Select instead of legacy `.search-input`, `.form-input`, `.form-select` if those are used. The current code uses regular input/select elements which may already be fine — only replace conscious legacy class wrappers.

    Add `lastSync` state, set it on successful load, pass `cachedAt` to PageShell per D-16.

    **B. Migrate BeneficiaryViewPage.tsx (per PGM-01):**

    Same pattern:
    - Add PageShell wrapper with title "Beneficiary Details"
    - Loading: CardGridSkeleton
    - Empty/Error: EmptyState variant
    - Normal: render beneficiary details using shadcn Card, Badge for status, proper Tailwind typography
    - No legacy CSS classes
    - Add cachedAt prop

    **C. Create regression tests (per D-04):**

    `__tests__/BeneficiariesPage.test.tsx`:
    - Mock `getBeneficiaries` to return sample data
    - Verify heading "Beneficiaries" renders
    - Verify a known beneficiary name appears in the table
    - Verify search input exists

    `__tests__/BeneficiaryViewPage.test.tsx`:
    - Mock API calls
    - Basic render test verifying page renders without crashing

    **Do NOT import index.css, do NOT use any manual `vi.mock('../../lib/api')` with incorrect paths — use the same mock pattern as DashboardPage test.**

    File location for tests: `kapwa-client/src/pages/__tests__/BeneficiariesPage.test.tsx` (create directory if not exists)
  </action>
  <verify>
    <automated>grep -c "PageShell" kapwa-client/src/pages/BeneficiariesPage.tsx && grep -c "PageShell" kapwa-client/src/pages/BeneficiaryViewPage.tsx && grep -c "DataTable" kapwa-client/src/pages/BeneficiariesPage.tsx && npm test -- --run 2>&1 | grep -E "(PASS|FAIL)" | head -5  </verify>
  <done>
    - BeneficiariesPage uses PageShell + DataTable + TableSkeleton + EmptyState with shadcn components
    - BeneficiaryViewPage uses PageShell + CardGridSkeleton + EmptyState
    - Zero legacy CSS classes remain on both pages
    - Both pages have regression tests passing with mock data
    - cachedAt prop wired to PageShell
  </done>
</task>

<task type="auto">
  <name>Task 3: IntakePage migration with regression test</name>
  <files>
    kapwa-client/src/pages/IntakePage.tsx,
    kapwa-client/src/pages/__tests__/IntakePage.test.tsx
  </files>
  <action>
    **Migrate IntakePage.tsx (per PGM-01):**

    This is a form-heavy page. Follow the same pattern per D-03 (refactor in place, swap wrappers, keep business logic):

    **Imports to add:**
    ```
    import { PageShell } from '@/components/PageShell';
    import { FormSkeleton } from '@/components/skeletons/FormSkeleton';
    import { EmptyState } from '@/components/EmptyState';
    import { Button } from '@/components/ui/button';
    ```
    Remove: `import '../index.css';`

    **Keep ALL existing business logic:**
    - Form state, field values, validation
    - Submit handlers, API calls
    - All form sections and fields

    **Replace the outer wrapper:**
    Old: `<div><div className="page-header"><h2 className="page-title">...</h2><p className="page-desc">...</p></div>...</div>`
    New: `<PageShell title="New Intake" description="GIS-based beneficiary intake form">...</PageShell>`

    **Replace loading state:**
    Old: `<div className="spinner" />`
    New: `<FormSkeleton />` inside PageShell

    **Replace form input classes:**
    Replace `.form-input`, `.form-label`, `.form-group`, `.form-select` with shadcn equivalents:
    - `.form-input` → `<Input />` component
    - `.form-label` → `<Label />` component
    - `.form-select` → `<Select />` component
    - `.form-group` → standard Tailwind `space-y-2` pattern

    Use the existing shadcn Form components from `@/components/ui/form.tsx` if the page uses structured forms. If the page uses raw HTML form elements with legacy classes, wrap them in shadcn Input/Label/Select wrappers but keep the form submission logic intact.

    **Replace legacy class buttons:**
    If any `<button className="btn btn-primary">` exists, replace with `<Button>`.

    **Add .no-print class:**
    Add `className="no-print"` to buttons, toolbar areas, and non-essential controls.

    **Add cachedAt prop:**
    If IntakePage loads any reference data (programs list, barangays), add `lastSync` state and pass `cachedAt` to PageShell. If IntakePage is pure form entry with no data fetching, skip cachedAt.

    **Create regression test:**
    `__tests__/IntakePage.test.tsx` — verify page renders title and at least one form field/button:
    ```
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { MemoryRouter } from 'react-router-dom';
    import { IntakePage } from '../IntakePage';

    describe('IntakePage', () => {
      it('renders intake form title', async () => {
        render(<MemoryRouter><IntakePage /></MemoryRouter>);
        expect(await screen.findByText(/New Intake|Intake/i)).toBeInTheDocument();
      });
    });
    ```

    **Do NOT:**
    - Import `index.css`
    - Use `.form-input`, `.form-label`, `.form-group`, `.form-select`, `.btn`, `.btn-primary` classes
    - Rewrite business logic, validation, or API calls
    - Break existing form submission flow
  </action>
  <verify>
    <automated>grep -c "PageShell" kapwa-client/src/pages/IntakePage.tsx && grep -c "FormSkeleton\|EmptyState" kapwa-client/src/pages/IntakePage.tsx && npm test -- --run 2>&1 | grep -E "(PASS|FAIL)" | head -5</automated>
  </verify>
  <done>
    - IntakePage uses PageShell, FormSkeleton, shadcn Input/Label/Select, Button
    - Zero legacy form CSS classes remain
    - Original form submission logic preserved intact
    - Regression test passes with mock data
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → page component | API responses rendered in DataTable — untrusted data crosses here |
| localStorage → cachedAt | lastSync timestamp stored locally — no network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Spoofing | Mock data in tests | accept | Test mocks are isolated to test runner, not bundled into production |
| T-11-02-02 | Information Disclosure | DataTable cell rendering | mitigate | API data rendered in cell values — no HTML injection risk since React escapes by default; DataTable uses `flexRender` which is safe |
| T-11-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required — all components already installed |
</threat_model>

<verification>
1. Run `npm test -- --run` in kapwa-client/ — verify all 5 page regression tests pass
2. Verify no legacy CSS class usages remain on migrated pages:
   - `grep -c 'className=".*spinner' kapwa-client/src/pages/DashboardPage.tsx` returns 0
   - `grep -c 'className=".*page-header' kapwa-client/src/pages/DashboardPage.tsx` returns 0
   - Similar checks for BeneficiariesPage, BeneficiaryViewPage, IntakePage
2. Manual: open each migrated page in browser, verify loading/empty/error states look correct
3. Verify cachedAt badge appears on pages with data fetching (>5 min threshold)
</verification>

<success_criteria>
1. [ ] DashboardPage uses shadcn components only — zero legacy CSS classes
2. [ ] BeneficiariesPage uses DataTable with search/filter — zero legacy classes
2. [ ] BeneficiaryViewPage uses Card layout — zero legacy classes
3. [ ] IntakePage uses shadcn form components — zero legacy classes
4. [ ] All 4 migrated pages have regression tests passing in CI
5. [ ] All pages wired with cachedAt prop for cache staleness
7. [ ] Loading/empty/error states present on each migrated page
</success_criteria>

<output>
Create `.planning/phases/11-page-migration-print-offline-ui/11-02-SUMMARY.md` when done
</output>
