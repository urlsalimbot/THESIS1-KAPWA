---
phase: 06-dashboards-notifications-role-completion
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - kapwa-server/src/dashboard/dashboard.controller.ts
  - kapwa-server/src/dashboard/dashboard.service.ts
  - kapwa-server/src/audit/audit.controller.ts
  - kapwa-server/src/audit/audit.service.ts
  - kapwa-server/src/beneficiaries/beneficiaries.controller.ts
  - kapwa-client/src/pages/MyAccessCardPage.tsx
  - kapwa-client/src/pages/MayorReportsPage.tsx
  - kapwa-client/src/pages/AuditorPage.tsx
  - kapwa-client/src/pages/ClaimantDashboardPage.tsx
  - kapwa-client/src/routes.tsx
  - kapwa-client/src/components/Layout.tsx
  - kapwa-client/src/lib/api.ts
autonomous: false
requirements:
  - ROL-03
  - ROL-04
  - ROL-05
must_haves:
  truths:
    - "Mayor can view aggregate-only reports with fund utilization, household counts, SLA — zero PII"
    - "Auditor can verify hash chain integrity and inspect consent ledger (read-only)"
    - "Claimant can view their Access Card at /my-access-card with print and replacement buttons"
    - "Claimant dashboard shows link/button to navigate to /my-access-card"
    - "Mayor and auditor roles removed from GET /dashboard @Roles() to prevent PII exposure"
  artifacts:
    - path: "kapwa-server/src/dashboard/dashboard.controller.ts"
      provides: "GET /dashboard/reports/mayor endpoint — aggregate-only, zero PII"
      contains: "reports/mayor"
    - path: "kapwa-client/src/pages/MyAccessCardPage.tsx"
      provides: "Read-only Access Card display with print + replacement for claimants"
      contains: "MyAccessCardPage"
    - path: "kapwa-client/src/pages/MayorReportsPage.tsx"
      provides: "Aggregate-only stat cards with export buttons"
      contains: "MayorReportsPage"
    - path: "kapwa-client/src/pages/AuditorPage.tsx"
      provides: "Hash chain verification status + consent ledger table"
      contains: "AuditorPage"
  key_links:
    - from: "dashboard.controller.ts"
      to: "dashboard.service.ts"
      via: "GET /dashboard/reports/mayor calls getMetrics + getSlaCompliance only (no recentCases)"
      pattern: "getMetrics.*getSlaCompliance"
    - from: "audit.controller.ts"
      to: "audit.service.ts"
      via: "GET /audit/consent-ledger calls auditService.getConsentLedger"
      pattern: "getConsentLedger"
    - from: "beneficiaries.controller.ts"
      to: "access-card service"
      via: "GET /beneficiaries/me/access-card returns AccessCard data for claimant"
      pattern: "accessCardService"
---

<objective>
**Goal:** Mayor sees aggregate reports only (no PII), auditor reads hash-chain verified logs and consent ledger, and claimant views their Access Card at a dedicated route.

**User story:** As a mayor, I want to see aggregate reports without PII; as an auditor, I want to verify hash chain integrity and inspect consent records; as a claimant, I want to view my Access Card with print and replacement options.

**Purpose:** Fulfill ROL-03 (claimant access card), ROL-04 (mayor aggregate reports), ROL-05 (auditor read-only logs).
**Output:** /dashboard/reports/mayor endpoint, /audit/consent-ledger endpoint, /beneficiaries/me/access-card endpoint, MyAccessCardPage, MayorReportsPage, AuditorPage.
</objective>

<execution_context>
@/home/typwtypw/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/typwtypw/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/.planning/phases/06-dashboards-notifications-role-completion/06-CONTEXT.md
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/.planning/phases/06-dashboards-notifications-role-completion/06-RESEARCH.md
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/.planning/phases/06-dashboards-notifications-role-completion/06-PATTERNS.md
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/.planning/phases/06-dashboards-notifications-role-completion/06-UI-SPEC.md
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-server/src/dashboard/dashboard.controller.ts
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-server/src/dashboard/dashboard.service.ts
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-server/src/audit/audit.controller.ts
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-server/src/audit/audit.service.ts
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-server/src/beneficiaries/beneficiaries.controller.ts
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-client/src/routes.tsx
@/home/typwtypw/Documents/NC/THESIS1-KAPWA/kapwa-client/src/components/Layout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mayor aggregate endpoint + Auditor consent ledger + Access Card endpoint</name>
  <behavior>
    - Test 1: GET /dashboard/reports/mayor returns aggregate-only fields (no PII like names, addresses)
    - Test 2: GET /dashboard/reports/mayor does NOT include recentCases
    - Test 3: GET /audit/consent-ledger returns consent records
    - Test 4: GET /audit/consent-ledger with beneficiaryId filter works
    - Test 5: GET /beneficiaries/me/access-card returns card data for claimant
    - Test 6: GET /dashboard without mayor in @Roles() returns 403 for mayor role
  </behavior>
  <files>
    kapwa-server/src/dashboard/dashboard.controller.ts
    kapwa-server/src/dashboard/dashboard.service.ts
    kapwa-server/src/audit/audit.controller.ts
    kapwa-server/src/audit/audit.service.ts
    kapwa-server/src/beneficiaries/beneficiaries.controller.ts
  </files>
  <action>
    Step A — Modify `kapwa-server/src/dashboard/dashboard.controller.ts`:
    - REMOVE 'mayor' from line 20 @Roles() on getDashboard() — mayor must NOT see PII endpoints per D-13/D-14
    - REMOVE 'mayor' from line 65 @Roles() on getMetrics() — same PII protection
    - Add new endpoint after line 72 (before getDailyTracker):
      @Get('reports/mayor')
      @Roles('mayor')
      @ApiOperation({ summary: 'Mayor aggregate reports - zero PII' })
      async getMayorReports(@Request() req: AuthenticatedRequest) {
        const metrics = await this.dashService.getMetrics();
        const sla = await this.dashService.getSlaCompliance();
        const servedToday = await this.dashService.getServedToday();
        return {
          fundUtilization: metrics.totalDisbursedAmount,
          uniqueHouseholds: metrics.uniqueHouseholds,
          caseStatusDistribution: metrics.byStatus,
          totalCases: metrics.totalCases,
          slaCompliance: sla,
          servedToday,
        };
      }
    - Follow PATTERNS.md lines 326-357 and RESEARCH.md Pattern 2 (lines 320-341)

    Step B — Modify `kapwa-server/src/dashboard/dashboard.service.ts`:
    - Add `async getServiceSummary(startDate?: Date, endDate?: Date)`:
      - Query interventions grouped by intervention_type with COUNT, SUM(amount), COUNT(DISTINCT household_id)
      - Optional date range filter on serviceDate
      - Return array of { interventionType, count, totalAmount, uniqueHouseholds }
      - Follow PATTERNS.md lines 359-386
    - Add `async getComplianceData()`:
      - Return getSlaCompliance() result (reuse existing method)

    Step C — Modify `kapwa-server/src/audit/audit.controller.ts`:
    - Add after line 38 (GET /audit/logs):
      @Get('consent-ledger')
      @Roles('admin', 'auditor')
      @ApiOperation({ summary: 'Read consent ledger (auditor read-only)' })
      async getConsentLedger(
        @Query('beneficiaryId') beneficiaryId?: string,
        @Query('limit') limit?: string
      ) {
        const limitNum = limit ? parseInt(limit, 10) : 50;
        return this.auditService.getConsentLedger(beneficiaryId, limitNum);
      }
    - Follow PATTERNS.md lines 486-523

    Step D — Modify `kapwa-server/src/audit/audit.service.ts`:
    - Add `async getConsentLedger(beneficiaryId?: string, limit = 50)`:
      - Build where clause (if beneficiaryId provided)
      - Find from this.consentRepo (ConsentLedger), ordered by grantedAt DESC
      - Take limit
      - Return records
    - Follow PATTERNS.md lines 506-523

    Step E — Modify `kapwa-server/src/beneficiaries/beneficiaries.controller.ts`:
    - Add after line 46 (GET me/consent):
      @Get('me/access-card')
      @Roles('claimant')
      @ApiOperation({ summary: 'Get claimant Access Card (read-only)' })
      async getMyAccessCard(@Request() req: AuthenticatedRequest) {
        const beneficiary = await this.benService.findByUserId(req.user.id);
        if (!beneficiary) throw new NotFoundException('Beneficiary profile not found');
        return this.benService.getAccessCard(beneficiary.id);
      }
    - Follow PATTERNS.md lines 527-541 and RESEARCH.md lines 439-453
    - Note: getAccessCard already exists on BeneficiariesService (from Phase 4 Access Card work) — just expose via new endpoint
  </action>
  <verify>
    <automated>
      cd kapwa-server && npx jest --testPathPattern="dashboard|audit|beneficiaries" --no-coverage 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    All 6 backend tests pass. Mayor endpoint returns aggregate-only data (no PII). Mayor gets 403 on GET /dashboard. Consent ledger endpoint returns records. Access card endpoint returns 200 for claimant.
  </done>
</task>

<task type="auto">
  <name>Task 2: MyAccessCardPage + MayorReportsPage + AuditorPage</name>
  <files>
    kapwa-client/src/pages/MyAccessCardPage.tsx
    kapwa-client/src/pages/MayorReportsPage.tsx
    kapwa-client/src/pages/AuditorPage.tsx
  </files>
  <action>
    Step A — Create `kapwa-client/src/pages/MyAccessCardPage.tsx`:
    - Page header: "My Access Card" with description "Your KAPWA Access Card — read-only view"
    - Load card data from GET /beneficiaries/me/access-card on mount
    - Toolbar: "Print Card" button (window.print()), "Request Replacement" button (navigates to existing loss/replacement workflow), "Back to Dashboard" link to /my-dashboard
    - Card layout: Code (mono bold #2E5C8A), beneficiary name + barangay, remaining service slots
    - Service Log table (18 rows max per D-02): Date | Service | Agency columns
    - Print styles via @media print (hide nav, header, toolbar via .no-print class pattern)
    - Empty state: "No Access Card found. Please contact the MSWDO office."
    - Loading: "Loading Access Card..." centered
    - Follow UI-SPEC.md P1 wireframe (lines 108-186) exactly
    - Follow PATTERNS.md lines 579-667 for component structure and copywriting

    Step B — Create `kapwa-client/src/pages/MayorReportsPage.tsx`:
    - Page header: "Reports" with description "Municipal program and compliance overview"
    - Fetch aggregate data from GET /dashboard/reports/mayor on mount
    - Six stat cards in 3x2 grid (Desktop) or 2x3 (tablet):
      1. Total Cases (metrics.totalCases)
      2. Unique Households (metrics.uniqueHouseholds)
      3. Fund Utilization (metrics.fundUtilization formatted as PHP)
      4. Served Today (metrics.servedToday)
      5. Cases by Status (metrics.caseStatusDistribution — counts only)
      6. SLA Compliance (metrics.slaCompliance.slaStatus with color)
    - Export toolbar section: "Export Reports" with [Export PDF] [Export CSV] buttons
      - Import ReportsExportButton from '../components/ReportsExportButton'
      - Use ReportsExportButton for each export: endpoint="/export/service-summary", format="pdf"
    - Date range picker (optional, simple text inputs for start/end date)
    - Zero PII anywhere in this page per D-14
    - Loading: "Loading reports..." centered
    - Empty: "No data available for the selected period."
    - Follow UI-SPEC.md P3 wireframe (lines 279-358) exactly
    - Follow PATTERNS.md lines 739-778 for component structure

    Step C — Create `kapwa-client/src/pages/AuditorPage.tsx`:
    - Page header: "Audit Logs" with description "Hash-chain verified records and consent ledger"
    - Load data from two endpoints on mount: GET /audit/verify-all and GET /audit/consent-ledger
    - Hash Chain Verification Status Panel:
      - Green status card if all valid: "All chains verified — integrity confirmed"
      - Red status card if any broken: "Chain integrity check failed — see details below"
      - Shows tables checked count, last verified timestamp
      - "Verify All Chains" button to re-verify
      - Export buttons: [Export PDF] [Export CSV] for audit logs
    - Tabs: "Audit Logs" | "Consent Ledger" (use state-based tab switching)
    - Audit Logs tab: table with columns Table | Record ID | Action | Timestamp
    - Consent Ledger tab: filter by beneficiary ID text input, table with Date | Channel | Purpose | Status
    - Export buttons use ReportsExportButton component
    - Loading states for hash chain, logs, consent ledger
    - Follow UI-SPEC.md P4 wireframe (lines 362-460) exactly
    - Follow PATTERNS.md lines 782-821 for component structure

    For all three pages:
    - Use existing page-header / page-title / page-desc CSS class pattern
    - Use fetch() with Authorization: Bearer token pattern from existing pages
    - Use lucide-react icons where appropriate
  </action>
  <verify>
    <automated>
      cd kapwa-client && npx vitest run --reporter=verbose 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    Three new pages render without errors. MyAccessCardPage shows card data with print/replacement buttons. MayorReportsPage shows aggregate-only stats with export buttons. AuditorPage shows hash chain status + consent ledger with tabs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Routes, nav, API functions + claimant dashboard link</name>
  <files>
    kapwa-client/src/routes.tsx
    kapwa-client/src/components/Layout.tsx
    kapwa-client/src/lib/api.ts
    kapwa-client/src/pages/ClaimantDashboardPage.tsx
  </files>
  <action>
    Step A — Modify `kapwa-client/src/routes.tsx`:
    - Add imports for MyAccessCardPage, MayorReportsPage, AuditorPage
    - Add after the /my-dashboard route (line 52):
      { path: '/my-access-card', element: <Private roles={['claimant']}><MyAccessCardPage /></Private> },
      { path: '/reports', element: <Private roles={['mayor']}><MayorReportsPage /></Private> },
      { path: '/audit-logs', element: <Private roles={['auditor']}><AuditorPage /></Private> },
    - Follow PATTERNS.md lines 824-841

    Step B — Modify `kapwa-client/src/components/Layout.tsx`:
    - Import Stamp, FileText, Shield from lucide-react (already imported at line 5)
    - Add to NAV_ITEMS array (at end, before the closing bracket):
      { path: '/my-access-card', label: 'My Access Card', icon: <Stamp size={20} />, roles: ['claimant'] },
      { path: '/reports', label: 'Reports', icon: <FileText size={20} />, roles: ['mayor'] },
      { path: '/audit-logs', label: 'Audit Logs', icon: <Shield size={20} />, roles: ['auditor'] },
    - Follow UI-SPEC.md Navigation section (lines 649-682) for role-based nav sets
    - Mayor nav should show: Dashboard (/), Daily Tracker /tracker, Reports /reports, MFA Settings
    - Auditor nav should show: Dashboard (/), Daily Tracker /tracker, Audit Logs /audit-logs, MFA Settings
    - Claimant nav should show: My Dashboard /my-dashboard, My Access Card /my-access-card
    - The existing visibleNav filter at line 72 handles role-based filtering automatically

    Step C — Modify `kapwa-client/src/lib/api.ts`:
    - Add `export async function getMyAccessCard()`: fetch GET /beneficiaries/me/access-card with auth
    - Add `export async function getMayorReports()`: fetch GET /dashboard/reports/mayor with auth
    - Add `export async function getConsentLedger(beneficiaryId?: string)`: fetch GET /audit/consent-ledger with optional beneficiaryId query param
    - Add `export async function verifyHashChains()`: fetch GET /audit/verify-all with auth
    - Follow PATTERNS.md lines 862-909 for exact function signatures

    Step D — Modify `kapwa-client/src/pages/ClaimantDashboardPage.tsx`:
    - Import Link from 'react-router-dom'
    - Add after the Case Status card (after line 67 closing div) a new Access Card link card:
      <div className="mb-4 rounded-lg border border-gray-200 bg-white p-4 flex items-center justify-between">
        <div>
          <p className="text-xs text-gray-500">Access Card</p>
          <p className="text-sm font-medium text-[#2E5C8A]">View your KAPWA Access Card</p>
        </div>
        <Link to="/my-access-card"
          className="rounded bg-[#2E5C8A] px-3 py-1.5 text-xs text-white hover:bg-[#1e3d5e]">
          View Card
        </Link>
      </div>
    - Follow PATTERNS.md lines 913-931
  </action>
  <verify>
    <automated>
      cd kapwa-client && npx vitest run --reporter=verbose 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    All four routes work with correct role guards. Nav shows correct items per role. API functions return data. ClaimantDashboardPage shows Access Card link.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Dashboard API | Mayor's aggregate report requests cross this boundary |
| Client → Audit API | Auditor's hash chain verification requests |
| Client → Beneficiaries API | Claimant's access card request (self-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | GET /dashboard (existing) | mitigate | REMOVE 'mayor' from @Roles() on existing getDashboard() — mayor can only use aggregate-only endpoint per D-13/D-14 |
| T-06-11 | Information Disclosure | GET /dashboard/reports/mayor | mitigate | Returns only aggregate counts from getMetrics() — no recentCases, no beneficiary names, no PII per D-14 |
| T-06-12 | Elevation of Privilege | Auditor endpoints | mitigate | All auditor endpoints gated by JwtAuthGuard + @Roles('admin', 'auditor') — no write operations exposed |
| T-06-13 | Information Disclosure | /beneficiaries/me/access-card | mitigate | Self-only via JWT user.id — claimant cannot query other beneficiaries' cards |
</threat_model>

<verification>

### Per-Task Verification

| Task | Automated Command | Criteria |
|------|-------------------|----------|
| 1 — Backend endpoints | `cd kapwa-server && npx jest --testPathPattern="dashboard|audit|beneficiaries" --no-coverage` | 6 tests passing |
| 2 — Three new pages | `cd kapwa-client && npx vitest run --reporter=verbose` | Pages render without errors |
| 3 — Routes/Nav/API | `cd kapwa-client && npx vitest run --reporter=verbose` | Routes accessible, nav filters correct |

### End-of-Plan Human Check (checkpoint:human-verify)

1. Login as mayor -> navigate to /reports -> verify aggregate-only cards, no PII
2. Login as auditor -> navigate to /audit-logs -> verify hash chain status, consent ledger
3. Login as claimant -> navigate to /my-access-card -> verify card display, print button
4. Login as mayor -> try GET /dashboard -> verify 403 (mayor removed from @Roles)
5. Login as claimant -> verify Access Card link visible on /my-dashboard
6. Click "Print Card" -> verify browser print dialog opens
7. Verify mayor sidebar shows: Dashboard, Daily Tracker, Reports, MFA Settings
8. Verify auditor sidebar shows: Dashboard, Daily Tracker, Audit Logs, MFA Settings
</verification>

<success_criteria>
1. GET /dashboard/reports/mayor returns fundUtilization, uniqueHouseholds, caseStatusDistribution, totalCases, slaCompliance — no PII fields
2. GET /audit/consent-ledger returns consent records with optional beneficiaryId filter
3. GET /beneficiaries/me/access-card returns card data for authenticated claimant
4. 'mayor' removed from GET /dashboard and GET /dashboard/metrics @Roles()
5. MyAccessCardPage shows card code, beneficiary info, 18-row service log, print + replacement buttons
6. MayorReportsPage shows 6 aggregate stat cards with export buttons
7. AuditorPage shows hash chain verification status + consent ledger table with tabs
8. Nav items reflect correct role-based sets
</success_criteria>

<output>
Create `.planning/phases/06-dashboards-notifications-role-completion/06-04-SUMMARY.md` when done
</output>
